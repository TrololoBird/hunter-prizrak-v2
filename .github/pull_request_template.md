## Что меняется и почему

<!-- Одним абзацем: какое требование FOUNDATION или какая страница курса это диктует. -->

## Проверки (§0, §7.3, §10.6)

- [ ] Гейты зелёные: `bash scripts/check.sh` — прогнан, код возврата 0 (не «печать зелёная»)
- [ ] Каждое новое число имеет источник ЛИБО замер с командой воспроизведения
- [ ] У замера есть контроль: показано, что прибор способен ответить ИНАЧЕ
- [ ] Для «совпало N из M» — второй контроль на заведомо несовпадающих данных
- [ ] Изменение расчёта предъявлено диффом повтора: `uv run python -m hunter replay --run-id last --diff`
- [ ] Новый гейт (если есть) вписан в `.github/workflows/ci.yml` и проверен ПОДСАЖЕННЫМ нарушением

## Чего проверить не удалось

<!-- Прямо и поимённо: нет данных, нет сети, нет прогона. Пустой раздел — тоже ответ. -->
