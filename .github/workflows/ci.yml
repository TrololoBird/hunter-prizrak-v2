name: CI

# FOUNDATION.md §7.4: гейт работает в CI постоянно. §7.5: тестов нет,
# верификация — гейты и живые прогоны.

on:
  # ⚠ `push` сужен до main 2026-08-06. Прежде стояло голое `push:`, то есть на любую
  # ветку, — и работа через ответвление давала ДВА прогона на один коммит: один по
  # пушу, второй по `pull_request`. Ветки проверяются запросом на слияние, main — пушем.
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

# ⚠ Права ВСЕГО файла, а не только задания. Добавлено 2026-08-06 по находке `zizmor`
# (`excessive-permissions`, средняя уверенность): при отсутствии этого блока новое
# задание молча получает права по умолчанию, которые у репозитория шире, чем нужно.
# Задание ниже своё `permissions` сохраняет — это не дубль, а нижняя граница: файл
# задаёт потолок для всех заданий, включая те, которых ещё нет.
permissions:
  contents: read

# Два пуша подряд запускали два прогона, и первый досчитывал впустую. Отменяется
# устаревший, а не новый: интересует состояние последнего коммита, а не промежуточного.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  gates:
    # ⚠ ДВЕ операционные системы, и вторая — не для полноты. Владелец работает на
    # Windows, а CI до 2026-08-06 проверял только Linux. За одну сессию Windows дал три
    # отказа, ни один из которых Ubuntu поймать не мог: заглушка `python` из Microsoft
    # Store молча ничего не исполняла, пути вида `/tmp` невидимы для Python вне Git Bash,
    # плюс перевод строк CRLF. Проверять надо ту среду, в которой запускают.
    #
    # `fail-fast: false` — обязателен: при `true` отказ одной ОС отменяет вторую, и
    # неизвестно, сломано ли там же. Гейты обязаны отчитаться оба.
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    # Умолчание GitHub — 360 минут. Задание 2026-08-06 висело 15 минут, не получив
    # раннера; без предела зависшее задание жгло бы шесть часов квоты.
    timeout-minutes: 20
    # Токену нужно только читать дерево. Права репозитория и так `read`, но здесь это
    # перестаёт зависеть от настройки, которую можно поменять мимо репозитория.
    permissions:
      contents: read
    env:
      # ⚠ БЕЗ ЭТОГО НЕ РАБОТАЕТ НИ ОДИН ГЕЙТ НА WINDOWS, и это не догадка: первый же
      # прогон матрицы 2026-08-06 упал на первом гейте с
      #   UnicodeEncodeError: 'charmap' codec can't encode characters
      # Раннер Windows отдаёт стандартный вывод в cp1252, а все 18 гейтов печатают
      # по-русски — падал бы каждый.
      #
      # Указано именно `PYTHONIOENCODING`, а не `PYTHONUTF8=1`: проверено контролем на
      # машине владельца — при `PYTHONIOENCODING=cp1252` режим UTF-8 НЕ спасает, потому
      # что переменная кодировки ввода-вывода его перебивает. С `utf-8` тот же гейт
      # проходит с кодом 0.
      PYTHONIOENCODING: utf-8
    defaults:
      run:
        # На Windows умолчание — PowerShell, и многострочные шаги ниже вели бы себя
        # иначе. Одна оболочка на обе ОС: шаги обязаны быть одним и тем же кодом.
        shell: bash
    steps:
      # ⚠ Версии подняты 2026-08-06: прежние (checkout@v4, setup-uv@v5) собраны под
      # Node.js 20, и раннер уже принудительно запускал их на Node.js 24, предупреждая об
      # этом в каждом прогоне. Обе новые версии объявляют `using: node24` — проверено
      # чтением их `action.yml` по тегу, а не по документации.
      # ⚠ ЗАКРЕПЛЕНО ХЕШЕМ КОММИТА, а не тегом (решение владельца 2026-08-07, находка
      # `zizmor`: `unpinned-uses`). Тег подвижен, хеш — нет, и это не теория: замер
      # `git ls-remote https://github.com/actions/checkout` в тот же день показал, что
      # `refs/tags/v7` и `refs/tags/v7.0.0` указывают на РАЗНЫЕ коммиты
      # (3d3c42e5… против 9c091bb2…). То есть «та же версия» завтра могла собраться
      # другим кодом. Оба тега лёгкие (разыменование `^{}` пусто), поэтому в `uses`
      # стоит именно коммит, а не объект тега.
      #
      # Версия остаётся в комментарии: Dependabot читает эту форму и обновляет хеш
      # вместе с ним, а человек видит, что закреплено.
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7
        with:
          # ⚠ Иначе `checkout` оставляет токен задания в `.git/config` рабочей копии, и
          # любой последующий шаг может им пушить. Находка `zizmor` (`artipacked`).
          # Здесь пушить нечем и незачем: задание только читает дерево и гоняет гейты.
          persist-credentials: false
      # ⚠ Версия пиньется ТОЧНО, и это не педантизм: с v8 проект перестал публиковать
      # плавающие мажорные теги (в v8.0.0 удалён `update-major-minor-tags`), то есть
      # `@v9` не существует и `@v9.0.0` — единственная рабочая форма. Проверено:
      # `git/matching-refs/tags/v9` отдаёт только `v9.0.0`.
      #
      # Что изменилось по дороге с v5 и нас касается:
      #   v6 — другой `cache-dependency-glob` по умолчанию (влияет на ключ кэша, не на
      #        результат); отсутствие `UV_CACHE_DIR` по умолчанию — только на своих
      #        раннерах, здесь `ubuntu-latest`;
      #   v9 — `prune-cache` по умолчанию стал `false`: кэш растёт, зато меньше нагрузка
      #        на PyPI. Умолчание оставлено авторское, своего решения тут нет.
      - uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9  # v9.0.0
        with:
          enable-cache: true
      # группа gates — независимый оракул индикаторов (§10.3)
      - run: uv sync --frozen --group gates

      - name: ruff
        run: uv run ruff check .

      - name: mypy
        run: uv run mypy

      - name: 'гейт: никакого молчания (§4.3)'
        run: uv run python gates/no_silent_failure.py

      - name: 'гейт: только публичные данные (§1, §5)'
        run: uv run python gates/public_data_only.py

      - name: 'гейт: словари между слоями запрещены (§10.1)'
        run: uv run python gates/no_loose_dicts.py

      - name: 'гейт: чистота расчёта (§10.3)'
        run: uv run python gates/purity.py

      - name: 'гейт: единственный писатель в боевую базу (§10.2)'
        run: uv run python gates/production_writer.py

      - name: 'гейт: индикаторы против независимого оракула (§10.3)'
        run: uv run python gates/indicator_oracle.py

      - name: 'гейт: комплектность источников, минимум 3 (§0.1)'
        run: uv run python gates/sources.py

      - name: 'гейт: все величины против опубликованных формул (§0)'
        run: uv run python gates/formula_reference.py

      - name: 'гейт: с какого бара величина определена (§4.3)'
        run: uv run python gates/indicator_availability.py

      - name: 'гейт: смоук-импорт всех модулей'
        run: uv run python gates/smoke_import.py

      # ⚠ ПОВЕДЕНЧЕСКИЕ гейты — они исполняют расчёт и сравнивают числа, а не форму.
      # До 2026-08-04 таких было два (оракулы индикаторов), и внешний разбор показал,
      # что именно поэтому дефекты расчёта жили до него: их нечем было поймать.
      - name: 'гейт: обе ветви бинирования дают один бин (§5)'
        run: uv run python gates/binning_agrees.py

      - name: 'гейт: правила курса на рядах с известным ответом (§2)'
        run: uv run python gates/course_rules.py

      # ⚠ Добавлен 2026-08-10. Инварианты геометрии до него не проверял никто: когда
      # владелец трижды сказал «геометрия сломана», ответ дал скрипт на коленке, нигде
      # не сохранившийся. Контроль подсадкой: снятая проверка направления цели в
      # `build_pp_setup` даёт код 1 и печатает отрицательный РР.
      - name: 'гейт: инварианты геометрии сделки (§2.7)'
        run: uv run python gates/geometry_invariants.py

      # ⚠ Четыре шага ниже добавлены 2026-08-04. Три первых гейта существовали с
      # 2026-08-03, но в CI ВПИСАНЫ НЕ БЫЛИ и потому ни разу не отработали на чужой
      # машине: гейт вне CI — это скрипт, который помнят, пока помнят. Нашлось при
      # сверке списка `gates/*.py` со списком шагов, а не гейтом: за самим CI
      # автоматической проверки полноты нет, и это остаётся дырой.
      - name: 'гейт: модели запрещают лишние поля (§10.1)'
        run: uv run python gates/models_forbid_extra.py

      - name: 'гейт: ссылки в документах ведут на существующее (§0)'
        run: uv run python gates/doc_refs.py

      - name: 'гейт: у каждого числа есть команда воспроизведения (§7.3)'
        run: uv run python gates/repro_commands.py

      - name: 'гейт: цитаты курса дословны (§0)'
        run: uv run python gates/course_citations.py

      # Добавлен 2026-08-06 вместе со службой 24/7 (А-1). Проверяет условие, отказ по
      # которому полностью бесшумен: `await` внутри снятия снимка не роняет ничего, он
      # лишь превращает срез одного момента в смесь двух.
      - name: 'гейт: неделимые участки не ждут (§4.3)'
        run: uv run python gates/atomic_sections.py

      # Добавлен 2026-08-06 вместе с настройкой Claude Desktop. Проверяет то, чем
      # проверяется всё остальное: объявление хуков, ключи настроек, шапки навыков.
      # Отказ здесь молчалив — приложение игнорирует неизвестный ключ без единого слова.
      - name: 'гейт: конфигурация Claude Code объявлена без опечаток'
        run: uv run python gates/claude_config.py

      # Добавлен 2026-08-08. Проверяет то, на чём стоит защита от подгонки порога задним
      # числом: хеш файла допуска, снятый ДО прогона, обязан сходиться с самим файлом.
      # Исполнялась эта проверка ровно ноль раз — держалась на внимательности человека,
      # сверяющего два шестидесятичетырёхзначных числа глазами.
      - name: 'гейт: хеши допусков сходятся с файлами (§7.3)'
        run: uv run python gates/tolerance_hashes.py

      # ⚠ Добавлен 2026-08-10 по находке CodeQL в первый же его прогон: два пробника
      # звали `engine.read_series` с тремя аргументами при сигнатуре из двух, то есть
      # команда воспроизведения из протокола падала TypeError. §7.3 требует команду у
      # каждого числа — но что она ИСПОЛНЯЕТСЯ, не проверял никто: пробники стояли вне
      # mypy (он покрывает src, scripts, gates). Тот же класс, что «команда проверки
      # хеша допуска не работает» (2026-08-08).
      - name: 'гейт: пробники вызывают то, что существует (§7.3)'
        run: uv run python gates/probes_callable.py

      # Добавлено 2026-08-17: третье пришествие одного дефекта. `bars_per_timeframe` и
      # `admission_required_bars` лежали в конфигурации мёртвыми — загрузчик молча
      # пропускал неизвестные ключи. Проверяется подсаженным нарушением на каждом прогоне.
      - name: 'гейт: загрузчики TOML отвергают неизвестный ключ (§10.1)'
        run: uv run python gates/config_forbids_unknown.py

      # Добавлено 2026-08-17 по предписанию фальсификации: разбор «адрес или основание»
      # у ссылок §N жил в голове и его ноль был свойством критерия. Критерий записан
      # кодом: §N обязан существовать в FOUNDATION.md, императив о методике — нести
      # референт рядом. Подсаженное нарушение проверяется на каждом прогоне.
      - name: 'гейт: ссылки §N существуют, императив о методике с референтом'
        run: uv run python gates/foundation_basis.py

      # Добавлено 2026-08-17 (решение владельца: jesse-модель). Правило фантомной
      # минуты (v=0 вне OHLC) доказано зондом на 2.1 млн корзин; гейт держит его
      # регрессию на синтетике с подсаженным фантомом — живых данных в CI нет.
      - name: 'гейт: сборка ТФ из 1m держит правило фантомной минуты'
        run: uv run python gates/resample_agrees.py

      - name: 'гейт: каждый гейт вызывается в CI'
        run: uv run python gates/ci_covers_gates.py

      # ⚠ Добавлено 2026-08-10, и это НЕ дубль Dependabot. Замер того же дня: GitHub
      # строит граф зависимостей из `uv.lock` (SBOM отдаёт наши 67 пакетов) и алерты по
      # нему шлёт — но ЧИНИТЬ не умеет: `uv` в списке экосистем Dependabot отсутствует
      # (docs.github.com, «Supported ecosystems and repositories»), значит запрос на
      # слияние с исправлением он не создаст НИКОГДА. Настройка
      # `dependabot_security_updates: enabled` при этом стоит «включено» — то есть
      # включена проверка, которая ничего не делает. Ровно та форма дефекта, которую
      # проект ловит везде: зелёное, потому что не смотрели.
      #
      # `pip-audit` — от PyPA, сверяет с базой PyPI Advisory / OSV. Контроль
      # фальсифицируемости проведён: на замке до правки того же дня
      # (`aiohttp==3.14.1`, `cryptography==49.0.0`) он печатает 4 уязвимости и код 1,
      # на исправленном — «No known vulnerabilities found» и код 0.
      #
      # Только ubuntu: база уязвимостей одна на обе ОС, а сеть в шаге — плата за запрос.
      - name: 'гейт: в зависимостях нет известных уязвимостей'
        if: matrix.os == 'ubuntu-latest'
        run: |
          uv export --no-hashes --format requirements-txt > /tmp/requirements-audit.txt
          uv run --with pip-audit pip-audit -r /tmp/requirements-audit.txt

      - name: 'гейт: вселенная читается (§5)'
        run: |
          uv run python -c "from hunter.config import load_universe as l; u=l(); print('вселенная: символов', len(u.symbols), 'ТФ', len(u.timeframes))"

      - name: 'гейт: схема леджера и read-only (§10.2, §10.6)'
        run: |
          uv run python -m hunter ledger --init
          uv run python -m hunter ledger

# ЖИВОЙ ПРОГОН В CI НЕВОЗМОЖЕН, и это замер, а не предположение.
# Прогон 2026-08-03 на раннере GitHub: Binance ответил HTTP 451
#   "Service unavailable from a restricted location according to 'b. Eligibility'"
# То есть §7.5 «верификация — гейты в CI и живые прогоны» здесь расщепляется:
# гейты живут в CI, живые прогоны — только на машине владельца.
#   uv run python -m hunter run --seconds 400 --seed-limit 500
# Держать в CI постоянно красную работу нельзя: она обучает не смотреть на CI.
