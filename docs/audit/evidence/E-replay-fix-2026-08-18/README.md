# E-replay-fix-2026-08-18 — контроль прибора повтора и дифф правок 1+2

## Что здесь лежит

* `control-unchanged-code.txt` — повтор НЕИЗМЕНЁННОГО кода на свежей базе:
  «совпало 5/5 построчно», код возврата 0. Это контроль «способен ли прибор
  ответить иначе»: до починки 2026-08-18 тот же повтор печатал «ИЗМЕНИЛОСЬ 5 из 5»
  при неизменённом коде (профиль прогона строился из свечей, повтор — из сделок
  по срезу, который никто не писал; разбор — журнал смены, тик ~12:00).
* `replay-diff-1p2.txt` — дифф §10.6 правок 1 (полоса якоря стопа ≤ высота базы)
  и 2 (чистка пула целей от структур старше кадра 180 баров), снятый ПОСЛЕ
  контроля на тех же кадрах.

## Отпечаток данных (замер по живому рынку — обязателен, правило 2026-08-09)

Кадры: `data/frames/last`, сняты 2026-08-18 командой
`uv run python -m hunter run --symbols 5 --run-id last` на коде БЕЗ правок 1+2
(правки на время базы отложены `git stash`, затем возвращены). Символы и строки
сохранённых карточек: ASTR 13439, BCH 14589, BTC 15663, ETH 15320, POL 15220.
Повтор той же команды на другой день даст другие числа — рынок уехал; сверять
следует с этим отпечатком, а не с новым прогоном.

## Числа диффа (посчитаны по `replay-diff-1p2.txt`, скрипт — grep по строкам ±)

* карточки: 5 из 5 изменились; строк убрано 6466, добавлено 335;
* стопы: пересчитаны у 162 сигналов — 104 ушли с якоря на «запас за структуру
  (стр. 33)», 58 остались якорными, но с БЛИЖНИМ якорем (прокол или стоповый
  объём внутри усечённой полосы; дословно совпавших строк стопов в − и + нет);
* риск: медиана «% от входа» по этим 162 стопам 5.39% → 1.255%;
* цели: убрано 6153 строки (4508 «цель» + 1645 «промежуточная»), добавлено 0;
  22 сделки стали «целей нет».

## ⚠ Чем эти числа НЕ являются

Сжатие риска — СМЕНА КОНВЕНЦИИ стопа, а не выигрыш стратегии (находка №8 аудита
правки): 30 новых стопов из 162 ближе 0.5% от входа, 9 — ближе 0.2%, при
круговой комиссии порядка 0.1% у таких стопов заметную долю риска съедает
комиссия. Улучшение R:R по этому диффу НЕ ЗАЯВЛЯЕТСЯ; проверка исходами — только
на журнале, накопленном ПОСЛЕ правки (запрет мерить на выходе сломанного
прибора).
