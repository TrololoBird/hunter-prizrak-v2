# Обзор: НАКОПЛЕНИЕ (флэт, база, консолидация) — 2026-08-07

Обзор по команде `/survey накопление`. Элемент 4 очереди
[survey-catalog.md](survey-catalog.md). Наш модуль — `src/hunter/accumulation.py`,
требование — FOUNDATION §2.1.

**Первый обзор по новому порядку:** курс читается ТЕКСТОМ И РИСУНКОМ, а собственный код
читается критически — как продукт ИИ-агента, а не как эталон.

---

## Фаза 0. Вопросы, курс текстом, курс рисунком, как у нас

| # | вопрос | курс: ТЕКСТ | курс: РИСУНОК (стр. 18, 22) | как у нас | вердикт |
|---|---|---|---|---|---|
| **Н1** | что задаёт границу базы | стр. 18: «Границы базы (хай и лой) чаще всего определяются первыми 2-мя точками» | точки пронумерованы 1–10, первые две задают коробку | первые две точки каждой стороны | ✅ следуем |
| **Н2** | граница — цена или ЗОНА | молчит | **зона: коробка нарисована двумя линиями**, точки лежат в полосе, а не на одной цене | зона `lo…hi` из первых двух точек | ✅ следуем рисунку |
| **Н3** | сколько точек нужно | стр. 22: «4 и более точек»; стр. 23: «(4+6+ точки границ)» | на схеме их 10 | `MIN_BOUNDARY_POINTS = 4` | ✅ следуем |
| **Н4** | должны ли точки ЧЕРЕДОВАТЬСЯ | молчит | **на схеме строгое чередование: 1,3,5,7,9 сверху и 2,4,6,8,10 снизу** | чередования НЕ требуем | ⚠ **расхождение с рисунком** |
| **Н5** | что делает прокол за границу | стр. 18: «если на 3++ точках были проколы за границы - стоп всегда ставится за этот прокол, т.к. это может быть расширением базы и в будущем цена может ходить уже в этом расширенном диапазоне» | прокол на схеме не нарисован | точка засчитана, зона **заморожена навсегда**, глубина запомнена для стопа | ⚠ **расхождение с курсом** |
| **Н6** | что считается выходом | стр. 23: «Пока цена не вышла из структуры - у нас нет уровня»; стр. 55 — подтверждение телами | выход нарисован стрелкой вверх за коробку | 2 полных тела подряд за замороженной границей | ✅ следуем |
| **Н7** | отдаётся ли НЕЗАКРЫТАЯ структура | стр. 23 говорит, что уровня у неё нет | молчит | отдаётся отдельным типом `OpenStructure`, уровня не даёт | ✅ следуем |
| **Н8** | ограничение на ШИРИНУ диапазона | молчит | коробка на схеме узкая, но числа нет | порога нет, ширина ЗАМЕРЯЕТСЯ и сообщается | курс молчит |
| **Н9** | ограничение на ДЛИТЕЛЬНОСТЬ | молчит | молчит | ограничения нет | курс молчит |
| **Н10** | что если границы сошлись | молчит | молчит | сброс структуры, счётчик `resets` | курс молчит |
| **Н11** | есть ли ВНЕШНИЙ барьер за границей | молчит | **есть: отдельная красная линия снаружи каждой границы с подписью ЛИМИТНЫЙ БАРЬЕР** | нет вовсе | ⚠ **у курса есть, у нас нет** |

**Курс отвечает на 6 вопросов из 11** (текстом на 4, только рисунком на 2).
**Мы следуем курсу на 4 из 6.**

## Три находки, полученные ДО обращения к чужим проектам

### 1. Н5 — цитата стр. 18 приведена в модуле не за то, что она говорит

Докстрока `accumulation.py:10` утверждает: «стр. 18 прокол за границу — „расширение базы",
а не слом». Проверка по странице показывает другое: курс говорит, что прокол **может быть
расширением базы и в будущем цена может ходить уже в этом расширенном диапазоне**.

То есть источник допускает, что диапазон РАСШИРИТСЯ. Наш код расширение запрещает
навсегда: `BoundaryZone` заморожена первыми двумя точками, и комментарий в `detect`
говорит прямо — «зона НЕ расширена».

⚠ У заморозки есть НАСТОЯЩЕЕ основание, и оно другое: стр. 13 («локальные хаи/лои
повторяются и не меняются») плюс замер 2026-08-04, где расширение зоны каждым экстремумом
дало 39 верхних точек против 6 нижних и структуру, не закрывавшуюся 316 баров. Основание
верное — **неверна ссылка**: стр. 18 цитируется за противоположное.

Это ровно тот случай, ради которого введена презумпция: комментарий, объявляющий источник,
оказался гипотезой, а не фактом.

### 2. Н11 — «лимитный барьер» есть у курса на рисунке и отсутствует у нас

На схеме стр. 18 снаружи каждой границы коробки идёт **отдельная красная линия с подписью
ЛИМИТНЫЙ БАРЬЕР** (надпись на рисунке; в текстовом слое страницы её нет). Все точки границ
лежат МЕЖДУ коробкой и этим барьером.

У нас такой величины нет ни в `BoundaryZone`, ни где-либо ещё. Есть `puncture` — самый
дальний заход за зону, — но это ЗАМЕРЕННАЯ величина постфактум, а барьер на схеме нарисован
как заданная граница, за которую точки не выходят.

### 3. Н4 — чередование точек нарисовано, но не требуется у нас

Нумерация на схеме строго чередуется: 1,3,5,7,9 сверху, 2,4,6,8,10 снизу. Наш `detect`
принимает точки в любом порядке и допускает несколько подряд с одной стороны.

⚠ Это перекликается с уже проверенным: в обзоре свингов гипотеза «навязать чередование»
была доведена до диффа повтора и **отвергнута нулём** (43 против разброса 38…45). Но там
чередование навязывалось СВИНГАМ, а здесь речь про точки ГРАНИЦ — другое множество, и
перенос вывода без замера был бы подменой.

---

## Что дальше

Фазы 1–6 (сбор не меньше 10 чужих реализаций, каталог, сверка, гипотезы, замер) —
следующим шагом. Имена предмета для сбора: *range*, *consolidation*, *base*, *box*,
*darvas box*, *rectangle*, *trading range* (Вайкофф), *accumulation/distribution schematic*.

## Команды воспроизведения (§7.3)

Числа фазы 0 получены чтением источника и уже проведёнными замерами. Проверяется так:

```bash
uv run --group tools python -c "import pymupdf; d=pymupdf.open('docs/course/Мини Курс по трейдингу от PrizrakTrade.pdf'); [p.get_pixmap(dpi=140).save(f'docs/course/pages/p{i:02d}.png') for i,p in enumerate(d,1)]"
```

После рендера открыть `docs/course/pages/p18.png` и `docs/course/pages/p22.png` — на них
видны нумерация точек 1–10 с чередованием и красная линия с подписью «лимитный барьер».
Текстовый слой тех же страниц:

```bash
uv run --group tools python -c "import pymupdf; d=pymupdf.open('docs/course/Мини Курс по трейдингу от PrizrakTrade.pdf'); print(d[17].get_text())"
```

Числа про отвергнутое чередование свингов (43 против разброса 38…45) — из уже
проведённого замера:

```bash
uv run python docs/audit/probes/probe_swing_alternation_seeds_2026-08-07.py
```

Числа 39/6/316 из замера 2026-08-04 приведены по комментарию в `accumulation.py:38`; их
собственный протокол лежит в невлитой ветке аудита, и здесь они служат объяснением, а не
результатом обзора.
