# Обзор: НАКОПЛЕНИЕ (флэт, база, консолидация) — 2026-08-07

Обзор по команде `/survey накопление`. Элемент 4 очереди
[survey-catalog.md](survey-catalog.md). Наш модуль — `src/hunter/accumulation.py`,
требование — FOUNDATION §2.1.

**Первый обзор по новому порядку:** курс читается ТЕКСТОМ И РИСУНКОМ, а собственный код
читается критически — как продукт ИИ-агента, а не как эталон.

> ## ⚠⚠ ОТМЕНА ВЫВОДОВ, 2026-08-07, вечер
>
> **Всё, что ниже фазы 0, читайте после этого раздела.** Обзор был проведён с нарушенной
> иерархией источников: рисунок курса я взял как гипотезу под нуль, а докстроку и
> собственный замер — как опору. Владелец указал на это прямо. Правильный порядок записан
> теперь в [CLAUDE.md](../../CLAUDE.md) и в команде `/survey`: **PDF курса — истина
> целиком; чего в нём нет — из корпуса разборов и веб-поиска; код и докстроки истиной не
> являются.**
>
> Курс перечитан заново: **59 страниц, текстом и картинкой** (было четыре, и выбрал их не
> я, а ссылки в докстроке модуля). Из 278 находок отменяются **четыре опубликованных
> вывода из одиннадцати**, и появляются два новых расхождения с курсом.

## Что оказалось неверно

| вопрос | было опубликовано | как на самом деле |
|---|---|---|
| **сколько отвечает курс** | было: на 8 вопросов из 11 | **на все 11** |
| **Н4** чередование | было: гипотеза, мера насыщена, вердикта нет, вопрос владельцу | **требование курса** — 13 схем без исключения. Внесено в код, дифф повтора предъявлен |
| **Н5** прокол | было: расходимся с курсом, и ноль из 14 чужих замораживает границу | **следуем курсу**: на схемах стр. 13, 19, 36, 40 проколовшая точка получает обычный номер, а коробка остаётся на прежней цене. Текст стр. 18 про расширение относится к СТОПУ |
| **Н8, Н9** пороги ширины и длительности | было: курс молчит, у чужих есть, у нас нет — кандидат в гипотезу | **курс отвечает: числового ограничения нет.** Размер меняет только способ набора позиции (стр. 30), силу задают ТФ и объём (стр. 22). Мы следуем |
| **Н10** схождение границ | было: делаем как большинство, ничего не нужно | **расхождение с курсом**: сужение — законный вид базы (стр. 34, 37), а мы такую структуру сбрасываем |

### Почему «Н5» был перевёрнут — разбор ошибки

Я сверил докстроку с ТЕКСТОМ стр. 18, увидел расхождение и объявил находку. На схемах той
же темы — стр. 13, 19, 36, 40 — прокол нарисован: точка получает номер в общей
последовательности, линия не двигается. То есть **картинка отвечала на вопрос прямо, а я
её не открыл**, потому что искал подтверждение текстом. Вывод «ноль из четырнадцати
замораживает границу, значит мы изобрели своё» был построен на чужих проектах при живом
ответе источника.

## Что курс говорит на самом деле — по всем одиннадцати

| вопрос | ответ КУРСА | у нас |
|---|---|---|
| **Н1** граница | первые две точки своей стороны (стр. 18); но границей бывает и уровень старшего ТФ (стр. 46, 54), и ПОК прежнего стопового объёма (стр. 39), и граница предыдущей структуры — «лесенка» (стр. 40) | **внесено 2026-08-08**: лесенка плюс второй проход движка за чужой границей ✅ |
| **Н2** цена или зона | ребро коробки везде нарисовано ОДНОЙ тонкой линией; зона есть у ПОК и у ПП, но не у границы базы | **внесено 2026-08-08**: `BoundaryZone.edge` — одна цена ✅ |
| **Н3** сколько точек | «4 и более точек» (стр. 22); на стр. 21 размечено ровно четыре, лишние касания внутри коробки не нумеруются | `MIN_BOUNDARY_POINTS = 4` ✅ |
| **Н4** чередование | обязательно, 13 схем; сторону первой точки задаёт направление входа цены | **внесено 2026-08-07** ✅ |
| **Н5** прокол | точку засчитывает, границу НЕ двигает | ✅ следуем |
| **Н6** выход | «2-3 полных тел свечей ЭТОГО ТФ» (стр. 55); ретест и продолжение относятся к УРОВНЮ после пробоя, а не к выходу | ✅ следуем; ретест и флип — в `levels.py` |
| **Н7** уровень у невышедшей | уровня нет (стр. 23), но границами она торгуется | ✅ следуем |
| **Н8** ширина | числового предела НЕТ | ✅ следуем |
| **Н9** длительность | числового предела НЕТ, вместо него привязка к ТФ | ✅ следуем |
| **Н10** сужение | курс называет это базой (стр. 34); на стр. 37 границы наклонные | **внесено 2026-08-08**: граница едет внутрь, `BoundaryZone.narrowed` ✅ |
| **Н11** лимитный барьер | только надпись на схемах стр. 18 и 19, объяснения курс не даёт; на прочих страницах снаружи границы — зона стопа 1-3% | нет вовсе |

### Цитаты, на которых держатся отменённые выводы

Н8, ширина — курс отказывается от порога сознательно, стр. 30:
«Если накопление очень большое ТФ 1Д-1Н-1М - то закуп всегда стоит делить на зону и на
уровень.» Размер меняет способ входа, а не право называться накоплением.

Н9, длительность — стр. 20: «Флет можно торговать на любом таймфрейме.»

Н10, сужение — стр. 34: «Стоповый объем может быть в сужении, в поджатии, с четкими
границами – это такое же накопление (база), но на более мелком ТФ, чем основное движение
актива, и обычно более «плотное» (часто большой объем в мелком диапазоне).»

## Семнадцать правил курса, которых обзор не спрашивал вовсе

Одиннадцать вопросов фазы 0 я вывел из НАШЕГО модуля — то есть повестку опять задал код.
Перечитывание курса дало правила, которых в списке не было. Ниже те, что касаются прямо
этого модуля:

1. **Износ уровня.** После первой отработки уровень удаляется; вход на 2-3 касании — только
   по слому структуры на младшем ТФ (стр. 25, 31).
2. **Ловушка как отдельный исход.** Пробой, не выбивший стоп, переворачивает уровень; тест
   с обратной стороны — точка входа (стр. 27, 29, 43, 45).
3. **Вложенность структур.** Внутри большой базы живут структуры младших ТФ, каждая со
   своим уровнем; младшие коробки к границам старшей не привязаны (стр. 20, 32, 36, 41).
4. **Несколько уровней у одной структуры — норма** (стр. 30, 31, 32, 36).
5. **Приоритет выхода** приписывается накоплению ещё во время формирования, по тренду и
   старшему ТФ (стр. 19, 39).
6. **Границы накопления — полноценные уровни**: к ним применяется тот же аппарат
   прокол/пробой/подтверждение, что и к ПОК (стр. 55).
7. **Стоповый объём — вид накопления**, торгуется как обычная база (стр. 34, 35).
8. **Лесенка структур**: граница предыдущей становится границей следующей (стр. 40).

Часть из них уже реализована в других модулях; проверка, какие именно, — отдельная работа,
и делать её надо снова от курса, а не от кода.

## Что сделано по итогам отмены

* **Н4 внесено в код.** `accumulation.detect` требует чередования; дифф повтора на трёх
  символах: BCH 151→145 строк, BTC 274→290, ETH 226→240. Разметка перестроилась заметно —
  у ETH 4ч уровень 1880 из активного стал пробитым и флипнутым, рядом появился новый
  шортовый 1925.
* **Докстрока про прокол исправлена во второй раз**: заморозка границы — требование курса,
  а не наше умолчание.
* **Иерархия источников записана** в `CLAUDE.md` и в `/survey` вместе с тремя запретами.

---

## Н1 и Н6, исполнено 2026-08-08

### Н6 — расхождения нет, и мой прежний вывод по нему был неверен

⚠ **Отмена собственного утверждения из таблицы выше.** Там записано: «курс требует ретеста
и продолжения сверх двух тел; у нас только тела». Страницы 42, 43, 45 и 28, прочитанные
глазами, говорят о другом — это глава ЛОВУШКИ, то есть что делать с уровнем ПОСЛЕ пробоя, а
не чем подтверждается выход из структуры. Я взял пересказ и склеил два разных предмета.

Ретест и флип у нас есть давно: `src/hunter/levels.py` несёт состояния `FLIPPED` и
`RETEST_FLIPPED` с дословной цитатой стр. 43.

| требование стр. 55 к выходу | как у нас |
|---|---|
| «2-3 полных тел свечей ЭТОГО ТФ» | `CONFIRM_BODIES = 2` — внутри диапазона |
| тело ЦЕЛИКОМ за границей, а не закрытие | `body_beyond` берёт `min/max(open, close)` |
| возврат той же или следующей свечой = прокол без подтверждения | исключён по построению: двумя полными телами подряд ни первая, ни вторая свеча внутрь не вернулась |
| смотреть на ТФ самой структуры (стр. 40, 46) | `detect` работает на барах своего ТФ; тень подтвердить выход не может |

Открыт единственный вопрос — выбор ВНУТРИ диапазона курса, и сам код это признаёт.
Замерено на 324 рядах:

| | 2 тела | 3 тела |
|---|---:|---:|
| закрытых накоплений | 5623 | 5227 |
| та же структура есть в обеих разметках | — | 1403 из 2976 |
| подтверждение позже | — | медиана 1 бар, максимум 30 |
| структур исчезло | — | **1573 (52.9%)** |
| структур появилось | — | 1385 |

**Кода это не меняет:** обе величины исполняют одно требование курса. Рекомендация —
оставить 2: тройка отсекает больше половины структур, а курс её лишь допускает, не
предписывая.

⚠ Первая редакция зонда печатала «совпало побитово: 0» как результат. Это число не могло
быть другим — полный ключ содержит бар подтверждения, а он сдвигается по построению.
Строка оставлена в зонде вместе с разоблачением.

### Н1 — «лесенка» внесена, две другие чужие границы отложены и почему

Стр. 40, рисунок: цена выходит вверх из коробки, и следующая коробка стоит НА её верхнем
ребре — верхнее ребро прежней структуры служит нижним ребром новой. Обе подписаны
«Структура 4ч тф», то есть лесенка складывается внутри одного ряда и одного ТФ.

Внесено: ребро, через которое цена вышла, предлагается следующей структуре как её
противоположное ребро — если попадает между её первыми двумя точками этой стороны. Новой
константы не введено, причинность не нарушена (прежняя структура закрыта и лежит слева).

**Контроль живости: 1728 накоплений из 5623 (30.7%) получили унаследованную границу.**

Дифф повтора: BCH 245→247 строк, BTC 371→381, ETH 339→329.

⚠ **Не сделано:** ПОК прежнего стопового объёма как граница (стр. 39) и уровень старшего ТФ
как граница (стр. 46, 54). Обе требуют ВТОРОГО прохода движка: уровни считаются ИЗ структур
(`levels.build_all` принимает `scans`), значит подать уровни обратно в структуры можно лишь
вторым проходом. У проекта уже есть прецедент ровно такой ошибки — контрольный скрипт
считал старшие ТФ позже младших, — поэтому вслепую, без предъявленной схемы порядка, я это
не трогал. Это следующий по величине пункт и он требует решения владельца о порядке.

### Сводка правок по курсу, 2026-08-07…08

| вопрос | что сделано | цена (дифф повтора, строк) |
|---|---|---|
| **Н4** чередование точек | внесено | BCH 151→145, BTC 274→290, ETH 226→240 |
| **Н2** граница — одна цена | внесено | BCH 145→233, BTC 290→363, ETH 240→348 |
| **Н10** сужение — тоже база | внесено | BCH 233→245, BTC 363→371, ETH 348→339 |
| **Н1** лесенка | внесено | BCH 245→247, BTC 371→381, ETH 339→329 |
| **Н6** выход | расхождения нет | — |

Живость каждой новой ветки проверена отдельно: сужение 642 структуры из 5524 (11.6%),
лесенка 1728 из 5623 (30.7%), прокол 277 зон из 636.

---

## Второй проход движка за чужой границей, 2026-08-08

Курс называет границей базы чужой уровень дважды и в РАЗНЫЕ стороны:

* **стр. 39** — ПОК стопового объёма МЛАДШЕГО ТФ держит цену, и на нём строится новая
  большая база: «Цена формирует стоповый объем, который держит цену, и не дает выйти вниз –
  и на его зоне/ПОКе формирует новое большое накопление»;
* **стр. 46 и 54** — уровень СТАРШЕГО ТФ держит младшую базу.

Уровни считаются ИЗ структур (`levels.build_all` принимает `scans`), поэтому подать их
обратно в структуры можно только вторым проходом.

### Устройство: два прохода, и порядок ТФ не может навредить ПО ПОСТРОЕНИЮ

```
проход 1:  ряды → структуры (без чужих границ) → уровни
           ↓ цены уровней + момент, с которого каждый известен
проход 2:  ряды → структуры (с чужими границами) → уровни  ← это и есть ответ
```

Все чужие цены берутся из ПРОХОДА 1, где чужих границ не было ни у кого. Поэтому вход
прохода 2 для любого ТФ не зависит ни от одного другого ТФ прохода 2 — переставь список
ТФ как угодно, ответ тот же. Проходов ровно два, до неподвижной точки никто не итерирует.

⚠ Это именно то место, где у проекта уже был дефект — «контрольный скрипт считал старшие ТФ
позже младших». Здесь он невозможен не по внимательности, а по устройству, и это
проверяется зондом, а не декларируется.

### ⚠ В собственной правке найдено заглядывание в будущее, и это было ДО замера

Первая редакция подавала анкеры одними ценами, без времени. Уровень чужого ТФ рождается на
баре, где его структура подтвердила выход; взять его границей базы, начавшейся РАНЬШЕ, —
опора на цену, которой в тот момент на графике ещё не было. Это I-5.

Дефект того же класса уже ловили: 2026-08-04 внешний разбор нашёл **196 целей из 366
(54%)**, созданных ПОЗЖЕ уровня, от которого строилась сделка, — ради этого в `Level` и
завели `created_at_ms`. Теперь анкер идёт парой (цена, момент известности) и допускается,
только если известен до открытия бара, на котором граница фиксируется.

Замер запускался уже после починки: числа ниже относятся к версии БЕЗ заглядывания.

### ⚠ Первая редакция контроля П2 почти ничего не проверяла

Она переставляла ТФ на входе `decide` — а `decide` сортирует их первой же строкой, так что
перестановка гасла раньше, чем доходила до механизма чужих границ. Проверка подтверждала бы
сортировку, а не устройство. Теперь порядок ломается внутри самого прохода 2, а сверка
карточки оставлена вторым, более слабым слоем.

### Что НЕ покрыто замером, и это названо числом

Каждый символ стоит четырёх полных проходов движка. Полный корпус из 25 наборов кадров
считался больше десяти минут, четыре набора — тоже. Замер идёт по ОДНОМУ набору; сколько
осталось за бортом, зонд печатает сам. Для П2 одного достаточно: независимость от порядка —
свойство устройства, а не статистики. Для П1 одного мало, и число там — нижняя оценка.

### Числа

| контроль | результат |
|---|---|
| **П1, живость** | границ, изменившихся ТОЛЬКО из-за второго прохода — **148**; совпавших с прежними — 75 |
| **П2, порядок ТФ** | разметка прохода 2 и карточка совпали побитово при обратном порядке ТФ на всех 3 символах ✅ |

148 из сотен границ — ветка живая и при этом НЕ поглотившая разметку. Если бы сработало на
большинстве, это значило бы, что правило применено слишком широко и его надо сужать.

**Дифф повтора** (§10.6 условие 2), run-id `alt-base`, база пересобрана кодом БЕЗ второго
прохода и проверена на совпадение сама с собой:

| символ | было | стало |
|---|---:|---:|
| BCH/USDT:USDT | 247 | 237 |
| BTC/USDT:USDT | 381 | 410 |
| ETH/USDT:USDT | 329 | 321 |

Команда воспроизведения контролей:

```bash
uv run python docs/audit/probes/probe_second_pass_2026-08-08.py
```

⚠ **Цена в машинном времени названа отдельно:** повтор стал считаться примерно вдвое
дольше — проход 1 строит уровни целиком, а профиль объёма и есть самая дорогая часть
конвейера. Дешевле не выходит: чужая граница это ПОК, а ПОК без профиля не посчитать.

## Что осталось владельцу — и это уже не «менять ли», а «как»

⚠ **Этот список закрыт 2026-08-08 — все четыре пункта разобраны выше.** Оставлен как есть,
чтобы было видно, что именно из него оказалось верным, а что нет:

1. **Н10, сужение** — внесено, но НЕ так, как здесь написано: «мы сбрасываем структуру»
   было предположением, а замер показал другое место — отказ «касание внутри базы».
2. **Н2, граница цена или зона** — внесено; напряжение внутри курса снято чтением стр. 39.
3. **Н1, чужая граница** — внесена лесенка (стр. 40); две другие формы отложены с
   названной причиной.
4. **Н6, ретест и продолжение** — пункт НЕВЕРЕН: ретест относится к уровню после пробоя, а
   не к выходу из структуры, и реализован в levels.py.

**Закрыто 2026-08-08 вторым проходом движка** — см. раздел выше. Порядок ТФ решать не
пришлось: устройство сделано так, что от него ничего не зависит.

---

---

## Фаза 0. Вопросы, курс текстом, курс рисунком, как у нас

| # | вопрос | курс: ТЕКСТ | курс: РИСУНОК (стр. 18, 22) | как у нас | вердикт |
|---|---|---|---|---|---|
| **Н1** | что задаёт границу базы | стр. 18: «Границы базы (хай и лой) чаще всего определяются первыми 2-мя точками» | точки пронумерованы 1–10, первые две задают коробку | первые две точки каждой стороны | ✅ следуем |
| **Н2** | граница — цена или ЗОНА | молчит | **зона: коробка нарисована двумя линиями**, точки лежат в полосе, а не на одной цене | зона `lo…hi` из первых двух точек | ✅ следуем рисунку |
| **Н3** | сколько точек нужно | стр. 22: «4 и более точек»; стр. 23: «(4+6+ точки границ)» | на схеме их 10 | `MIN_BOUNDARY_POINTS = 4` | ✅ следуем |
| **Н4** | должны ли точки ЧЕРЕДОВАТЬСЯ | молчит | **на схеме строгое чередование: 1,3,5,7,9 сверху и 2,4,6,8,10 снизу** | чередования НЕ требуем | ⚠ **расхождение с рисунком** |
| **Н5** | что делает прокол за границу | стр. 18: «если на 3++ точках были проколы за границы - стоп всегда ставится за этот прокол, т.к. это может быть расширением базы и в будущем цена может ходить уже в этом расширенном диапазоне» | прокол на схеме не нарисован | точка засчитана, зона **заморожена навсегда**, глубина запомнена для стопа | ⚠ **расхождение с курсом** |
| **Н6** | что считается выходом | стр. 23: «Пока цена не вышла из структуры - у нас нет уровня»; стр. 55 — подтверждение телами | выход нарисован стрелкой вверх за коробку | 2 полных тела подряд за замороженной границей | ✅ следуем |
| **Н7** | отдаётся ли НЕЗАКРЫТАЯ структура | стр. 23 говорит, что уровня у неё нет | молчит | отдаётся отдельным типом `OpenStructure`, уровня не даёт | ✅ следуем |
| **Н8** | ограничение на ШИРИНУ диапазона | молчит | коробка на схеме узкая, но числа нет | порога нет, ширина ЗАМЕРЯЕТСЯ и сообщается | курс молчит |
| **Н9** | ограничение на ДЛИТЕЛЬНОСТЬ | молчит | молчит | ограничения нет | курс молчит |
| **Н10** | что если границы сошлись | молчит | молчит | сброс структуры, счётчик `resets` | курс молчит |
| **Н11** | есть ли ВНЕШНИЙ барьер за границей | молчит | **есть: отдельная красная линия снаружи каждой границы с подписью ЛИМИТНЫЙ БАРЬЕР** | нет вовсе | ⚠ **у курса есть, у нас нет** |

**Курс отвечает на 8 вопросов из 11** (текстом на 5 — Н1, Н3, Н5, Н6, Н7; только рисунком
на 3 — Н2, Н4, Н11). **Мы следуем курсу на 5 из 8** (Н1, Н2, Н3, Н6, Н7), расходимся на 3
(Н4, Н5, Н11).

⚠ **Исправлено 2026-08-07 при сверке фазы 3.** В первой редакции здесь стояло «отвечает на
6 из 11 (текстом на 4, рисунком на 2), следуем на 4 из 6» — пересчёт по строкам таблицы
выше даёт 8 и 5. Ошибка была в шапке, не в таблице: сама таблица с самого начала помечала
восемь строк как отвеченные. Это ровно тот случай, ради которого главные числа фазы
выносятся отдельной строкой — иначе несходящуюся сумму никто не проверяет.

## Три находки, полученные ДО обращения к чужим проектам

### 1. Н5 — цитата стр. 18 приведена в модуле не за то, что она говорит

Докстрока `accumulation.py:10` утверждает: «стр. 18 прокол за границу — „расширение базы",
а не слом». Проверка по странице показывает другое: курс говорит, что прокол **может быть
расширением базы и в будущем цена может ходить уже в этом расширенном диапазоне**.

То есть источник допускает, что диапазон РАСШИРИТСЯ. Наш код расширение запрещает
навсегда: `BoundaryZone` заморожена первыми двумя точками, и комментарий в `detect`
говорит прямо — «зона НЕ расширена».

⚠ У заморозки есть НАСТОЯЩЕЕ основание, и оно другое: стр. 13 («локальные хаи/лои
повторяются и не меняются») плюс замер 2026-08-04, где расширение зоны каждым экстремумом
дало 39 верхних точек против 6 нижних и структуру, не закрывавшуюся 316 баров. Основание
верное — **неверна ссылка**: стр. 18 цитируется за противоположное.

Это ровно тот случай, ради которого введена презумпция: комментарий, объявляющий источник,
оказался гипотезой, а не фактом.

### 2. Н11 — «лимитный барьер» есть у курса на рисунке и отсутствует у нас

На схеме стр. 18 снаружи каждой границы коробки идёт **отдельная красная линия с подписью
ЛИМИТНЫЙ БАРЬЕР** (надпись на рисунке; в текстовом слое страницы её нет). Все точки границ
лежат МЕЖДУ коробкой и этим барьером.

У нас такой величины нет ни в `BoundaryZone`, ни где-либо ещё. Есть `puncture` — самый
дальний заход за зону, — но это ЗАМЕРЕННАЯ величина постфактум, а барьер на схеме нарисован
как заданная граница, за которую точки не выходят.

### 3. Н4 — чередование точек нарисовано, но не требуется у нас

Нумерация на схеме строго чередуется: 1,3,5,7,9 сверху, 2,4,6,8,10 снизу. Наш `detect`
принимает точки в любом порядке и допускает несколько подряд с одной стороны.

⚠ Это перекликается с уже проверенным: в обзоре свингов гипотеза «навязать чередование»
была доведена до диффа повтора и **отвергнута нулём** (43 против разброса 38…45). Но там
чередование навязывалось СВИНГАМ, а здесь речь про точки ГРАНИЦ — другое множество, и
перенос вывода без замера был бы подменой.

---

## Команды воспроизведения (§7.3)

Числа фазы 0 получены чтением источника и уже проведёнными замерами. Проверяется так:

```bash
uv run --group tools python -c "import pymupdf; d=pymupdf.open('docs/course/Мини Курс по трейдингу от PrizrakTrade.pdf'); [p.get_pixmap(dpi=140).save(f'docs/course/pages/p{i:02d}.png') for i,p in enumerate(d,1)]"
```

После рендера открыть `docs/course/pages/p18.png` и `docs/course/pages/p22.png` — на них
видны нумерация точек 1–10 с чередованием и красная линия с подписью «лимитный барьер».
Текстовый слой тех же страниц:

```bash
uv run --group tools python -c "import pymupdf; d=pymupdf.open('docs/course/Мини Курс по трейдингу от PrizrakTrade.pdf'); print(d[17].get_text())"
```

Числа про отвергнутое чередование свингов (43 против разброса 38…45) — из уже
проведённого замера:

```bash
uv run python docs/audit/probes/probe_swing_alternation_seeds_2026-08-07.py
```

Числа 39/6/316 из замера 2026-08-04 приведены по комментарию в `accumulation.py:38`; их
собственный протокол лежит в невлитой ветке аудита, и здесь они служат объяснением, а не
результатом обзора.

---

## Фаза 1. Сбор чужих реализаций

Интернет проверен первым делом: `github.com`, `mql5.com` и веб-поиск отвечают.
`grep.app` вернул `HTTP 429`, API GitHub для поиска по коду — `HTTP 401`, поэтому поиск по
коду шёл клонированием и локальным грепом, как в предыдущих обзорах.

### Поисковые запросы, дословно (все 2026-08-07)

1. `github python code detect trading "range" OR "consolidation" detection indicator source support resistance box repository`
2. `github "darvas box" indicator source code python OR pine repository`
3. `github repository "wyckoff" accumulation distribution schematic detection code python source trading range spring`
4. `github pine script open source "range detection" OR "range box" OR "consolidation box" indicator code pivot highs lows range breakout`
5. `github repository code "rectangle pattern" OR "trading range" detection algorithm python swing highs lows tolerance percent horizontal support resistance`
6. `github "smart money concepts" python OR pine library code detect range OR "equal highs" OR "premium discount" swing structure source`
7. `mql5.com code base free indicator source "darvas" OR "flat detection" OR "range box" OR "consolidation" mq5 download`
8. `mql5 code base free indicator "flat" OR "range detection" OR "sideways" market detector mq5 source download 2020..2025`
9. `github python repository detect "support and resistance" zones clustering price levels touches code algorithm open source library`
10. `github python code "consolidation" detection function highs lows within percent threshold breakout stock screener repository source`
11. `mql5 code base free "Support and Resistance" OR "Price Zones" OR "Rectangle" indicator mq5 source code download 2022 2023 2024`
12. `github repository pine script open source "accumulation" OR "trading range" OR "range detection" indicator .pine file swing pivot box breakout 2024 2025`

**Имён предмета перебрано девять:** *range*, *consolidation*, *base*, *box*, *darvas box*,
*rectangle*, *trading range* (Вайкофф), *accumulation/distribution schematic*, *sideways/flat*.
**Каналов перебрано шесть:** поиск репозиториев GitHub, тематические страницы GitHub,
веб-поиск, CodeBase на mql5.com (загрузка архивом), зеркала Pine внутри репозиториев,
коллекции индикаторов.

### Команды загрузки

```bash
for r in white07S/TradingPatternScanner Eesita/Wyckoff-AI-Assistant \
         joshyattridge/smart-money-concepts day0market/support_resistance \
         pranjal-joshi/Screeni-py fmzquant/strategies; do
  git clone --depth 1 "https://github.com/$r.git" "$(echo $r | tr '/' '_')"
done
for id in 21706 498 7771 37415 57684 74421 74492 45132; do
  curl -sSL -o "$id.zip" "https://www.mql5.com/en/code/download/$id"
done
```

⚠ **CodeBase снова отдал архив там, где страница не отдаёт исходник.** Это тот самый канал,
который в обзоре прокола против пробоя я сперва объявил непроходимым; теперь он вошёл в
порядок работы и дал восемь файлов из восемнадцати.

⚠ **Скачанное только читалось.** Ни компиляции, ни запуска, ни установки зависимостей
чужих проектов — правило команды и хук `.claude/hooks/guard_destructive.py`.

### Независимость: не сравнение текстов, а сравнение хешей

Восемнадцать ядер, нормализация (комментарии, отступы, пробелы внутри строк, имена
переменных → `v0,v1,…`, префиксы пространств имён Pine v5), сравнение sha1.

```bash
uv run python docs/audit/probes/probe_accumulation_clones_2026-08-07.py <каталог со скачанным>
```

| | |
|---|---:|
| ядер прочитано | 18 |
| различных хешей | 16 |
| **семей клонов найдено хешем** | **1** (3 копии) |
| копий, найденных хешем | 2 |
| копий, найденных ЧТЕНИЕМ сверх хеша | 2 |
| **засчитано независимых реализаций** | **14** |

**Семья, найденная хешем** — три Pine-скрипта Дарваса с хешем `e959c55f6b86`:
[Darvas-Box-Buy-Sell](https://github.com/fmzquant/strategies/blob/master/Darvas-Box-Buy-Sell.md),
Darvas-Box-Breakout-and-Risk-Management (порт на v5) и 52-Week-High-Low-Box (тот же код на
дневных данных). Формула `TopBox = valuewhen(barssince(high > k1[1]) == boxp - 2 and box1, NH, 0)`
у всех троих совпадает посимвольно после нормализации.

⚠ **Хеш нашёл не всё, и это записано как предел метода, а не как ноль.** Три реализации
Дарваса на MQL — [21706](https://www.mql5.com/en/code/21706), [498](https://www.mql5.com/en/code/498)
и [7771](https://www.mql5.com/en/code/7771) — суть одна и та же машина из пяти состояний с
теми же условиями, но 21706 и 7771 записаны цепочкой `if`, а 498 — через `switch`. Хеши
разошлись; совпадение видно чтением. Прибор печатает эту границу сам отдельной строкой
контроля, чтобы «семей: 1» нельзя было прочесть как «копий больше нет».

Контроль прибора (печатается всегда, без него ноль ничего не значит): переименование
переменных, отступы и комментарии дают ОДИН хеш; порт Pine v4→v5 даёт ОДИН хеш; разная
логика даёт РАЗНЫЕ хеши.

### Итог сбора

| | |
|---|---:|
| попыток загрузки/клонирования | 20 |
| просмотрено проектов | 23 |
| **засчитано по КОДУ** | **14** ✅ предел 10 взят |
| отсеяно как несамостоятельные | 4 копии |
| отсеяно по прочим причинам | 5 |
| собрано ПО ОПИСАНИЮ (в счёт не идёт) | 4 |

**Отсеяно по прочим причинам, поимённо:**

* [Gifted87/TradingBot](https://github.com/Gifted87/TradingBot) — README обещает разбор по
  Вайкоффу, в коде разбора нет: файлы грузятся в Gemini и «анализ» делает модель по
  промпту. Расчёта в коде нет — не реализация;
* [arabacibahadir/sup-res](https://github.com/arabacibahadir/sup-res) и
  [sheevv/find-swing-highs-swing-lows](https://github.com/sheevv/find-swing-highs-swing-lows) —
  детекторы свингов, предмета обзора (диапазона) в них нет;
* [harryguiacorn/TradingView-Proprietary-Indicators](https://github.com/harryguiacorn/TradingView-Proprietary-Indicators),
  [LouisLetcher/quant-pine](https://github.com/LouisLetcher/quant-pine),
  [everget/tradingview-pinescript-indicators](https://github.com/everget/tradingview-pinescript-indicators),
  [ArunKBhaskar/PineScript](https://github.com/ArunKBhaskar/PineScript) — коллекции Pine без
  единого детектора диапазона: полосы, осцилляторы, ликвидность ICT;
* `getupandCROW/TradingView` — репозиторий не найден (`404`).

### Собрано ПО ОПИСАНИЮ (отдельная колонка, в счёт НЕ идёт)

Исходник закрыт либо не отдаётся страницей, но правило заявлено:

| источник | заявленное правило | к какому вопросу |
|---|---|---|
| [Consolidation Boxes](https://www.tradingview.com/script/bZgrdgUs-Consolidation-Boxes/) | коробка рисуется, когда серия свечей ЗАКРЫВАЕТСЯ внутри заданного диапазона; число свечей — параметр | Н3, Н6 |
| [Range & Consolidation Detector](https://www.tradingview.com/script/Jr95X651-Range-Consolidation-Detector/) (everget) | зона низкой волатильности внутри полосы шириной в ATR вокруг средней | Н1, Н8 |
| [Rectangle Breakout Patterns](https://kr.tradingview.com/script/ccfxajNI-Rectangle-Breakout-Patterns) | пивоты → подсчёт КАСАНИЙ уровня → прямоугольник | Н1, Н3 |
| [Wyckoff Phase Classifier](https://www.tradingview.com/script/9ap94IDt-Wyckoff-Phase-Classifier/) | последовательность SC → AR → ST → Spring → Test внутри торгового диапазона | Н5 |

⚠ Все четыре заявляют то же, что уже есть в выборке по коду, и ни один не заявляет
ни заморозки границ, ни внешнего барьера.

---

## Фаза 2. Каталог: четырнадцать реализаций

Сгруппированы по ответу на ГЛАВНЫЙ вопрос элемента — **Н1, что задаёт границу базы**.
Три семейства, значит главный вопрос выбран верно (все двадцать в одном семействе означали
бы обратное).

### Семейство А. Границу задают ЭКСТРЕМУМЫ ОКНА БАРОВ — 7 из 14

| проект | язык | ★ / дата | лицензия | Н1: граница | Н6: выход |
|---|---|---|---|---|---|
| [white07S/TradingPatternScanner](https://github.com/white07S/TradingPatternScanner) `detect_channel` | Python | ★302 / 2023-08-02 | своя | `rolling(window).max()` и `.min()` по High/Low | не отвечает |
| [pranjal-joshi/Screeni-py](https://github.com/pranjal-joshi/Screeni-py) `validateConsolidation` | Python | ★697 / 2026-08-05 | MIT | max и min ЗАКРЫТИЙ за окно | не отвечает |
| [BreakOutBox](https://www.mql5.com/en/code/37415) (DarkRyd3r) | MQL5 | 2021-12-07 | CodeBase | бегущие max/min ЗАКРЫТИЙ | закрытие за коробкой, 1 бар |
| [Darvas Box](https://github.com/fmzquant/strategies/blob/master/Darvas-Box-Buy-Sell.md) (ceyhun), семья из 3 | Pine | 2022-05-31 | MPL-2.0 | хай, не перебитый `boxp-2` бара; низ = `lowest(low, boxp)` | `crossover(close, TopBox)`, 1 закрытие |
| [Darvas_Box](https://www.mql5.com/en/code/21706) (Scriptor/GODZILLA), семья из 3 | MQL4/5 | 2008…2018 | CodeBase | машина из 5 состояний по high/low | не отвечает (только рисует) |
| [Ranging Market Detector](https://www.mql5.com/en/code/57684) (phade) | MQL5 | 2025-03-30 | CodeBase | high/low баров окна `backstep` | шаг закрытия больше ATR×множитель |
| [Support and Resistance](https://www.mql5.com/en/code/45132) (Mullerp04) | MQL5 | 2023-07-27 | CodeBase | max за `period`, сверенный с max за `period+overlook` | тень за коробкой, 1 бар |

### Семейство Б. Границу задают СВИНГИ И ПИВОТЫ — 6 из 14 (сюда же попадаем МЫ)

| проект | язык | ★ / дата | лицензия | Н1: граница | Н2: цена или зона |
|---|---|---|---|---|---|
| [Consolidation Zones - Live](https://github.com/fmzquant/strategies/blob/master/Consolidation-Zones-Live.md) (LonesomeTheBlue) | Pine | 2022-05-24 | MPL-2.0 | `highest(conslen)`/`lowest(conslen)` в момент набора счётчика зигзаг-пивотов | цена, две линии |
| [TrendScalp-FractalBox](https://github.com/fmzquant/strategies/blob/master/TrendScalp-FractalBox-3EMA.md) (vireshdb) | Pine | 2022-05-23 | GPL-3.0 | ПОСЛЕДНИЙ завершённый 5-баровый фрактал каждой стороны | цена |
| [joshyattridge/smart-money-concepts](https://github.com/joshyattridge/smart-money-concepts) `liquidity` | Python | ★1915 / 2026-04-03 | MIT | ПЕРВЫЙ свинг задаёт уровень, следующие присоединяются | **ЗОНА** ±1% глобального размаха |
| [day0market/support_resistance](https://github.com/day0market/support_resistance) `_cluster_prices_to_levels` | Python | ★469 / 2023-07-06 | нет файла | кластеризация ВСЕХ пивотов, уровень = среднее/медиана кластера | цена + допуск слияния |
| [Adaptive S/R Zones](https://www.mql5.com/en/code/74421) (TalalEissa) | MQL5 | 2026-06-27 | CodeBase | подтверждённый пивот глубины 3 | **ЗОНА**, полуширина `0.18×ATR` |
| [SR Zone Scanner](https://www.mql5.com/en/code/74492) (FoxQCW) | MQL5 | 2026-06-30 | CodeBase | пивоты, слияние ближе 5 пипсов | **ЗОНА** шириной 8 пипсов |

### Семейство В. Границу задаёт ИНДИКАТОР ВОЛАТИЛЬНОСТИ — 1 из 14

| проект | язык | ★ / дата | лицензия | Н1: граница |
|---|---|---|---|---|
| [Eesita/Wyckoff-AI-Assistant](https://github.com/Eesita/Wyckoff-AI-Assistant) | Python | ★22 / 2025-04-23 | MIT | полосы Боллинджера: `BB_Upper` и `BB_Lower` |

### Приёмы дословно

Единственная реализация, где зона границы задаётся ВОЛАТИЛЬНОСТЬЮ, а не самими точками —
[Adaptive S/R Zones](https://www.mql5.com/en/code/74421), MQL5 CodeBase, лицензия площадки:

```cpp
const double candidate_width = MathMax(_Point * 2.0, atr * InpZoneWidthATR);
const double merge_distance  = MathMax(candidate_width, atr * InpMergeDistanceATR);
```

Единственная реализация, где коробка ЯВНО РАСШИРЯЕТСЯ каждым новым баром —
[Consolidation Zones - Live](https://github.com/fmzquant/strategies/blob/master/Consolidation-Zones-Live.md), Pine, MPL-2.0:

```pinescript
if conscnt == conslen
    condhigh := H_
    condlow  := L_
else
    condhigh := max(condhigh, high)
    condlow := min(condlow, low)
```

Жёсткий предел на ШИРИНУ диапазона, [Screeni-py](https://github.com/pranjal-joshi/Screeni-py), MIT:

```python
hc = data.describe()['Close']['max']
lc = data.describe()['Close']['min']
if ((hc - lc) <= (hc*percentage/100) and (hc - lc != 0)):
```

Жёсткий предел на ДЛИТЕЛЬНОСТЬ, [BreakOutBox](https://www.mql5.com/en/code/37415), MQL5:

```cpp
int BoxHeight=30,BoxLength=140;
if(range<BoxHeight*_Point*10 && range_time>=BoxLength) {
   break_out_box=true;
   box_high=highest;
   box_low=lowest;
```

### ПОКРЫТИЕ ПО ВОПРОСАМ: четырнадцать проектов — это не четырнадцать ответов

| вопрос | отвечают по КОДУ | по описанию | достаточно ли для «у большинства так» |
|---|---:|---:|---|
| **Н1** что задаёт границу | 14 | 3 | да |
| **Н2** цена или зона | 14 | 0 | да |
| **Н3** сколько точек нужно | 4 | 2 | **мало данных** |
| **Н4** чередуются ли точки | 2 | 0 | **мало данных** |
| **Н5** что делает прокол | 8 | 1 | да |
| **Н6** что считается выходом | 7 | 1 | да (ровно половина) |
| **Н7** отдаётся ли незакрытая | 6 | 0 | **мало данных** |
| **Н8** предел ШИРИНЫ | 4 | 1 | **мало данных** |
| **Н9** предел ДЛИТЕЛЬНОСТИ | 4 | 1 | **мало данных** |
| **Н10** границы сошлись | 4 | 0 | **мало данных** |
| **Н11** внешний барьер | **0** | 0 | **никто не рассматривает** |

⚠ Шесть вопросов из одиннадцати идут с пометкой «мало данных». По ним фразы «мы делаем не
как все» в фазе 3 не будет: сказать так можно только там, где «все» существуют.

### Контроль случайностью у чужих: ноль из четырнадцати

Проверено грепом по `shuffle`, `permutation`, `np.random`, `MathRand`, `monte carlo`,
`null hypothesis` во всех восемнадцати файлах. Ни одного нуля, ни одной проверки, что
результат не шум. Тесты есть у четырёх питоновских проектов из пяти (2, 2, 5, 5 файлов);
у Pine и MQL тестов нет по устройству площадок.

⚠ Единственное найденное употребление случайности — противоположное контролю.
[Wyckoff-AI-Assistant](https://github.com/Eesita/Wyckoff-AI-Assistant) при отказе загрузки
котировок **подставляет сгенерированное случайное блуждание** (`generate_simulated_data`) и
ищет spring и upthrust уже в нём; пользователю это видно только строкой в журнале. Это ровно
тот дефект, против которого в нашем §4.3 стоит требование ВИДИМОГО отказа: «нет данных» и
«вот вам разметка» здесь выглядят одинаково.

---

## Фаза 3. Сверка: курс, курс рисунком, мы, поле

Порядок колонок не переставляется: арбитр — курс, чужое большинство не отменяет цитату и
не заполняет молчание источника.

| вопрос | курс: ТЕКСТ | курс: РИСУНОК | как у нас | как у чужих (из 14) |
|---|---|---|---|---|
| **Н1** граница | стр. 18: первые 2 точки | первые две из 1–10 | первые две точки стороны | 7 — окно баров, 6 — свинги, 1 — Боллинджер; **первые две точки: 0** |
| **Н2** цена или зона | молчит | зона (две линии) | зона `lo…hi` | зона 3, цена 11 |
| **Н3** сколько точек | стр. 22: 4 и более | точек 10 | `MIN_BOUNDARY_POINTS = 4` | отвечают 4: пороги 2, 2, 5 пивотов, счёт без порога |
| **Н4** чередование | молчит | **строгое: 1,3,5,7,9 / 2,4,6,8,10** | не требуем | отвечают 2 (зигзаг даёт по построению; ещё один требует ≥3 баров между касаниями) |
| **Н5** прокол | стр. 18: возможно расширение диапазона, стоп за прокол | не нарисован | точка засчитана, зона ЗАМОРОЖЕНА, глубина сохранена | отвечают 8: расширяют 2, перезапускают 2, заменяют границу 1, закрывают уровень 1, отдельный сигнал 1, выход 1; **замораживают 0** |
| **Н6** выход | стр. 23 + стр. 55: 2-3 тела | стрелка за коробку | 2 полных тела ПОДРЯД | отвечают 7: одно закрытие 3, одна тень 2, новый пивот 1, шаг закрытия 1; **двух тел подряд не требует НИКТО** |
| **Н7** незакрытая структура | стр. 23: уровня нет | молчит | отдаётся `OpenStructure`, уровня не даёт | отвечают 6, все рисуют незакрытую коробку как результат |
| **Н8** предел ширины | молчит | молчит | порога нет, ширина замеряется | отвечают 4, **все четверо ставят порог** |
| **Н9** предел длительности | молчит | молчит | ограничения нет | отвечают 4, все ставят минимум |
| **Н10** границы сошлись | молчит | молчит | сброс структуры | отвечают 4: отбрасывают или сливают |
| **Н11** внешний барьер | молчит | **есть, подписан на схеме** | нет вовсе | **0 из 14** |

### Распределение по девяти пунктам

**А. Курс говорит (8 вопросов).**

1. **мы следуем курсу — 5** (Н1, Н2, Н3, Н6, Н7). Расхождение с полем здесь не находка:
   у чужих проектов нашего источника нет. По Н1 мы расходимся с полем ПОЛНОСТЬЮ (ноль из
   четырнадцати берут первые две точки) и всё равно правы — так сказано на стр. 18.
2. следуем, причина не записана — 0.
3. **МЫ РАСХОДИМСЯ С КУРСОМ — 3** (Н4, Н5, Н11). Высший приоритет обзора.
4. текст и рисунок курса расходятся между собой — 0 в этом элементе.

**Б. Курс молчит (3 вопроса).**

5. как большинство — 1 (Н10: отбрасываем вырожденный диапазон, как и все ответившие).
6. иначе, причина записана — 0.
7. иначе, причина НЕ записана — 0.
8. **у них есть решение, которого нет у нас — 2** (Н8, Н9). Оба с пометкой «мало данных»
   (отвечают 4 из 14), поэтому идут замером, а не выводом.
9. иначе, чем ВСЕ, при молчании курса — 0.

⚠ **Пункт 9 пуст, и это результат, а не пустая клетка.** Презумпция фазы 0 говорила: где
курс молчит, а мы всё-таки что-то делаем, — первое подозрение на самодельность агента. Ни
одного такого места в накоплении не нашлось: там, где мы расходимся с полем, за нами стоит
цитата, а там, где цитаты нет (Н8, Н9, Н10), мы либо ничего не решаем, либо решаем как все.

⚠ **Зато нашлось обратное, и оно опаснее.** По **Н5** мы расходимся И с курсом, И со всеми
четырнадцатью сразу: ноль чужих реализаций замораживает границу. Это пункт 3, а не 9 —
курс говорит, и он важнее поля, — но совпадение поля против нас снимает возможность
объяснить заморозку «так принято». Она держится ровно на двух вещах: стр. 13 и замере
2026-08-04.

## Фаза 4–5. Гипотезы и живой замер

Допуск №1: [tolerance-accumulation.md](tolerance-accumulation.md),
sha256 `00aa4f5949a9b1b43f6d93cf7e5df1617cd7c6a37a9385bb3ced9f69e8815a63`.
Допуск №2: [tolerance-accumulation-alt.md](tolerance-accumulation-alt.md),
sha256 `4ce256bcd292afb15b532331698cb07ab4d759012425f6ad9211fc92c2397a6d`.

⚠ **Оба хеша — вторые, и почему, сказано здесь.** Первые редакции допусков не несли команды
воспроизведения; это поймал гейт `repro_commands` уже после прогонов. Хеш, снятый с
неполного файла, ничего не удостоверяет, поэтому команда дописана, файлы пере-хешированы и
**замеры перегнаны заново против новых хешей**. Пороги при этом не тронуты — сравнить
редакции можно по истории git.

Корпус: 18 рядов, 13 545 баров, 305 закрытых накоплений.

### Контроль копии: пройден

Зонд держит свою копию `accumulation.detect` с воротами на приёме точки. С выключенными
воротами копия обязана совпасть с боевой функцией — **совпала на всех 18 рядах**. Без этой
строки числа Н4 мерили бы расхождение копии, а не действие правила.

### Н4. Чередование: ГИПОТЕЗА НЕПРОВЕРЯЕМА этой конструкцией, вердикта нет

| | чередование | нуль (медиана 5 затравок) |
|---|---:|---:|
| точек отвергнуто | 900 из 2636 (34%) | те же 900, но случайно |
| **мера №1: рядов изменилось** | 18 из 18 | 18 из 18 (все затравки) |
| **мера №2: накоплений различается** | 497 (163.0%) | 482 (158.0%), разброс 465…494 |

**Обе меры насыщены.** Правило рисунка, взятое буквально, выбрасывает треть всех точек
границы, и при такой доле разметка перестраивается целиком независимо от того, ПО КАКОМУ
признаку точки выброшены. Нуль отличается от чередования на 3.1% — при пороге 25%.

⚠ **Формально порог сработал и сказал «отвергнуть». Этот вывод я не публикую.** Прибор,
который не может ответить иначе, отвечает про себя, а не про рынок: «отвергнуто» здесь
неотличимо от «мера уперлась в потолок». Контроль насыщения записан во втором допуске ДО
прогона и сработал сам.

**Что известно без вердикта:** буквальное чередование отбрасывает 900 точек из 2636 и
уменьшает число закрытых накоплений с 305 до 294 (−3.6%).

**Что нужно, чтобы вопрос стал проверяемым:** вмешательство, меняющее единицы процентов
точек, а не треть, — например, чередование только ПОСЛЕ формирования зоны; либо корпус с
размеченными вручную структурами, где есть с чем сверять. Выбирать такую конструкцию
сейчас, увидев результат, — подгонка, поэтому это предложение, а не сделанный замер.

### ⚠ Перегон вскрыл дефект самого зонда: нуль был невоспроизводим

Перегон затевался ради хеша допуска, а нашёл другое. Два запуска подряд дали у меры №2
РАЗНЫЕ числа нуля: медианы 481 и 479, разброс 477…489 против 470…490. Причина —
затравка ряда бралась встроенным `hash((sym, tf))`, а хеш строк в Python солится на каждый
процесс. Нуль, который нельзя повторить, §7.3 не фиксирует, и порог против такого нуля
ничего не значит.

Исправлено: затравка берётся устойчивым `sha256` от `символ|ТФ`. Проверено двумя запусками
подряд — оба зонда печатают побитово одно и то же. Приведённые выше числа получены уже
после исправления.

⚠ Дефект был бы НЕ НАЙДЕН, если бы не чужая причина: без гейта `repro_commands` я бы не стал
перегонять и опубликовал бы неповторимый нуль как результат. Это довод в пользу правила
«гонять `scripts/check.sh`, а не смотреть на печать».

### Н5. Прокол жив: 277 зон из 636

| | |
|---|---:|
| зон границ всего | 636 |
| с проколом сверху | 156 |
| с проколом снизу | 121 |

Ветка «точка засчитана, зона не расширена, глубина сохранена для стопа» срабатывает на
**43.6% зон**. Ноля нет — заморозка наблюдаема, и ссылка на стр. 18 в модуле относится к
реально работающему коду. Неверна была формулировка, а не поведение.

### Н8. Ширина: порога нет, и хвост уходит далеко

| | |
|---|---:|
| ширина зоны границы, медиана | 0.888% |
| минимум | 0.013% |
| **максимум** | **122.860%** |
| шире 10% (порог двух независимых чужих проектов) | 9.8% структур |

По шкале, записанной ДО прогона (<5% — порог не нужен; 5…20% — наблюдение; >20% — находка),
это **наблюдение без вывода**. Но максимум приводится отдельно: зона границы шириной 122.9%
от собственной середины — это не граница, а две случайные цены, и никакой проверки, которая
бы это отсекла, у нас нет.

### Н9. Длительность: порог не нужен

| | |
|---|---:|
| длительность, медиана | 30 баров |
| минимум / максимум | 10 / 199 баров |
| короче 5 баров (нижний предел четырёх чужих) | **0.0%** |
| длиннее 140 баров (`BoxLength` у BreakOutBox) | 1.6% |

По той же шкале — **порог ничего не отсекает**. Минимум в 10 баров получается сам собой из
требования четырёх фракталов, и вводить константу незачем. Вопрос закрыт.

---

## Фаза 6. Решения

### Внесено в код: одна правка, расчёт не меняется

**Н5 — исправлена цитата в докстроке `src/hunter/accumulation.py`.** Было: «прокол за
границу — расширение базы, а не слом». Стало: дословная цитата «стоп всегда ставится за
этот прокол» плюс прямое признание, что заморозка границы — НАША, а не курса, и опирается
на стр. 13 и замер 2026-08-04.

Это правка комментария, а не расчёта: диффа повтора она не требует и не даёт. Поведение
проверено замером — ветка прокола жива на 277 зонах из 636.

### Вопросы владельцу — четыре, и ни один не закрывается правкой

1. **Н4, чередование точек границы.** Курс рисует его строго; мы не требуем. Гипотеза
   доведена до двух замеров с порогами, записанными заранее, и **обе меры насыщены** —
   правило рисунка выбрасывает треть точек, и при такой доле нуль неотличим от гипотезы.
   Вопрос: считать ли нумерацию 1–10 на схеме ПРАВИЛОМ или иллюстрацией? От ответа зависит,
   стоит ли строить более тонкое вмешательство.
2. **Н11, лимитный барьер.** На схеме стр. 18 он есть и подписан, у нас его нет, и **ни у
   одной из 14 чужих реализаций тоже**. Правила, на каком расстоянии он стоит, ни курс
   текстом, ни поле не дают. Вопрос: это отдельная величина или другое имя для того, что мы
   уже храним как `puncture` (стоп ставится за прокол — стр. 18)?
3. **Н8, ширина зоны границы.** Порога нет; медиана 0.888%, но **максимум 122.860%**. Зона
   такой ширины — не граница, а две случайные цены. Четыре чужих реализации из четырёх
   ответивших порог ставят. Вопрос: вводить ли отсечку и по какому числу — курс его не
   называет, а брать чужое было бы выбором без источника.
4. **Н5 как поведение.** Ноль из четырнадцати замораживает границу; курс допускает
   расширение диапазона. Заморозка держится на стр. 13 и одном замере 2026-08-04.
   Вопрос: оставляем как есть? Моя рекомендация — оставить: замер, который её ввёл,
   показывал прямой вред расширения (39 верхних точек против 6 нижних, структура не
   закрывалась 316 баров).

### Закрыто без изменений

* **Н9, длительность.** Порога нет и не нужен: структур короче 5 баров **0.0%**, минимум
  10 баров получается сам из требования четырёх фракталов.
* **Н10, схождение границ.** Делаем как все ответившие — сбрасываем структуру.
* **Н1, Н2, Н3, Н6, Н7.** Следуем курсу. Расхождение с полем здесь ожидаемо и находкой не
  является: по Н1 наш ответ («первые две точки») не даёт **ни одна из 14** чужих
  реализаций, и это ровно то, что написано на стр. 18.

## Команды воспроизведения фаз 1–5 (§7.3)

Независимость выборки (18 ядер, хеши, семьи) — каталог со скачанным передаётся аргументом,
как его собрать, описано в фазе 1:

```bash
uv run python docs/audit/probes/probe_accumulation_clones_2026-08-07.py <каталог>
```

Замер Н4 (мера в рядах), Н5, Н8, Н9:

```bash
uv run python docs/audit/probes/probe_accumulation_questions_2026-08-07.py
```

Замер Н4 второй мерой (в накоплениях) с контролем насыщения:

```bash
uv run python docs/audit/probes/probe_accumulation_alt_fine_2026-08-07.py
```

Хеши допусков, снятые ДО прогонов:

```bash
uv run python -c "import hashlib,pathlib; print(hashlib.sha256(pathlib.Path('docs/audit/tolerance-accumulation.md').read_bytes()).hexdigest())"
uv run python -c "import hashlib,pathlib; print(hashlib.sha256(pathlib.Path('docs/audit/tolerance-accumulation-alt.md').read_bytes()).hexdigest())"
```
