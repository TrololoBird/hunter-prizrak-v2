# План: от честной разметки — к проверяемому преимуществу

Составлен 2026-08-26 по приказу владельца: «составь полноценный план где укажешь как
сейчас, что требуется изменить и для чего и какая конечная версия продукта будет».

⚠ **Это протокол решений, а не канон.** Каждый пункт несёт референт: страницу курса,
внешний источник или замер с подвыборкой. Числа помечены тем, КТО их получил: мои — этой
смены, чужие — со ссылкой на смену-источник. Ни один порог здесь не выдуман: где порог
предлагается, рядом стоит внешняя ссылка либо слово курса.

---

## 1. КАК СЕЙЧАС

### 1.1 Что система делает

Конвейер одной строкой (`engine.decide`; «единственная точка, где считается сигнал» — 
утверждение его же докстроки, я его не перепроверял):

```
бары → свинги (зигзаг) → структуры (боковики) → уровень = ПОК профиля по окну структуры
     → судьба уровня (активен / отработан / пробит) → решение об эмиссии
     → геометрия (вход = ПОК, стоп = за коробку + запас, цели = встречные уровни)
     → карточка + карта бота + леджер → исход по барам ПОСЛЕ записи → приёмка
```

Что владелец получает сегодня: карточку по символу (в живом прогоне — 138 строк на один
символ), график, сводку доски по вселенной, событийные уведомления, приёмку после каждого
прогона и три проверочных запроса к леджеру.

⚠ **Поправка 2026-08-26, перепроверкой этого же документа.** Здесь стояло «приёмка из 16
разделов». Разделов ПЕЧАТАЕТСЯ 17, а номеров израсходовано 16: номер «2» стоит ДВАЖДЫ —
«2. СВЕЖЕСТЬ КАДРОВ» и «2. ПРОПУСКИ». Это остаток правки 2026-08-23, которая объявила
нумерацию «теперь 0..15 подряд»; подряд она не идёт. Дефект мелкий и живой — кандидат в
Ф2 вместе с остальной приёмкой продукта.

### 1.2 Что проверено и держится

| свойство | чем подтверждено |
|---|---|
| детерминизм карточки | ⚠ `replay --diff` ИЗ ЭТОЙ СРЕДЫ НЕ ПРОГНАН: в свежем клоне нет кадров (`data/frames` не в git), команда честно отвечает «прогон last не найден» и падает кодом 1. Проверено ДРУГИМ способом: две версии кода на ОДНИХ кадрах дали посимвольно одинаковый текст (138 строк) |
| форма и инварианты | 23 гейта, `scripts/check.sh` падает по коду возврата; прогон 2026-08-26 — код 0 |
| зона внутри структуры | ограничение в схеме леджера `boundary_lo ≤ zone_lo AND zone_hi ≤ boundary_hi` + гейт |
| стоп за структурой с запасом | моя карточка 2026-08-26: стоп 106.193 = 103.1 × 1.03, **4.56% от входа** |
| цель вне своей структуры | обе цели (94.56 и 87.385) вне коробки входа 98.82…103.1 |
| сторона уровня по стр. 43 | одна формула на проект (`levels.other_side`), слито 2026-08-26 |

### 1.3 Что измерено на живых данных

**Мои замеры 2026-08-26** (Crypto.com, SOL/USD, ТФ 15м/1ч/4ч по 299 закрытых баров;
профиль 1м×12038 + 5м×5015 + 15м×3008):

* структур 11 → уровней построено **5** → торгуется лимитками **1**;
* на 4ч — **0 уровней из 3 структур**, все отказы по покрытию окна профиля;
* "чётких" структур (6+ точек границ, стр. 23) — **4 из 11**;
* у единственной сделки: вход 101.56, стоп 4.56%, цели в 6.89% и 13.96%, плановый **РР 1.51**;
* флипнутых уровней в выборке — **0 из 12** (4 символа), то есть путь стр. 43 живыми
  данными этой смены не проверялся;
* биржа отдаёт **все** минуты: 0 пропусков из 300 у шести символов разной ликвидности —
  гипотеза "венью не отдаёт пустые минуты" **опровергнута**, отказы покрытия на стенде
  были артефактом моей закачки.

**Замеры прошлой смены** (2026-08-23/24, 23 символа, 132 размеченных уровня, 300 баров —
их подвыборка, не моя):

* медиана планового РР **0.401**, доля РР ≥ 1 — **8%**; после правок отбора целей на той
  же выборке 1.367 и 71%;
* сумма R по 132 уровням **−16.22**, средний **−0.222R** (без безубытка);
* **96.6%** эмитируемых сделок — без КРУПНОЙ цели после честной даты отбора;
* ПОК лежит ПОСЕРЕДИНЕ своей коробки: медиана 0.511 у лонгов, 0.519 у шортов;
* запас стопа 3% цены в единицах ATR14: **6.9×** (15м), 2.4× (1ч), **0.9×** (4ч);
* высота строки профиля при `TV_ROWS = 100` гуляет 0.0136%…0.2013% цены — **в 15 раз**;
* двугорбых структур **6.5%** при контроле на случайных окнах 16.5%;
* смена правила раскладки объёма внутри бара двигает строку ПОК у **35.3%** окон.

### 1.5 ⚠ ПЕРЕПРОВЕРКА ЧУЖИХ ЧИСЕЛ СВОИМ ЗАМЕРОМ

По приказу владельца 2026-08-26: «код и док строки и записки внутри проекта могут быть
выдуманы, лгать и содержать ошибки». Правило свода требует того же. Числа прошлой смены
перемерены МОИМ прибором на СВЕЖИХ данных.

**Отпечаток моей выборки:** 10 символов Crypto.com (BTC, ETH, SOL, XRP, DOGE, ADA, AVAX,
LINK, DOT, LTC), ТФ 15м/1ч/4ч по 300 баров, профиль 1м×6000 + 5м×4000 + 15м×2500,
2026-08-26; построено **40 уровней**. Их выборка — 23 символа, 132 уровня, 300 баров,
2026-08-24. Выборки РАЗНЫЕ, и это названо, а не спрятано.

| величина | их число | моё | вердикт |
|---|---|---|---|
| ПОК в коробке, лонги | 0.511 | **0.455** (n=17, от 0.146 до 0.704) | порядок тот же, «посередине» — огрубление |
| ПОК в коробке, шорты | 0.519 | **0.569** (n=23, от 0.281 до 0.873) | то же |
| плановый РР, медиана | 1.367 | **2.143** (n=19) | у меня ВЫШЕ; довод об «РР<1» опровергается ещё раз |
| доля РР ≥ 1 | 71% | **63%** | сопоставимо |
| доля РР ≥ 3 (стандарт стр. 9) | не называлась | **42%** | новое число |
| без КРУПНОЙ цели | 96.6% | **52.5%** (21 из 40) | ⚠ РАСХОЖДЕНИЕ, разбор ниже |
| 3% цены в ATR, 15м | 6.9× | **6.6×** | сходится |
| 3% цены в ATR, 1ч | 2.4× | **2.8×** | сходится |
| 3% цены в ATR, 4ч | 0.9× | **1.1×** | сходится |
| разброс высоты строки | «в 15 раз» | **4.8×** (0.0229%…0.1107%) | у меня меньше; выборка втрое короче |

**Разбор расхождения по целям.** Их 96.6% посчитаны по ЭМИТИРУЕМЫМ сделкам (уровень,
которого цена ещё не касалась), мои 52.5% — по ВСЕМ размеченным уровням. Это разные
подвыборки, и сравнивать их нельзя; ни одно из чисел другого не опровергает. Что общее:
голод целей реален и на моей выборке — у половины уровней крупной цели нет. Моя воронка
по стокам, поштучно (21 случай):

| сток | случаев |
|---|---|
| все кандидаты внутри собственной структуры входа | 6 |
| встречной стороны среди живых нет | 4 |
| все стоят не по ходу сделки | 3 |
| есть ТОЛЬКО промежуточные цели ТФ-1 (стр. 24) | 3 |
| к моменту решения все сняты отработкой либо не существовали | 2 |
| все на ТФ-2 и ниже либо старше ТФ+1 (стр. 24) | 2 |
| у всех структура старше кадра 180 баров | 1 |

⚠ **Контроль: мог ли прибор ответить иначе.** ПОК в коробке даёт значения от 0.146 до
0.873 — не заперт у середины; РР даёт от долей единицы до 10.65 — не заперт ниже единицы;
воронка даёт семь РАЗНЫХ стоков, а не один; отношение «3% к ATR» падает с ростом ТФ
монотонно (6.6 → 2.8 → 1.1), как и обязано, раз ATR с ТФ растёт, — это проверка вменяемости
прибора, а не доказательство верности числа.

### 1.6 ⚠ ЧИСЛО ИЗ ЧУЖОЙ ЗАПИСКИ, ПРОВЕРЕННОЕ ПО ПЕРВОИСТОЧНИКУ

«Цель автора на стр. 33 — 22.55%» стояло в записках проекта. В ТЕКСТОВОМ СЛОЕ страницы 33
этого числа НЕТ вовсе (единственный процент там — «1-3%»). Число оказалось на КАРТИНКЕ:
метка измерителя TradingView «3.184 (22.55%) 3184». Записка верна, но проверяема только
глазами по рисунку — ровно как требует правило свода про рисунок как источник.

**И на той же картинке видно то, чего в записках не было.** Прочитано мной с изображения
страницы: вход ≈ 14.132, цели-линии 17.129 и 17.325, стоп-линии 13.300 и 12.920, и отдельная
метка «−0.416 (−3.12%)» между ними — то есть запас за структурой отмерен автором как
**процент от ЦЕНЫ КРАЯ структуры**, а не от цены входа и не от высоты коробки. Именно так
считает наш `stop_beyond_edge`: 103.1 × 1.03 = 106.193 на моей карточке.

Отсюда арифметика (моя, по прочитанным с рисунка меткам): риск от входа до безопасного
стопа 8.6% цены, до рискового — 5.9%; награда 22.55%. **РР авторского примера — от 2.6 до
3.8.** Это третье, независимое опровержение отозванного «РР ниже единицы по построению»:
у самого автора сделка идёт с РР около трёх.

### 1.4 Чего НЕТ — и что из этого следует

| чего нет | следствие, которое уже наступило |
|---|---|
| отчёта об экспектанси по НАКОПЛЕННОМУ леджеру | всё, что известно о качестве, — срезы на 300 барах; на 4ч разрешилось 43% сделок и все шесть были стопами |
| издержек в расчёте исхода | карточка прямо пишет "комиссии, фандинг и проскальзывание не моделируются нигде" |
| модели частичного выхода | безубыток стр. 15 неизмерим, исход закрывается первой целью целиком |
| правила "сколько сделок достаточно" | нет черты, ниже которой число не читается как вывод |
| короткого ответа оператору | 138 строк на символ; раздел фигур пишет "вход: от ближайшего уровня", когда уровней ноль |
| сводки отказов ПОКРЫТИЯ по ТФ | ⚠ поправлено 2026-08-26: перекос виден ЧАСТИЧНО. Отказы класса «нет ряда intrabar-ТФ» считаются по ТФ и уходят в ЖУРНАЛ деградацией; добор участков по ТФ печатается в разделе 9 приёмки словами «перекос виден здесь». Не сведён по ТФ ровно тот класс, который и режет уровни, — «окно покрыто не полностью», и воронка «структур → уровней» есть только в карточке символа, а не в отчёте прогона |

### 1.7 КОРПУС: урок автора о структуре — чужой разбор, проверенный с моей стороны

Владелец передал отчёт ДРУГОЙ смены о разборе видеоурока: по её словам, разбор на 424
строки лёг файлом с именем `video-2026-08-26-structure-lesson` в каталог разборов.

⚠ **ЧЕГО Я НЕ ПРОВЕРЯЛ И НЕ МОГ.** Такого файла в моём дереве НЕТ и ни в одном коммите он
не значится — он не закоммичен и живёт в контейнере той смены. Ссылки на него здесь нет
намеренно: путь, которого в репозитории не существует, — битая ссылка, и гейт `doc_refs`
её ловит (поймал у меня же при первой редакции этого раздела). Видео я не видел. Их числа по
леджеру (640 активных уровней, 0 без границ) не проверить: базы в этой среде нет. Всё
ниже — их утверждения, а справа то, что удалось проверить МНЕ, по коду и по курсу.

| их утверждение | что проверил я |
|---|---|
| структура рождается на ТРЁХ точках и откате, рамки автор ставит в этот момент | курс прочитан мной по PDF: стр. 22 текстом и стр. 24 рисунком — «4+ точки границ». Наш код: `MIN_BOUNDARY_POINTS = 4`, на сторону 2. **Если чтение видео верно, расходятся автор-практик и его же курс** |
| граница — линия по свинговым экстремумам, а не min/max окна | у нас ЕСТЬ ОБЕ величины: `TradingRange.upper/lower.edge` — линия по точкам, `Level.boundary_*` = `price_range` = (min low, max high) баров. Уровню отдана КОРОБКА — по приказу владельца 2026-08-18. Цена выбора измерена ниже |
| прокол границу не двигает (координаты коробки совпали до пикселя) | **подтверждено кодом:** `edge` не переопределяется, расширение до прокола живёт отдельным `extended_edge` для стопа (стр. 18). Наш расчёт это уже делает |
| поджатие, увиденное на старшем ТФ, отдаётся младшему | **подтверждено: такого правила у нас нет вовсе.** `boundary_narrowed` считается у своей структуры, межтаймфреймового наследования нет ни строки |
| дефект `_beyond_price`: при `boundary_hi == 0` шортовый уровень молча выбывает с доски, лонговый остаётся | **подтверждено в ЗАКОММИЧЕННОМ коде:** `z.boundary_lo > price if long else z.boundary_hi < price`. При нуле у шорта `0 < цена` истинно, у лонга `0 > цена` ложно. Асимметрия по стороне реальна |

**ЦЕНА КОНВЕНЦИИ ГРАНИЦЫ — МОЙ ЗАМЕР** (10 символов, 40 сопоставленных пар
уровень↔структура, 2026-08-26; стоп по обеим конвенциям с тем же запасом 3%):

| величина | коробка (как сейчас) | линия по точкам |
|---|---|---|
| ширина, во сколько раз коробка шире | медиана **1.33×**, макс **6.01×** | — |
| стоп от входа, медиана | **4.76%** | 4.52% |
| стоп от входа, максимум | **8.08%** | 6.69% |
| плановый РР, медиана | 2.14 | 2.14 |
| доля сделок с РР ≥ 3 | 42% | **47%** |

⚠⚠ **И ВОТ КОНТР-ФАКТ, КОТОРОГО В ЧУЖОМ РАЗБОРЕ НЕТ.** У самого автора на стр. 33 риск
сделки — 5.9–8.6% цены (раздел 1.6, прочитано мной с рисунка). Наша коробка даёт медиану
**4.76%**. То есть переход на линию по точкам сделал бы наш стоп ЕЩЁ УЖЕ (4.52%), то есть
ДАЛЬШЕ от авторского примера, а не ближе к нему. Выбор конвенции не сводится к «автор
рисует линией» — числа тянут в разные стороны.

⚠ **И довод чужой смены проверяем не полностью.** «Февральский лой ≈66.1 на 12% ниже
границы ≈75.3 и в коробку не включён» доказывает конвенцию ТОЛЬКО если этот лой лежит
ВНУТРИ временного окна структуры. Лежит он вне окна — наблюдение не значит ничего. Кадров
у меня нет; проверка называется, а не выполняется.

---

## 2. ДИАГНОЗ

**Главное.** Система доведена до состояния "честная разметка": она воспроизводима,
инварианты держатся, отказы называются. Но петля "сделка → исход → вывод" НЕ ЗАМКНУТА.
Всё, что проект знает о качестве своих сигналов, получено срезами на 300 барах, и сам же
проект справедливо назвал такой срез ложью окна. Значит любая следующая правка метода
принимается вслепую — не потому, что расчёт плох, а потому, что мерить нечем.

Ниже — механизмы, которые задают числа, каждый со своим источником.

### 2.1 ⚠ ОТЗЫВ: «РР < 1 по построению» — МОЁ УТВЕРЖДЕНИЕ, И ОНО НЕВЕРНО

Здесь стояло: вход — ПОК, стоп — за всю коробку плюс запас, ПОК посередине базы, значит
плечо до цели — половина базы, плечо риска — вторая половина плюс запас, и РР ниже
единицы выходит арифметически.

**Отозвано 2026-08-26 по возражению владельца.** Ошибка механическая: плечо «половина
базы» — это тейк по встречной границе СВОЕЙ базы, то есть правило флэта стр. 19, и оно
было ОТОЗВАНО 2026-08-24 как чужая сделка (коммит edbeac4). В нынешнем расчёте награда —
расстояние до ДРУГОГО уровня по стр. 24, и к высоте коробки она отношения не имеет. Я
описал модель, которой в проекте больше нет, и выдал её следствие за закон.

Опровергается выдачей самой системы — той карточкой, что приведена в разделе 1.3:

| величина | число | в высотах коробки |
|---|---|---|
| риск: стоп 106.193 − вход 101.56 | 4.633 (4.56% от входа) | 1.08 |
| награда до первой цели 94.56 | 6.99 (6.89%) | 1.63 → **РР 1.51** |
| награда до второй цели 87.385 | 14.175 (13.96%) | 3.31 → **РР 3.06** |

Вторая цель даёт ровно курсовой стандарт 1к3 (стр. 9). И вход не в середине коробки: ПОК
стоит на 64% её высоты, то есть даже посылка «половина базы» на этом примере не держится.

⚠ И ещё два независимых опровержения, полученных перепроверкой (разделы 1.5 и 1.6): на моей
выборке из 40 уровней медиана планового РР — **2.143**, доля РР ≥ 3 — **42%**; а у САМОГО
АВТОРА на стр. 33 сделка идёт с РР около трёх.

⚠ **Второй отзыв, в том же месте.** Числа «медиана планового РР 0.401, доля РР ≥ 1 — 8%»
я подал как описание НЫНЕШНЕЙ системы. Это замер ДО правок отбора целей той же смены; на
той же выборке ПОСЛЕ правок — **1.367 и 71%**. Отозванное состояние было процитировано
как текущее.

Что из измеренного УЦЕЛЕЛО, но с поправкой: ПОК лежит в среднем БЛИЗКО к середине коробки —
их медианы 0.511/0.519, мои 0.455/0.569 при разбросе от 0.146 до 0.873. Значит риск сделки
сопоставим с высотой структуры плюс запас, но словом «посередине» это огрубляется: у части
уровней ПОК стоит у самого края. Верна посылка про порядок величины, неверен был вывод о
награде.

### 2.2 Настоящий дефект: пул целей ГОЛОДАЕТ, а не переполнен

Здесь стояло, что плотность уровней делает цель ближней. Это тоже неверно, и по двум
причинам сразу.

Первая: уровни ТФ-1 (для сделки 15м — это 5м) мерилом РР НЕ ЯВЛЯЮТСЯ. Стр. 24 называет их
промежуточными, код печатает их отдельной ролью и в расчёт РР не берёт. Тысячи структур на
младшем ТФ не могут утянуть РР вниз — они в этой мере не участвуют.

Вторая, и она важнее: у сделок КРУПНОЙ цели часто нет ВОВСЕ — у **96.6%** эмитируемых по их
замеру и у **52.5%** всех размеченных уровней по моему (разные подвыборки, раздел 1.5). Их воронка по остатку, поштучно: у 11 встречные
уровни впереди отработаны (стр. 25 велит такой уровень удалить), у 5 живые, но структура
старше кадра 180 баров, у 5 есть только промежуточная цель ТФ-1, у 3 впереди уровни лишь
своей стороны, у 1 впереди пусто.

**То есть вопрос не «почему цель близко», а «почему цели нет».** И это меняет содержание
работы: не отсекать близкое, а разобрать пять стоков воронки и назвать, какие из них
методические (стр. 25 — законный сток), а какие приборные (структура старше кадра —
решение владельца 2026-08-18; уровень старшего ТФ не построен — покрытие профиля, Ф7).

### 2.3 Издержки: РЕШЕНИЕ ВЛАДЕЛЬЦА — НЕ УЧИТЫВАТЬ

Здесь стоял довод, что комиссия и фандинг обязаны войти в R. Владелец 2026-08-26 дословно:
**«Так же издержки и фандинг не стоит учитывать, это не АЛГОтрейдинг, не скальп, а сделки
с RR 1:3 которые позволяет пережить и коммисию и фандинг!»** Решение принято, фаза снята.

Цена решения названа один раз и числом, чтобы она была известна, а не забыта: при стопе
около 4.5% цены круговая комиссия тейкера (0.04–0.06% за сторону) съедает порядка **2%
риска** — довод владельца арифметически верен. Фандинг 0.09% в сутки при медианном
удержании 102 бара даёт те же ~2% риска на 15м и до трети риска на 4ч, и только у лонгов
при положительной ставке. Если однажды это понадобится — это ОДНА печатная строка рядом с
исходом, не трогающая R.

### 2.4 Разрешение профиля двигает ЦЕНУ ВХОДА

ПОК есть вход. Он зависит не от числа строк, а от ВЫСОТЫ строки, и она у нас гуляет в 15
раз между окнами, тогда как корпус говорит, что у автора постоянна цена строки ≈0.07%.
Инструмент, который мы моделируем, оба режима поддерживает штатно: при "Number of Rows"
шаг вычисляется как `(верх − низ) / число строк / шаг цены`, при "Ticks Per Row" задаётся
напрямую. То есть переход на постоянную высоту строки — не отступление от прибора автора,
а его же второй штатный режим.

### 2.5 Модель выхода не та, что в курсе

Курс закрывает у первой цели ПОЛОВИНУ. Стр. 9, пример автора: «делаем тейк 50% и забираем
2Р». Безубыток защищает ОСТАТОК (стр. 15). Наш `outcome.resolve`
закрывает сделку целиком первой целью. Отсюда две беды сразу: безубыток в этой модели
"улучшает" результат, подменяя стопы нулями (замер прошлой смены: −1.79 против честных
−16.22), и любые числа R несравнимы с тем, что описывает курс.

### 2.6 Отказы снабжения перекошены по ТФ

Правило покрытия окна профиля — ЖЁСТКОЕ: не хватило хотя бы одного бара — уровень не
строится. Чем старше ТФ, тем длиннее окно и тем вероятнее недобор; на моей карточке 4ч
дал 0 уровней из 3. Гипотезу "биржа не отдаёт пустые минуты" я проверил и ОПРОВЕРГ — она
отдаёт все. Значит вопрос не в венью, а в том, доводит ли боевой добор `backfill_profile_bars`
покрытие до полного. **Из этой среды это не проверить: боевого хранилища нет.**

---

## 3. ЧТО МЕНЯТЬ — ПО ФАЗАМ

Порядок не по трудоёмкости, а по одному правилу: **сначала то, без чего следующее
нечем судить.**

### Ф0. Замкнуть петлю измерения — отчёт экспектанси по леджеру

**Зачем.** Пока нет накопленного вперёд числа, каждая правка метода принимается на веру.
Внешний консенсус о размере выборки однозначен: 30 сделок — предварительное чтение,
**100 — минимум, при котором число вообще что-то значит**, 200 — убедительно, 500 —
статистически сильно; доверительный интервал по биному: `1.96 · √(p(1−p)/n)`.

**Что делаем.** Команда `hunter ledger --expectancy`: по ЗАКРЫТЫМ сделкам леджера —
экспектанси в R с интервалом, винрейт "6 из 10" с интервалом, разрез по ТФ и по корзинам
стр. 48, и первой строкой — вердикт о выборке словами ("сделок 42 — это предварительное
чтение, не вывод"). Плюс правило свода: **правка метода не обосновывается выборкой ниже
названной черты.**

**Чем проверим.** Синтетический леджер с известным исходом воспроизводит известную
экспектанси; интервал расширяется при уменьшении N (контроль: прибор способен ответить
иначе). Расчёт не трогается — карточки не требуются.

**Чего НЕ делаем.** Не подгоняем правило под накопленное. Не пересчитываем старые сигналы
новым кодом.

### Ф1. Разобрать ГОЛОД ЦЕЛЕЙ до механизма

**Зачем.** У 96.6% эмитируемых сделок нет крупной цели (замер прошлой смены). Это не
"цель близко" и не порог на выходе — это пустой пул, и пять его стоков уже перечислены
поштучно. Пока сток не назван методическим или приборным, править нечего: правило стр. 25
про удаление отработанного уровня — законный сток, а непостроенный уровень старшего ТФ —
отказ прибора, и лечатся они противоположным образом.

**Что делаем.** Воронка целей становится ПЕЧАТНЫМ числом прогона, разложенным по стокам, а
не разовым замером: сколько сделок потеряли цель на каждом фильтре. Затем по каждому стоку
— вердикт с референтом: методический (оставляем и объясняем владельцу) или приборный
(идёт в работу).

**Чем проверим.** Сумма по стокам равна числу сделок без цели — контроль сходимости;
подсадка "фильтр отключён" меняет разложение. Две карточки не нужны: расчёт не трогается,
пока сток не назван.

**Чего НЕ делаем.** Не ослабляем фильтры, чтобы "появились цели". Цель, взятая у
отработанного уровня, — это цель к тому, чего по курсу больше нет.

### Ф2. Продукт-ответ: короткая голова карточки

**Зачем.** Владелец не трейдер и не программист. 138 строк — это данные, а не ответ.

**Что делаем.** Первые строки карточки — "ЧТО ДЕЛАТЬ СЕЙЧАС": сторона, зона входа, стоп,
первая цель, РР, одна фраза "почему сейчас" со страницей курса, и ЗНАМЕНАТЕЛЬ ("из 529
рынков карта у N, уровней M, торгуется лимитками K"). Остальное — ниже, без сокращений.
Заодно снимается несогласованность "вход: от ближайшего уровня" при нуле уровней: в моей
первой карточке этой смены такая строка встретилась **18 раз** при «уровней нет».

**Чем проверим.** Две карточки на одних кадрах: старая и новая; глазами читается 20+ строк.

⚠ **Сюда же — проверенный мной дефект доски.** `tgbot._beyond_price` при незаписанной
границе (`boundary_hi == 0`) считает, что цена прошла СКВОЗЬ уровень, и молча снимает его
с доски — но только ШОРТОВЫЙ: у лонга при `boundary_lo == 0` условие ложно. Асимметрия по
стороне и тихая потеря строк. Сегодня дефект спящий (по чужому замеру в боевом леджере
уровней без границ ноль — проверить отсюда нечем), но лечится одной строкой: нулевая
граница означает «неизвестно», а не «пройдено».

### Ф3. Высота строки профиля вместо числа строк

**Зачем.** См. 2.4. Это цена входа каждой сделки.

**Что делаем.** Сначала — ВТОРОЙ проход по видео корпуса: "≈0.07%" снято с одного экрана,
и одного наблюдения мало. Подтвердится — боевой профиль переводится на `RowSize.TICKS`.
До того карточка ПЕЧАТАЕТ высоту строки, чтобы владелец видел разрешение, стоящее за
ценой входа.

**Чем проверим.** Распределение сдвига ПОК при переходе; две карточки; контроль на
заведомо несовпадающих данных.

### Ф4. Реализовать слово "сопоставимый" у цели

**Зачем.** Стр. 24 требует, чтобы целью был сопоставимый уровень, а стр. 22 определяет силу
через ТФ и объём. Сегодня цель отбирается по стороне, месту и ступени ТФ — по силе НЕ
отбирается вовсе. ⚠ Мотив здесь НЕ "цели слишком близко": это утверждение отозвано
(раздел 2.2). Мотив — качество цели: сделка ведётся к уровню, про который не спрошено,
удержит ли он цену.

**Что делаем.** Сначала перечитать стр. 22 и 24 ЦЕЛИКОМ, вместе с рисунками, и записать
строку "что из чего следует" — проект уже спотыкался на обращённой импликации при
безупречной цитате. Подтвердится чтение — сопоставимость входит в отбор; измерения ТФ и
объёма остаются РАЗДЕЛЬНЫМИ, в одно число не сворачиваются.

**Чем проверим.** Две карточки; и обязательно — сколько сделок ЛИШИЛОСЬ цели: при уже
голодающем пуле (Ф1) это прямая плата, и она может перевесить пользу.

**Чего НЕ делаем.** Не режем стоп, не вводим фильтр по РР, не двигаем вход к границе.
⚠ Порядок обязателен: Ф4 идёт ПОСЛЕ Ф1. Ужесточать отбор целей, не разобрав, почему их и
так нет, — значит лечить выход, не посмотрев на вход.

### Ф5. Модель частичного выхода

**Зачем.** См. 2.5. Без неё безубыток курса неизмерим, а R несравним с курсом.

**Что делаем.** Исход считается по долям: 50% у первой крупной цели (стр. 9), остаток
ведётся с безубытком (стр. 15). Старое число полного выхода остаётся рядом для сверки.

**Чем проверим.** Два отчёта леджера; названо прямо, что числа с прежними несравнимы.

### Ф6. Назвать то, что сегодня молчит

Три вещи, каждая — строка в карточке, ноль риска для расчёта:

* **двугорбый профиль.** У классики он называется Double Distribution, и вторичный VPOC
  там "так же важен, как главный"; тонкая зона между горбами (LVN) — место ускорения
  цены. У нас 6.5% структур двугорбы, и владельцу не сообщается, что ПОК выбран из двух
  почти равных пиков;
* **запас стопа в единицах ATR.** 3% цены — это 6.9 ATR на 15м и 0.9 ATR на 4ч. Внешние
  работы дают 2× ATR как рабочую величину и прямо предупреждают: "фиксированный процент,
  работающий в одном режиме, в другом опасен»; один замер даёт 2× ATR на 24% лучшую
  экспектанси, чем фиксированные 5%. **Правило курса не меняется** — расхождение
  печатается числом рядом со стопом;
* **отказы снабжения по ТФ** — первоклассное число приёмки, а не строка в журнале.

### Ф8. Конвенция границы структуры — РЕШЕНИЕ ВЛАДЕЛЬЦА

**Зачем.** Граница — это плечо риска: стоп ставится за неё. У проекта живут ОБЕ величины,
и уровню отдана коробка баров по приказу владельца 2026-08-18 («ЕСЛИ зона выходит за
структуру то СТРУКТУРА ОПРЕДЛЕННА НЕ ВЕРНО»). Корпус 2026-08-26 читает у автора другое —
линию по свинговым точкам. Это вопрос «ЧТО РИСОВАТЬ», то есть решение владельца, а не
агента.

**Что делаю я, а не он.** Предъявляю цену выбора числами (раздел 1.7) и называю, что
контр-факт со стр. 33 тянет в другую сторону. Расчёт не трогаю, пока владелец не выбрал.

**Чем проверим, если выбор состоится.** Две карточки на одних кадрах; распределение
ширины стопа и доли РР ≥ 3 до и после; и обязательно — сколько уровней ПЕРЕСТАЛО
удовлетворять инварианту «зона внутри структуры», потому что при узкой границе он может
начать падать, а он записан в схему леджера ограничением.

**Чего НЕ делаем.** Не меняем конвенцию по одному кадру видео и не откатываем приказ
08-18 молча.

### Ф7. Данные и охват

Проверить в бою (не из этой среды): доводит ли `backfill_profile_bars` покрытие профиля до
полного на старших ТФ. Мера — доля структур, не давших уровня по причине покрытия, в
разрезе по ТФ, за неделю боевых прогонов. Если доля не падает — это первый кандидат в
следующую фазу, потому что теряются ровно те ТФ, которым курс обещает лучший винрейт.

---

## 4. КОНЕЧНАЯ ВЕРСИЯ ПРОДУКТА

**Что это будет.** Сигнально-аналитическая система, которая:

1. **отвечает коротко и по делу.** Первые строки — что делать сейчас, со стопом, целью,
   РР и знаменателем. Разворот — по требованию;
2. **знает себе цену.** Рядом с сигналом — накопленная экспектанси его корзины в R, с
   размером выборки и интервалом, и с честной пометкой, когда выборки мало;
3. **считает после издержек.** Комиссия и фандинг внутри R, а не в примечании;
4. **говорит на языке курса.** Вход — ПОК, стоп — за структуру с запасом, цели — по
   стр. 24, выход — частями по стр. 9/15. Каждая величина в карточке несёт страницу;
5. **называет отказ.** Нет уровня — сказано, почему и со знаменателем. Нет цели — сказано,
   на каком фильтре кончились кандидаты. Нет данных — сказано, каких;
6. **воспроизводима.** Те же кадры → тот же текст, доказано `replay`;
7. **не исполняет ордера и не хранит ключей.** Это осталось и останется.

**Чего в ней НЕ будет.** Порогов, выдуманных ради красивых чисел. Фильтра по РР.
Платных зависимостей. Второго мнения, кроме источников: курс говорит, ЧТО рисовать;
классика, статистика и проверенные реализации — ВЕРНО ЛИ посчитано; исходник библиотеки —
как устроен транспорт.

---

## 5. ПРИЁМКА ПЛАНА — числа названы заранее

| фаза | что обязано измениться | как поймём |
|---|---|---|
| Ф0 | появляется число, которого сегодня нет | отчёт печатает экспектанси, интервал и вердикт о выборке; контроль на синтетике |
| Ф1 | воронка целей печатается по стокам | сумма по стокам равна числу сделок без цели; подсадка "фильтр отключён" меняет разложение |
| Ф2 | владелец видит ответ, а не дамп | две карточки; 20+ строк прочитаны глазами |
| Ф3 | высота строки перестаёт гулять в 15 раз | распределение сдвига ПОК; две карточки |
| Ф4 | цель отбирается по СИЛЕ, а не только по месту | две карточки + названная плата: сколько сделок лишилось цели |
| Ф5 | безубыток становится измеримым | два отчёта; несравнимость с прежними числами названа |
| Ф6 | три молчания становятся строками | карточка до/после |
| Ф7 | доля отказов покрытия по ТФ падает либо названа неснижаемой | недельная сводка боевых прогонов |

**Черта, ниже которой выводы не делаются:** 100 закрытых сделок в разрезе; 30–99 —
только предварительное чтение с этой пометкой в тексте.

---

## 6. РИСКИ И ЧЕГО ПЛАН НЕ ОБЕЩАЕТ

* **Он не обещает прибыль.** Он обещает измеримость: после Ф0 станет видно, есть ли
  преимущество вообще. Возможный честный исход — "на этой выборке преимущества нет", и он
  обязан быть доложен так же прямо, как положительный;
* **выборка копится календарным временем.** 100 закрытых сделок в разрезе по ТФ — это
  недели работы службы, а не прогон. Ускорить их бэктестом нельзя: проект уже отверг
  повтор по старым кадрам как разницу двух версий кода на мёртвом рынке;
* **Ф3, Ф4 и Ф5 — правки РАСЧЁТА.** Каждая обязана предъявляться двумя карточками на свежих
  данных и может ухудшить числа; ухудшение докладывается, а не прячется;
* **боевую часть (Ф7) из среды разработки не проверить** — и это сказано прямо, а не
  закрыто предположением;
* **риск переобучения на собственные замеры.** Защита — правило свода: расхождение с
  курсом не объясняется собственным замером; и внешний контроль перед всякой правкой.

---

## 7. ВНЕШНИЕ ИСТОЧНИКИ, НА КОТОРЫЕ ОПИРАЕТСЯ ПЛАН

* размер выборки и доверительный интервал экспектанси — [edgeflo](https://www.edgeflo.com/blog/sample-size-trading),
  [Traders Second Brain](https://traderssecondbrain.com/guides/how-many-trades-to-know-if-strategy-works),
  [Medium: статистическая значимость в бэктесте](https://medium.com/@trading.dude/how-many-trades-are-enough-a-guide-to-statistical-significance-in-backtesting-093c2eac6f05);
* walk-forward как способ отличить подгонку от преимущества — [Wikipedia](https://en.wikipedia.org/wiki/Walk_forward_optimization),
  [QuantInsti](https://blog.quantinsti.com/walk-forward-optimization-introduction/);
* стоп по ATR против фиксированного процента — [Volatility Box](https://volatilitybox.com/research/volatility-adjusted-stop-losses/),
  [QuantVPS](https://www.quantvps.com/blog/atr-stop-loss),
  [Traders Second Brain](https://traderssecondbrain.com/guides/stop-loss-placement-methods);
* сетка профиля объёма, режимы Ticks Per Row / Number of Rows — [TradingView, Fixed Range](https://www.tradingview.com/support/solutions/43000707985-fixed-range-volume-profile/),
  [TradingView, Visible Range](https://www.tradingview.com/support/solutions/43000703076-visible-range-volume-profile/);
* двойное распределение и вторичный VPOC — [Bookmap](https://bookmap.com/blog/understanding-market-profile-bell-shaped-curves-single-and-multiple-distributions-explained),
  [Trader Dale](https://www.trader-dale.com/how-to-trade-the-double-distribution-profile-volume-profile-trading/),
  [Overcharts](https://www.overcharts.com/en/helpcenter/docs/volume-profile-distribution-types/);
* издержки перпов: комиссия и фандинг — [Coinbase](https://www.coinbase.com/learn/perpetual-futures/understanding-funding-rates-in-perpetual-futures),
  [BloFin](https://blofin.com/en/academy/education/crypto-trading-fees);
* отбор и слияние уровней по силе и близости — [TradersPost](https://blog.traderspost.io/article/support-resistance-trading-automation),
  [altFINS](https://altfins.com/knowledge-base/support-and-resistance-lines/).

⚠ Внешний источник в этом проекте отвечает на вопрос "ВЕРНО ЛИ посчитано", а не "ЧТО
рисовать». Ни одна ссылка выше не отменяет правила курса — каждая либо называет
расхождение числом, либо даёт аппарат измерения.

---

## 8. КОМАНДЫ ВОСПРОИЗВЕДЕНИЯ (§7.3)

Числа раздела 1.2 (гейты, коды возврата):

```bash
bash scripts/check.sh            # 23 гейта, падает по коду возврата
uv run python -m hunter check    # живое состояние, вердикт по-русски (§7.5)
uv run python -m hunter ledger   # три проверочных запроса, соединение на чтение
```

Числа раздела 1.3, полученные мной 2026-08-26, сняты РАЗОВЫМ замером в переписке и в
репозиторий не положены (правило «зонд ИИ не проверяет код ИИ»). Способ повторить назван
полностью: свежие бары `api.crypto.com/exchange/v1/public/get-candlestick` (по 300 на
запрос, глубже — листанием `end_ts`), ряды 15м/1ч/4ч по 300 баров и профильные 1м/5м/15м,
затем `engine.decide(symbol, series, TVWindows(symbol, tick, profile_series), tfs)` и
`card.render`. Отпечаток выборки: один символ SOL/USD, 299 закрытых баров на ТФ, профиль
1м×12038 + 5м×5015 + 15м×3008, 2026-08-26.

Числа прошлой смены (раздел 1.3, вторая половина) взяты из
[.claude/rules/open-method-questions.md](../../.claude/rules/open-method-questions.md) и
сообщений коммитов 41c02c2, dafa01b, edbeac4, 58f2359, 7235ed1; их подвыборка — 132
размеченных уровня, 23 символа Crypto.com, ТФ 15м/1ч/4ч по 300 баров. Я их НЕ
перепроверял и помечаю как чужие.
