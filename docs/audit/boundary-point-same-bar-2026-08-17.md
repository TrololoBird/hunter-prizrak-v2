# Одна свеча — две точки границы базы. Решено по курсу

**Вопрос владельца.** В базе встречается свеча, чей ХАЙ касается верхней границы, а ЛОЙ —
нижней. Считать её ДВУМЯ точками (одной верхней и одной нижней) или ОДНОЙ?

**Решение: ДВУМЯ. Текущее поведение детектора верно, правка не требуется.**

Основание — размеченный пример самого автора курса (§1). Наш детектор уже считает так же
(§2), поэтому изменений в коде нет.

⚠ Владелец в ходе разбора поручил править детектор по рисунку стр. 18, опираясь на мой
замер: якобы одна структура из пяти нарушала чередование точек. Замер был ОПРОВЕРГНУТ
перепроверкой (см. `session-handoff-2026-08-17.md` §4.1), правка не сделана — править
было нечего.

---

## 1. КУРС (источник №1) — считает двумя точками

### Прямых слов нет

Определения точки границы в курсе НЕТ. Проверены все 14 страниц со словом «точк» и все 3
с «касан». Ни «точка = свеча», ни «= тень», ни «= экстремум», ни «= касание» нигде не
сформулировано.

Слов «фрактал», «экстремум», «свинг», «зигзаг», «бар», «пик», «впадина» — **0 страниц из
69**. Единственное определение экстремума, стр. 3: «Хай - максимальная цена в
определенный промежуток времени», причём «промежуток» не задан.

Правила минимального расстояния между точками во времени тоже нет: счёт свечей в курсе
есть только у прокола (стр. 5, 6), подтверждения ПП (стр. 55) и схемы пробой-закреп-
ретест (стр. 43, 44) — это про другое.

### На схемах случая нет

Проверены все страницы с нумерованными точками: 13 (1–12), 18 (1–10), 19, 21 (три схемы
по 1–4), 23 (1–8), 35 (1–6), 38 (1–6), 39 (1–8), 41 (1–6), 57 (1–8 и 1–6), 58 (1–6).
Ни на одной схеме нет двух номеров на одной вертикали и двух подряд идущих номеров на
одной стороне.

### Но на РЕАЛЬНОМ графике случай есть — и размечен ДВУМЯ точками

Стр. 13, WOO/USDT 1ч. Замер по встроенному растру PDF (2475×1248; рамка базы: верх
y=381.5, низ y=785.5, высота 404 px; шаг бара 6 px по автокорреляции):

| что | значение |
|---|---|
| бар в колонке x=1670: размах | 515 px = **127% высоты базы** |
| его хай | на 33% высоты базы ВЫШЕ верхней границы (прокол) |
| его лой | в 4.5% высоты базы от нижней границы |
| соседние бары ±10 штук: размах | 17–53%, до уровня лоя не доходит НИ ОДИН |

**Метка 11 стоит прямо над хаем этой свечи; впадина, помеченная 12, нарисована на уровне
y=766 — это лой ТОЙ ЖЕ свечи с точностью до пикселя.**

Стр. 19 — та же база другим скриншотом, разметка стрелками касаний: зелёная стрелка
касания нижней границы стоит ровно в колонке этой свечи (центр стрелки x=1779.0 при
колонке свечи 1778–1779), красная стрелка касания верха — на ~4 бара левее.

**Контроль, что прибор различает случаи:** разрыв меток 11→12 составляет 4.8 бара, тогда
как все прочие соседние пары на этой базе — 8.8, 26, 23, 15.7, 16.5, 44, 20.5, 13.5,
16.5, 11.7 бара. Эта пара в **2.4 раза теснее** следующей самой тесной.

### ⚠ Чего это свидетельство НЕ даёт

* **Словами не сказано.** Вывод построен на рисунке. По правилам проекта рисунок — такой
  же источник, как абзац, но это именно рисунок.
* **Пример ЕДИНСТВЕННЫЙ.** Других свечей с размахом >100% базы внутри размеченных баз на
  реальных графиках курса нет.
* **Ломаная нарисована от руки, и метки разведены по горизонтали:** пик стоит на 2 бара
  левее свечи, впадина правее. Совпадает точно только УРОВЕНЬ ЦЕНЫ впадины с лоем свечи.
  Объяснение («от руки нельзя нарисовать ломаную, уходящую вниз и возвращающуюся по одной
  вертикали») правдоподобно, но это ИНТЕРПРЕТАЦИЯ, а не цитата.

Файлы для проверки глазами: `docs/course/pages/p13-tight.png`, `p13-native-11-12.png`,
`p19-tight.png`, `p19-base.png` (в git не идут, перерендерить командой из CLAUDE.md).

---

## 2. НАШ ДЕТЕКТОР уже считает двумя — совпадает с курсом

`swings.detect` строит фрактал Билла Вильямса: пять баров, экстремум на среднем. Условия
верхнего и нижнего фрактала наложены на РАЗНЫЕ поля свечи (`high` и `low`), поэтому
внешний бар удовлетворяет обоим одновременно — запрета в определении нет.

**Контроль на подстроенном ряду** (бар 2 имеет и наибольший хай, и наименьший лой из
семи): детектор вернул `[('high', 2), ('low', 2)]` — оба фрактала на одном баре.

**На живых данных**, 500 баров каждого ряда:

| символ | 5м | 15м | 1ч |
|---|---|---|---|
| BTW | 4 | 6 | 12 |
| BTC | 1 | 2 | 1 |
| ETH | 0 | 4 | 2 |

---

## 3. ЦЕНА ОТВЕРГНУТОГО ВАРИАНТА

Если бы такую пару считали ОДНОЙ точкой, структура могла бы упасть ниже порога «4 и более
точек» (стр. 22). Замер на 198 структурах (4 символа × 4 ТФ, по 500 баров):

| | структур | доля |
|---|---|---|
| всего | 198 | — |
| с общим баром у обеих границ | 13 | **6.6%** |
| потеряли бы квалификацию (<4 точек) | 6 | **3.0%** |

То есть отвергнутый вариант выбросил бы 3% структур вопреки размеченному примеру курса.

---

## 4. КЛАССИКА (источник №3) — к вопросу применима ограниченно

Курс высказался, поэтому классика здесь не решающая. Зафиксировано для протокола.

**Lo, Mamaysky, Wang, «Foundations of Technical Analysis» (Journal of Finance 2000,
NBER w7613), стр. 16 — ЦИТАТА ПРОВЕРЕНА ПО ТЕКСТУ СТАТЬИ, не по сниппету:**

> «An important advantage of using this kernel regression approach to identify patterns is
> the fact that it ignores extrema that are "too local." For example, a simpler
> alternative is to identify local extrema from the raw price data directly, that is,
> identify a price Pt as a local maximum if Pt−1 < Pt and Pt > Pt+1 and vice versa for a
> local minimum. The problem with this approach is that it identifies too many extrema and
> also yields patterns that are not visually consistent with the kind of patterns that
> technical analysts find compelling.»

⚠ **И ОГРАНИЧЕНИЕ, найденное при проверке.** Их определение работает на ряду ОДНОГО
значения на момент времени (цена, а не свеча). На таком ряду одна точка физически не
может быть и максимумом, и минимумом — условия взаимоисключающи, и наш случай в их
постановке НЕ ВОЗНИКАЕТ. Довод применим лишь как общее предупреждение о слишком частых
экстремумах. В первой редакции ответа владельцу я подал его сильнее, чем он есть.

Прочее из классики (внешний бар устанавливает оба экстремума в одной свече; диапазон
требует не менее двух касаний КАЖДОЙ границы; у Вайкоффа правила нет) собрано поиском по
вторичным источникам и здесь НЕ приводится: владелец справедливо забраковал блоги
брокеров и Medium как ненадёжные.

---

## 5. ОТРАСЛЬ: 62 источника, единого ответа НЕТ

Сбор завершён. Владелец забраковал блоги брокеров и Medium — здесь только исходный код
(читан, часть ИСПОЛНЕНА), рецензируемые статьи и справочники вендоров.

| позиция | источников | кто |
|---|---|---|
| допускают совпадение, считают ДВУМЯ точками | **13** | QuantConnect Lean (+юнит-тест), Stock.Indicators Fractal и Pivots (+тест на реальных данных), pandas-ta детектор, scipy, finta, backtrader, EA31337, Pine FAQ, Optuma, **наш `swings.detect`** |
| запрещают либо схлопывают в ОДНУ | **17** | ta4j, smartmoneyconcepts, строитель зигзага pandas-ta, nautilus_trader, jbn/ZigZag, TTR, Lean ZigZag, Stock.Indicators ZigZag, npzigzag, AmiBroker, Wealth-Lab, Quantower, MT ZigZag, **P&F high/low method**, Bry&Boschan, Harding&Pagan |
| запрещают ТОЛЬКО потому, что ряд один (совпадение невозможно) | **9** | Lo/Mamaysky/Wang, Chang&Osler, Osler, Glattfelder, Tsang, Chung&Bellotti, Xylogiannopoulos, Neely&Weller, Stock.Indicators при `EndType.Close` |
| молчат | **23** | вся документация «независимых рядов»: Pine reference, MQL5/MQL4 `iFractals`, MetaTrader Help, NinjaTrader, TradeStation, cTrader, MotiveWave, thinkorswim, Sierra Chart, Bulkowski, Keogh, Silagadze + 9 библиотек, где такой функции нет вовсе |

**Самое прямое свидетельство «за две точки» — QuantConnect Lean**, где случай назван в
коде и закреплён тестом:

```csharp
// Can be the bar forms both high and low pivot points at the same time
return (decimal)PivotPointType.Both;
OnNewPivotPointFormed(highPoint); OnNewPivotPointFormed(lowPoint);
```

**Правил разрешения в отрасли найдено ЧЕТЫРЕ, и они разные:** приоритет по направлению
колонки (P&F: «Use the high when another X can be drawn and then ignore the low»),
приоритет верха (smartmoneyconcepts), противоположный предыдущему (ta4j), не считать ни за
что (nautilus_trader). Единого ответа нет — тем весомее размеченный пример автора курса.

⚠ **Молчание распределено не случайно.** Ни один справочник класса «независимые ряды», где
совпадение тривиально возможно, о нём не говорит; говорят те, кто строит ОДНУ ломаную и
вынужден случай разрешать. Отсутствие оговорки не значит, что случая нет.

---

## 6. НАЙДЕННОЕ ПОПУТНО: молчаливое последствие в `pereprior`

Проверено на реальных данных (4 символа × 4 ТФ, по 500 баров), не принято на веру.

`pereprior._one` ищет ломаемый экстремум строго левее второго: `s.index <
last_other.index`. Значит противоположный свинг, стоящий НА ТОМ ЖЕ баре, в кандидаты не
попадает — берётся более ранний, и **зона тени ПП строится по ДРУГОЙ свече**.

| замер | значение |
|---|---|
| хаёв, у которых лой на том же баре | **55 из 1025 (5.37%)** |
| из них ломаемым берётся более ранний лой | **55 из 55 (100%)** |
| расхождение зон тени, медиана | **0.83%** цены |
| размах расхождения | от 0.01% до **27.39%** |
| случаев с расхождением > 1% цены | **24 из 55** |

Контроль: затронуто 5.37% хаёв, а не все — прибор различает случаи.

⚠ Отдельно: `sorted(swings.swings, key=lambda s: s.index)` при равных индексах даёт
порядок, заданный порядком двух `if` в `detect` (сначала HIGH, потом LOW). Внутри одной
свечи «что было раньше, хай или лой» из OHLC НЕИЗВЕСТНО в принципе — этот порядок фикция,
хоть и детерминированная.

### ВОПРОС ЗАКРЫТ 2026-08-17: строгое неравенство обосновано, докстрока написана

Требование владельца — обосновать источником либо записать в докстроку. Сделано и то и
другое; логика НЕ менялась, добавлены только комментарии (`git diff --stat`: 35 строк,
все вставки).

**Обоснование.** Стр. 50 требует лой, ИЗ КОТОРОГО вырос хай, — то есть событие,
случившееся РАНЬШЕ. У внешнего бара порядок хая и лоя внутри свечи из OHLC НЕИЗВЕСТЕН:
четыре числа не говорят, что было первым. Включить такой лой значило бы предположить, что
он был раньше хая, — предположение, которого в данных нет.

**Это тот же принцип, что проект уже применил в `outcome.py`**, дословно из его докстроки:
«по бару OHLC неизвестно, что случилось раньше — стоп или цель… Выбрать „обычно сначала
стоп“ значило бы придумать правило, которого нет ни в данных, ни в курсе (§4.3)». Там
неоднозначность помечается, здесь недоказуемый кандидат не берётся — в обоих случаях
выдумка отвергается, а не разрешается в чью-то пользу.

**Цена выбора названа в докстроке, а не спрятана:** у 5.37% хаёв зона ПП строится по
соседней свече, в 24 случаях из 55 расхождение больше процента цены. Лечится только
данными мельче бара (тики либо минутки внутри бара), которых у этого расчёта нет.

Отдельно назван и произвольный порядок: у двух свингов с одним `index` он задаётся
порядком двух `if` в `swings.detect`, то есть детерминирован, но произволен. На отбор не
влияет, потому что строгое `<` отсеивает их раньше; названо для следующей правки, которая
захочет неравенство ослабить.

⚠ **Дифф повтора на прогоне `btw2` показал изменение расчёта, и это НЕ следствие правки.**
Проверено снятием правки (`git stash`): без неё дифф тот же. Расчёт изменили правки
профиля, попавшие в дерево после снятия кадров `btw2`. Мои 35 строк — комментарии.

⚠ Первая попытка проверить это подстроенным рядом ПРОВАЛИЛАСЬ: оба варианта (лой на том же
баре и лой отдельным баром) дали 0 переприоров — ряд не создал условий вовсе. Контроль это
показал сразу, и замер был переделан на реальные данные. Одинаковый ответ на заведомо
разных входах — признак недействительного замера, а не результата.

---

## 7. ЧТО ОСТАЛОСЬ НЕПРОВЕРЕННЫМ

* **Книжная ветка закрыта частично.** Дословных цитат из Bill Williams «Trading Chaos»,
  Мёрфи, Эдвардса-Маги, Шабакера, du Plessis НЕТ: archive.org из этого окружения
  недоступен, Google Books отдаёт пустые ответы, пиратские PDF сознательно не качались.
  Самая перспективная цель — du Plessis, «The Definitive Guide to Point and Figure»:
  правило приоритета для этого бара должно быть там сформулировано книжно.
* **Исходник стандартного `ZigZag.mq5` / `Fractals.mq5` не получен** — поиск по GitHub дал
  0, справка MetaTrader алгоритма не описывает.
* **Park & Irwin (2007)** за платным доступом, ничем не подтверждён.
* **Поведение Lean не исполнено** — `dotnet` в системе нет; вывод по исходнику и тесту.
* Часть цитат в разделах документации снята через модель-читателя WebFetch, а не прочитана
  первым лицом.
* **Второй пример в курсе не найден.** Решение стоит на одной базе, показанной дважды
  (стр. 13 и 19).
* **Прибыльность правила не измерялась.** Всё выше — о соответствии разметке автора, а не
  о том, что так торговать выгоднее.

---

## Команда воспроизведения

```bash
uv run python -m hunter run --run-id btw --seconds 60
```

Замеры §2 и §3 сделаны разовыми скриптами в scratchpad (§7.4 — зонд отвечает на один
вопрос и удаляется); оба воспроизводятся вызовом `swings.detect` и `accumulation.detect`
на барах, полученных этим прогоном.
