# Контроль фальсифицируемости: способен ли каждый прибор выдать ДРУГОЙ ответ

Дата: 2026-08-04. Повод — замечание владельца: результат по переприорам оказался
артефактом расчёта, поданным как утверждение, и потому под сомнение попали все прежние
числа. Замечание принято: ниже не рассуждение о доверии, а сплошная проверка.

## Что проверяется

Не «верно ли число», а **может ли прибор выдать иной ответ**. Ответ, которого прибор
выдать НЕ СПОСОБЕН, — свойство прибора, а не данных. Это общая форма всех дефектов
сессии: 0% сужений, 0 истинных ПП, 435 пробоев, «нарушений 0» у гейта, не открывшего
ни одного файла.

Выборка: 8 символов × 5 ТФ = **40 пар**, по 1000 закрытых баров.
BTC · ETH · SOL · ONDO · BNB · XRP · DOGE · LINK, ТФ 5м/15м/1ч/4ч/1Д.
Всего структур 860, проверок событий на границах 1720.

## Результат

| прибор | исходы | вердикт |
|---|---|---|
| §2.1 накопление | структур на пару 13 … 29 (медиана 22); распадов 0 … 8; хвост есть у 32, нет у 8 | отвечает |
| §2.4 прокол/пробой | puncture **342**, breakout **983**, unresolved **141**, open **1**, заходов не было **253** | **все пять исходов** |
| §2.5 переприор | истинных **28**, ранних **52** | отвечает |
| §2.3 размещение стопового | inside **120**, below **95**, above **35**, before **296** | **все четыре** |
| §2.8 тренд | up 13, down 16, none 11 | отвечает |
| §2.8 согласие с приоритетом | by_trend **325**, against_trend **320**, no_priority **215** | отвечает |
| §2.9 дивергенции | конвергенций 10, дивергенций 6; RSI 6/3, MACD 4/3 | **обе формы, оба индикатора** |
| §2.9 сужение Боллинджера | посчитано 40 из 40, перцентили 1% … 83% → **исправлено: 1% … 85%** | отвечает |
| §2.9 EMA200 | посчитана 40 из 40 ⚠ см. поправку | отвечает |

> **ПОПРАВКА 2026-08-06 — перцентиль полос был занижен.**
>
> `factors.squeeze` отсеивал неопределённые значения только как `None`, а `polars_talib`
> отдаёт их как **NaN**. NaN не попадал в числитель (`w <= last` с NaN всегда ложно), но
> попадал в знаменатель. Перекос точный и односторонний:
>
>     p_исправленный = p_напечатанный × N / (N − 19),   19 = период полос (20) минус 1
>
> Формула проверена на 10 парах: предсказание совпало с пересчётом до сотых во всех
> десяти. Здесь ряд брался `limit=1000`, то есть N = 999 и множитель 1.0194:
> **1% → 1%, 83% → 85%.** Вывод строки («отвечает») поправка не меняет: разброс от
> единиц до восьмидесяти процентов остаётся разбросом.
>
> Строку про EMA200 поправка тоже задевает, но иначе и **непроверяемо**: `ma_touch`
> при неопределённой EMA возвращал фактор со значением `nan`, а не отказ. Значит «40 из
> 40» могло включать пары, где EMA200 не определена (ряд короче 200 баров). На данных
> того дня это уже не перепроверить; на сегодняшних десяти парах все ряды по 999 баров,
> и неопределённых нет.
>
> Разбор и замеры: [`false-numbers-2026-08-06.md`](false-numbers-2026-08-06.md)
| §2.6 сужение структуры | обе стороны **1**, одна сторона **112**, нет **558**, мало фракталов 189 | отвечает, но «обе стороны» 1 из 671 |

## Что контроль нашёл по дороге

**§2.4 `UNRESOLVED` срабатывает 141 раз.** В отчёте по §2.4 было написано «после
исправления не встретилась ни разу» — это была одна выборка из четырёх уровней. Ветка
живая.

**§2.8 дал сплошной `no_priority` 860 из 860 — и это оказался дефект ПРИБОРА, не модуля.**
Контрольный скрипт заполнял словарь прогонов в том же цикле, по которому шла проверка, а
старшие ТФ обрабатываются позже младших: на момент проверки 5м старших в словаре ещё не
было. `priority.resolve`, проверенная в изоляции, работает верно. Исправлено: все прогоны
считаются до проверок.

Это важный случай: **не всякий подозрительный ноль — дефект модуля**, но каждый обязан
быть разобран. Разбор здесь занял один вызов функции с заведомыми данными.

## Чего этот контроль НЕ доказывает

Он показывает, что ни один прибор не заперт в одном ответе. Он **не показывает, что числа
верны**: отвечающий прибор может быть откалиброван неправильно. Совпадение с методом автора
проверяется только на корпусе, и эта фаза не начиналась.

## Что осталось непроверенным

- §2.7 геометрия в контроль не входила: РР — арифметика от уже проверенных величин.
  Наблюдался разброс 0.04 … 6.12, то есть «золотой стандарт 1к3» достижим, но
  систематически не мерился.
- §2.2 уровень ПОК в контроль не входил: требует архива сделок на каждую структуру, и на
  860 структурах это отдельная работа по объёму.
- Доля сходящихся структур: 1 из 671 здесь против 1 из 204 в `figures-2026-08-04.md` —
  разные выборки, и обе слишком малы, чтобы говорить о доле.
- Корпус не привлекался.

## Как воспроизвести (§7.3)

```bash
uv run python scripts/probes.py breach
uv run python scripts/probes.py factors
uv run python scripts/probes.py pereprior
uv run python scripts/probes.py structures
```

Контроль §2.8 (согласие с приоритетом) отдельным зондом НЕ сохранён — он и был тем самым скриптом, в котором нашлась ошибка порядка вычислений.
