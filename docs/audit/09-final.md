# 09. Итог независимого аудита hunter-prizrak-v2

**Область:** HEAD `5e0edb14cf47145fb98836533d436b74254ffacf`, тег `pre-audit-2026-08-06`,
ветка `audit/2026-08-06`. В `main` не коммитилось ничего.
**Критерий:** `docs/FOUNDATION.md`. **Арбитр методики:** мини-курс PrizrakTrade и корпус.
**Дата:** 2026-08-06.

---

## 1. Опровергнутые ожидания — первым делом (§7.6)

### 1.1. Главное: контроль замера R-01 опроверг ожидание аудитора

Я ожидал, что уровень ПОК, посчитанный проектом (бины шириной в тик), и уровень,
посчитанный как у автора (профиль на 24 строки), — одна и та же величина с разной
разрешающей способностью, и что расхождение убывает с ростом числа строк.

**Нулевой контроль это опроверг.** Доля совпадений двух приборов на настоящих данных
статистически неотличима от доли совпадений на данных, где связь цены с объёмом разорвана:
29.1% против 26.3% при 24 строках, 28.6% против 30.3% при 50, 37.7% против 39.4% при 100.
То есть согласие приборов там, где оно есть, объясняется шириной строки, а не тем, что оба
нашли настоящий пик объёма.

### 1.2. Гипотеза D-04 из задания на аудит оказалась неверна

Утверждалось: правило «2–3 полных тел» курс формулирует только для уровня ПП, а §2.4
распространил его на любой уровень самовольно. **Курс распространяет его сам**, стр. 55, в
одном предложении: «Уровень ПП как, и уровни границ накопления или объемные уровни, могут
Прокалываться или Пробиваться, и требуют подтверждения». §2.4 цитирует, а не обобщает.

### 1.3. Я дважды ошибся в собственных проверках и сообщаю об этом

* **Нулевой контроль R-01, редакция 1** был не нулём: я подавал «равномерный объём» как
  единицу на занятую ячейку, а это плотность присутствия сделок — величина с пиком там же,
  где у объёма. Оба прибора её находили, согласие выходило 100%, и я едва не доложил «ответ
  хуже случайного». Вывод был бы неверен. Контроль переделан, результат отброшен целиком
  (`trials.md` №2).
* **Пробник гейтов после правки** менял умолчание периода, которое гейт всё равно
  передаёт явно, — и показал 0 там, где правка на самом деле сработала. Годная инъекция
  меняет тело вычисления. Оба прогона сохранены.
* Кроме того, первая редакция моей же правки гейта молча сузила охват с 4 величин до 3, а
  первая редакция реестра величин объявила мёртвыми четыре свойства, из которых мёртвым
  оказалось одно (ошибка в границах слова при грепе).

Если бы этих трёх мест никто не проверил, отчёт содержал бы четыре ложных утверждения.

---

## 2. Что проверено и НАРУШЕНИЙ НЕТ

Отрицательный результат ценнее положительного ровно там, где «ноль» мог означать
«ничего не искали». Поэтому у каждой строки — способ проверки.

| проверено | как | результат |
|---|---|---|
| §2.9 — индикаторы не порождают и не гейтят сигнал | трассировка `engine.decide` → `emit.hold_reason`; `hold` выводится ТОЛЬКО из `LevelStatus` | **чисто**; индикаторы живут в отдельном разделе печати карточки |
| §3 — запрещённые подсистемы | греп по 18 именам | **ни одной**; два совпадения — комментарий и предупреждение владельцу |
| §3 — составные метрики `*_score`, `*_confidence`, `*_quality`, `*_pressure`, `*_bias` | греп | **ноль** |
| §4.1 — абсолютные проценты | греп констант `*_PCT` | шесть, все названы курсом прямо |
| §4.2 — весовые суммы | греп | **ноль**; все совпадения по слову «вес» — вес запросов Binance |
| §6 — незакрытая свеча отбрасывается | ЗАМЕР с контролем: сырой ответ ccxt на всех трёх ТФ СОДЕРЖАЛ незакрытый бар | отброшено **ровно** незакрытое |
| §6 — часы сведены с биржей | ЗАМЕР: `now_ms − local_ms = 194` при `offset = 193` | сдвиг применяется |
| гейт чистоты (Э-1 прошлого разбора) | пробник `from . import clock` в чистом модуле | **код 1 — ловит**, дыра закрыта |
| ошибка курса ATH/ATL | греп | **не скопирована** |
| смоук-импорт | 28 модулей ОТДЕЛЬНЫМИ процессами | 28 из 28 |
| исходники вне VCS | `git check-ignore` по всем файлам | **пусто** |
| ruff, mypy strict, 18 гейтов | `bash scripts/check.sh`, код возврата сохранён | зелено |

**Дисциплина проекта высока.** Правило §0 соблюдается: каждое число метода сопровождается
страницей курса, и дословность цитат проверяется гейтом (205 цитат, расхождений 0).
Найденное ниже — не следствие небрежности, а следствие того, что часть вопросов ещё не
задавалась.

---

## 3. Находки

Полный реестр со статусами — `findings.md`. Здесь — существо.

### 3.1. P1 · Три индикаторных гейта не проверяли код проекта — **устранено**

§10.3 обещает: «Гейт считает ATR/RSI/MACD **проектным кодом** и эталонной библиотекой».
Фактически гейты звали `polars_talib` напрямую и сравнивали **две чужие библиотеки друг с
другом**. Изменение в `hunter/indicators.py` проезжало мимо всех трёх при зелёном коде
возврата. Найдено пробником с заведомым нарушением; исправлено; проверено повторным
пробником. Репро: `repro/M-06.sh`.

Это подтверждение находки Н-5 прошлого разбора, простоявшей открытой с 2026-08-04.

### 3.2. P1 · Сто честных отказов подряд, и все на одном ТФ — **устранено в части видимости**

Замер на 305 структурах: уровень строится у 91.7% структур на 15м, у 87.0% на 1ч, у 50.8%
на 4ч, у **7.8% на 1Д** и у **0.0% на 1Н**.

Каждый отказ честен и назван — §4.3 соблюдён поштучно. Но отказы никто не складывал по
измерению, вдоль которого возможен перекос, и оператор видел карту, состоящую ровно из тех
таймфреймов, которые курс считает слабее (стр. 48). Правило "у отказов обязана быть сводка
по измерению" записано владельцем в `CLAUDE.md` 2026-08-04 — механизмом оно не стало.

Сводка добавлена в карточку. Причина перекоса НЕ устранена и устранима не правкой: чтобы
построить профиль недельной структуры, нужен поток сделок за месяцы, а публичный `aggTrade`
такой глубины не отдаёт. Это противоречие §5 (профиль на реальных сделках) с §2.8
(главенство старших ТФ), и разрешает его владелец, а не аудитор.

### 3.3. P1 · §2.5 не входит в расчёт сигнала — не устранено

`engine.decide` объявлен единственной точкой расчёта; `pereprior.detect` там не вызывается
вовсе — переприор считается только при печати карточки. Следствие: уровень, которого цена
коснулась, переводится в режим "вход только по слому структуры на младшем ТФ" (стр. 25,
31), а наступление слома система не отслеживает. Оператор получает инструкцию,
исполнимость которой машина не проверяет. Репро: `repro/A-02.sh`.

Не устранялось намеренно: подключение меняет состав эмиссии, то есть это изменение расчёта,
а конформанс на корпусе (фаза 6) в этом аудите не выполнен и сравнивать «до/после» не с чем.

### 3.4. P2 по букве, тяжелейшее по последствиям · ПОК проекта — другая величина

Автор строит уровень инструментом фиксированного профиля объёма TradingView (стр. 63),
делящим структуру на фиксированное число строк — по умолчанию 24. §5 задаёт бины шириной
`tickSize`: медиана 6 178 строк на структуру, максимум 98 964. Разница разрешающей
способности — двести пятьдесят раз.

Замер на 175 структурах при допуске 0.10 ATR, зафиксированном до прогона: совпадение
**29.1%**, медианное расхождение 0.314 ATR = 9.2% высоты структуры, 95-й перцентиль
2.15 ATR. Нулевой контроль (см. 1.1) показывает, что и эти 29% информации о совпадении
пиков не несут.

**Почему это тяжелее всего прочего:** ПОК — цена входа. От неё считаются стоп, РР, цели,
лестница закупа и момент флипа. Шкала severity не различает «число посчитано неверно» и
«число посчитано верно, но, возможно, не то»; здесь второе.

Не устраняется правкой: это выбор §5 против инструмента автора, решение владельца.

### 3.5. P2 · Отбор структур — функция конвенции, которой в курсе нет

Курс нигде не определяет, какой экстремум считается хаем. Проект выбрал фрактал Вильямса
на пяти барах и закрыл пробел по §0.2 корректно — тремя независимыми источниками. Замер
чувствительности при столь же законных альтернативах:

* число структур **186…470** (в 2.5 раза);
* доля истинных переприоров против ранних меняется **на 80%** (10…18);
* тренд, задающий СТОРОНУ сделки, определяется на **7…14 рядах из 18**.

Из 305 боевых структур с ближайшей альтернативой совпадают 122 (40%).

**Смягчающее и существенное:** там, где две конвенции находят одну и ту же структуру, её
границы почти тождественны (медиана сдвига 0.000–0.149 ATR). Неустойчив ОТБОР структур, а
не расчёт их границ. Это разные дефекты, и лечатся они по-разному.

### 3.6. P2 · §2 объявлен полным перечнем и не полон — **устранено в документе**

Прочитаны напрямую (текстовый слой PDF плюс просмотр схем) все четырнадцать гипотез
задания. **Тринадцать подтверждены, одна опровергнута.** Двенадцать из тринадцати — одного
класса: правило курса отсутствует в §2. Из них девять уже РЕАЛИЗОВАНЫ в коде, то есть §2
отстал не от курса, а от собственной реализации.

§2 дополнен, §3 получил раздел 3.1 «что в курсе есть и всё равно не реализуется — с
основанием»: хеджирование и доливка (исключены по §1), фигуры, лимитный барьер,
расшифровка ATH/ATL.

### 3.7. P2 · §2.6 утверждал не пробел, а неверное — **исправлено**

«Фигуры… отдельной логики не требуют» — но вымпел имеет СХОДЯЩИЕСЯ границы (видно на схеме
стр. 57), а детектор накопления при схождении зон структуру бросает, и `BoundaryZone` —
пара горизонтальных чисел. Треугольники исключены по построению, а не обработаны.

### 3.8. P2 · Спецификация не позволяет воссоздать систему

Субагент, читавший ТОЛЬКО `FOUNDATION.md` и не имевший доступа к коду, нашёл **99
недоспецифицированных мест, из них 53 меняют результат**, и 20 элементов, которых в
документе нет вовсе. Из одиннадцати существенных мест его добросовестная реализация
совпала с проектной в пяти; в трёх расхождениях (ширина бина, цена бина, определение
свинга) выбор прямо двигает цену входа.

Два из этих трёх затем проверены замерами (3.4 и 3.5) и подтвердились.

### 3.9. Мелкое

**М-01 (P2)** — прочтение правила о первых двух точках спорно. Стр. 18 дословно:
«Границы базы (хай и лой) чаще всего определяются первыми 2-мя точками.» Схема той же
страницы нумерует точки чередующимися — 1, 3, 5 сверху и 2, 4, 6 снизу, — то есть первые
две это по одной на границу, и граница выходит линией, а роль зоны играет отдельный
элемент (лимитный барьер). Код читает как первые две точки каждой стороны. Курс словом не
различает.

**М-05 (P3)** — «стоп 1–3%» не говорит, процентов чего. Код берёт процент от цены границы;
альтернатива — от высоты структуры, и на узкой структуре это разница в разы.

**М-07 (P3)** — в кэше сделок лежат файлы старой схемы имени, без шага цены.

**А-01 (P2)** — этап 6 (Telegram) отсутствует целиком, `aiogram` стоит в БОЕВЫХ
зависимостях, а `README.md` в настоящем времени обещает: «сообщает оператору в Telegram».
Само по себе незавершение этапа нормально; ненормально, что документ, который читает
владелец-непрограммист, описывает несуществующую способность.

---

## 4. Реестр статусов

| ID | severity | статус закрытия |
|---|---|---|
| М-06 индикаторные гейты | P1 | **устранено**, проверено пробником после правки |
| М-02 сводка отказов по ТФ | P1 | **устранено** в части видимости; причина перекоса — решение владельца |
| А-02 §2.5 вне расчёта | P1 | **принято к сведению** — правка меняет расчёт, нужен конформанс |
| М-03 ПОК — другая величина | P2 / тяжелейшее | **принято к сведению** — решение владельца по §5 |
| М-04 чувствительность к свингу | P2 | **принято к сведению** — арбитраж требует фазы 6 |
| С-01 §2 неполон | P2 | **устранено** правкой §2 и §3 |
| С-02 §2.6 неверен | P2 | **устранено** правкой §2.6; детектор фигур — решение владельца |
| М-01 «первые 2 точки» | P2 | **принято к сведению**, отмечено в §2.1 |
| А-01 этап 6 | P2 | **частично**: отмечено; `README.md` не правился |
| С-03 недоспецифицированность | P2 | **частично**: закрыты места, попавшие в §2 |
| М-05 «процент чего» | P3 | **принято к сведению**, отмечено в §2.7 |
| М-07 мусор в кэше | P3 | **принято к сведению** |
| Э-1 гейт чистоты | — | **закрыта до аудита**, проверено пробником |

---

## 5. QA над самим аудитом

<!-- QA-СЕКЦИЯ -->

---

## 6. Что требует решения владельца — не правки

1. **§5 против инструмента автора.** Бины по `tickSize` или профиль на фиксированное число
   строк. От этого зависит цена входа во всей системе.
2. **§5 против §2.8.** Профиль на реальных сделках делает уровни старших ТФ непостроимыми.
   Либо суррогат профиля из барных объёмов для 1Д/1Н, либо явный отказ от старших ТФ.
3. **Подключать ли §2.5 к решению** — то есть проверять ли слом на младшем ТФ машинно.
4. **Реализовать ли фигуры и лимитный барьер** — обе требуют новых подсистем.
5. **Правка §0** под фактическую практику: нужна категория «выбор внутри диапазона,
   названного источником» и категория «операционная величина транспорта».
6. **Этап 6 (Telegram)** — строить или убрать `aiogram` из боевых зависимостей и поправить
   `README.md`.

---

## 7. Ограничения САМОГО аудита

### 7.1. Пределы независимости

Аудит ИИ-кода ИИ-агентом даёт **управленческую** независимость: у меня не было
обязательств перед прежними решениями, и я мог опровергать что угодно. Техническую
независимость он даёт лишь **частично**: у меня и у автора кода общая архитектура, а
значит общие слепые зоны. Конкретно: я склонен принимать за доказательство тот же класс
аргументов, который убедил его; там, где он ошибся способом, свойственным моей
архитектуре, я ошибусь так же и не замечу.

Три из моих собственных ошибок (раздел 1.3) — иллюстрация: во всех трёх я построил
проверку, которая по построению не могла обнаружить то, что искала. Это ровно тот дефект,
который аудит ищет в коде.

Частичное средство — QA-фаза с чистым контекстом (раздел 5): проверяющий не видел моих
рассуждений и получил задание опровергать. Но и он той же архитектуры.

### 7.2. Что получено прямым чтением, а что пересказом

| источник | класс |
|---|---|
| текст курса (страницы 3, 10, 11, 16, 18, 19, 22–26, 33, 36, 55, 57, 58, 63) | **прямое чтение** извлечённого текстового слоя |
| схемы курса (страницы 18, 20, 26, 57, 63) | **прямой просмотр изображения** |
| `docs/FOUNDATION.md`, `src/`, `gates/` | **прямое чтение** |
| результаты замеров R-01, R-02, транспорта, гейтов | **исполнение**, вывод сохранён в `evidence/` |
| остальные 55 страниц курса | **не читались** |
| корпус разборов (42 файла) | **не читался**, кроме греповых выборок |
| находки прошлого разбора (`HANDOFF-2026-08-05.md`) | прочитан целиком; его собственные числа — **пересказ**, на HEAD проверены выборочно (Э-1, Н-5, А-8, §2.5, Б-4) |
| поведение TradingView (число строк профиля по умолчанию) | **пересказ**, прямой проверки в приложении не делалось |

### 7.3. Израсходовано испытаний: 14

`trials.md`. Двенадцать содержательных на выборке 18 рядов и 305 структур — это
**разведка, а не проверка гипотезы**. Ни одно число здесь не сопровождается доверительным
интервалом и не является оценкой вероятности.

Практическое следствие: утверждение «параметризация X лучше Y» на этой же выборке уже
недопустимо — двенадцать вариантов просмотрено, и выбор лучшего из просмотренных есть
подгонка. Для такого утверждения нужна отдельная выборка.

### 7.4. Почему совпадение с разметкой автора не доказывает прибыльности

Даже если бы система воспроизводила уровни автора идеально, это означало бы одно:
методика перенесена верно. **Верна ли сама методика — другой вопрос,** и разметка на него
не отвечает: автор публикует свои разборы, а не свои результаты, и подтверждённое
совпадение с разметкой есть подтверждение переноса, а не проверка метода.

Кроме того, в системе не смоделированы комиссии, фандирование и проскальзывание, а
медианный стоп порядка 0.7% цены означает, что круговая комиссия тейкера съедает заметную
долю риска. Любое число вида «средний R» без этого завышено.

Ответ на вопрос «работает ли метод» может дать только накопление реальных исходов за
содержательный срок. Этот путь сократить нельзя, и ничто в этом аудите его не сокращает.

---

## 8. ЧТО ОСТАЛОСЬ НЕПРОВЕРЕННЫМ

Раздел обязателен по §7.6; отчёт без него недостоверен.

1. **Фаза 6 — конформанс на корпусе НЕ ВЫПОЛНЕНА.** Ни одна зона, ни один уровень не
   сопоставлены с разметкой автора. Это самый большой пропуск аудита: без него нельзя
   сказать, какая из параметризаций верна, и находки 3.4 и 3.5 остаются вопросами без
   арбитра. Замер `author-poc-2026-08-04.md`, который делал это раньше, самим проектом
   помечен как требующий перемера (Н-8) и не перемерялся.
2. **Реестр величин неполон.** Разобраны три класса из 255 публичных имён; величина за
   величиной не прослежена.
3. **`run.py` (1 444 строки) и `store.py` (709) прочитаны выборочно.** Леджер, запись
   исходов, служба 24/7 — по этим путям аудита фактически не было.
4. **Открытые находки прошлого разбора проверены выборочно.** Подтверждены Э-1 (закрыта),
   Н-5, А-8, §2.5-в-эмиссии, Б-4 (закрыта именем кэша). Остальные строки статуса
   «[ВЫВЕДЕНО]» из `HANDOFF-2026-08-05.md` — Л-1…Л-4 (целостность леджера), Б-5
   (неатомарная запись кэша), Д-4, Д-8, А-3, А-5, А-6, Э-2, Э-4, Н-7, Н-10, Р-4, Р-5, Р-6
   — **на HEAD не проверялись**.
5. **Повтор `a1` даёт 605 изменённых строк ещё до моих правок.** Проверено, что моим это
   не является (откат `card.py` расхождение сохраняет), и наиболее вероятное объяснение —
   накопленные изменения расчёта с 2026-08-06, то есть штатная работа повтора.
   Но **альтернатива не исключена**: находка А-3 прошлого разбора утверждает, что повтор
   негерметичен и читает живой кэш. Различить их я не пытался.
6. **Служба 24/7 не запускалась.** `hunter check` наблюдает 400 секунд; дрейф часов за
   час (`MAX_SYNC_AGE_MS`, число прямо помечено в коде как незамеренное) не проверен.
7. **55 страниц курса из 69 как изображения не просматривались.** Схемы могут содержать
   правила, которых нет ни в тексте курса, ни в §2 — ровно так найден лимитный барьер.
8. **Число строк профиля TradingView по умолчанию (24) взято пересказом**, а не проверено
   в самом приложении. Если оно иное, числа замера R-01 сдвинутся; направление вывода —
   нет, поскольку любой фиксированный счёт строк на два-три порядка меньше числа тиковых
   бинов.
9. **Ни один замер не повторялся на независимой выборке.** Всё измерено на BTC, ETH и BCH,
   и весь суточный кэш сделок покрывает 4.5 месяца. Символы вселенной, кроме трёх,
   в замеры не входили вовсе.

---

## 9. Команды воспроизведения (§7.3)

Все числа этого отчёта получены исполнением, а не рассуждением. Полный набор:

```bash
bash scripts/check.sh
```

```bash
uv run python -m hunter check
```

```bash
uv run python docs/audit/probes/probe_transport_live_2026-08-06.py
```

```bash
uv run python docs/audit/probes/probe_poc_binning_v2_2026-08-06.py
```

```bash
uv run python docs/audit/probes/probe_swing_sensitivity_2026-08-06.py
```

```bash
bash docs/audit/repro/M-06.sh
```

```bash
bash docs/audit/repro/M-02.sh
```

```bash
bash docs/audit/repro/A-02.sh
```

```bash
uv run python -m hunter replay --run-id a1 --diff
```

Сохранённый вывод каждой — в `docs/audit/evidence/`, рядом с командой и кодом возврата.
