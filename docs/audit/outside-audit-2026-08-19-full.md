# Сторонний аудит hunter-v2: 77 несоответствий курсу, корпусу и классике

Задание владельца 2026-08-19: «представь что ты сторонний аудитор и впервые видишь проект
и мини курс и корпус призрака... выяви все не соответствия. составь список из 50
необходимых изменений» и следом «минимум 50 число должно остаться! добери то чего не
хватает». Список собран из **77** пунктов — все, что пережили проверку, оставлены.

⚠ Этот файл ЗАМЕНЯЕТ черновик `docs/audit/outside-audit-2026-08-19.md`, написанный тем же
днём по поверхностному проходу. Тот черновик был остановлен владельцем вопросом «ты
прошелся по всем модулям? ты точно прочел корпус и мини курс?» — и вопрос был по делу:
курс был прочитан только текстовым слоем, корпус — одним файлом из четырнадцати, код —
докстроками пятнадцати модулей из тридцати четырёх.

## Как собран этот список

| заход | чем занимался | результат |
|---|---|---|
| источники | 3 агента прочитали **все 69 страниц курса КАК ИЗОБРАЖЕНИЯ** (`docs/course/pages/p01.png`…`p69.png`), 3 — корпус разборов, конспекты и корпусные документы `docs/audit/` | 382 правила с референтами |
| проект | 6 агентов прочитали **все 34 модуля `src/hunter` целиком**, включая `src/hunter/exchange.py` (3153 строки), `src/hunter/run.py` (2477), `src/hunter/store.py` (1355) | 247 наблюдений о фактическом поведении |
| классика ТА | 4 агента, каждый обязан взять 3-5 надёжных источников на элемент (CME, вендорские справки, учебники) | вошло в свод |
| сверка | 7 тематических агентов; обоим сводам дан статус ГИПОТЕЗ, каждый пункт перепроверялся по файлу и по странице | 77 находок |
| опровержение | 11 агентов, задача — **опровергнуть**, по умолчанию находка неверна | 77 устояло, 0 опровергнуто, 12 уточнено |

## ⚠⚠ Чего этот список НЕ доказывает — читать до пунктов

**1. Опровергнуто НОЛЬ из 77, и это само по себе подозрительно.** Прибор, который никого
не опроверг, стоит рядом с гейтом, который никогда не падает. Контроль «мог ли
проверяющий ответить иначе» проведён и ответ ДА, но слабое: 12 находок оспорены по
существу, и одна из них (D-13) оказалась не дефектом, а зафиксированным решением
владельца от 2026-08-18. Такие пункты помечены знаком ⚠ в указателе.

**2. НИ ОДНО ЧИСЛО из полей «Чем видно» не перепроверено.** Проверяющим это было прямо
запрещено ради скорости: они сверяли КОД и СТРАНИЦУ, а не замеры. Значит числа в списке —
утверждения агентов, а не свидетельства. Перед правкой по любому пункту число снимается
заново.

**3. Два пункта я проверил лично, и оба стоят:**
* `RR_EMIT_MIN = 3.0` объявлен порогом эмиссии и **не подключён ни к чему**: во всём
  репозитории одна строка `src/hunter/geometry.py:277` и ни одного использования. Отсюда
  ОТЗЫВ моего собственного доклада владельцу этой смены: я сказал, что порог отсекает 979
  сигналов из 1136 — он не отсекает ни одного;
* сигналов без цели в боевом леджере — **292 из 1484 (19.7%)**, из них 282 уровневых и
  10 от ПП. Находка называла 281 из 1154; расхождение — рост леджера, а не ошибка.
  Контроль: у 1192 сигналов цель есть, значит поле не пусто поголовно.

**4. Кавычки в тексте пунктов — ОБЫЧНЫЕ, а не «ёлочки», намеренно.** Конвенция проекта
отдаёт «ёлочки» только дословной цитате, сверенной посимвольно с текстовым слоем PDF.
Цитаты, приведённые агентами, посимвольно не сверялись, поэтому все они снижены до
обычных кавычек. Одна такая цитата уже поймана проверяющим как неверная — см. ⚠ у C-11.

## Порядок работ

Правки идут сверху вниз по разделам: сначала A (расчёт считает не то), затем B (правило
источника не исполнено), затем C (посчитано и не предъявлено), затем D (приборы) и E.
Внутри раздела — в порядке номеров. Перед каждой правкой: снять число заново, после —
дифф повтора §10.6, если тронут расчёт карточки.

## Указатель

Знак ⚠ — проверяющий оспорил формулировку; читать уточнение в теле пункта.

| № | находка |
|---|---|
| A-1 | "Тест ПП" засчитывается, когда цена из зоны вообще не выходила: пробой меряется по одному краю зоны, тест — по всей… |
| A-2 | Сделка от ПП: "ближний край" означает в двух модулях РАЗНЫЕ цены, а стоп стоит не "за хай/лой", а на границе тела т… |
| A-3 | BoundaryZone.puncture набирается только из свинговых точек — записанный прокол мельче фактического захода за границ… |
| A-4 | "Границы базы" в карточке включают свечи ВЫХОДА из структуры |
| A-5 | Сетка профиля натянута на ценовой диапазон, которого нет в гистограмме — ПОК и зона считаются по мёртвым строкам |
| A-6 | Детектор накопления не отличает флэт от НЕ-флэта: 97% ряда объявлено накоплением, на перемешанных барах — столько же |
| A-7 | Якорь стопа берётся из БУДУЩЕГО относительно рождения уровня |
| A-8 | "Поджатие" (одна граница горизонтальна) не распознаётся: его касания выбрасываются, граница остаётся на внешнем экс… |
| A-9 | Прокол меряется от ЛИНИИ детекции, а стоп "за прокол" — от КОРОБКИ: в 74% случаев прокол лежит ВНУТРИ напечатанных… |
| A-10 | Сетка профиля и "границы базы" натянуты на коробку СО СВЕЧАМИ ВЫХОДА, а объём в неё кладётся без них — ПОК уезжает… |
| A-11 | Отработкой уровня считается ЛЮБОЕ касание зоны — курс требует забора самого ПОК либо зоны "с хорошей реакцией" |
| A-12 | "Сила уровня по объёму" — свойство соседей, а не уровня: одна и та же структура получает разную плотность в боевом… |
| A-13 | "Коробка" прижатого накопления и "границы" самого уровня — две разные величины, напечатанные рядом одинаково |
| A-14 | Ордера "на зону" не существует: вход — всегда одна цена, ПОК |
| A-15 | Лестница закупа собирается без стороны сделки: у 52% лестниц средняя ТВХ ХУЖЕ одиночного входа |
| A-16 | Дробление закупа — только подпись: РР, стоп и исход считаются от одной цены, а карточка внутри одного блока сама се… |
| A-17 | Исполнение лимитки считается касанием диапазона бара: цена, ПЕРЕПРЫГНУВШАЯ вход, даёт "сделки не было" |
| A-18 | "Цена пришла к уровню" определено в проекте дважды и по-разному: карта снимает лимитки по ЗОНЕ, журнал требует ПОК ⚠ |
| A-19 | "Прокол за границу" никогда не лежит за границей: якорь меряется от линии детекции, а границы — коробка ХАЙ…ЛОЙ |
| A-20 | Якорь стопа берётся из БУДУЩЕГО: свинги и стоповые объёмы не фильтруются по времени рождения уровня |
| A-21 | Стоп сделки от переприора ставится РОВНО на край зоны — "за" превращено в "=", и такие сигналы уже 312 штук в леджере |
| B-22 | Фрактал: равенство соседних баров — проект утверждает, что источники требуют строгого неравенства; два из трёх проц… ⚠ |
| B-23 | Переприор объявляется без предшествующего тренда: внутри чистого нисходящего тренда печатается "ранний ПП в шорт"… |
| B-24 | У прокола нет НИЖНЕЙ границы глубины: касание уровня тенью на один тик уже объявляет уровень отработанным ⚠ |
| B-25 | Полярность (поддержка↔сопротивление) объявлена во всех текстах проекта и не исполнена ни в одном расчёте: Level.fli… ⚠ |
| B-26 | Точки границ считаются СУММОЙ по обеим сторонам — классический минимум касаний НА ЛИНИЮ этим порогом выразить нечем… |
| B-27 | Объём не участвует в различении прокола и пробоя, хотя курс на этом месте молчит, классика единодушна, а данные объ… |
| B-28 | Ничья ПОК отбрасывает уровень, хотя процедура источников её РАЗРЕШАЕТ ("ближайшая к середине диапазона") |
| B-29 | Граница базы по первым двум точкам (прямое правило стр. 18) вычисляется и никуда не доходит |
| B-30 | "Истинный ПП" присваивается без теста, и такой сигнал уходит в леджер |
| B-31 | Стоп сделки от ПП: ветвь "за базу в месте слома" отсутствует, а стоп ставится РОВНО на край зоны — медианный риск 0… |
| B-32 | Тренд не участвует в разметке структуры: стр. 57/58 "первая точка по тренду" и "вход на 6 касании" не реализованы |
| B-33 | Сигнал ПРОТИВ тренда старшего ТФ эмитируется без ограничения и без записи признака в леджер |
| B-34 | Объём не может перебить таймфрейм в отборе уровней, хотя курс называет ровно этот случай |
| B-35 | Флип не переворачивает сторону уровня: Level.flipped() не вызывается ниоткуда |
| B-36 | Курс даёт вход отработанному и флипнутому уровню, проект не производит для них ни одного числа — а это 96% карты |
| B-37 | Поздний возврат объявлен проколом, то есть ОТРАБОТКОЙ, хотя курс задаёт проколу срок в 1-2 свечи |
| B-38 | Флаг "закуп дробим / один ордер" напечатан, но лестницей не управляет, и ордера НА ЗОНУ в лестнице нет никогда |
| B-39 | Вход по слому структуры на младшем ТФ вычисляется, называется владельцу и не порождает ни одной сделки |
| B-40 | Целый раздел "ТОРГОВЛЯ ВО ФЛЕТЕ" (стр. 18-20) не реализован: входа от ГРАНИЦЫ живой базы нет ни одного |
| B-41 | Флипнутый уровень: владельцу сказано "торгуется ретестом", а вход по нему не строится вообще |
| B-42 | Вход "на 6 касании" и резерв на доливку (стр. 57-58) отсутствуют, хотя сама фигура детектируется |
| B-43 | Правило стр. 51 "истинный + ранний ПП рядом → закуп делим на 2 части" неисполнимо по построению |
| B-44 | Доливка/повторный вход отсутствует как класс, хотя курс называет её в пяти местах |
| B-45 | Перевод стопа в безубыток отсутствует в проекте целиком, и отсутствие не названо как ограничение |
| B-46 | Вторая ветвь стопа ПП — "или базу в месте слома" — не реализована вовсе |
| B-47 | Полоса поиска якоря 2-5% выключена потолком высоты базы у двух третей уровней — курсовое "идеально стоп прятать за… ⚠ |
| B-48 | Рисковый стоп реализован только одной из двух названных курсом опор |
| C-49 | Карточка обещает владельцу, что сделка от ПП "эмитируется в леджер", — её никто не эмитирует |
| C-50 | "Поздний возврат без единого тела за уровнем" объявлен ПРОКОЛОМ, то есть ОТРАБОТКОЙ, и уровень снимается — школа за… ⚠ |
| C-51 | Заявленное число строк TV_ROWS = 100 боевым расчётом не исполняется: сетка выходит 19…149 строк, ровно 100 — у 4.6%… ⚠ |
| C-52 | Число точек границ закрытой структуры не предъявляется — правило "4 и более точек" владелец проверить не может |
| C-53 | Половина экстремумов внутри структуры выброшена молча — сводки отказов нет |
| C-54 | Рамка структуры на графике выше и ниже своих свечей — тот самый симптом, на который владелец жаловался глазами |
| C-55 | "Накопление у уровня" печатает ЛИНИИ под именем "коробка" |
| C-56 | "Тренд не определён" склеивает сходящуюся структуру и отсутствие тренда |
| C-57 | В карточке уровня нет ни одной отметки времени — свежая структура и структура возрастом в тысячи баров выглядят оди… |
| C-58 | Отчёт прогона называет РР "по рисковому стопу", хотя считает его по выставляемому — владелец читает число под невер… ⚠ |
| C-59 | Карточка печатает стоп только в процентах ОТ ВХОДА; запас ОТ ГРАНИЦЫ — величина, которую нормирует курс, — не печат… ⚠ |
| C-60 | Карточка печатает ярлык якоря рядом со стопом, который якорь не ставил |
| C-61 | RR_EMIT_MIN объявлена порогом эмиссии и владельцу доложено, что она отсекает 86.2% сигналов, — константу не читает… |
| C-62 | График бота — то, что владелец реально смотрит, — не рисует стоп вовсе |
| D-63 | Прибора, сверяющего записанный прокол с фактическим экстремумом, в проекте нет: единственный гейт прокола проверяет… |
| D-64 | Верхний тик базы не входит в сетку профиля: цена, равная top, оказывается ВНЕ строк и прижимается вниз — у 33% уровней |
| D-65 | Прибор "слом структуры на младшем ТФ" не способен ответить "нет": он смотрит последний переприор всего ряда, а не с… ⚠ |
| D-66 | Прибор проверки слома на младшем ТФ не способен ответить "нет": 2953 ответа из 2953 — "подтверждён" ⚠ |
| D-67 | RR_EMIT_MIN объявлен порогом эмиссии входа и владельцу доложен как действующий, но в коде не подключён ни к одной в… |
| D-68 | Вход эмитируется и без единой цели: 281 сигнал из 1154 записан с target=NULL, и выиграть такая сделка не может |
| D-69 | Зонд качества эмиссии считает РР, R и дистанцию стопа по stop_risky — стопу, который не выставляется, не печатается… |
| D-70 | Леджер смешивает две конвенции стопа; разрез по recorded_at, объявленный проектом ОБЯЗАТЕЛЬНЫМ, не сделан нигде |
| D-71 | Владельческий запрос "чем куплены выигрыши — геометрия сделок" усредняет дистанцию стопа по уровневым и ПП-сигналам… |
| D-72 | Гейт стопа зелёный на конфигурациях, которых боевой путь не производит |
| D-73 | Три справочных стопа и доля частичной фиксации вычисляются и не читаются никем; параметры запаса у build_setup корм… |
| D-74 | hunter check — единственный вердикт по-русски — не содержит ни одной строки о стопе |
| D-75 | Ни леджер, ни карта не хранят стоп и границу вместе — курсовое правило о стопе по журналу проверить нечем |
| E-76 | Владельцу доступны ДВЕ разные процедуры области стоимости под одним именем: hunter profile печатает зону не по тому… |
| E-77 | Докстрока PPSetup утверждает, что сделка от ПП в эмиссию не идёт, — она идёт в леджер, и там уже 312 строк ⚠ |
---

# A. Расчёт неверен — проект считает НЕ ТО — 21 пунктов

### A-1. "Тест ПП" засчитывается, когда цена из зоны вообще не выходила: пробой меряется по одному краю зоны, тест — по всей зоне

**Источник.** Классика определяет возврат к пробитому уровню ДВУМЯ условиями, и оба у проекта отсутствуют. Bulkowski, throwback: https://thepatternsite.com/throwbacks.html — "After an upward the breakout from a chart pattern, price lifts off then falters and returns to the launch price. This curling price behavior is called a throwback if it occurs within a month of the breakout"; там же требование отрыва: "The stock must not slide along the breakout price but zoom up, curl around and return to, or come close to, the breakout price or chart pattern trendline (boundary)". Bulkowski, pullback (зеркальный случай, наш): […]

**Проект.** src/hunter/pereprior.py:84-100 `_test_index`: цикл `for i in range(from_index, len(bars))` — верхней границы окна НЕТ вовсе, тест ищется до конца ряда; условие теста — `lo <= bars[i].close <= hi`, то есть закрытие внутри ВСЕЙ зоны тени, без требования, чтобы цена перед этим зону покинула. МЕХАНИЗМ НАЗВАН: пробой в pereprior.py (`edge = hi if side is PPSide.SHORT else lo`) меряется по ОДНОМУ краю зоны, а тест — по ВСЕЙ зоне [zone_lo, zone_hi]. Между этими двумя определениями лежит полоса шириной во всю тень, в которой цена ОДНОВРЕМЕННО "за уровнем" (два полных тела ниже zone_hi) и "вернулась на тест" (закрытие […]

**Правка.** 1) Свести пробой и тест к ОДНОЙ величине (правило проекта "прибор смотрит на ту же величину"): если пробой считается по ближнему краю, то и выход из зоны, и возврат считать от него же — иначе полоса двойного смысла остаётся по построению. 2) Ввести обязательное УСЛОВИЕ ОТРЫВА: тест не засчитывается, пока цена не ушла за уровень на названную величину (у Bulkowski это "must not slide along the […]

**Чем видно.** Зонд: C:\Users\3EC2~1\AppData\Local\Temp\claude\C--Users-------Documents-hunter-v2\db8721f4-1545-4a47-9813-1ac82f4a3bd8\scratchpad\probe_audit.py, блок B (`uv run python <путь>`). Напечатано: "B: ПП true/short слом бара 5 зона [5.0, 7.5] подтверждён на 12 тест на 13" и "цена между подтверждением и тестом опускалась только до 6.4, нижний край зоны 5.0 — из зоны она НЕ выходила: True". Тест на СЛЕДУЮЩЕМ баре после […]

**Проверка (опровергнуть не удалось).** pereprior.py:84-100 `_test_index`: `for i in range(from_index, len(bars))` — верхней границы окна нет, условие ровно `lo <= bars[i].close <= hi` по всей зоне, требования отрыва/выхода из зоны нет; зона строится как (bar.low, min(open,close)) — то есть шире одного края, по которому меряется пробой.

### A-2. Сделка от ПП: "ближний край" означает в двух модулях РАЗНЫЕ цены, а стоп стоит не "за хай/лой", а на границе тела той же свечи — риск равен длине одной тени

**Источник.** Правило проекта, введённое ревизией 2026-08-18: "Две сущности под одним человеческим именем — дефект по построению"; и правило владельца "стоп 3-5% за структуру". Классика ставит стоп ЗА экстремум структуры, а не внутрь свечи, и всегда с запасом: Эдвардс-Маги и Мёрфи о фильтрах проникновения — https://stockdisciplines.com/stock-trends-penetrations/ : "a 3% penetration of the trendline by the closing price is a sensible way to define a trend penetration", а для стопов, выставленных брокеру, "a 6% penetration by the stock's low price is reasonable" — то есть даже стоп-цена относится к ЛОЮ структуры плюс запас, а […]

**Проект.** Одно и то же словосочетание "БЛИЖНИЙ край зоны" обозначает две разные цены: src/hunter/pereprior.py (комментарий перед `edge = hi if side is PPSide.SHORT else lo`) — ближний край для ШОРТА это `zone_hi`; src/hunter/geometry.py:470-472 — "вход — БЛИЖНИЙ край зоны тени", и код geometry.py:495 берёт для шорта `entry, stop = pp.zone_lo, pp.zone_hi`, то есть ближним назван `zone_lo`. Далее geometry.py:473 утверждает "стоп — за ДАЛЬНИЙ край зоны (это и есть хай/лой слома, стр. 50)" — но для шорта дальний край это `zone_hi = min(open, close)` сломанной свечи, то есть ГРАНИЦА ТЕЛА, а хай/лой этой свечи (`bar.low`) взят […]

**Правка.** 1) Развести имена: одно слово "ближний край" на две цены — ровно тот дефект, из-за которого 51.7% зон вылезали за структуру и никто этого не видел; назвать края по стороне подхода и проверить, что расчёт, инвариант и печать берут ОДНО поле. 2) Пересмотреть стоп: стр. 50 говорит "стопом за хай/лой ИЛИ БАЗУ в месте слома" — сейчас не выполнено ни то, ни другое, стоп сидит внутри свечи. 3) Замерить […]

**Чем видно.** Зонд: C:\Users\3EC2~1\AppData\Local\Temp\claude\C--Users-------Documents-hunter-v2\db8721f4-1545-4a47-9813-1ac82f4a3bd8\scratchpad\probe_audit.py, блок B. Напечатано: "сетап: вход 5.0 стоп 7.5 риск 2.5 = 50.00% от входа", где 5.0 — это `bar.low` сломанной свечи (её "лой", по стр. 50 — место стопа), а 7.5 — `min(open, close)` той же свечи; риск 2.5 равен ровно длине её нижней тени. Строки кода: […]

**Проверка (опровергнуть не удалось).** geometry.py:495 для шорта `entry, stop = pp.zone_lo, pp.zone_hi`, а pereprior.py:80 даёт zone_lo=bar.low (тень) и zone_hi=min(open,close) (граница тела) — то есть вход на лое, стоп внутри свечи, риск ровно длина нижней тени; докстрока geometry.py:470-474 при этом утверждает "стоп — за ДАЛЬНИЙ край… это и есть хай/лой слома" и "стоп РОВНО […]

### A-3. `BoundaryZone.puncture` набирается только из свинговых точек — записанный прокол мельче фактического захода за границу у 62.1% сторон структуры, медиана недобора 36% высоты базы

**Источник.** КУРС, стр. 18 (дословно, сверено с PDF): "Но если на 3++ точках были проколы за границы - стоп всегда ставится за этот прокол". КЛАССИКА определяет и границу диапазона, и глубину её прокола по ЭКСТРЕМУМАМ ЦЕНЫ, а не по подтверждённым фракталам (5 источников): 1) StockCharts ChartSchool, S&R — "Support can be established with the previous reaction lows, while resistance can be established by using the previous reaction highs" https://chartschool.stockcharts.com/table-of-contents/chart-analysis/support-and-resistance ; 2) Wyckoff, StockCharts ChartSchool — "The lows of the SC and the ST and the high of the AR set […]

**Проект.** src/hunter/accumulation.py:158 `BoundaryZone.puncture`. Значения `hi_punct`/`lo_punct` присваиваются ИСКЛЮЧИТЕЛЬНО внутри обработки свинговых точек (accumulation.py:437-451, 463-469, 488-495, 510-512) — цена берётся из `SwingSet`; ни один бар вне свингов в расчёт не входит. Сверх того accumulation.py:383 `if kind is last_kind: continue` (правило чередования сторон) выбрасывает 20.4% подтверждённых свингов — 1788 из 8765 на том же корпусе, и выброшенные не могут стать проколом даже если они самые дальние. При этом `puncture` — САМЫЙ СИЛЬНЫЙ якорь стопа: src/hunter/levels.py:800-801, и по докстроке `stop_anchor` […]

**Правка.** Набирать `puncture` по `high`/`low` БАРОВ окна структуры за замороженной границей (свинги оставить для нумерации точек и для правила "3++ точки"), после чего перемерить стопы диффом повтора §10.6 и доложить владельцу двумя числами. Правило чередования сторон при этом не трогать — оно с рисунков курса; чинится не оно, а источник глубины прокола.

**Чем видно.** uv run python <scratchpad>/audit_punct_gap_2026-08-19.py 60 и <scratchpad>/audit_punct_gap_bysides_2026-08-19.py 60 (ROOT=C:/Users/Антон/Documents/hunter-v2). ОТПЕЧАТОК ДАННЫХ: 60 рядов из data/frames, 32406 баров, 879 закрытых структур, 1758 сторон. РЕЗУЛЬТАТ: фактический экстремум внутри окна структуры глубже записанного прокола у 1091 стороны из 1758 (62.1%); недобор — медиана 36.1% высоты базы (1.13% цены), 90-й […]

**Проверка (опровергнуть не удалось).** accumulation.py: hi_punct/lo_punct присваиваются только внутри веток обработки свинговых точек (price из SwingSet), обращения к high/low баров там нет; правило чередования `if kind is last_kind: continue` действительно выбрасывает точки до учёта прокола. Числа evidence не перепроверял.

### A-4. "Границы базы" в карточке включают свечи ВЫХОДА из структуры

**Источник.** стр. 23 курса, дословно: "Пока цена не вышла из структуры - у нас нет уровня, уровень появляется когда цена полноценно выходит из структуры на данном ТФ - тогда мы натягиваем профиль объема и выделяем уровень." Рисунок стр. 13 подтверждает: правая вертикаль коробки стоит на последней пронумерованной точке (12), а не на импульсе выхода; на стр. 18 последняя стрелка зигзага уходит за правый край рамки.

**Проект.** src/hunter/levels.py:515 — `seg = bars[acc.first_index:acc.last_index + 1]`, где `acc.last_index == acc.exit.confirmed_at_index`, то есть в срез входят ОБА бара подтверждения выхода (CONFIRM_BODIES=2). Из этого среза берутся `boundary_lo/hi` (levels.py:423-424) — та самая строка "границы X…Y", которую читает владелец (src/hunter/card.py:186). В 195 строках ВЫШЕ, в той же цепочке вызовов, `structure_window_ms` (levels.py:320) режет окно по `acc.exit.first_body_index - 1` с докстрокой "Свечи ВЫХОДА в окно не входят… тела выхода это уже не накопление". Две несовместимые границы структуры в одной функции `build_all`.

**Правка.** В `levels.build_all:515` строить срез по тому же правилу, что и окно профиля: `seg = bars[acc.first_index:acc.exit.first_body_index]`. Одна функция `structure_bars(acc, bars)` на оба места, чтобы вторая копия правила не разошлась снова.

**Чем видно.** uv run python -c "from hunter import store,swings,accumulation as ac; n=w=0\nfor d in store.saved_symbols('last'):\n for tf in store.saved_timeframes('last',d):\n b=store.read_bars('last',d,tf)\n ..." → структур 9780, коробка шире окна профиля 6168 (63.1%); раздутие высоты медиана ×1.064, p75 ×1.212, p90 ×1.415, max ×6.56. Разобранный пример: BCH/USDT:USDT 1ч, окно структуры 2026-04-11 06:00…2026-04-12 00:00, свечи […]

**Проверка (опровергнуть не удалось).** levels.py:515 seg = bars[acc.first_index:acc.last_index+1] (last_index = бар подтверждения выхода) даёт boundary_lo/hi, тогда как structure_window_ms (levels.py:310-320) режет окно по acc.exit.first_body_index-1 с докстрокой "Свечи ВЫХОДА в окно не входят" — две несовместимые границы в одной цепочке.

### A-5. Сетка профиля натянута на ценовой диапазон, которого нет в гистограмме — ПОК и зона считаются по мёртвым строкам

**Источник.** стр. 26 курса, дословно: ""Натягивая" профиль на структуру – важно захватить все свечи структуры!" Диапазон профиля — размах СВЕЧЕЙ СТРУКТУРЫ, то есть ровно тех, чей объём в этот профиль и вошёл.

**Проект.** src/hunter/levels.py:384-385 — `build_tv(hist, bottom=price_range[0], top=price_range[1])`. Гистограмма `hist` собрана по окну `structure_window_ms` (levels.py:503, 512), то есть БЕЗ свечей выхода, а `bottom/top` берутся из `price_range` — размаха СО свечами выхода (levels.py:515). Верхние и нижние строки сетки не получают ни одной сделки; `volume_profile.build_tv` добавляет пустую строку в область стоимости бесплатно (`covered + 0.0 > target` ложно), а `TVProfile.clamped_volume` — сторож, который должен был это поймать, — не читается ни одним модулем.

**Правка.** Подавать в `build_tv` размах баров ОКНА ПРОФИЛЯ (`min(low)`, `max(high)` по `bars[acc.first_index:acc.exit.first_body_index]`). Правка сходится с находкой №1: после неё `price_range`, окно профиля и границы становятся одной величиной.

**Чем видно.** Пересчёт ПОК/зоны на верном диапазоне по кадрам `last` (9701 уровень, построен обоими способами): доля сетки ВНЕ реальных баров окна — медиана 5.98%, p90 29.34%, max 84.75%; ПОК сдвинулся у 3964 уровней (40.9%), зона VAL/VAH — у 3922 (40.4%); сдвиг ПОК медиана 0.0056% цены, p90 0.032%, max 4.68%. То есть ТВХ меняется у 40% уровней. Отпечаток: прогон `last`, 5 символов.

**Проверка (опровергнуть не удалось).** levels.py:382-386 build_tv(hist, bottom=price_range[0], top=price_range[1]) — hist собран по structure_window_ms (без свечей выхода), а price_range пришёл из seg со свечами выхода (levels.py:515-519); строки сетки вне баров окна пусты и попадают в область стоимости бесплатно.

### A-6. Детектор накопления не отличает флэт от НЕ-флэта: 97% ряда объявлено накоплением, на перемешанных барах — столько же

**Источник.** стр. 13 курса, дословно: "Флэт - это боковой тренд, при котором в течении периода времени локальные хаи/лои повторяются и не меняются." Стр. 22: "Флет/накопление (ренж/бaза) - накопление с конкретными границами, имеющая в рамках своего ТФ понятные 4 и более точек." Рисунок стр. 20 показывает восемь маленьких коробок внутри одной большой С БОЛЬШИМИ ПУСТЫМИ ПРОМЕЖУТКАМИ — сплошного покрытия у автора нет.

**Проект.** src/hunter/accumulation.py:266 `detect` — первые две точки каждой стороны принимаются БЕЗУСЛОВНО, где бы они ни стояли: ветки `if len(hi_px) < 2` / `if len(lo_px) < 2` не проверяют ничего. Ни критерия "повторяются и не меняются", ни критерия "конкретные границы" в модуле нет. Единственная величина, которая их мерит, — `BoundaryZone.width_pct` (accumulation.py:185) — вычисляется и НЕ ЧИТАЕТСЯ ни расчётом, ни карточкой, ни гейтом (грепом по репозиторию единственный потребитель — scripts/probes.py:271). Её собственная докстрока обещает калибровку "на корпусе (этап 3)", которой не было.

**Правка.** Ввести проверку "чётких границ" до принятия пары: разброс двух первых точек стороны не больше заданной доли высоты базы (величина берётся замером по разметке автора на скриншотах корпуса, а не подбором), иначе `reset()` с названной причиной. До появления референта — печатать `width_pct` обеих сторон в карточке, чтобы владелец увидел глазами, что зовётся "конкретной границей".

**Чем видно.** Покрытие ряда закрытыми структурами по кадрам `last`: BCH 15м 97.8%, ETH 15м 97.8%, ASTR 1ч 97.8%, ETH 5м 97.6%, BTC 5м 97.3% (1328 структур на 51840 баров, распадов 6). КОНТРОЛЬ, что прибор способен ответить иначе и что ответ не лучше случайного: (а) те же бары BTC 5м с ПЕРЕМЕШАННЫМИ приращениями — 1689 структур, покрытие 97.8%, то есть БОЛЬШЕ, чем на настоящих данных; (б) синтетический флэт (синус ±1% в коридоре) […]

**Проверка (опровергнуть не удалось).** accumulation.py:271-272 (ветки `if len(hi_px) < 2` / `len(lo_px) < 2`) принимают первые две точки стороны безусловно; width_pct (accumulation.py:185) сама в докстроке признаёт, что порог не задан и число "сообщается наружу для калибровки на корпусе (этап 3)". Числа покрытия и контроль на перемешанных барах не перепроверял.

### A-7. Якорь стопа берётся из БУДУЩЕГО относительно рождения уровня

**Источник.** стр. 18 курса, дословно: "Если в диапазоне 2-5% от границы есть стоповый объем – база мелкого ТФ - или Лой того же ТФ или ТФ-1 - идеально стоп прятать за них." Стоп ставится в момент сделки — значит якорь обязан существовать на момент рождения уровня; заглядывание вперёд запрещено правилом проекта (I-5), ради которого заведено `Level.created_at_ms`.

**Проект.** src/hunter/levels.py:550 — `stop_anchor(...)` получает `svs` из `classify_stop_volume` по ВСЕМ закрытым накоплениям младших ТФ и `sw_prices` по ВСЕМ свингам ряда, без единого условия на время. `src/hunter/stop_volume.py:105 _placement` знает только BEFORE/ABOVE/BELOW/INSIDE — ветки AFTER нет, поэтому структура младшего ТФ, закрывшаяся уже ПОСЛЕ рождения уровня, получает метку "внутри/над/под" как своя. Тот же класс дефекта проект уже ловил у ЦЕЛЕЙ (196 из 366 = 54%, разбор 2026-08-04) и завёл ради него `alive_at`; к стопу та же мерка не приложена.

**Правка.** В `levels.build_all` фильтровать кандидатов по времени до вызова `stop_anchor`: стоповый объём — только с `bars[acc.exit.confirmed_at_index].open_ms + tf_ms <= created_at_ms` уровня; свинг — только с `bars[sw.confirmed_at_index].open_ms + tf_ms <= created_at_ms`. Добавить `Placement.AFTER` в `stop_volume._placement`, чтобы отброшенное было названо, а не исчезло.

**Чем видно.** Зонд по кадрам `last` (9780 структур): якорь найден у 4677 (47.8%) — свинг 2512, стоповый объём 805, прокол 1360. Якорь стал ИЗВЕСТЕН ПОЗЖЕ `created_at_ms` уровня у 1649 из 4677 (35.3%): свинг 1283 из 2512 (51.1%), стоповый объём 366 из 805 (45.5%), прокол 0 из 1360 (он принадлежит своей структуре по построению). Отпечаток: прогон `last`, 5 символов, 6 ТФ.

**Проверка (опровергнуть не удалось).** levels.py:545-552 в stop_anchor передаются svs и sw_prices, собранные по ВСЕМ свингам/накоплениям ряда без условия на created_at_ms; stop_volume.py:105-114 _placement возвращает только BEFORE/ABOVE/BELOW/INSIDE — ветки AFTER нет.

### A-8. "Поджатие" (одна граница горизонтальна) не распознаётся: его касания выбрасываются, граница остаётся на внешнем экстремуме

**Источник.** стр. 34 курса, дословно: "Стоповый объем может быть в сужении, в поджатии, с четкими границами – это такое же накопление (база)…" Стр. 57: "треугольник это структура накопления (стоповый), просто в сужении, может быть "с поджатием"." Рисунок стр. 57 показывает поджатие как ГОРИЗОНТАЛЬНУЮ верхнюю границу с поднимающейся нижней.

**Проект.** src/hunter/accumulation.py:366 `converging` требует схождения ОБЕИХ сторон: для нового хая — `price < hi_px[-1] and lo_px[-1] > lo_px[-2]`. При горизонтальной верхней границе условие `hi_px[-1] < hi_px[-2]` ложно, поэтому КАЖДЫЙ поднимающийся лой уходит в `continue` на accumulation.py:499 ("касание внутри базы") — он не нумеруется, границу не двигает, в `point_indices` не попадает. Двусторонний критерий сужения референта не имеет: докстрока `detect` сама называет его собственным правилом ("засчитывается, только когда сходятся ОБЕ стороны"), а курс формы "поджатие" и "в сужении" перечисляет через запятую как […]

**Правка.** Разделить `converging` на две ветки по стр. 57/58: ПОДЖАТИЕ — одна граница неподвижна, вторая монотонно идёт внутрь (принимать точку, двигать только её сторону); РАВНЫЕ ГРАНИЦЫ — обе идут навстречу (нынешнее правило). Признак формы (`horizontal | pinch | wedge`) положить в `BoundaryZone` и напечатать в карточке рядом с "база в сужении".

**Чем видно.** Синтетический прогон детектора (три эталонные формы, шаг 5м): ПОДЖАТИЕ (верх ровный 100, низ 96→99.2) — свингов 8, точек засчитано 5, сужений 0, границы 96.781…100.020, то есть нижняя граница осталась на ВТОРОМ лое вместо поджатой 99.2; ТРЕУГОЛЬНИК с обеими сходящимися границами — точек 8, сужений 4, границы 98.280…98.420 (работает — это и есть контроль, что прибор способен ответить иначе); ПРЯМОУГОЛЬНИК — точек 6 […]

**Проверка (опровергнуть не удалось).** accumulation.py:375-380 converging требует схождения обеих сторон (`price < hi_px[-1] and lo_px[-1] > lo_px[-2]` и симметрично), поэтому при горизонтальной верхней границе поднимающийся лой уходит в `continue` (accumulation.py:498-499) и границу не двигает; двусторонний критерий референта в курсе не имеет.

### A-9. Прокол меряется от ЛИНИИ детекции, а стоп "за прокол" — от КОРОБКИ: в 74% случаев прокол лежит ВНУТРИ напечатанных границ

**Источник.** стр. 18 курса, дословно: "Но если на 3++ точках были проколы за границы - стоп всегда ставится за этот прокол, т.к. это может быть расширением базы и в будущем цена может ходить уже в этом расширенном диапазоне." Прокол по смыслу лежит ЗА границей, иначе "расширение базы" бессмысленно.

**Проект.** `BoundaryZone.puncture` считается относительно `edge` — линии по первым двум точкам (src/hunter/accumulation.py:455, 505). В `levels.stop_anchor:801` он добавляется в кандидаты БЕЗУСЛОВНО, минуя и полосу 2-5%, и потолок высоты базы, а `boundary`, от которого отмеряется всё остальное, подан из коробки ХАЙ…ЛОЙ (levels.py:550). Медиана отношения коробка/линии — 1.824. Докстрока `stop_anchor` утверждает "прокол по построению принадлежит своей же структуре" — замером опровергается.

**Правка.** После слияния границ (находки №1 и №7) считать прокол от ТОЙ ЖЕ величины, что предъявляется как граница. Пока величины две — отбрасывать кандидата-прокол, попавший внутрь `[boundary_lo, boundary_hi]`, и печатать это как названный отказ, а не молча полагаться на пол 3%.

**Чем видно.** Зонд по кадрам `last`: структур с проколом 4935 из 9780 (50.5%); проколов, лежащих ВНУТРИ коробки границ, — 3640, медиана глубины 20.2% высоты базы (p75 25.8%). Видно в карточке: строка "якорь: прокол" стоит при основании стопа "запас за структуру", то есть якорь напечатан, но на стоп не влияет — его перебил пол 3%.

**Проверка (опровергнуть не удалось).** levels.py:545-551 передаёт в stop_anchor boundary_lo/hi (коробка price_range, levels.py:423-424) и zone накопления, а внутри stop_anchor:800-801 zone.puncture добавляется в кандидаты БЕЗУСЛОВНО, минуя полосу band_lo/band_hi — то есть прокол меряется от edge зоны, а всё прочее от коробки. Числа evidence не перепроверял.

### A-10. Сетка профиля и "границы базы" натянуты на коробку СО СВЕЧАМИ ВЫХОДА, а объём в неё кладётся без них — ПОК уезжает у 40.9% уровней

**Источник.** Стр. 23: "Пока цена не вышла из структуры - у нас нет уровня, уровень появляется когда цена полноценно выходит из структуры на данном ТФ - тогда мы натягиваем профиль объема и выделяем уровень." Стр. 26 (текст): "важно захватить все свечи структуры!" — то есть свечи СТРУКТУРЫ, а не выхода. Рисунки это показывают прямо: на стр. 26 верхнее ребро бирюзовой коробки проходит НИЖЕ хая свечи выхода (зелёная свеча на правом ребре уходит далеко выше коробки), на стр. 30 нижнее ребро коробки "ХАЙ…ЛОЙ" проходит ВЫШЕ лоя красной свечи выхода. У автора коробка кончается ДО свечей выхода.

**Проект.** src/hunter/levels.py:320-321 — `structure_window_ms` исключает тела выхода (`last_inside = acc.exit.first_body_index - 1`), и по этому окну строится ГИСТОГРАММА. Но src/hunter/levels.py:515-519 подаёт `price_range` по `bars[acc.first_index:acc.last_index + 1]`, где `acc.last_index == acc.exit.confirmed_at_index` (accumulation.py:571,581), то есть +2 бара выхода. Тот же кортеж идёт в `bottom`/`top` сетки TV (levels.py:384-385) и в `boundary_lo/hi` (levels.py:423-424). Итог: диапазон сетки и объём в ней взяты по РАЗНЫМ множествам баров — у TradingView, чей прибор здесь воспроизводится, они всегда одни и те же.

**Правка.** В `build_all` (levels.py:515) считать `seg = bars[acc.first_index : acc.exit.first_body_index]` — ровно те бары, по которым построена гистограмма. Тогда сетка, "границы базы" и объём снова одна сущность.

**Чем видно.** Зонд по кадрам `last` (9780 структур, 5 символов): коробка шире окна профиля у 6168 (63.1%); раздутие высоты базы медиана +6.38%, p90 +41.5%, max +555.6%; ПОК по коробке отличается от ПОК по настоящей структуре у 3964 из 9701 (40.9%), сдвиг медиана 0.006% цены, p90 0.032%, max 4.91%; доля строк сетки, под которыми нет НИ ОДНОГО бара, медиана 6.0%, p90 29.3%, max 84.7% (пустая строка добавляется в зону бесплатно: […]

**Проверка (опровергнуть не удалось).** levels.py:320 `last_inside = acc.exit.first_body_index - 1` — гистограмма строится без свечей выхода, а levels.py:515 `seg = bars[acc.first_index:acc.last_index + 1]` даёт диапазон сетки и boundary_lo/hi по коробке С барами выхода. Разные множества баров подтверждены по коду; проценты (63.1%, 40.9%) не перепроверялись.

### A-11. Отработкой уровня считается ЛЮБОЕ касание зоны — курс требует забора самого ПОК либо зоны "с хорошей реакцией"

**Источник.** Рисунок стр. 24: выноска "тест объемов сам уровень не забрали" стоит к зелёной стрелке на ВЕРХНЕЙ жёлтой линии уровня (27.396), нижняя линия 26.424 не достигнута — и уровень остаётся рабочим, дальше на схеме подписана "новая твх". Стр. 31 (текст): "ВАЖНО: если цена ранее забирала зону и уже получила от нее хорошую лонг реакцию, уровень лимитными ордерами больше не торгуем" — условий ДВА, забор зоны И реакция. Стр. 25: "Как только уровень был отработан на 1 касание" — и в скобках там же "увидели хорошую реакцию". Рисунок стр. 31 разводит эти события во времени: "Цена забрала зону и получила лонг реакцию" […]

**Проект.** src/hunter/levels.py:1190-1194 — `first_test_index` считает касанием любое пересечение бара с [zone_lo, zone_hi]: `if bars[i].low <= hi and bars[i].high >= lo`. Ни достижения ПОК, ни реакции не проверяется; слова "реакция" в расчёте нет вовсе (грепом по src оно есть только в докстроках). Через levels.py:1083 это снимает лимитки, а src/hunter/emit.py:105-109 убирает уровень из эмиссии целиком.

**Правка.** Развести два поля — `zone_touched` (кромка зоны) и `poc_touched` (`low <= price <= high`), как их разводит стр. 24. Снимать лимитки по `poc_touched`, а по `zone_touched` — только вместе с измеренной реакцией (ход внутрь на заданную долю высоты базы), как требует стр. 31.

**Чем видно.** Кадры `last`, 9715 построенных уровней: 178 активных уровней потеряли лимитки ТОЛЬКО касанием зоны; у 169 из них (94.9%) цена НИ РАЗУ не дошла до самого ПОК — буквально случай стр. 24. Глубина захода бара, снявшего лимитки, медиана 10.1% ширины зоны, p25 4.2%, мельче 10% зоны — 49.4% случаев. Торгуемых лимитками уровней при этом всего 198: правило отсекает почти столько же, сколько оставляет. Без чтения кода это […]

**Проверка (опровергнуть не удалось).** levels.py:1190-1192 — `for i in ...: if bars[i].low <= hi and bars[i].high >= lo: return i`, где lo/hi это zone_lo/zone_hi; проверки достижения ПОК нет, а грep по src/hunter/*.py по `reaction|poc_touched` не дал ни одного вхождения — измерения реакции в расчёте нет вовсе.

### A-12. "Сила уровня по объёму" — свойство соседей, а не уровня: одна и та же структура получает разную плотность в боевом прогоне и в ответе бота

**Источник.** Стр. 22: "Сила уровня определяется ТФ и объемом" — сила приписана УРОВНЮ. Стр. 63: индикатор назван "Профиль объема видимой области", то есть окно задаётся экраном символа, а не набором найденных уровней.

**Проект.** src/hunter/levels.py:593 — окно композита `oldest = min(lv.structure_from_ms for lv in built)` берётся по НАБОРУ УЖЕ ПОСТРОЕННЫХ уровней. При `frame_bars=180` (сборка бота по запросу, tgbot.py:639 и tgbot.py:646) структуры старше кадра отсеиваются раньше (levels.py:504-509), `oldest` сдвигается — и тот же самый уровень получает другой знаменатель `vrvp_total_qty`/`vrvp_total_bins`, значит другую `vrvp_density` (levels.py:228).

**Правка.** Считать композит по окну СИМВОЛА (от первого бара самого длинного собранного ряда до последнего), а не от `min(structure_from_ms)` построенных уровней; либо считать его до применения рамки `frame_bars`.

**Чем видно.** Один и тот же символ, одни и те же кадры, два пути расчёта (`build_all(..., frame_bars=None)` против `frame_bars=180`): ASTR 14 общих уровней — плотность разошлась у 14 (100%), медиана 28.9%, max 45%; BCH 23/23, медиана 11.6%; BTC 28/28, медиана 11.6%, max 27%; ETH 23/23, медиана 5.2%, max 23%; POL 28/28, медиана 6.5%, max 15%. Порог, по которому бот помечает "объём ×N к среднему" и выбирает сильнейшего среди […]

**Проверка (опровергнуть не удалось).** levels.py:593 действительно `oldest = min(lv.structure_from_ms for lv in built)` — знаменатель композита берётся по НАБОРУ уже построенных уровней, значит отсев по frame_bars меняет окно и плотность у того же уровня. Числа evidence не перепроверял.

### A-13. "Коробка" прижатого накопления и "границы" самого уровня — две разные величины, напечатанные рядом одинаково

**Источник.** Рисунок стр. 30: выноски "ХАЙ" и "ЛОЙ" указывают на верхнее и нижнее рёбра бирюзовой коробки — граница структуры это экстремумы свечей. Стр. 28 разводит конфигурации 2/5/7 именно положением НОВОЙ КОРОБКИ относительно линии уровня: "5 - цена пробили уровень, закрепилась, сформировав новую структуру НАД уровнем."

**Проект.** src/hunter/levels.py:1152-1159 — `pressed_structures` заполняет `box_lo`/`box_hi` из `acc.lower.edge` и `acc.upper.edge`, то есть ЛИНИЙ детекции по точкам свингов, и по ним же делит на BELOW/ABOVE/ASTRIDE и проверяет пересечение с зоной. А "границы" того же уровня двумя сотнями строк выше (levels.py:423-424) — коробка ХАЙ…ЛОЙ. src/hunter/engine.py:340-346 печатает координаты `box_lo…box_hi`, и src/hunter/card.py:234 ставит их строкой "накопление у уровня: …" рядом со строкой "границы …" (card.py:185) — обе пары чисел выглядят однородно.

**Правка.** В `pressed_structures` брать коробку той же природы, что и `Level.boundary_lo/hi`, — `min(low)…max(high)` баров прижатой структуры; тогда и пересечение с зоной, и деление на конфигурации 2/5/7 считаются по той величине, которую владелец читает.

**Чем видно.** Кадры `last`, 9780 структур: отношение (коробка ХАЙ…ЛОЙ)/(линии детекции) — медиана 1.824, p25 1.462, p75 2.577, max 372.3; шире вдвое и более у 41.5% структур. Значит и "прижато к зоне", и класс 2/5/7 считаются по сущности, которая у 41.5% структур вдвое уже предъявленной. (Попутно: докстроки проекта называют это отношение 3.156 — на нынешних данных медиана 1.824, число устарело.)

**Проверка (опровергнуть не удалось).** levels.py:1152 `lo, hi = acc.lower.edge, acc.upper.edge` — и деление на BELOW/ABOVE/ASTRIDE, и проверка пересечения с зоной идут по линиям детекции, тогда как границы уровня после слияния 2026-08-18 — коробка ХАЙ…ЛОЙ. Отношение 1.824 не перепроверял.

### A-14. Ордера "на зону" не существует: вход — всегда одна цена, ПОК

**Источник.** стр. 30 курса, текстовый слой PDF: "- чтобы точно "забрало" ваш ордер - используем 2-3 ордера, на зону, и на уровень ПОК. Если накопление очень большое ТФ 1Д-1Н-1М - то закуп всегда стоит делить на зону и на уровень."

**Проект.** src/hunter/geometry.py:441-446 — `entry=level.price` и `ladder=tuple(sorted({level.price, *(x.price for x in inner)}))`: в лестницу попадают ТОЛЬКО ПОК уровня и ПОКи вложенных уровней. Поля `entry_zone_lo/entry_zone_hi` (geometry.py:443-444) вычисляются и в лестницу не идут никогда. Зонд по кадрам `last` (5 символов, 9715 уровней, 198 сетапов): "вход != ПОК: 0", "цена зоны попала в лестницу: 1 из 198" (единственный случай — совпадение кромки зоны с ПОК вложенного уровня). Карточка при этом печатает владельцу обещание второго ордера: src/hunter/card.py:70 ENTRY_LABEL["limit"] = "лимитки на ПОК и зону (стр. 30)" […]

**Правка.** В `geometry.build_setup` добавлять в `Setup.ladder` цену зоны, обращённую к цене (zone_lo для лонга, zone_hi для шорта) — безусловно при `split_orders` и как второй ордер по стр. 30 в остальных случаях; в `emit.outcome_of`/`outcome.resolve` исполнением считать первый сработавший ордер набора, а не единственный ПОК.

**Чем видно.** sed -n '12653,12656p' data/frames/last/BTC_USDT_USDT/card.txt — строка "вход: лимитки на ПОК и зону (стр. 30)", зона 63484.6…63730.6, а лестница "63595.3 · 63673.75" кромок зоны не содержит. Зонд: C:\Users\3EC2~1\AppData\Local\Temp\claude\C--Users-------Documents-hunter-v2\db8721f4-1545-4a47-9813-1ac82f4a3bd8\scratchpad\probe_entry.py (отпечаток: прогон last, 5 символов, 9715 уровней)

**Проверка (опровергнуть не удалось).** geometry.py:441-446 буквально: entry=level.price, entry_zone_lo/hi проставляются, а ladder=tuple(sorted({level.price, *(x.price for x in inner)})) — кромки зоны в лестницу не попадают никогда; при этом card.py:69-71 ENTRY_LABEL['limit'] печатает "лимитки на ПОК и зону (стр. 30)".

### A-15. Лестница закупа собирается без стороны сделки: у 52% лестниц средняя ТВХ ХУЖЕ одиночного входа

**Источник.** стр. 32 курса: "Как торговать: лучше закуп делать на все уровни, что бы ваша средняя твх была максимально безопасная."

**Проект.** src/hunter/levels.py:644-689 `nested()` — в докстроке прямо: "Сторона НЕ фильтруется… Отбор по стороне — работа вызывающего, здесь его нет", и вызывающий (src/hunter/geometry.py:446) его тоже не делает: в лестницу попадает любой вложенный ПОК внутри коробки родителя, в том числе ВЫШЕ входа у лонга и НИЖЕ входа у шорта. Замер по ТЕКСТУ сохранённых карточек (5 символов, 71 лестница длиннее одного ордера): у 48 (67.6%) есть ордер по невыгодную сторону от ПОК, у 37 (52.1%) напечатанная "средняя при равных долях" хуже одиночного входа — то есть прямо противоположна цели, названной стр. 32. Контроль (прибор способен […]

**Правка.** В `geometry.build_setup` фильтровать `inner` по ходу сделки (для лонга `x.price <= level.price`, для шорта `>=`) перед сборкой `ladder`; либо, если курс читается как "все уровни", печатать рядом со средней явную пометку, что она хуже входа, — но тогда правило стр. 32 не исполняется по определению.

**Чем видно.** sed -n '12653,12656p' data/frames/last/BTC_USDT_USDT/card.txt — "ЛОНГ 15м ПОК 63595.3 … лестница 63595.3 · 63673.75 средняя при равных долях 63634.525" (средняя ВЫШЕ входа у лонга); sed -n '11199,11202p' того же файла — "ШОРТ 15м ПОК 73575.75 … средняя 73259.50833333" (средняя НИЖЕ входа у шорта). Зонд: scratchpad/probe_ladder.py

**Проверка (опровергнуть не удалось).** levels.nested() (levels.py:644+) возвращает любые вложенные уровни внутри коробки родителя, а вызывающий geometry.py:446 берёт их ПОКи в ladder без всякого сравнения с level.price и без стороны — ордера по невыгодную сторону входа попадают в набор по построению.

### A-16. Дробление закупа — только подпись: РР, стоп и исход считаются от одной цены, а карточка внутри одного блока сама себе противоречит

**Источник.** стр. 30 курса: "Если накопление очень большое ТФ 1Д-1Н-1М - то закуп всегда стоит делить на зону и на уровень. Если накопление маленькое - 5м-1ч – эффективнее входить 1 ордером от уровня."; стр. 32: "средняя твх"

**Проект.** `Setup.split_orders` (src/hunter/geometry.py:445) выводится из ЯРЛЫКА ТФ (`BIG_STRUCTURE_TFS={"1d","1w"}`, geometry.py:59) и не связан с составом `ladder`; читает его только карточка (src/hunter/card.py:256). `Setup.average_entry_equal_shares` (geometry.py:138) не читает никто, кроме печати (card.py:264). `Setup.rr()` (geometry.py:177) и `emit.outcome_of` (src/hunter/emit.py:127) считают риск и R от `setup.entry` — одиночного ПОК. Следствия, видимые в карточке: (а) 15м-уровень печатает "один ордер" и СЛЕДУЮЩЕЙ строкой лестницу из двух-шести цен; (б) 1 из 7 сетапов 1d/1w печатает "закуп дробим" при лестнице из […]

**Правка.** Выводить `split_orders` из фактического состава лестницы (len(ladder) > 1), а не из ТФ; риск, РР и R считать от `average_entry_equal_shares` набора (или от первого исполненного ордера), чтобы журнал мерил ту сделку, которую советует карточка.

**Чем видно.** sed -n '11199,11205p' data/frames/last/BTC_USDT_USDT/card.txt — строка "один ордер" и следом "лестница 72646.55 · 72966.1 · 73162.55 · 73416.75 · 73575.75 · 73789.35". Зонд probe_entry.py: "split_orders: 7", "split_orders без лестницы (>=2): 1 [('ASTR/USDT:USDT','1d','0.0050740')]", "длина лестницы: {1:128, 2:48, 3:15, 4:4, 5:2, 6:1}"

**Проверка (опровергнуть не удалось).** geometry.py:445 split_orders=level.timeframe in BIG_STRUCTURE_TFS ({'1d','1w'}, geometry.py:59) — от ярлыка ТФ, не от состава ladder; card.py:255-265 печатает "закуп дробим/один ордер" отдельно от строки лестницы, а emit.outcome_of (emit.py:142) передаёт в resolve entry=em.setup.entry, то есть одиночный ПОК; average_entry_equal_shares […]

### A-17. Исполнение лимитки считается касанием диапазона бара: цена, ПЕРЕПРЫГНУВШАЯ вход, даёт "сделки не было"

**Источник.** стр. 30 курса: "надежнее всего брать от уровня ПОК (немного выше/ниже, т.к. идеально может не доходить) - т.к. в этом случае ТВХ будет на уровне"; лимитный ордер исполняется, когда цена дошла ДО его цены или прошла её, а не только когда бар накрыл её диапазоном

**Проект.** src/hunter/outcome.py:55 `_touched(bar, price) = bar.low <= price <= bar.high` — единственный тест исполнения входа. Условие ДВУСТОРОННЕЕ, тогда как стоп и цель проверяются односторонне (outcome.py:88-90). Если цена перепрыгнула вход между барами (разрыв в ряду или быстрый ход), лимитка считается неисполненной НАВСЕГДА. Смещение одностороннее: перепрыгивается ровно та сторона, куда цена ушла ПРОТИВ сделки (для лонга — вниз), то есть журнал теряет именно убыточные сделки.

**Правка.** Для лонга исполнение: `bar.low <= entry`; для шорта: `bar.high >= entry`; цена исполнения — `min(entry, bar.open)` / `max(entry, bar.open)`, чтобы гэп не записывался как вход по лучшей цене.

**Чем видно.** Контроль (прибор обязан был ответить "стоп"): uv run python -c "from decimal import Decimal; from hunter.outcome import resolve; from hunter.levels import LevelSide; from hunter.models import Bar; b=[Bar(open_ms=0,open=110,high=112,low=109,close=111,volume=1),Bar(open_ms=300000,open=95,high=97,low=94,close=95,volume=1),Bar(open_ms=600000,open=95,high=96,low=88,close=89,volume=1)] […]

**Проверка (опровергнуть не удалось).** outcome.py:55 `_touched(bar,price) = bar.low <= price <= bar.high` — двустороннее условие входа, тогда как строкой ниже (outcome.py:87-90) стоп и цель проверяются односторонне (bar.low<=stop / bar.high>=target); перепрыгнувшая вход цена даёт NOT_FILLED навсегда.

### A-18. "Цена пришла к уровню" определено в проекте дважды и по-разному: карта снимает лимитки по ЗОНЕ, журнал требует ПОК

**Источник.** стр. 31 курса: "если цена ранее забирала зону и уже получила от нее хорошую лонг реакцию, уровень лимитными ордерами больше не торгуем"; стр. 30 курса: "1 - цена забирает объемную зону… 2 - цена забирает идеально уровень ПОК" — курс разводит эти два события и предписывает ордер на КАЖДОЕ

**Проект.** src/hunter/levels.py:1160-1194 `first_test_index` считает касанием заход в ЗОНУ (`bars[i].low <= zone_hi and bars[i].high >= zone_lo`) — этим снимаются лимитки и уровень уходит в отказ. src/hunter/outcome.py:55 считает сделку состоявшейся только при достижении ПОК. Одно человеческое событие под двумя разными предикатами: уровень объявляется отработанным и снятым с торговли, а журнал по тому же уровню пишет "цена до входа не дошла".

**Правка.** Один предикат касания с двумя явными полями `zone_touched` (стр. 31 — снимает лимитки) и `poc_touched` (стр. 30 — исполняет ордер на ПОК); первый ордер лестницы исполнять по зоне, как требует стр. 30.

**Чем видно.** scratchpad/probe_zone_vs_poc.py — из 255 сверенных сигналов level+not_filled (кадры last, 5 символов) у 9 (3.5%) цена ЗАБРАЛА зону уровня и не коснулась ПОК; пример: BTC/USDT:USDT 15м лонг, вход 62450.0, зона 62228.8…62755.0, бар 62704.0…62974.4

**Проверка (опровергнуть не удалось).** levels.py:1192-1195 касанием считает `bars[i].low <= zone_hi and bars[i].high >= zone_lo`, outcome.py:55 требует bar.low<=ПОК<=bar.high — два разных предиката под одним человеческим событием.

⚠ **Уточнение проверяющего.** Расхождение не случайное: levels.first_test_index в докстроке прямо обосновывает зону стр. 31 (снятие лимиток), а outcome — ПОК по стр. 30. Дефект в том, что два предиката не разведены явными полями и журнал по снятому уровню пишет "до входа не дошла", а не в том, что одна из […]

### A-19. "Прокол за границу" никогда не лежит за границей: якорь меряется от линии детекции, а границы — коробка ХАЙ…ЛОЙ

**Источник.** стр. 18 курса, текстовый слой дословно: "Но если на 3++ точках были проколы за границы - стоп всегда ставится за этот прокол, т.к. это может быть расширением базы и в будущем цена может ходить уже в этом расширенном диапазоне". Это ЕДИНСТВЕННЫЙ безусловный императив курса о стопе.

**Проект.** `src/hunter/accumulation.py:158` — `BoundaryZone.puncture` есть ЦЕНА СВИНГА, сравнённая с `edge` (линия по первым двум точкам стороны, `accumulation.py:449` и `accumulation.py:486`). `src/hunter/levels.py:519` — `boundary_lo/hi` = (min low, max high) ВСЕХ баров той же структуры. `src/hunter/levels.py:800` подаёт прокол в `stop_anchor`, куда границей передана КОРОБКА (`levels.py:551`). Свинг-точка структуры по построению лежит внутри коробки, значит "прокол за границу" невозможен. `src/hunter/card.py:250` печатает владельцу "якорь: прокол за границу".

**Правка.** Записывать прокол как экстремум БАРА за коробкой (`bar.low < boundary_lo` / `bar.high > boundary_hi`), а не цену свинга за линией детекции — тогда "за границу" станет правдой и §18 заработает. Либо, если решено, что коробка уже поглощает прокол, снять `StopAnchorSource.PUNCTURE` целиком вместе с подписью карточки и плечом гейта, и записать это решение — сейчас поле существует, печатается и лжёт.

**Чем видно.** Зонд "scratchpad/probe_punct.py" (кадры `data/frames/last`, 5 символов, 9780 структур): проколов 6189, из них ЗА коробкой — 0 (0.0%), ВНУТРИ — 3640 (58.8%, медиана глубины внутрь 20.2% высоты базы, p90 45.9%, макс 87.8%), ровно на ребре — 2549. Контроль: прибор различает три класса, два непустые. Второй зонд "scratchpad/probe_stop2.py" на построенных уровнях: 1357 из 1357 якорей-проколов внутри коробки (100%) […]

**Проверка (опровергнуть не удалось).** accumulation.py:158 puncture — цена свинга относительно edge (линии стороны), а в stop_anchor из levels.py:551-553 подаётся boundary_lo/hi, которые на levels.py:517-519 суть (min low, max high) всех баров структуры; свинг-цена по построению внутри этой коробки. Число 0/6189 не перепроверял.

### A-20. Якорь стопа берётся из БУДУЩЕГО: свинги и стоповые объёмы не фильтруются по времени рождения уровня

**Источник.** стр. 18: "Если в диапазоне 2-5% от границы есть стоповый объем – база мелкого ТФ - или Лой того же ТФ или ТФ-1 - идеально стоп прятать за них". Стоп — величина, существующая В МОМЕНТ входа; курс не знает якоря, которого ещё нет.

**Проект.** `src/hunter/levels.py:538-539` подаёт в стоповые объёмы `y_scan.closed` — ВСЕ закрытые накопления младшего ТФ до конца ряда; `src/hunter/levels.py:549` подаёт `sw.swings` — ВСЕ свинги ряда. Ни одного условия на `created_at_ms` уровня. `src/hunter/stop_volume.py:105` (`_placement`) не имеет класса AFTER: младшая структура, закрывшаяся ПОЗЖЕ хозяина, получает метку INSIDE/ABOVE/BELOW наравне со своей. Якорь идёт в `Setup.stop` → `emit.ledger_stop` → `outcome.resolve` (R = ±1), то есть весь R леджера считается по стопу, привязанному к цене из будущего. Проект уже ловил этот класс у ЦЕЛЕЙ и завёл ради него […]

**Правка.** В `levels.build_all` перед вызовом `stop_anchor` резать оба списка по моменту рождения уровня: свинги — по `bars[sw.confirmed_at_index].open_ms + шаг <= lvl.created_at_ms`, стоповые объёмы — по `acc.exit.confirmed_at_index` того же условия; в `stop_volume._placement` завести класс AFTER и исключать его. Контроль: та же доля обязана упасть до 0, а число уровней с якорем — заметно снизиться (сейчас […]

**Чем видно.** Зонд "scratchpad/probe_stop2.py" (прогон `last`, 9715 уровней): якорь сопоставлен по времени у 3304 уровней, из них ИЗ БУДУЩЕГО 1646 (49.8%) — свинг 1285 из 2510 (51.2%), стоповый объём 361 из 794 (45.5%); медиана отставания 2397 баров своего ТФ, максимум 51441. Контроль (прибор отвечает иначе): "из прошлого" 1658. Независимый второй проход только по свингам "scratchpad/probe_ahead2.py": 3416 из 6687 (51.1%) […]

**Проверка (опровергнуть не удалось).** levels.py:534-548 передаёт y_scan.closed и все sw.swings без единого условия на created_at_ms/индекс подтверждения; в stop_anchor времени нет вовсе, только ценовая полоса. Доли 49.8% не перепроверял.

### A-21. Стоп сделки от переприора ставится РОВНО на край зоны — "за" превращено в "=", и такие сигналы уже 312 штук в леджере

**Источник.** стр. 50 дословно: "Торговля ПП – точкой входа является тест ПП, со стопом за хай/лой или базу в месте слома, если она есть". Стр. 55: "Уровнем ПП является вся зона Тени свечи". Тот же проект признал перевод "за" в "=" выдумкой: `src/hunter/geometry.py:369` — "это перевод слова за в знак равенства, сделанный кодом, то есть выдумка" — и починил это для стопов уровней 2026-08-18.

**Проект.** `src/hunter/geometry.py:495` и `:501` — `entry, stop = pp.zone_lo, pp.zone_hi` (шорт) и `pp.zone_hi, pp.zone_lo` (лонг): стоп ровно на дальнем ребре тени, без запаса. Докстрока `PPSetup` (`geometry.py:473`) это признаёт: "запаса курс для ПП не даёт, поэтому стоп РОВНО за краем, без процента" — но стр. 19/33/36/58 задают запас ЗА СТРУКТУРУ как общее правило, а стр. 55 прямо приравнивает уровень ПП к границам накопления и объёмным уровням ("могут Прокалываться или Пробиваться").

**Правка.** В `build_pp_setup` ставить стоп ЗА зону с запасом тем же механизмом, что у уровней (`STOP_MARGIN_MIN_PCT`…`MAX_PCT` от цены края), и не эмитировать ПП-сделку, у которой запас вырождается. До правки — вычистить или пометить в леджере уже записанные 312 строк, иначе они продолжат портить R.

**Чем видно.** `uv run python -c "import sqlite3;c=sqlite3.connect('file:data/ledger.sqlite3?mode=ro',uri=True);print(c.execute(\"select count(*) from signals where kind='pp'\").fetchone())"` → 312. Разбор по дистанции: медиана стопа 0.11% от входа, минимум 0.0009%, 271 из 312 ближе 0.5%, 67 ближе 0.05%. Исходов уже 201: стоп 108, цель 47, неоднозначно 46. Для сравнения у уровневых сигналов медиана стопа 5.57%. Собственный запрос […]

**Проверка (опровергнуть не удалось).** geometry.py:495 и :501 — entry, stop = pp.zone_lo, pp.zone_hi (и зеркально), никакого with_margin/STOP_MARGIN; сравнение с починенным "за≠=" у уровней корректно. 312 строк леджера не перепроверял.


---

# B. Правило источника не исполнено — 27 пунктов

### B-22. Фрактал: равенство соседних баров — проект утверждает, что источники требуют строгого неравенства; два из трёх процитированных источников говорят обратное

**Источник.** 1) ЭТАЛОННАЯ РЕАЛИЗАЦИЯ MetaQuotes `Fractals.mq4` (встроенный индикатор MT4; кодовая база https://www.mql5.com/en/code/7982 , читаемое зеркало https://github.com/CThompson18/MQL5/blob/master/Fractals.mq4 , сырой текст https://raw.githubusercontent.com/CThompson18/MQL5/master/Fractals.mq4 ). Дословно, помимо стандартной ветви на 5 баров есть ещё ЧЕТЫРЕ: "//----6 bars Fractal ... if(dCurrent==High[i+1] && dCurrent>High[i+2] && dCurrent>High[i+3] && dCurrent>High[i-1] && dCurrent>High[i-2])"; "//----7 bars Fractal ... if(dCurrent>=High[i+1] && dCurrent==High[i+2] && dCurrent>High[i+3] && dCurrent>High[i+4] && […]

**Проект.** src/hunter/swings.py:190 и :193 — соседи сравниваются строго с обеих сторон (`all(b.high < mid.high for b in left)` и то же для `right`), окно ровно 5 баров (`FRACTAL_BARS = 5`, swings.py:80). Докстрока swings.py:25 заявляет "Все три сходятся: ПЯТЬ баров, экстремум на СРЕДНЕМ, соседи строго слабее", докстрока swings.py:175-177 — "Соседи сравниваются строго … Плато из одинаковых максимумов фракталом не является ни по одному из трёх источников". Оба утверждения — об ИСТОЧНИКАХ, и оба неверны: thinkorswim на той же процитированной странице описывает ровно случай равенства и признаёт фрактал, MT5/MT4 пишут "at least […]

**Правка.** 1) Привести детекцию к эталону MetaQuotes: слева допускать `<=` с растяжкой окна до 9 баров по четырём ветвям исходника (фрактал ставится на ПОЗДНЕЙШИЙ бар плато), справа оставить строгое `<`; `confirmed_at_index` при этом не меняется (правые два бара те же). 2) Переписать докстроку swings.py:25 и :175-177: снять фразу "ни по одному из трёх источников" и назвать, что thinkorswim и справка MT […]

**Чем видно.** Зонд с контролем: C:\Users\3EC2~1\AppData\Local\Temp\claude\C--Users-------Documents-hunter-v2\db8721f4-1545-4a47-9813-1ac82f4a3bd8\scratchpad\probe_audit.py (`uv run python <путь>`). Ряд хаёв [1, 2, 5, 5, 3, 1] — это ровно ветвь "6 bars Fractal" эталона: напечатано "A: свингов найдено = ()", то есть НОЛЬ. КОНТРОЛЬ (прибор способен ответить иначе): тот же ряд с 4.9 вместо второй пятёрки — "A-контроль: свинги = […]

**Проверка (опровергнуть не удалось).** swings.py:80 FRACTAL_BARS=5, строки 190/193 действительно `all(b.high < mid.high ...)` строго с обеих сторон; докстроки на строках 25 ("Все три сходятся… соседи строго слабее") и 175-177 ("Плато… не является ни по одному из трёх источников") присутствуют дословно. Внешние источники (MQL4/thinkorswim) не перепроверял — вердикт по коду.

⚠ **Уточнение проверяющего.** Устояла в части кода и докстрок. Утверждение о содержании MetaQuotes/thinkorswim не проверялось.

### B-23. Переприор объявляется без предшествующего тренда: внутри чистого нисходящего тренда печатается "ранний ПП в шорт", то есть продолжение подаётся как разворот

**Источник.** Классика определяет разворот ТОЛЬКО относительно уже существующего тренда — это и есть различие BOS (продолжение) и CHoCH (разворот) в современной разметке структуры. StockCharts ChartSchool, Dow Theory: https://chartschool.stockcharts.com/table-of-contents/market-analysis/dow-theory — "An uptrend is defined by prices that form a series of rising peaks and rising troughs (higher highs and higher lows)… An uptrend is considered in place until a lower low forms and the ensuing decline exceeds the previous low", то есть сигнал разворота адресован именно ВОСХОДЯЩЕМУ тренду. IncredibleCharts, Dow Theory — Trends: […]

**Проект.** src/hunter/pereprior.py:103 `_detect_side` не проверяет состояние тренда НИ РАЗУ. Условие "раннего ПП в шорт" — только `updated = last_other.price > prev_other.price` ложно (хай не обновлён) плюс слом предыдущего лоя. В нисходящем тренде каждый следующий хай ниже предыдущего и каждый следующий лой ломается — значит условие выполняется НА КАЖДОМ звене падения, и `_detect_side` возвращает последнее из них как "смену приоритета". `swings.trend` (стр. 12) в проекте посчитан и лежит рядом — engine.py:207 `trend=swings.trend(sw)`, — но в `pereprior.detect` не передаётся и нигде не сверяется. Владельцу это печатается […]

**Правка.** 1) Дать `pereprior.detect` тот же `Trend`, что уже считает `engine`, и НАЗВАТЬ в модели ПП, был ли предшествующий тренд противоположным (разворот) или тем же (продолжение) — не выбрасывая событие, а помечая его: выброс был бы фильтром на выходе, а тут нужна честная классификация на входе. 2) Печатать метку в карточке рядом со словом "ранний": владелец читает "разворот", а видит продолжение. 3) […]

**Чем видно.** Зонд: C:\Users\3EC2~1\AppData\Local\Temp\claude\C--Users-------Documents-hunter-v2\db8721f4-1545-4a47-9813-1ac82f4a3bd8\scratchpad\probe_trend.py (`uv run python <путь>`). Ряд с хаями 25 → 22 и падающими лоями. Напечатано: "тренд по стр.12: direction=TrendDirection.DOWN holds_for=2", "хаи по порядку: [25.0, 22.0]" — и тут же "ПП early/short: слом 12.0 подтверждён на 13". КОНТРОЛЬ (прибор не заперт в одном ответе): […]

**Проверка (опровергнуть не удалось).** Прочитал `_detect_side` (pereprior.py:103-150) целиком: Trend не принимается параметром и нигде не сверяется, классификация ранний/истинный держится только на `updated = last_other.price > prev_other.price` плюс слом. В сигнатуре функции тренда нет.

### B-24. У прокола нет НИЖНЕЙ границы глубины: касание уровня тенью на один тик уже объявляет уровень отработанным

**Источник.** Курс определяет прокол (стр. 6) и прокол тенью (стр. 46), но о минимальной ГЛУБИНЕ проникновения молчит — значит по иерархии проекта молчание заполняет классика, а классика ставит фильтр единодушно. Эдвардс-Маги и Мёрфи, критерии проникновения: https://stockdisciplines.com/stock-trends-penetrations/ — ценовой фильтр "a 3% penetration of the trendline by the closing price", временной фильтр "the stock's closing price be below the trendline for two consecutive days. A one-day close below the line would not count", и отдельный "6% penetration by the stock's low price" для стопов. StockCharts ChartSchool […]

**Проект.** src/hunter/breach.py:116 `_beyond` — `bar.high > level` / `bar.low < level`, без всякого порога: заход за уровень открывает любое превышение на один тик. Дальше breach.py:239 при возврате в срок объявляет `BreachKind.PUNCTURE`, а у него `worked_off = True` с комментарием "Уровень ОТРАБОТАН, реакция валидна" (breach.py:69-70). Асимметрия внутри одного модуля: для ПРОБОЯ проект держит строгий стандарт (два ПОЛНЫХ тела подряд, `body_beyond`, breach.py:109) — это фактически временной фильтр Мёрфи, и он законен; для ПРОКОЛА стандарт нулевой. Вердикт при этом несёт статус уровня (`levels.status` через `first_verdict` […]

**Правка.** НЕ добавлять порог наугад (это был бы порог на выходе). Порядок: 1) замерить распределение ГЛУБИНЫ прокола — `(extreme - level)/level` по всем событиям `PUNCTURE` на 5 символах × 4 ТФ, с отпечатком данных; 2) назвать, какая доля "отработок" держится на глубине меньше одного тика инструмента и меньше типичного шума бара (например, медианы `high-low`); 3) если доля значима — ввести минимальную […]

**Проверка (опровергнуть не удалось).** breach.py:116 `_beyond` — `bar.high > level` / `bar.low < level` без порога, тогда как соседний `body_beyond` (:109) для пробоя требует полного тела; breach.py:239 при возврате в срок ставит PUNCTURE, у которого worked_off=True (:105-106).

⚠ **Уточнение проверяющего.** Находка о механизме верна; сама предложенная правка (порог) в fix корректно отложена до замера.

### B-25. Полярность (поддержка↔сопротивление) объявлена во всех текстах проекта и не исполнена ни в одном расчёте: Level.flipped() не вызывается ниоткуда, у флипнутого уровня setup=None

**Источник.** КЛАССИКА (5 независимых, учебные/справочные): 1) CFA Level 1, конспект программы, AnalystPrep — "The change in polarity principle asserts that once breached, a support level becomes a resistance level. Similarly, resistance levels become support levels upon a breach" https://analystprep.com/cfa-level-1-exam/portfolio-management/trend-support-resistance-lines-change-polarity/ ; 2) StockCharts ChartSchool, Support & Resistance — "Once the price breaks below a support level, the broken support level can turn into resistance" https://chartschool.stockcharts.com/table-of-contents/chart-analysis/support-and-resistance […]

**Проект.** src/hunter/levels.py:301 — метод `Level.flipped()` реализован и НЕ ВЫЗЫВАЕТСЯ НИОТКУДА: единственное упоминание во всём дереве (src, gates, scripts) — докстрока levels.py:895. Сторона уровня после пробоя не меняется никогда. src/hunter/emit.py:78 `hold_reason` для `LevelState.FLIPPED` возвращает непустую строку → src/hunter/engine.py:280 `setup=None if hold else geometry.build_setup(...)` → у пробитого уровня НЕТ входа, стопа и цели ни при каких данных. src/hunter/render.py:246 `if z.entry_rule in ("limit", "retest_flipped")` — ветка НЕДОСТИЖИМА: обрамляющее условие render.py:242 требует `z.state == "active"`, а […]

**Правка.** Строить сетап флипнутого уровня из `Level.flipped()`: сторона противоположная, вход — ретест с обратной стороны, стоп за накопление (стр. 43), — и записывать в БД уже перевёрнутую сторону, иначе карта продолжает показывать сопротивление как лонг-зону (это и есть "наслоение встречных зон" в чистом виде). До того как это сделано, убрать недостижимую ветку render.py:246: код не должен утверждать […]

**Чем видно.** grep -rn "flipped()" src gates scripts → одна строка (определение src/hunter/levels.py:301), ноль вызовов; src/hunter/engine.py:280; src/hunter/render.py:242-246 против src/hunter/levels.py:1094-1096; src/hunter/emit.py:78-81

**Проверка (опровергнуть не удалось).** grep -rn "flipped()" по src/gates/scripts дал только докстроку levels.py:895 (плюс несвязанное имя гейта c_breakout_is_flipped) — ни одного вызова; render.py:242-246 действительно требует `z.state == "active"` вокруг ветки с `retest_flipped`, то есть ветка недостижима.

⚠ **Уточнение проверяющего.** Определение метода на levels.py:301 грепом по `flipped()` не попало (объявление `def flipped(`), но вызовов всё равно ноль — суть находки не меняется.

### B-26. Точки границ считаются СУММОЙ по обеим сторонам — классический минимум касаний НА ЛИНИЮ этим порогом выразить нечем, а курсовое "6+" удовлетворяется набором 4+2

**Источник.** КЛАССИКА считает касания ПО КАЖДОЙ ЛИНИИ, а не в сумме (5 источников): 1) Bulkowski, Rectangle Tops, identification guidelines — "Price should touch one trendline at least three times and twice on the other trendline, using distinct peaks and valleys, but be flexible" https://thepatternsite.com/recttops.html (минимум 3+2); 2) StockCharts ChartSchool, Rectangle — "At least two equivalent reaction highs are required to form the upper resistance line and two equivalent reaction lows to form the lower support line" https://chartschool.stockcharts.com/table-of-contents/chart-analysis/chart-patterns/rectangle (2 на […]

**Проект.** src/hunter/accumulation.py:76 `MIN_BOUNDARY_POINTS = 4` и accumulation.py:549 `if len(hi_px) + len(lo_px) < min_points` — порог на СУММУ; `Accumulation.points` (accumulation.py:228) тоже сумма. Боевой прогон идёт с умолчанием: переопределений `min_points` в src нет (src/hunter/engine.py:36, src/hunter/run.py:23 зовут `detect` без него). При умолчании 4 требование выполняется структурой кода как ровно 2+2 — это классический МИНИМУМ StockCharts и на одно касание меньше минимума Булковски (3+2). Второе число курса при этом выразить нечем: `min_points=6` удовлетворяется набором 4+2, а не 3+3, — то есть усилить […]

**Правка.** Заменить сумму на условие по стороне: `len(hi_px) >= k and len(lo_px) >= k`. Тогда выражаются и "4+" курса (k=2), и "6+" (k=3), и минимум Булковски. Выбор k внутри курсового диапазона сделать замером с контролем (сколько структур и уровней теряется при k=3 и как это меняет карточку), а не умолчанием: сейчас взят слабый конец диапазона молча.

**Проверка (опровергнуть не удалось).** accumulation.py:76 MIN_BOUNDARY_POINTS = 4 и строка `if len(hi_px) + len(lo_px) < min_points` — порог именно на сумму; комментарий рядом сам признаёт недостижимость ветки при умолчании, но по стороне условия нет, значит 3+3 текущим параметром не выражается.

### B-27. Объём не участвует в различении прокола и пробоя, хотя курс на этом месте молчит, классика единодушна, а данные объёма в проекте есть

**Источник.** КУРС МОЛЧИТ ИМЕННО ЗДЕСЬ — проверено сплошным грепом по текстовому слою PDF (термины объем/объём/пробо/прокол/подтвержд): объём привязан к СИЛЕ уровня (стр. 22: "Сила уровня определяется ТФ и объемом (смотрим по VRVP, иногда в маленькой часовой наторговке может быть объем больше, чем в 4ч-1д накоплении...)"), а подтверждение выхода задано только телами (стр. 55: "требуют подтверждения (то есть закрытия под/над уровнем ПП 2-3 полных тел свечей ЭТОГО ТФ)"). Про объём ПРИ ПРОБОЕ в курсе нет ни строки → по иерархии говорит классика. КЛАССИКА (4 источника): 1) StockCharts ChartSchool, Rectangle — "If volume declines […]

**Проект.** В src/hunter/breach.py, src/hunter/accumulation.py и src/hunter/emit.py НЕТ НИ ОДНОГО обращения к `Bar.volume` (grep -n "volume" по трём файлам — пусто). `first_breach` (breach.py:120-235) решает судьбу уровня исключительно через `body_beyond` (полное тело за уровнем) и `steps_between` (окно возврата 2 бара). Докстрока breach.py:225-245 перечисляет консультацию с классикой ПО ЭТОМУ ЖЕ ВОПРОСУ — но только про закрытия и время; в сводке docs/audit/classics-tolerance-2026-08-09.md, вопрос 2, восемь строк, объём не разобран ни в одной. При этом объём в проекте собран и обработан: volume_profile.py, stop_volume.py […]

**Правка.** Внести объёмный признак захода в `Breach` ПОЛЕМ (объём баров захода к средней окна структуры), напечатать его в карточке и замерить связь с исходом — но НЕ вводить как фильтр, пока не показано, что вход считается верно (CLAUDE.md: "Порог на выходе не лечит расчёт на входе"). Заодно закрыть дыру в собственной сводке классики: вопрос "чем классика подтверждает пробой" разобран там наполовину — по […]

**Проверка (опровергнуть не удалось).** grep "volume" по src/hunter/breach.py и src/hunter/emit.py дал пустой вывод; решение в breach.py строится на телах свечей и окне возврата, докстрока 220-240 перечисляет классику только про закрытия и время.

### B-28. Ничья ПОК отбрасывает уровень, хотя процедура источников её РАЗРЕШАЕТ ("ближайшая к середине диапазона")

**Источник.** Четыре независимых надёжных источника дают тай-брейк ЧАСТЬЮ ОПРЕДЕЛЕНИЯ ПОК, а не опцией: 1) CQG, Market Profile Value Areas (справка вендора) — https://help.cqg.com/cqgic/25/Documents/marketprofilevalueareasmpva.htm — дословно: "If several prices have the same max TPO, then the price that is closest to the middle price range is the POC price." 2) WindoTrader, Market Profile Glossary (справочник по методу Steidlmayer) — https://www.windotrader.com/market-profile/market-profile-glossary-index/ — ПОК определён как "the longest row of Time, Price and Volume (TPOs) occurring closest to the center of the range during […]

**Проект.** C:\Users\Антон\Documents\hunter-v2\src\hunter\volume_profile.py:246-252 (build_tv) и :318-331 (point_of_control) — при равном объёме двух и более строк возвращается NotReady, уровень не строится: C:\Users\Антон\Documents\hunter-v2\src\hunter\levels.py:388-390 превращает это в "ПОК не построен". Обоснование записано в докстроке C:\Users\Антон\Documents\hunter-v2\src\hunter\volume_profile.py:156-157: "НИЧЬЯ ПОК — NotReady... справка молчит, а выбор "первая попавшаяся строка" был бы молчаливым решением". Посылка "справка молчит" верна только для справки TradingView; источники уровня 3 (классика ТА) опрошены не […]

**Правка.** Заменить NotReady на правило источников: из строк с равным максимумом брать ближайшую к СЕРЕДИНЕ диапазона профиля (индекс, ближайший к (rows_built−1)/2), остаточную ничью (две строки на равном расстоянии от середины) решить одним названным вслух правилом и записать его рядом со ссылкой на CQG/WindoTrader. Ту же правку внести в point_of_control (:318-331) — иначе тиковый эталон и боевая сетка […]

**Чем видно.** uv run python "C:\Users\3EC2~1\AppData\Local\Temp\claude\C--Users-------Documents-hunter-v2\db8721f4-1545-4a47-9813-1ac82f4a3bd8\scratchpad\probe_va_coverage_2026-08-19.py" — повтор кадров data/frames/shift18-live (5 символов), перехват levels.build_tv: "вызовов build_tv успешных: 9718, ничьих ПОК (NotReady): 33" → 0.34% профилей теряются на ничьей. uv run python […]

**Проверка (опровергнуть не удалось).** volume_profile.build_tv при len(winners) > 1 возвращает NotReady ("ПОК по сетке TV неоднозначен"), и point_of_control делает то же самое ("Ничья — неоднозначность, а не повод выбрать первый") — тай-брейк "ближайшая к середине диапазона" не реализован нигде.

### B-29. Граница базы по первым двум точкам (прямое правило стр. 18) вычисляется и никуда не доходит

**Источник.** стр. 18 курса, дословно: "Границы базы (хай и лой) чаще всего определяются первыми 2-мя точками." Рисунок стр. 13 показывает это буквально: верхняя линия коробки идёт по точкам 1 и 3, а точки 7 (~0.2295) и 11 (~0.2305) торчат ВЫШЕ неё, точки 4, 10, 12 — ниже нижней. Коробка автора НЕ захватывает все свечи; "захватить все свечи" стр. 26 говорит про ПРОФИЛЬ, а не про линии границ.

**Проект.** Линии считаются (`accumulation.BoundaryZone.edge`), ими проверяются касания, сужение и выход из структуры — и на этом они кончаются. В `Level` они не попадают: `boundary_lo/hi` = коробка (levels.py:423-424). Докстрока `Level.boundary_hi` прямо признаёт отказ от них: "В уровень они НЕ попадают: потребителя… не нашлось ни одного". Значит стоп "за структуру", высота базы (потолок полосы якоря), проверка вложенности (levels.py:687) и строка карточки "границы" — все отмеряются от величины, которой стр. 18 не определяет.

**Правка.** Положить в `Level` оба поля — `edge_lo/edge_hi` (линии стр. 18) рядом с `boundary_lo/hi` (коробка стр. 26/30) — и назвать их в карточке РАЗНЫМИ словами: "границы (первые 2 точки)" и "размах свечей". Стоп и высоту базы считать от линий (стр. 18/33/58 говорят "за границу"/"за структуру" именно про них), профиль — от размаха.

**Чем видно.** Зонд по кадрам `last`, 9780 структур: коробка/линии по высоте — медиана 1.824, p90 4.088; на рабочей стороне уровня расхождение линии и коробки медиана 0.044% цены, p90 0.655%, max 88.5%, больше 1% цены у 5.4% уровней. Разобранный пример (тот же BCH 1ч): линии 437.81…441.95, напечатанные границы 424.64…441.95.

**Проверка (опровергнуть не удалось).** В Level кладётся boundary_lo/hi = price_range (коробка, levels.py:423-424); edge живёт только внутри accumulation.BoundaryZone и используется в stop_anchor для стоповых объёмов, в Level не попадает ни одним полем. Расхождение величин подтверждается кодом; числа не проверял.

### B-30. "Истинный ПП" присваивается без теста, и такой сигнал уходит в леджер

**Источник.** стр. 50 курса, дословно: "ПП в Шорт – цена ломает последний лой, из которого был последний хай, и подтверждает его тестом снизу." И там же: "Торговля ПП – точкой входа является тест ПП, со стопом за хай/лой или базу в месте слома, если она есть." Тест — половина определения истинного ПП и единственная точка входа.

**Проект.** src/hunter/pereprior.py:229 — `kind=PPKind.TRUE if updated else PPKind.EARLY`: вид назначается ТОЛЬКО по факту обновления противоположного экстремума, тест в классификации не участвует. `tested_at_index` может быть `None`, и объект всё равно зовётся истинным. `engine.PPSignal.emitted` (src/hunter/engine.py:140) требует только `setup.target is not None` — наличия теста не требует.

**Правка.** В `_detect_side` присваивать `PPKind.TRUE` только при `tested_at_index is not None`; до теста отдавать состояние "слом подтверждён, теста нет". В `PPSignal.emitted` добавить `pp.tested_at_index is not None` — стр. 50 не даёт точки входа раньше теста.

**Чем видно.** Прямо в сохранённой карточке, соседние строки: data/frames/last/BCH_USDT_USDT/card.txt:14055-14056 — "5м ранний в ШОРТ … теста не было" и следом "сделка от ПП (стр. 50): вход 203.6 стоп 203.64 цель 203.59 РР 0.25 — эмитируется в леджер". Счёт по кадрам `last`: ПП всего 60, "теста не было" у 28, из них 11 помечены "эмитируется в леджер"; истинных ПП 21, из них БЕЗ теста 10 (47.6%). Отпечаток: прогон `last`, 5 […]

**Проверка (опровергнуть не удалось).** pereprior.py:229 `kind=PPKind.TRUE if updated else PPKind.EARLY` — тест не участвует, tested_at_index вычисляется отдельной строкой и может быть None; engine.py:141 `emitted` требует только `self.setup.target is not None`.

### B-31. Стоп сделки от ПП: ветвь "за базу в месте слома" отсутствует, а стоп ставится РОВНО на край зоны — медианный риск 0.18% цены

**Источник.** стр. 50 курса, дословно: "Торговля ПП – точкой входа является тест ПП, со стопом за хай/лой ИЛИ БАЗУ В МЕСТЕ СЛОМА, ЕСЛИ ОНА ЕСТЬ." Стр. 54, вариант 1: "Торговля :Вход от уровня в ЛОНГ, стоп ЗА СТРУКТУРУ."

**Проект.** src/hunter/geometry.py:489 `build_pp_setup(pp, opposite)` — сигнатура не принимает накоплений вовсе, ветви "за базу в месте слома" в проекте нет. Стоп: `entry, stop = pp.zone_lo, pp.zone_hi` (шорт) — РОВНО дальний край зоны тени, без запаса. Это тот же "за = равно", который докстрока `geometry.beyond_anchor` (geometry.py:369) для уровней объявляет выдумкой и отзывает: "перевод слова за в знак равенства, сделанный кодом, то есть выдумка". Для ПП пола наподобие `DEFAULT_MARGIN_PCT` нет — риск задаётся шириной тени одной свечи.

**Правка.** Передавать в `build_pp_setup` `AccumulationScan` того же ТФ; если закрытая структура накрывает бар слома (`acc.first_index <= pp.broken_index <= acc.last_index`) — брать стоп за её границу с курсовым запасом стр. 33/58, иначе — за дальний край зоны ПП с тем же запасом. Печатать в карточке, какая ветвь сработала.

**Чем видно.** Зонд по строкам сохранённых карточек `last` (60 ПП-сделок, мера снята с ТЕКСТА карточки, не с объекта): риск |вход−стоп|/вход — медиана 0.183%, p90 4.22%, max 13.86%; у 34 сделок из 60 (57%) риск меньше 0.5% цены. Пример строки: card.txt:14058 "вход 203.59 стоп 203.51" — риск 0.039%.

**Проверка (опровергнуть не удалось).** geometry.py:489 `def build_pp_setup(pp, opposite)` — накоплений в сигнатуре нет; строка 496 `entry, stop = pp.zone_lo, pp.zone_hi` для шорта — стоп ровно дальний край зоны, никакого запаса/пола. Медианный риск из evidence не перепроверял.

### B-32. Тренд не участвует в разметке структуры: стр. 57/58 "первая точка по тренду" и "вход на 6 касании" не реализованы

**Источник.** стр. 57 курса, дословно: "Торгуем по тренду. Если тренд Лонговый - ПЕРВАЯ точка берется сверху. Если тренд Шортовый - ПЕРВАЯ точка берется с низу." Стр. 58: "Строим по тренду: если тренд лонговый, первая точка идет сверху, если тренд шортовый первая точка идет снизу." Стр. 57: "Если не успели взять от уровня, то ждем 6 касание и берем на 6 касании".

**Проект.** `accumulation.detect` тренда не знает вовсе — слова `trend`/`Trend` в src/hunter/accumulation.py нет ни разу (грепом файл чист). Первой точкой становится просто самый ранний подтверждённый фрактал, то есть начало сквозной нумерации стр. 18/57 назначается порядком прихода, а не стороной тренда. Счётчика касаний в проекте нет: грепом по src/hunter нет ни `touch_count`, ни ветки "6 касание" в `emit`/`geometry`. Раз номер 1 назначается произвольно, номер 6 не определён даже при появлении счётчика.

**Правка.** Передать `Trend` родного ТФ в `accumulation.detect` и начинать нумерацию с той стороны, которую называет стр. 57/58; положить `ordinal` каждой точки в `BoundaryZone`, а число касаний каждой стороны — в `Level`. Тогда "вход на 6 касании" становится проверяемым условием, а не пробелом.

**Чем видно.** grep -n "trend\|Trend" src/hunter/accumulation.py → пусто. grep -rn "touch_count\|6 касан" src/hunter/*.py → пусто. Видно в карточке: строка структуры печатает "в сужении N", но ни номера первой точки, ни числа касаний сторон в ней нет.

**Проверка (опровергнуть не удалось).** grep -c "rend" src/hunter/accumulation.py → 0 (ни trend, ни Trend); grep touch_count по src/hunter/*.py → пусто. Нумерация ordinal назначается порядком прихода фракталов (accumulation.py:451,501).

### B-33. Сигнал ПРОТИВ тренда старшего ТФ эмитируется без ограничения и без записи признака в леджер

**Источник.** стр. 18 курса, дословно: "- Можно торговать в обе стороны, но первая позиция всегда берется по тренду." Стр. 47: "Локальную позиция можно рассматривать, имея позицию по тренду – в виде хэджа и/или на уменьшенный объем риска."

**Проект.** src/hunter/emit.py:68 `hold_reason` — единственное правило отбора эмиссии; ветки по `agreement` в нём нет, проверяются только состояние уровня, `price_beyond`, `price_in_zone` и `limit_orders_allowed`. Карточка называет нарушение словами (src/hunter/card.py:186, "ПРОТИВ тренда — курс допускает только хеджем…"), но геометрия строится и сигнал уходит. `store.record_signal` (src/hunter/store.py:772) поля `agreement` не принимает вовсе — леджер физически не может ответить, хуже ли сделки против приоритета.

**Правка.** Либо добавить в `hold_reason` ветку "против приоритета старшего ТФ — стр. 18/47: только вторым входом при открытой прибыльной позиции", либо (если владелец решит эмитировать) записывать `agreement` колонкой в `signals` и печатать долю в сводке исходов — иначе правило стр. 47 непроверяемо по собственному журналу.

**Чем видно.** Счёт по сохранённым карточкам `last` (мера снята с текста карточки): строк уровней 9715, с построенной геометрией сделки 159; среди них помечены "ПРОТИВ тренда" 37 (23.3%). По всем строкам: против тренда 4756, по тренду 4949, приоритет не определён 10. grep -n "agreement" src/hunter/store.py → пусто.

**Проверка (опровергнуть не удалось).** emit.hold_reason (emit.py:68-110) читает только state, price_beyond, price_in_zone и limit_orders_allowed — ветки по agreement нет; grep agreement по store.py → пусто (единственное вхождение в emit.py:45 — поле модели).

### B-34. Объём не может перебить таймфрейм в отборе уровней, хотя курс называет ровно этот случай

**Источник.** Стр. 22: "Сила уровня определяется ТФ и объемом (смотрим по VRVP, иногда в маленькой часовой наторговке может быть объем больше, чем в 4ч-1д накоплении - от таких структур стараемся брать, ожидая импульсной реакции)." То есть курс прямо называет конфигурацию, в которой МЛАДШИЙ ТФ сильнее старшего, и велит брать именно его.

**Проект.** src/hunter/tgbot.py:871-883 — `_strength` возвращает кортеж `(order.get(z.timeframe, 0), z.vrvp_density)`; сравнение кортежей лексикографическое, значит плотность объёма читается ТОЛЬКО при равном ТФ. Этот же ключ решает оба прохода дедубликации (tgbot.py:952 и tgbot.py:972), а второй проход склеивает уровни с взаимно вложенными зонами и оставляет одного. Случай стр. 22 ("часовой объёмнее суточного") по построению недостижим.

**Правка.** В склейке зон (tgbot.py:972) поставить плотность ПЕРВЫМ измерением либо оставлять оба уровня, когда младший плотнее старшего более чем в k раз; ТФ использовать разрешением ничьей. Порог k — решение о риске, оно владельца, но лексикографический порядок исполняет стр. 22 наполовину.

**Чем видно.** Кадры `last`, активные уровни: правило склейки зон срабатывает 139 раз, и в 71 из них (51.1%) выброшен уровень с БОЛЬШЕЙ плотностью объёма — тот самый, который курс велит брать. Медиана плотности композита по ТФ НЕ растёт со старшинством: 5м 1.57, 15м 1.59, 1ч 1.55, 4ч 1.49, 1Д 1.26 — младшие структуры плотнее, то есть "маленькая часовая наторговка" стр. 22 в данных массовая, а отбор её не видит.

**Проверка (опровергнуть не удалось).** tgbot.py:885 `return order.get(z.timeframe, 0), z.vrvp_density` — кортеж, а сравнения на строках 952 и 972-973 лексикографические (`_strength(z) > _strength(kept)`, sort по `-*_strength`), значит плотность читается только при равном ТФ; докстрока сама признаёт "ТФ решает первым". Числа (139/71, медианы плотности) не перепроверялись.

### B-35. Флип не переворачивает сторону уровня: `Level.flipped()` не вызывается ниоткуда

**Источник.** Стр. 43, вариант 1: "Уровень лонг/шорт менятся для нас на противоположный". Стр. 28, вариант 3: "теперь для нас это уровень поддержки (в лонг) для новой позиции" — то есть после пробоя сторона у уровня ДРУГАЯ, и именно её читает трейдер.

**Проект.** src/hunter/levels.py:301-309 — метод `Level.flipped()` написан и документирован, но `git grep '\.flipped()'` по src, gates и scripts даёт НОЛЬ вызовов. `status` (levels.py:1092-1094) выставляет `LevelState.FLIPPED`, а сторона остаётся прежней: src/hunter/store.py:1026-1032 пишет `lvl.side.value` как есть, src/hunter/card.py:169 печатает `SIDE_LABEL[lvl.side.value]`, src/hunter/render.py:114 красит линию по `z.side`. Читатель видит "ЛОНГ … флипнут" и обязан перевернуть сторону в уме.

**Правка.** В `map_levels`/`sync_levels` класть `lvl.flipped()` при `state is LevelState.FLIPPED` (уровень остаётся тем же объектом, меняется только `side`, как и написано в докстроке метода), либо печатать сторону словами "был ЛОНГ, теперь ШОРТ".

**Чем видно.** Кадры `last`: 4372 из 9715 уровней (45.0%) в состоянии flipped. Боевой леджер data/ledger.sqlite3: 20751 из 49806 строк (41.7%) flipped, из них side='long' 9799 и side='short' 10952 — все со стороной ДО флипа. Проверка без чтения кода: uv run python -m hunter ledger, либо sqlite3 запросом "select state,side,count(*) from levels group by 1,2"; на графике бота линия флипнутого уровня окрашена в цвет прежней стороны.

**Проверка (опровергнуть не удалось).** Метод есть на levels.py:301-309, а grep по src/gates/scripts даёт только упоминание в докстроке (levels.py:895) и имя гейта course_rules.py:141 — ни одного вызова `.flipped()`; сторона у флипнутого уровня остаётся прежней.

### B-36. Курс даёт вход отработанному и флипнутому уровню, проект не производит для них ни одного числа — а это 96% карты

**Источник.** Стр. 43: "По возврату цены на ретест уровня - открываем позицию и ставим СТОП за накопление". Стр. 31: "Позицию от уровня смотрим только по факту слома структуры на более мелких ТФ." Стр. 25: "можно рассматривать вход от 2 или 3 касания только по факту слома структуры на младшем ТФ". Во всех трёх случаях курс называет ВХОД, а не отказ.

**Проект.** src/hunter/emit.py:79-86 — `hold_reason` возвращает непустую строку для `WORKED_OFF` и `FLIPPED`, из-за чего src/hunter/engine.py:274-284 не строит `Setup`, а `SymbolDecision.emissions` (engine.py:182-185) их не содержит. Ни входа, ни стопа, ни цели, ни строки в леджере. При этом карточка (card.py:186, ENTRY_LABEL) печатает у этих уровней "вход: по ретесту с обратной стороны…" и "вход: только по слому структуры на младшем ТФ (стр. 31)", а бот (tgbot.py:1261) пишет "N пробитых не в списке — по стр. 43 они торгуются ретестом с обратной стороны". Обещание есть, чисел нет.

**Правка.** Либо строить геометрию для `EntryRule.RETEST_FLIPPED` (вход = ретест ПОК с обратной стороны, стоп за накопление — стр. 43) и для `CONFIRMATION` (вход по подтверждённому слому младшего ТФ), либо убрать из карточки и из сообщения бота фразы, обещающие вход, которого расчёт не даёт.

**Чем видно.** Кадры `last`: 9339 из 9715 уровней (96.1%) — flipped (4372) или worked_off (4967); торгуемых лимитками 198 (2.0%). Боевой леджер: entry_rule retest_flipped 20471, confirmation 27693, limit 900 — у 98.2% строк карты напечатано правило входа, под которое нет ни одной цены. Видно в card.txt: строка "вход: …" стоит сразу над строкой "СДЕЛКИ НЕТ: …".

**Проверка (опровергнуть не удалось).** emit.py:79-86 возвращает непустой hold_reason и для WORKED_OFF, и для FLIPPED, причём сам текст называет разрешённый курсом вход ("вход по ретесту в ДРУГУЮ сторону") — то есть правило признано, а геометрия под него не строится. Доли из evidence не проверял.

### B-37. Поздний возврат объявлен проколом, то есть ОТРАБОТКОЙ, хотя курс задаёт проколу срок в 1-2 свечи

**Источник.** Стр. 5 и стр. 6 (текст, дословно одинаково): "Прокол - термин применяется к любому уровню на графике (ПОК, горизонтальный, ПП и т.д.). Обозначает ситуацию, в которой цена прошла за уровень - и вернула обратно той же или следующей 1-2 свечами." Стр. 7: "Пробой – цена прошла за какой-то уровень и в данный момент остается за ним." Рисунок стр. 6 с подписью "Во всех случаях - цена не уходит за уровень целыми свечами" даёт условие на ТЕЛА, но ни на одном из четырёх вариантов цена не стоит за уровнем дольше двух-трёх свечей — то есть рисунок срок не отменяет, а подтверждает.

**Проект.** src/hunter/breach.py:240 — `return made(BreachKind.PUNCTURE if best_run == 0 else BreachKind.UNRESOLVED, i)` в ветке позднего возврата (`back > return_bars`). PUNCTURE даёт `worked_off = True` (breach.py:104-107) и состояние `LevelState.WORKED_OFF` (levels.py:1095). Обоснование в комментарии breach.py:214-229 — "поздний возврат курс не разбирает вовсе", а курс его разбирает: стр. 5 даёт проколу срок, стр. 7 называет остающуюся за уровнем цену пробоем. Второй голос (классика) применён поверх текста первого, а не там, где первый молчит.

**Правка.** Вернуть `UNRESOLVED` для позднего возврата без тел (или завести отдельный класс "поздний возврат", не равный отработке), и отдельно предъявить владельцу цену этого решения — уровни перестанут молча уходить в "отработан".

**Чем видно.** Кадры `last`: из 4967 вердиктов "отработан" 925 (18.6%) получены этой веткой. Число свечей, которые цена простояла за уровнем в этих случаях: медиана 3, p90 5, максимум 12 — при курсовом "той же или следующей 1-2 свечами". Десять самых длинных: 12 свечей на 5м, по 11 на 5м/4ч/1ч/15м. Владельцу видно в card.txt как "отработан" + "событие: прокол" у уровня, за которым цена стояла двенадцать свечей.

**Проверка (опровергнуть не удалось).** breach.py:240 действительно отдаёт PUNCTURE при `back > return_bars`, если тел за уровнем не было; комментарий breach.py:214-229 сам обосновывает это тем, что "поздний возврат курс не разбирает вовсе", и тут же цитирует курсовое "1-2 свечами" — то есть классика применена поверх говорящего текста, а не в его молчании.

### B-38. Флаг "закуп дробим / один ордер" напечатан, но лестницей не управляет, и ордера НА ЗОНУ в лестнице нет никогда

**Источник.** Стр. 30: "используем 2-3 ордера, на зону, и на уровень ПОК", "то закуп всегда стоит делить на зону и на уровень" (для 1Д-1Н-1М) и "Если накопление маленькое - 5м-1ч – эффективнее входить 1 ордером от уровня."

**Проект.** src/hunter/geometry.py:445 — `split_orders = level.timeframe in BIG_STRUCTURE_TFS` — просто ярлык ТФ. src/hunter/geometry.py:446 — `ladder = tuple(sorted({level.price, *(x.price for x in inner)}))`: только ПОК родителя и ПОК вложенных уровней стр. 32. Цены ЗОНЫ (`zone_lo`/`zone_hi`), которую стр. 30 называет одним из двух-трёх ордеров, в лестнице нет ни разу. Флаг и лестница не связаны: src/hunter/card.py:256 печатает "закуп дробим"/"один ордер", а src/hunter/card.py:258-264 печатает лестницу независимо от флага.

**Правка.** При `split_orders` класть в лестницу ближнюю кромку зоны и ПОК (стр. 30), при его отсутствии лестницу из вложенных уровней на 5м-1ч не строить либо явно назвать её другим правилом (стр. 32) и не печатать рядом "один ордер".

**Чем видно.** Кадры `last`, 198 торгуемых лимитками уровней: на 5м/15м/1ч их 172, и у 61 (35.5%) лестница из двух и более цен при напечатанном "один ордер"; на 1Д/1Н их 7, и у одного лестница из одной цены при напечатанном "закуп дробим". Ни у одного из 198 в лестнице нет цены зоны. Видно прямо в card.txt: строка "стоп … один ордер" и следом строка "лестница X · Y средняя при равных долях Z".

**Проверка (опровергнуть не удалось).** geometry.py:445 `split_orders = level.timeframe in BIG_STRUCTURE_TFS` — чистый ярлык ТФ, а geometry.py:446 строит ladder из level.price и ПОК вложенных, без zone_lo/zone_hi; card.py:256 печатает флаг, card.py:258-264 печатает лестницу по условию len(ladder)>1, независимо от флага.

### B-39. Вход по слому структуры на младшем ТФ вычисляется, называется владельцу и не порождает ни одной сделки

**Источник.** стр. 25 курса: "можно рассматривать вход от 2 или 3 касания только по факту слома структуры на младшем ТФ"; стр. 31: "Позицию от уровня смотрим только по факту слома структуры на более мелких ТФ"

**Проект.** src/hunter/emit.py:68-113 `hold_reason` для WORKED_OFF и для "цена уже касалась уровня" возвращает НЕПУСТУЮ строку → src/hunter/engine.py:277 `setup=None if hold else geometry.build_setup(...)` → эмиссии нет (engine.py:182-185 отдаёт только те решения, у которых построен setup). src/hunter/engine.py:349 `_mtf_break` условие проверяет и печатает текстом, но входа не строит — докстрока это признаёт ("эмиссию НЕ порождает"). Замер на кадрах last текущим кодом: 9517 уровней без сделки, из них у 2953 (31.0%) напечатано "слом на 5m/15m подтверждён" — то есть курсовое условие входа выполнено, и вход не выдан ни разу.

**Правка.** При `EntryRule.CONFIRMATION` и непустом подтверждённом сломе строить `Setup` (вход — ПОК и зона, стоп — за структуру по стр. 33/58) и эмитировать с полем `entry_kind="confirmation"`, чтобы леджер отличал его от лимитного.

**Чем видно.** grep -c "слом на младшем ТФ:" data/frames/last/*/card.txt (9517 строк) и grep "слом на младшем ТФ:" … | grep -c "подтверждён" (2953). В карточке эти две строки стоят подряд: "СДЕЛКИ НЕТ: отработан на первое касание … вход только по слому структуры на младшем ТФ (стр. 31)" → "слом на младшем ТФ: слом на 5m подтверждён"

**Проверка (опровергнуть не удалось).** emit.hold_reason (emit.py:79-112) для WORKED_OFF и для "цена уже касалась уровня" возвращает непустую строку, engine.py:279 `setup=None if hold else geometry.build_setup(...)`, а _mtf_break (engine.py:349) только вычисляет текст слома и в setup/эмиссию не входит.

### B-40. Целый раздел "ТОРГОВЛЯ ВО ФЛЕТЕ" (стр. 18-20) не реализован: входа от ГРАНИЦЫ живой базы нет ни одного

**Источник.** стр. 19 курса: "Варианты торговли - набор позиции максимально ближе к верхней или нижней границы; - СТОП прятать с запасом за структуру (границы) 1-5%… - это для торговли лимитными ордерами"; стр. 18: "Если тренд восходящий, то флет торгуем от нижней границы до верхней ( Лонг )… первая позиция всегда берется по тренду"; стр. 35: "можно зайти в позицию еще до выхода цены из стопового"; стр. 54: "Вход от нижней границы накопления + по возврату на тесте уровня в ЛОНГ"

**Проект.** Единственная точка построения входа — `geometry.build_setup` (src/hunter/geometry.py:305), и в ней `entry=level.price`, то есть ПОК УЖЕ ЗАКРЫТОГО накопления. Незакрытая структура существует в модели (`accumulation.OpenStructure`, src/hunter/accumulation.py:238, поле `AccumulationScan.open_tail`, accumulation.py:261) и доходит ровно до одной строки карточки (src/hunter/card.py:147-148) — ни границ, ни входа, ни стопа она не даёт. Правило стр. 23 ("пока цена не вышла из структуры — уровня нет") распространено с УРОВНЯ ПОК на любой вход, чего курс не пишет: стр. 18-19 и стр. 35 описывают вход внутри живой базы […]

**Правка.** Ввести второй вид входа `entry_kind="boundary"` от границы живой структуры (`open_tail`) с ≥4 точками: сторона — по тренду своего ТФ (стр. 18), стоп — запас 1-5% за границу (стр. 19), первая цель — противоположная граница с фиксацией 50% (стр. 19).

**Чем видно.** grep -h "незакрытая структура: открыта" data/frames/last/*/card.txt — 17 живых структур с 4-6 точками границ на 5 символов × 6 ТФ; ни одна не даёт строки "стоп/лестница/цель". Проверка отсутствия потребителя: grep -rn "open_tail" src gates — только accumulation.py (объявление) и card.py:147 (печать)

**Проверка (опровергнуть не удалось).** grep по src/gates: open_tail только объявление (accumulation.py:261, 614), печать (card.py:147-148), упоминание в докстроке levels.py:1143 и гейт course_rules.py:219 — ни одного потребителя, строящего вход/стоп; build_setup (geometry.py:305) строится от Level, а не от открытой структуры.

### B-41. Флипнутый уровень: владельцу сказано "торгуется ретестом", а вход по нему не строится вообще

**Источник.** стр. 43 курса: "По возврату цены на ретест уровня - открываем позицию и ставим СТОП за накопление"

**Проект.** src/hunter/levels.py:895 `EntryRule.RETEST_FLIPPED` выдаётся; src/hunter/emit.py:80-82 `hold_reason` для `LevelState.FLIPPED` возвращает непустую строку → setup не строится → сигнала нет; src/hunter/tgbot.py:1257 при этом пишет владельцу: "N пробитых не в списке — по стр. 43 они торгуются ретестом с обратной стороны". Замер на кадрах last: 4372 из 9715 уровней (45.0%) в состоянии FLIPPED, сделок по ним 0. Дополнительно ветка `retest_flipped` в src/hunter/render.py:246 и tgbot.py:818 недостижима (это правило выдаётся только состоянию FLIPPED, которое в оба списка не пускают), то есть на графике такой уровень не […]

**Правка.** Строить `Setup` для FLIPPED от `Level.flipped()` (вход по ретесту той же линии с обратной стороны, стоп за накопление — стр. 43) и эмитировать; либо, пока это не сделано, убрать из сообщения бота утверждение, что этот класс торгуется.

**Чем видно.** Зонд probe_entry.py: "причины отказа входа: {'флипнут': 4372, 'отработан': 4967, 'уже касалась': 175, 'цена в зоне': 3}", "сделок: 198". Текст обещания — src/hunter/tgbot.py:1257

**Проверка (опровергнуть не удалось).** emit.hold_reason возвращает непустую строку для LevelState.FLIPPED ("пробит и флипнут — стр. 43… вход по ретесту в ДРУГУЮ сторону"), то есть setup не строится; tgbot.py:1257-1260 в комментарии и тексте прямо утверждает, что класс по стр. 43 торгуется.

### B-42. Вход "на 6 касании" и резерв на доливку (стр. 57-58) отсутствуют, хотя сама фигура детектируется

**Источник.** стр. 57 курса: "Торгуем : От уровня в Лонг. Если не успели взять от уровня, то ждем 6 касание и берем на 6 касании ( + оставляем на доливку на случай, если цена решит расширить структуру и уйти в накопление с равными границами…)"; стр. 58 повторяет то же для треугольника с равными границами

**Проект.** Счётчика касаний в проекте нет вовсе: grep по src на touch_count/"6 касан"/"шестое" даёт 0 совпадений; `levels.first_test_index` (src/hunter/levels.py:1160) отдаёт только ПЕРВОЕ касание. При этом объект курса детектируется: `accumulation` считает структуры в сужении (`narrowed`), и карточка их печатает — на 5м 235-317 штук на символ. То есть вымпел найден и получает ровно то же обращение, что прямоугольник (уровень после выхода), а два его собственных входа — от уровня и на 6-м касании — не выдаются никогда. Реестр долга (docs/audit/course-gap-register-2026-08-09.md:81-83) относит это к "не реализуемо" с […]

**Правка.** Ввести `touch_count` по чередованию сторон (стр. 57: при лонговом тренде ПЕРВАЯ точка сверху) и эмитировать вход при номере 6; резерв на доливку — отдельным полем сделки, а не молчанием.

**Чем видно.** grep -rn "touch_count\|6 касан" src gates — пусто; grep -h "в сужении" data/frames/last/BTC_USDT_USDT/card.txt — "5м … в сужении 288", "15м … в сужении 114"

**Проверка (опровергнуть не удалось).** grep -rn "touch_count" src gates — пусто; счётчика касаний в коде нет, есть только first_test_index (первое касание), которое и использует engine._mtf_break.

### B-43. Правило стр. 51 "истинный + ранний ПП рядом → закуп делим на 2 части" неисполнимо по построению

**Источник.** стр. 51 курса: "Иногда бывает истинный ПП и ранний ПП +- рядом, тогда закуп лучше делить на 2 части (пример на скриншоте)"

**Проект.** src/hunter/pereprior.py:238-250 `detect` возвращает максимум ОДИН переприор на сторону ("Последний подтверждённый переприор каждой стороны"), а src/hunter/pereprior.py:134 в `_detect_side` перезаписывает переменную `found` на каждой удачной итерации и отдаёт последний. Истинный и ранний ПП — это ОДНА сторона (kind TRUE/EARLY, pereprior.py:34-40), значит второй из пары молча выбрасывается и до `engine.decide` (engine.py:284-299), карточки и леджера не доходит никогда. Двух входов по стр. 51 быть не может ни при каких данных.

**Правка.** Возвращать из `_detect_side` пару (последний истинный, последний ранний) одной стороны и строить в `build_pp_setup` два ордера с общим стопом; сейчас даже сосчитать, как часто такая пара встречается, нечем.

**Чем видно.** src/hunter/pereprior.py:134 (`found = Pereprior(...)` в цикле, без накопления) и pereprior.py:248-250 (по одному ПП на сторону). Проверка: в леджере kind='pp' 312 сигналов, и по построению не более одного на (символ, ТФ, сторона) в момент прогона

**Проверка (опровергнуть не удалось).** pereprior._detect_side держит одну переменную found, перезаписываемую в цикле (pereprior.py:134), а detect (238-250) собирает максимум по одному ПП на сторону — пара "истинный + ранний" одной стороны наружу выйти не может.

### B-44. Доливка/повторный вход отсутствует как класс, хотя курс называет её в пяти местах

**Источник.** стр. 16 курса: "где вы по новой доливаете объем к уже открытой позиции, тем самым ваше точка входа смещается выше, и вы передвигаете стоп в безубыток"; стр. 19: "Если цена возвращается - и нет доп факторов за разворот – добираете снова те же 50% позиции"; стр. 24: "На этом новом уровне можно: - открыть новую позицию; - "долить" к текущей позиции; - или просто передвинуть стоп изначальной позиции "в профит""; стр. 39: "цель - верхняя граница, где кроем часть 30-50%, и с низу каждый раз повторно откупаем"; стр. 62: "Торговля : в несколько закупов"

**Проект.** Сущности доливки в расчёте нет: grep по src на "долив|добор" даёт только archive.py/exchange.py про добор ДАННЫХ. `Setup.partial_take_pct = 50.0` (src/hunter/geometry.py:175) вычисляется и не читается ни карточкой, ни леджером, ни гейтами. Повторный вход невозможен по построению: после первого касания `levels.status` (levels.py:1019+) снимает лимитки навсегда, а `emit.hold_reason` больше сделки не даёт. Из трёх действий стр. 24 система выражает одно (новая позиция), и то только до первого касания.

**Правка.** Ввести признак сигнала `kind ∈ {new_position, add_to_position}` с предусловием стр. 10-11 (доливка только к прибыльной позиции) и переносом стопа в БУ (стр. 16); пока это не сделано — печатать отказ строкой в самой сделке, а не только в реестре долга, иначе владелец видит одиночный вход там, где курс велит несколько.

**Чем видно.** grep -rn "долив\|добор" src/hunter/*.py — ни одного попадания про позицию; grep -rn "partial_take_pct" src gates scripts — только объявление geometry.py:53 и поле geometry.py:175

**Проверка (опровергнуть не удалось).** grep -rn partial_take_pct по src/gates/scripts даёт только geometry.py:175 (объявление поля) — ни один потребитель его не читает; сущности доливки/добора позиции в src нет.

### B-45. Перевод стопа в безубыток отсутствует в проекте целиком, и отсутствие не названо как ограничение

**Источник.** Курс требует перевода в БУ шесть раз. Стр. 19: "Цена показала реакцию и ушла внутрь базы – ставите стоп в б/у". Стр. 16: "ваше точка входа смещается выше, и вы передвигаете стоп в безубыток". Стр. 11: "позиции в профите, и стоят стопы в безубытке". Стр. 43: "Мы закрываем позицию в БУ" и "закрываем свою позицию в БУ". Стр. 44: "быть готовым выйти в б/у при пробое уровня – на ретесте". Стр. 24: "или просто передвинуть стоп".

**Проект.** "git grep -niE "безубыт|б/у|в бу" src/hunter/*.py" даёт только КОММЕНТАРИИ и тексты подписей (`emit.py:97,104`, `engine.py:328`, `geometry.py:93`, `levels.py:920,1114`) — ни одного поля, события или ветви расчёта. `src/hunter/outcome.py:27-42` знает пять исходов, безубытка среди них нет; `outcome.resolve` (`outcome.py:59`) сравнивает бар только со стопом и целью. Шапка `outcome.py:1-13` называет ОДНО ограничение (неоднозначный бар) и о ненамоделированном безубытке молчит, тогда как отчёт прогона (`run.py:2414`) специально предупреждает про комиссии.

**Правка.** Завести событие "реакция" (ход внутрь базы на заданный курсом признак — стр. 19) и `OutcomeKind.BREAKEVEN` с r = 0, либо, если моделировать сейчас не решено, ЯВНО написать в шапке `src/hunter/outcome.py` и в отчёте `run.print_report` строку "перевод стопа в безубыток (стр. 19, 16, 43, 44) не моделируется — все такие сделки записаны как −1R". Сейчас владельцу не сказано ни того, ни другого.

**Чем видно.** `git grep -c "безубыт" src/hunter/ gates/` → нет ни одного вхождения в расчёте. Следствие в числах: у уровневых сигналов 37 исходов "стоп" из 118 закрытых; каждый из них по методу мог закрыться в БУ, а записан как −1R. Знаменатель: `uv run python -m hunter ledger`.

**Проверка (опровергнуть не удалось).** grep по src/hunter даёт ноль вхождений BREAKEVEN/breakeven; OutcomeKind (outcome.py:27-42) знает пять исходов без БУ, resolve сравнивает бар только со стопом и целью. Цитаты курса не перепроверял постранично.

### B-46. Вторая ветвь стопа ПП — "или базу в месте слома" — не реализована вовсе

**Источник.** стр. 50 дословно: "со стопом за хай/лой ИЛИ БАЗУ В МЕСТЕ СЛОМА, ЕСЛИ ОНА ЕСТЬ". Стр. 54, вариант 1: "Вход от уровня в ЛОНГ, стоп за структуру". То есть у стопа ПП две ветви, и приоритет у базы, когда она есть.

**Проект.** `src/hunter/geometry.py:489-506` (`build_pp_setup`) знает ровно одну ветвь — дальний край зоны тени. Ни `scans`, ни `Accumulation` в функцию не подаются, поиска накопления в месте слома нет. `StopBasis` (`geometry.py:64`) имеет два значения, оба про уровень, и к ПП не применяется.

**Правка.** Передать в `build_pp_setup` закрытые накопления того же ТФ, искать структуру, чьё окно накрывает бар подтверждения слома, и при её наличии брать стоп за её границу (с запасом), пометив основание. Замер "сколько ПП имели базу и у скольких она сработала" печатать в карточке — иначе мёртвую ветвь снова нельзя будет отличить от отсутствующей.

**Чем видно.** `src/hunter/geometry.py` — в теле нет ни одного обращения к накоплениям. Замер, который покажет цену: сколько ПП имеют закрытое накопление, пересекающее зону слома, и у скольких из них стоп по базе был бы ДАЛЬШЕ края тени; сейчас это число не считает никто.

**Проверка (опровергнуть не удалось).** Тело build_pp_setup (geometry.py:489-506) прочитано целиком: параметры только pp и opposite, ни scans, ни Accumulation, стоп всегда дальний край зоны тени.

### B-47. Полоса поиска якоря 2-5% выключена потолком высоты базы у двух третей уровней — курсовое "идеально стоп прятать за них" на 5м и 15м не работает

**Источник.** стр. 18: "Если в диапазоне 2-5% от границы есть стоповый объем – база мелкого ТФ - или Лой того же ТФ или ТФ-1 - идеально стоп прятать за них". Полоса задана курсом в процентах ОТ ГРАНИЦЫ и ни к высоте базы, ни к ТФ не привязана.

**Проект.** `src/hunter/levels.py:791-796` — `band_lo = max(band_lo, boundary - base_height)` (зеркально для шорта). Докстрока (`levels.py:770-786`) честно говорит, что условие "умеет ОПУСТОШИТЬ" полосу, но числа не даёт, и в карточку это не выводится: уровень с пустой полосой и уровень, у которого якоря просто нет, печатаются одинаково.

**Правка.** Либо предъявлять владельцу это как решение с ЧИСЛОМ (карточка обязана печатать "полоса якоря пуста: высота базы 0.9% меньше 2%" вместо молчания), либо привязать полосу к высоте базы, а не к цене (2-5% высоты базы вместо 2-5% цены) — второе ближе к масштабу рисунков курса. Сейчас правило курса выключено у большинства уровней и это не видно ниоткуда.

**Чем видно.** Зонд "scratchpad/probe_stop2.py" (прогон `last`, 9715 уровней): полоса опустошена у 6265 (64.5%) — 5м 5244 из 6652 (78.8%), 15м 980 из 2295 (42.7%), 1ч 41 из 619 (6.6%), 4ч 0 из 133, 1Д 0 из 16. Контроль: доля падает монотонно с ростом ТФ до нуля, то есть прибор не заперт. На 5м живёт 68.5% всех уровней.

**Проверка (опровергнуть не удалось).** levels.py:791-796 действительно двигает дальний край до boundary∓base_height, и докстрока сама признаёт, что полоса умеет опустеть; но там же указано, что это решение владельца по курсу-масштабу, а не пропуск. Доли по ТФ не перепроверял.

⚠ **Уточнение проверяющего.** Опустошение полосы — не "неисполненное правило", а ЗАФИКСИРОВАННОЕ решение владельца 2026-08-18 (docstring levels.py:766-772 прямо ссылается на "делай 1+2" протокола signals-trader-audit-2026-08-18.md). Настоящая находка — только вторая половина: число опустошённых полос нигде […]

### B-48. Рисковый стоп реализован только одной из двух названных курсом опор

**Источник.** стр. 33 дословно: "Рисковый СТОП: прямо за лой структуры ИЛИ ЖЕ ЗА ОБЪЕМНУЮ ЗОНУ". Курс называет две опоры через "или".

**Проект.** `src/hunter/geometry.py:449` — `stop_risky=edge`, то есть только граница структуры. Опора "за объёмную зону" (`level.zone_lo` / `zone_hi`, VAL/VAH) нигде не считается. Вдобавок `edge` берётся РОВНО на границе — то самое "за = равно", которое `beyond_anchor` (`geometry.py:369`) объявил выдумкой для выставляемого стопа.

**Правка.** Либо добавить второй рисковый стоп по кромке объёмной зоны и печатать оба (курс называет оба), либо удалить `stop_risky` целиком — сейчас поле неполно исполняет правило и при этом читается только зондом качества (см. находку о scripts/probes.py).

**Чем видно.** `src/hunter/geometry.py` — единственное присвоение `edge`. `src/hunter/geometry.py` — в стопах не участвуют.

**Проверка (опровергнуть не удалось).** Стр. 33 PDF дословно: "Рисковый СТОП: прямо за лой структуры или же за объемную зону", а geometry.py:449 даёт stop_risky=edge — только граница структуры; zone_lo/zone_hi в расчёте стопов не участвуют.


---

# C. Посчитано, но владелец этого не видит — 14 пунктов

### C-49. Карточка обещает владельцу, что сделка от ПП "эмитируется в леджер", — её никто не эмитирует

**Источник.** Проверяется внутри проекта и потому не требует внешнего источника; правило нарушено собственное — "всё предъявляемое обязано быть проверяемо без чтения кода" и "владельцу предъявляется расчёт, а не обещание". Опорная запись самого проекта: докстрока src/hunter/geometry.py:465-468 говорит ПРЯМО ОБРАТНОЕ карточке — "Здесь сделка СТРОИТСЯ И ПЕЧАТАЕТСЯ картой; в эмиссию она не идёт — новый ТИП сигнала меняет состав леджера, и это отдельный заход".

**Проект.** src/hunter/card.py:374 — `tgt = f"цель {_num(ps.target)} РР {ps.rr:.2f} — эмитируется в леджер"`. Строка печатается безусловно, как только у сделки от ПП есть цель и РР. При этом `PPSignal`/`PPSetup` не встречаются в src/hunter/emit.py ни разу (grep по `PPSignal|pp_signals|PPSetup` даёт совпадения только в engine.py и geometry.py), функций эмиссии ПП нет. Владелец, читающий карточку, видит РР и слово "эмитируется" и вправе ждать этот сигнал в леджере и в исходах — его там не будет никогда, и отсутствие прочтётся как "сигналов не было", а не как "печать соврала".

**Правка.** Заменить строку на честную: "цель X РР Y — в леджер НЕ идёт (тип сигнала не заведён)", либо завести эмиссию ПП и тогда оставить фразу. Пока не сделано ни того, ни другого, любая статистика по ПП, которую владелец попробует свести по леджеру, будет пустой при непустой карточке. Проверка одной командой: `uv run python -m hunter check` и грепом по emit.py на `PPSetup`.

**Чем видно.** grep -n "эмитируется в леджер" src/hunter/card.py → card.py:374; grep -n "PPSignal|pp_signals|PPSetup" src/hunter/emit.py → пусто (совпадения только в src/hunter/engine.py:114,128,168,285,293,303).

**Проверка (опровергнуть не удалось).** card.py:374 печатает `— эмитируется в леджер` безусловно при наличии цели и РР; grep по emit.py на PPSetup/PPSignal/pp_signal вернул пусто. Докстрока geometry.py:465-468 прямо говорит "в эмиссию она не идёт".

### C-50. "Поздний возврат без единого тела за уровнем" объявлен ПРОКОЛОМ, то есть ОТРАБОТКОЙ, и уровень снимается — школа закрытий, на которую опирается обоснование, такого вывода не делает

**Источник.** КЛАССИКА говорит "пробоя не было", а не "уровень отработан" (5 источников): 1) Bulkowski — пробой засчитывается по CLOSE за границей паттерна; busted-пробой — когда цена прошла менее 10% и закрылась за противоположной границей https://thepatternsite.com/BustRectangles.html ; 2) StockCharts ChartSchool, S&R — "The resistance level was confirmed when the stock failed to advance past 42.5", то есть НЕСОСТОЯВШИЙСЯ выход уровень ПОДТВЕРЖДАЕТ, а не расходует https://chartschool.stockcharts.com/table-of-contents/chart-analysis/support-and-resistance ; 3) Wikipedia — "The more often a support/resistance level is […]

**Проект.** src/hunter/breach.py:233 — `return made(BreachKind.PUNCTURE if best_run == 0 else BreachKind.UNRESOLVED, i)`: заход ЛЮБОЙ длительности, если за уровнем не было ни одного полного тела, объявляется проколом. Дальше по цепочке: src/hunter/levels.py:1097 → `LevelState.WORKED_OFF`; src/hunter/emit.py:70 → "отработан на первое касание… лимитками не торгуется"; src/hunter/store.py:964 → уровень СНИМАЕТСЯ ("снятие происходит только по состоянию worked_off или flipped"). Обоснование в докстроке breach.py:225-245 названо "школой ЗАКРЫТИЙ" и дополнено ссылками на LuxAlgo и скрипт vikprimus — это поставщик коммерческих […]

**Правка.** Развести два разных ответа. "Пробоя не было" (ни одного тела за уровнем, возврат вне окна 1-2 свечей) по всем пяти источникам означает, что уровень выстоял, — такой уровень не должен получать `worked_off` и сниматься из леджера. Прокол по стр. 6 оставить строго за возвратом в окне 1-2 свечей. Ожидание гейта `c_late_return_no_bodies_is_puncture` менять только вместе с записью, почему прежнее было […]

**Проверка (опровергнуть не удалось).** breach.py: `return made(BreachKind.PUNCTURE if best_run == 0 else BreachKind.UNRESOLVED, i)` после проверки back > return_bars; обоснование в докстроке рядом опирается в том числе на LuxAlgo и vikprimus (коммерческий поставщик и скрипт TradingView), правило закреплено гейтом c_late_return_no_bodies_is_puncture в списке course_rules.

⚠ **Уточнение проверяющего.** Проверена только цепочка breach.py и наличие гейта; переход в WORKED_OFF и снятие из леджера по коду не прослеживал.

### C-51. Заявленное число строк TV_ROWS = 100 боевым расчётом не исполняется: сетка выходит 19…149 строк, ровно 100 — у 4.6% профилей

**Источник.** TradingView, Fixed Range Volume Profile — https://www.tradingview.com/support/solutions/43000480324-fixed-range-volume-profile/ — "Ticks Per Row = (Histogram Top - Histogram Bottom) / Number of Rows / Tick size": высота строки квантуется тиками, поэтому число строк действительно может разойтись с заказанным — так что САМ КОД вендору не противоречит. Противоречит ПРЕДЪЯВЛЕНИЕ: у TradingView Number of Rows — параметр инструмента, а не характеристика построенной сетки, и вендор нигде не обещает, что чувствительность к N измерима по заказанному N. Sierra Chart […]

**Проект.** C:\Users\Антон\Documents\hunter-v2\src\hunter\volume_profile.py:228 ticks_per_row = max(1, round(ticks_total / rows)) — целое число тиков; :231 rows_built = ceil(ticks_total / ticks_per_row). При ticks_total < 150 всегда ticks_per_row = 1, то есть "сетка TradingView" ВЫРОЖДАЕТСЯ В ТИКОВУЮ, и боевой прибор совпадает с диагностическим build() по построению. Докстрока C:\Users\Антон\Documents\hunter-v2\src\hunter\volume_profile.py:91-109 обосновывает выбор 100 замером чувствительности E-070 ("согласие сетки с тиковым ПОК — 29.1% при 24, 28.6% при 50, 37.7% при 100, 49.1% при 200") и словами "Константа вынесена […]

**Правка.** 1) Заменить round на арифметическое округление (floor(x + 0.5)) — это и есть формула вендора; 2) вынести rows_built в отчёт/карточку рядом с зоной, чтобы построенное разрешение было ВИДНО, а не выводилось из константы; 3) перемерить E-070 по ПОСТРОЕННОМУ числу строк, а не по заказанному, и переписать докстроку TV_ROWS так, чтобы она называла реальное распределение (19…149, ровно 100 у 4.6%) и […]

**Чем видно.** uv run python "C:\Users\3EC2~1\AppData\Local\Temp\claude\C--Users-------Documents-hunter-v2\db8721f4-1545-4a47-9813-1ac82f4a3bd8\scratchpad\probe_grid_ties_clamp_2026-08-19.py" (кадры data/frames/shift18-live, 2 символа, 3858 профилей): "rows_built (запрошено 100)" — распределение от 19 до 149; "!= 100: 3679 из 3858" (95.4%); ">=110: 724" (18.8%). Ровно 100 строк — 179 из 3858 = 4.6%. Аналитический контроль формулы […]

**Проверка (опровергнуть не удалось).** volume_profile.py:228 ticks_per_row = max(1, round(ticks_total/rows)), :231 rows_built = ceil(ticks_total/ticks_per_row) — построенное число строк действительно не равно заказанному, и при ticks_total<150 ticks_per_row=1 (вырождение в тиковую). Числа evidence не перепроверял.

⚠ **Уточнение проверяющего.** Сам код формуле вендора не противоречит; дефект — в предъявлении и в том, что E-070 мерил по заказанному N.

### C-52. Число точек границ закрытой структуры не предъявляется — правило "4 и более точек" владелец проверить не может

**Источник.** стр. 22 курса, дословно: "Флет/накопление (ренж/бaза) - накопление с конкретными границами, имеющая в рамках своего ТФ понятные 4 и более точек." Стр. 23: "Уровень накопления относятся к тому ТФ, на котором четко видно его структуру (4+6+ точки границ) и работают на этом ТФ." То есть 4 — минимум, 6+ — норма.

**Проект.** Число считается (`Accumulation.points`), но в `Level` его нет ни одним полем (src/hunter/levels.py:51-110) и в карточке оно печатается ТОЛЬКО для незакрытой структуры (src/hunter/card.py:148 "открыта N баров, точек M"). У закрытых структур, то есть у всех, кто породил уровень, число точек не видно нигде — ни в карточке, ни в боте, ни на графике. Сама проверка порога в детекторе недостижима при умолчании: `if len(hi_px) + len(lo_px) < min_points` (accumulation.py:552) при `min_points=4` ложна всегда, требование стр. 22 выполняется структурой цикла, а не проверкой.

**Правка.** Добавить `Level.boundary_points_hi/lo`, печатать в строке уровня "точек 2+2" и помечать `качество: 4+ (минимум) / 6+ (норма стр. 23)". Тогда правило станет видимым глазами, как того требует правило проекта о предъявляемой величине.

**Чем видно.** Распределение числа точек по кадрам `last` (9780 структур): 4 точки — 4740 (48.5%), 5 — 2872, 6 — 1270, 7 — 459, 8 — 231, дальше хвост. То есть почти половина уровней стоит на конструктивном минимуме, и по карточке это неразличимо. grep -n "точек" src/hunter/card.py → единственное вхождение на строке 148 (незакрытый хвост).

**Проверка (опровергнуть не удалось).** grep "точек" src/hunter/card.py даёт ЕДИНСТВЕННОЕ вхождение на строке 148 — ветка открытого хвоста (sc.open_tail.points); в levels.py поля points нет вовсе (grep пусто). Ветка min_points на accumulation.py:552 сама помечена в комментарии как недостижимая при умолчании 4.

### C-53. Половина экстремумов внутри структуры выброшена молча — сводки отказов нет

**Источник.** Правило проекта (CLAUDE.md): "у отказов обязана быть СВОДКА по измерению, вдоль которого возможен перекос. Один отказ — данные; сто отказов, все на одном ТФ, — дефект." Отбрасывается ровно то, из чего строятся границы и считаются касания стр. 22/23/57.

**Проект.** Два молчаливых `continue` в src/hunter/accumulation.py: строка 384 (свинг той же стороны, что последний принятый — правило чередования) и строки 448/499 ("касание внутри базы — курс его не нумерует"). Ни счётчика, ни поля в `AccumulationScan`, ни строки в карточке. Единственный счётчик `resets` смешивает две причины и при умолчании описывает только одну ("границы сошлись"): у BTC 5м он равен 6 при 1328 структурах.

**Правка.** Завести в `AccumulationScan` два счётчика — `dropped_alternation` и `dropped_inside` — с разбивкой по ТФ и печатать их в строке "ТРЕНД И СТРУКТУРА" рядом с "распадов N".

**Чем видно.** Зонд по кадрам `last`: экстремумов, попавших в жизнь закрытых структур, — 98238; НЕ стали точками границы 49567 (50.5%), из них 37338 лежат внутри коробки (ветка "касание внутри базы"), 12229 отброшены чередованием/прочим. Отдельно по всем рядам: правило чередования выбрасывает 21183 свинга из 98703 (21.5%). Видно в карточке: "5м баров 51840 структур 1328 распадов 6" — отказов напечатано шесть, а их сорок девять […]

**Проверка (опровергнуть не удалось).** accumulation.py:384 — `if kind is last_kind: ... continue` (чередование), строки 448/499 — `if not converging(price, kind): continue` ("касание внутри базы"); в AccumulationScan (стр. 257-264) единственный счётчик `resets` с докстрокой "зоны сошлись либо точек не хватило", отдельных счётчиков отброшенных точек нет, карточка (card.py:156) […]

### C-54. Рамка структуры на графике выше и ниже своих свечей — тот самый симптом, на который владелец жаловался глазами

**Источник.** стр. 30 курса — рисунок: выноски "ХАЙ" и "ЛОЙ" указывают на верхнее и нижнее рёбра бирюзовой коробки, то есть рамка обязана лежать по экстремумам свечей, которые она обводит. Рисунок стр. 13: рамка построена на своих барах, проколы торчат НАРУЖУ, но пустого воздуха внутри рамки нет.

**Проект.** src/hunter/render.py:193-210 — рамка рисуется по горизонтали от `bar_index(z.from_ms)` до `bar_index(z.to_ms)`, то есть по ОКНУ ПРОФИЛЯ (без свечей выхода), а по вертикали от `z.boundary_lo` до `z.boundary_hi`, то есть по КОРОБКЕ СО свечами выхода (`ZoneSpec.boundary_lo`, render.py:97 ← levels.py:423). Высота рамки берётся у баров, которые в её горизонтальный отрезок не входят.

**Правка.** Устранить расхождение в источнике (находка №1) — после неё рамка совпадёт со своими свечами автоматически. До этого — рисовать рамку по горизонтали до `structure_last_index`, чтобы бары, задавшие её высоту, были внутри неё.

**Чем видно.** Зонд по кадрам `last`, повторяющий арифметику `render.bar_index`: у 3652 уровней из 9780 (37.3%) рамка выше или ниже ВСЕХ свечей внутри собственной рамки; пустой воздух — медиана 9.3% высоты рамки, p90 31.5%, max 328.7%. Способ увидеть: открыть график бота по BCH на 1ч и найти бирюзовую рамку, чей нижний край проходит заметно ниже всех свечей внутри неё.

**Проверка (опровергнуть не удалось).** render.py:193-194 — `i0 = bar_index(z.from_ms)`, `i1 = bar_index(z.to_ms)` (окно профиля, без свечей выхода), а вертикаль на строках 196-198 берётся из `z.boundary_lo/boundary_hi`, которые в levels.py:515-519 посчитаны по `bars[acc.first_index:acc.last_index+1]`, то есть со свечами выхода. Расхождение множеств баров подтверждено по коду […]

### C-55. "Накопление у уровня" печатает ЛИНИИ под именем "коробка"

**Источник.** Правило проекта, введённое владельцем 2026-08-18: "Если величина ПРЕДЪЯВЛЯЕТСЯ… то расчёт, инвариант и гейт обязаны брать ОДНО И ТО ЖЕ поле. Две сущности под одним человеческим именем — дефект по построению". Стр. 28 курса, на которую ссылается строка, различает конфигурации 2/5/7 положением НОВОЙ КОРОБКИ относительно линии уровня.

**Проект.** src/hunter/levels.py:1152 — `lo, hi = acc.lower.edge, acc.upper.edge`, и эти же два числа кладутся в поля с именами `box_lo`/`box_hi` (levels.py:1158). `engine._pressed_note` печатает их словом "коробка". В той же карточке двумя строками выше "границы" того же уровня печатаются по коробке ХАЙ…ЛОЙ, которая в медиане в 1.824 раза шире. Владелец читает два ряда чисел одинакового вида, посчитанных по разным сущностям.

**Правка.** Считать `pressed_structures` по той же величине, что печатается словом "границы" (размах баров прижатой структуры), либо переименовать поля в `edge_lo/edge_hi` и печатать "линии границ", а не "коробка".

**Чем видно.** data/frames/last/BTC_USDT_USDT/card.txt:14 — строка "накопление у уровня: конфигурация 5, над уровнем (стр. 28): 66408.1…67236.5, …" рядом со строкой 12 "границы 65716.7…66655.5". Первые числа — линии, вторые — коробка. Замер отношения: коробка/линии медиана 1.824, p90 4.088 (9780 структур, прогон `last`).

**Проверка (опровергнуть не удалось).** levels.py:1152 действительно `lo, hi = acc.lower.edge, acc.upper.edge`, и эти же величины кладутся в поля `box_lo=lo, box_hi=hi` (строка 1157-1158), тогда как "границы" в карточке идут от `boundary_lo/hi`, посчитанных по размаху баров (levels.py:515-519). Две разные сущности под именем "коробка" — по коду подтверждено; отношение 1.824 не […]

### C-56. "Тренд не определён" склеивает сходящуюся структуру и отсутствие тренда

**Источник.** стр. 12 курса, дословно: "Восходящий тренд (лонг-тренд) - движение цены на выбранном ТФ, при котором каждый следующий ЛОЙ выше предыдущего. Нисходящий тренд (шорт-тренд) - … каждый следующий ХАЙ ниже предыдущего." Случая "оба сразу" курс не называет, но стр. 57-59 называют его отдельной торгуемой фигурой: "треугольник это структура накопления (стоповый), просто в сужении", "Пример роста через каскад сужающихся структур".

**Проект.** src/hunter/swings.py:163 — `if up and down: return Trend(direction=NONE, holds_for=0)`; та же величина возвращается, когда не выполнено ни одно условие (swings.py:169). Карточка печатает для обоих случаев одну фразу "тренд не определён (держится на 0)" (src/hunter/card.py:158). Сходящаяся структура — это клин, который курс велит торговать по тренду и от уровня; в карточке он неотличим от "рынок никуда не идёт".

**Правка.** Добавить `TrendDirection.CONVERGING` (или отдельное поле `converging: bool`) и печатать "сходящаяся структура (клин, стр. 57)" вместо "не определён", сохранив глубину обеих последовательностей.

**Чем видно.** Зонд по кадрам `last`: рядов с "не определён" — 13; из них "оба условия сразу" (лои растут И хаи падают) — 8, "ни одного" — 5. Видно в карточке: data/frames/last/BTC_USDT_USDT/card.txt строки 5-7 — "15м … тренд не определён (держится на 0)", "1ч … тренд не определён", "4ч … тренд не определён" — какая из трёх строк клин, а какая пустой рынок, из карточки узнать нельзя.

**Проверка (опровергнуть не удалось).** swings.py:163 `if up and down: return Trend(direction=TrendDirection.NONE, holds_for=0)` и строка 169 возвращают идентичное значение; ни `CONVERGING`, ни поля `converging` в swings.py/card.py нет (грep пуст), карточка (card.py:158) печатает один ярлык TREND_LABEL для обоих случаев.

### C-57. В карточке уровня нет ни одной отметки времени — свежая структура и структура возрастом в тысячи баров выглядят одинаково

**Источник.** Стр. 48: "Чем старше ТФ – тем выше винрейт, но дольше отработка." Стр. 23 привязывает рождение уровня к моменту выхода из структуры, стр. 27 — отработку к ТФ структуры: возраст структуры и момент события у автора всегда на экране (коробка нарисована на своих барах, стрелка отработки — на своём баре).

**Проект.** src/hunter/card.py:169-186 печатает сторону, ТФ, ПОК, зону, объём, состояние, границы, правило входа и приоритет — и ни одного поля времени. `created_at_ms`, `structure_from_ms`, `structure_to_ms`, `resolved_at_ms` и номер бара события в card.py не встречаются вовсе (грепом по файлу). Между тем эти же поля управляют картинкой (render.py рисует бокс по from_ms/to_ms и значок судьбы по resolved_at_ms) и фильтром бота `near_structure` (tgbot.py:821, рамка 180 баров) — то есть проект СЧИТАЕТ возраст важным везде, кроме единственного артефакта, который читает владелец.

**Правка.** Печатать в строке уровня возраст конца структуры в барах своего ТФ (и бар разрешения события у отработанных/флипнутых). Величина детерминированная — это разность индексов, а не отметка часов, поэтому дифф повтора §10.6 от неё не поедет.

**Чем видно.** Кадры `last`: у 376 активных уровней возраст конца структуры в барах СВОЕГО ТФ — медиана 7726, p90 27394, максимум 51023; старше 180 баров (рамки, которой бот их прячет) — 340 из 376 (90%). Среди 198 торгуемых лимитками — 174 старше 180 баров. Открыть data/frames/last/BTC_USDT_USDT/card.txt и попробовать отличить свежий уровень от уровня, чья структура кончилась семь тысяч баров назад: в тексте нет ни одного числа […]

**Проверка (опровергнуть не удалось).** src/hunter/card.py:169-186 печатает сторону, ТФ, ПОК, зону, объём, состояние, границы, вход, приоритет; grep по card.py на created_at_ms/structure_from_ms/structure_to_ms/resolved_at_ms не даёт ни одного вхождения. Числа evidence не перепроверялись.

### C-58. Отчёт прогона называет РР "по рисковому стопу", хотя считает его по выставляемому — владелец читает число под неверным именем

**Источник.** Правило проекта: "предъявляемое обязано быть проверяемо без чтения кода" (§7.5/§7.6). Курс стр. 9 задаёт РР как отношение к РИСКУ сделки, то есть к тому стопу, который реально стоит.

**Проект.** `src/hunter/run.py:2409`: `print(f" РР (рисковый стоп, тот же, что в леджере): {_spread(r.emitted_rr)}")`. Считается же оно в `run.py:1619` как `em.setup.rr(em.ledger_stop)`, то есть по ВЫСТАВЛЯЕМОМУ стопу. Ту же неправду повторяет докстрока модели: `src/hunter/models.py:927` — "РР каждой эмиссии по рисковому стопу". Рисковым стоп перестал быть 2026-08-05 (`emit.py:52`).

**Правка.** Заменить подпись на "РР по выставляемому стопу (тот же, что в карточке и в леджере)" в `run.py:2409` и переписать докстроку `models.py:927`. Заодно проверить остальные подписи чисел на слово "рисковый".

**Чем видно.** `src/hunter/models.py` против `src/hunter/run.py`. Имя занижает стоп: рисковый стоит НА границе, выставляемый — на 3-5% за ней, то есть подпись обещает худший РР, чем напечатан.

**Проверка (опровергнуть не удалось).** run.py:2409 печатает "РР (рисковый стоп, тот же, что в леджере)", а run.py:1619 считает rr = em.setup.rr(em.ledger_stop); докстрока models.py:927 повторяет "по рисковому стопу".

⚠ **Уточнение проверяющего.** Вторая половина подписи ("тот же, что в леджере") верна — неверно только слово "рисковый".

### C-59. Карточка печатает стоп только в процентах ОТ ВХОДА; запас ОТ ГРАНИЦЫ — величина, которую нормирует курс, — не печатается нигде

**Источник.** Курс нормирует запас ОТ ГРАНИЦЫ, а не от входа, во всех четырёх местах. Стр. 33: "Безопасный СТОП за дно структуры с запасом 1-3%". Стр. 19: "СТОП прятать с запасом за структуру (границы) 1-5%". Стр. 36: "Стоп ставим также, как и в торговле накоплений - за структуру 1-3%". Стр. 58: "Стоп всегда прячем за всю структуру с запасом 1-3%". Расстояние вход→стоп курсом не нормируется вовсе.

**Проект.** `src/hunter/card.py:253-257` печатает `f"стоп {_num(s.stop)} — {STOP_BASIS_LABEL[...]} ({_pct(s.entry, s.stop)} от входа)"`. Граница напечатана строкой выше (`card.py:~168`, "границы 78970.4…79199.7"), но разность владелец обязан считать в уме. Именно это число породило жалобу "какой микро стоп 0.3% ?!": в сохранённой карточке `data/frames/last/BTC_USDT_USDT/card.txt:4316` стоит "стоп 79429 … (0.47% от входа)" при границе 79199.7 — запас от границы 0.29%, то есть НАРУШЕНИЕ курсовых 1-3%, и увидеть это по строке нельзя.

**Правка.** В `card.py:253` печатать ОБЕ величины: "стоп X — запас за структуру: 3.00% за границей (стр. 33: 1-3%), 5.41% от входа". Тогда курсовое правило проверяется глазами по одной строке, а жалоба на "микро-стоп" становится проверяемой без чтения кода.

**Чем видно.** `grep -n "стоп .* от входа" data/frames/last/BTC_USDT_USDT/card.txt | head` — ни в одной строке нет запаса от границы. `src/hunter/card.py` → 0.

**Проверка (опровергнуть не удалось).** card.py:253-256 печатает только "({_pct(s.entry, s.stop)} от входа)"; PDF подтверждает, что курс нормирует запас именно от структуры: стр. 33 "Безопасный СТОП за дно структуры с запасом 1-3%", стр. 36 "за структуру 1-3%", стр. 58 "за всю структуру с запасом 1-3%".

⚠ **Уточнение проверяющего.** Цитата стр. 19 в находке приведена с "1-5%": в текстовом слое строка обрывается на "СТОП прятать с запасом за" — процент там не подтверждён; во всех подтверждённых местах курс говорит 1-3%.

### C-60. Карточка печатает ярлык якоря рядом со стопом, который якорь не ставил

**Источник.** Правило проекта (CLAUDE.md, раздел о приборе и предъявляемой величине): "оператор обязан видеть НЕ ТОЛЬКО число, но и правило, по которому оно получено" — так это и записано в `geometry.Setup.stop_basis`.

**Проект.** `src/hunter/card.py:248-257`: `anchor_note` добавляется при любом `lvl.stop_anchor_source is not None`, независимо от `s.stop_basis`. Когда пол 3% (`geometry.py:438`) перебивает якорный стоп, основание становится MARGIN, но подпись "якорь: прокол за границу / стоповый объём / лой-хай своего ТФ" всё равно печатается. Читатель видит "стоп X — запас за структуру (стр. 33) … якорь: прокол за границу" и делает вывод, что стоп стоит за проколом.

**Правка.** В `card.py:248` печатать `anchor_note` только при `s.stop_basis is StopBasis.ANCHOR`, а при MARGIN писать "якорь найден (прокол/стоповый/свинг), но ближе курсового запаса — стоп держит запас". Иначе подпись противоречит основанию, напечатанному в той же строке.

**Чем видно.** Зонд "scratchpad/probe_stop2.py" (прогон `last`, 9715 уровней): якорь есть у 4661, из них у 1379 (29.6%) стоп поставлен ПОЛОМ, а не якорем; все 1357 якорей-проколов попадают именно в этот класс. Контроль: "стоп по якорю" 3282 — прибор различает.

**Проверка (опровергнуть не удалось).** card.py:248-250: anchor_note формируется по условию `lvl.stop_anchor_source is not None` и подклеивается в строку стопа безусловно, тогда как основание печатается отдельно из STOP_BASIS_LABEL[s.stop_basis] — связи между ними в коде нет. Долю 29.6% не перепроверял.

### C-61. RR_EMIT_MIN объявлена порогом эмиссии и владельцу доложено, что она отсекает 86.2% сигналов, — константу не читает ни один путь кода

**Источник.** Правило CLAUDE.md: "слова „работает“, „инвариант держится“ НЕ ОЗНАЧАЮТ, что метод считается верно" и "число не докладывается, пока не проверено, что оно могло быть другим". Курс стр. 9 даёт 1к3 как ОРИЕНТИР ("золотой стандарт"), а не как фильтр.

**Проект.** `src/hunter/geometry.py:277` — `RR_EMIT_MIN = 3.0` с докстрокой "минимальное РР эмитируемого сигнала … Владелец может сменить порог одной строкой". `git grep -n RR_EMIT_MIN` по всему репозиторию даёт ровно два вхождения: это объявление и абзац `docs/audit/session-2026-08-17-auto.md:1303` ("отсекает 979 сигналов из 1136 (86.2%) … и 142 из 145 на нынешнем расчёте"). В `emit.hold_reason` (`emit.py:68`) и в `engine.decide` (`engine.py:271-284`) отбора по РР нет ни одного; `src/hunter/run.py` пишет в леджер все эмиссии без проверки РР.

**Правка.** Либо удалить константу вместе с абзацем журнала смены и доложить владельцу отзыв числа "86.2%", либо, если фильтр действительно нужен, — но тогда сначала предъявить, что вход в него считается верно (правило "порог на выходе не лечит расчёт на входе"). Сейчас система ведёт себя не так, как о ней доложено.

**Чем видно.** `git grep -n "RR_EMIT_MIN\|risk_reward\|\.rr(" -- src/hunter` — в `src/hunter/emit.py` и `src/hunter/engine.py` ни одного вхождения. В леджере 1154 уровневых сигнала, из них с целью и РР ниже 3 — большинство: `uv run python -m hunter ledger`.

**Проверка (опровергнуть не удалось).** grep по всему репозиторию: RR_EMIT_MIN встречается только в src/hunter/geometry.py:277 и в абзаце docs/audit/session-2026-08-17-auto.md:1303; в emit.py и engine.py нет ни одного вхождения rr/risk_reward/RR. Число 86.2% из evidence не перепроверял.

### C-62. График бота — то, что владелец реально смотрит, — не рисует стоп вовсе

**Источник.** Курс рисует стоп КАЖДЫЙ раз, когда показывает сделку: стр. 33 — две красные линии "СТОП" с ценниками 13.300 и 12.920 и мерная стрелка "-0.416 (-3.12%)"; стр. 19 — красный блок с подписью "СТОП" под нижней границей; стр. 10 — красный блок инструмента позиции от линии уровня.

**Проект.** `src/hunter/render.py` рисует бокс структуры (`:185`), линию уровня, зону входа (`:232-247`, `:310-312`) и линию текущей цены (`:409`). Слов `stop`/`стоп` в модуле нет ни одного (кроме комментария `:203` со ссылкой на стр. 18). `chart_png` получает `ZoneSpec` (`render.py:61`) из таблицы `levels` через `tgbot.read_map` (`tgbot.py:443`), где стопа нет по построению.

**Правка.** Провести `Setup.stop` (и цель) до `ZoneSpec` и рисовать красный блок "граница → стоп" так же, как это делает автор на стр. 19/33. Пока стопа на картинке нет, дефекты стопа владелец может найти только в 13-тысячестрочном файле карточки — где четыре из четырёх дефектов смены он и находил.

**Чем видно.** `src/hunter/render.py` → одна строка комментария. Открыть любой ответ бота: на картинке есть коробка, линия ПОК и бокс входа, стопа нет.

**Проверка (опровергнуть не удалось).** grep -in "stop|стоп" по src/hunter/render.py не дал ни одной строки вовсе (находка ожидала хотя бы один комментарий) — стоп в отрисовке не участвует, тогда как курс стр. 33 требует "Безопасный СТОП … Рисковый СТОП" как часть разметки сделки.


---

# D. Приборы: не ловят того, ради чего написаны — 13 пунктов

### D-63. Прибора, сверяющего записанный прокол с фактическим экстремумом, в проекте нет: единственный гейт прокола проверяет НОМЕР точки на рукотворном ряду

**Источник.** CLAUDE.md, раздел "Прибор обязан смотреть на ТУ ЖЕ величину, которую видит владелец": мера снимается с предъявляемой величины, а инвариант на внутреннем поле о предъявленном не говорит ничего. Предъявляемая величина здесь — стоп в карточке; курс, стр. 18: "стоп всегда ставится за этот прокол".

**Проект.** gates/course_rules.py:255 `c_puncture_needs_third_point` — единственная проверка прокола во всём наборе гейтов. Она гоняет РУЧНОЙ ряд `_pair_structure(107.0, 108.0, 93.0, 92.0, hi3=109.0, lo3=91.0)`, где прокол по построению совпадает со свингом, и сверяет НОМЕР точки (3++ по сквозной нумерации), а не ГЛУБИНУ. Сравнения `BoundaryZone.puncture` с max(high)/min(low) окна структуры нет ни в одном гейте и ни в одном зонде. Ближайший по теме замер docs/audit/evidence/E-110-alternation (seeds.txt) мерил приём "самый крайний свинг из серии" против случайного выбора — но по числу ИЗМЕНИВШИХСЯ УРОВНЕЙ; вердикт "не лучше […]

**Правка.** Новый гейт сейчас писать нельзя (CLAUDE.md: пока есть неразобранный дефект расчёта). Значит — зонд в момент правки предыдущего пункта: он сравнивает `puncture` с экстремумом баров окна и печатает долю расхождений с контролем на сохранённых карточках прежнего расчёта. Гейт заводить после того, как расчёт починен.

**Чем видно.** gates/course_rules.py:255-289; docs/audit/evidence/E-110-alternation/seeds.txt

**Проверка (опровергнуть не удалось).** gates/course_rules.py:255 c_puncture_needs_third_point гоняет _pair_structure(107,108,93,92,hi3=109,lo3=91) и сверяет edge/puncture на синтетическом ряду, где прокол совпадает со свингом; сравнения puncture с экстремумом баров окна в наборе гейтов нет.

### D-64. Верхний тик базы не входит в сетку профиля: цена, равная top, оказывается ВНЕ строк и прижимается вниз — у 33% уровней

**Источник.** Фиксированный профиль натянут на ВЕСЬ ценовой размах выбранных свечей, включая экстремумы: TradingView, Fixed Range Volume Profile — https://www.tradingview.com/support/solutions/43000480324-fixed-range-volume-profile/ — "Ticks Per Row = (Histogram Top - Histogram Bottom) / Number of Rows / Tick size", то есть [Histogram Bottom, Histogram Top] — закрытый диапазон, и верхняя строка обязана содержать Histogram Top. То же у TradingView, Volume Profile indicators: basic concepts — https://www.tradingview.com/support/solutions/43000502040-volume-profile-indicators-basic-concepts/ (профиль строится по всему диапазону […]

**Проект.** C:\Users\Антон\Documents\hunter-v2\src\hunter\volume_profile.py:222 ticks_total = int((top − bottom) / tick); :231 rows_built = ceil(ticks_total / ticks_per_row); :240-243 row = floor((price − bottom) / row_height), и при row >= rows_built объём объявляется "прижатым". Механизм: когда ticks_total делится на ticks_per_row нацело (а при ticks_per_row = 1 это ВСЕГДА), bottom + rows_built·row_height == top, поэтому цена ровно top даёт row == rows_built, то есть ВНЕ сетки. Хай базы — это price_range[1] из levels.build_level:382-387, и объём на нём есть всегда: profile_source раскладывает объём бара равномерно по […]

**Правка.** Строки сделать закрытыми сверху на последней: row = min(floor((price − bottom)/row_height), rows_built − 1) без объявления объёма прижатым, ЛИБО (лучше) считать выход за диапазон только по условию price > top или price < bottom, а не по индексу строки. Тогда clamped_volume снова начнёт означать то, что написано в его докстроке. Раз поле объявлено сторожем — его обязан кто-то читать: либо печатать […]

**Чем видно.** uv run python "C:\Users\3EC2~1\AppData\Local\Temp\claude\C--Users-------Documents-hunter-v2\db8721f4-1545-4a47-9813-1ac82f4a3bd8\scratchpad\probe_grid_ties_clamp_2026-08-19.py" (кадры data/frames/shift18-live, 2 символа, 3858 профилей): "clamped направление: {'above': 1270, 'below': 0, 'both': 0, 'none': 2588}" — прижатие есть у 32.9% профилей и ВСЕГДА только сверху, ни одного случая снизу; "доля прижатого объёма: […]

**Проверка (опровергнуть не удалось).** volume_profile: ticks_total = int((top-bottom)/tick), rows_built = ceil(ticks_total/ticks_per_row), row = floor((price-bottom)/row_height), и при row >= rows_built объём идёт в clamped — при ticks_per_row=1 rows_built == ticks_total, поэтому цена ровно top даёт row == rows_built и всегда объявляется прижатой, что противоречит докстроке […]

### D-65. Прибор "слом структуры на младшем ТФ" не способен ответить "нет": он смотрит последний переприор всего ряда, а не слом у уровня

**Источник.** Стр. 25: "можно рассматривать вход от 2 или 3 касания только по факту слома структуры на младшем ТФ". Стр. 31: "Позицию от уровня смотрим только по факту слома структуры на более мелких ТФ." Слом относится к УРОВНЮ и к моменту после касания.

**Проект.** src/hunter/engine.py:391-399 перебирает `y_read.perepriors` и принимает первый ПП нужной стороны с `at >= touch_ms`. Но src/hunter/pereprior.py:238-250 (`detect`) отдаёт "последний подтверждённый переприор каждой стороны" за ВЕСЬ ряд — максимум два объекта. Значит вопрос фактически звучит "подтверждён ли последний ПП ряда позже касания", а он почти всегда у конца ряда. Близость ПП к цене уровня не проверяется вовсе.

**Правка.** Искать переприоры младшего ТФ в окне [бар касания, конец ряда] и рядом с ценой уровня (например, зона ПП пересекается с зоной уровня), то есть завести в `pereprior` выдачу всех ПП окна, а не только последнего на сторону. Прибор обязан уметь напечатать "слома не было".

**Чем видно.** Прогон engine.decide по кадрам `last`, 5 символов, 9715 уровней: "слом на младшем ТФ подтверждён" — 2953; "младше этого ТФ рядов не собрано" — 6564; "лимитки ещё разрешены" — 198. Ответов "слома на … после касания не было" и "касания ещё не было" — НОЛЬ. То есть на всех 2953 адресованных случаях прибор ответил "да" и иного ответа не выдал ни разу. В карточке это строка "слом на 15м подтверждён, зона ПП …" под каждым […]

**Проверка (опровергнуть не удалось).** Прочитал обе функции: цикл engine.py:392-399 идёт максимум по двум объектам, отданным detect, и никакой проверки расстояния до уровня в нём нет.

⚠ **Уточнение проверяющего.** Ветки отказа в коде ЕСТЬ (engine.py:388 "касания ещё не было", engine.py:400 "слома на … после касания не было") — неверна формулировка "не способен напечатать". Устоявшая часть: pereprior.detect (pereprior.py:238-250) отдаёт только ПОСЛЕДНИЙ подтверждённый ПП каждой стороны за […]

### D-66. Прибор проверки слома на младшем ТФ не способен ответить "нет": 2953 ответа из 2953 — "подтверждён"

**Источник.** стр. 25 курса: "вход от 2 или 3 касания только по факту слома структуры на младшем ТФ"; условие обязано ОТСЕИВАТЬ, иначе оно не условие

**Проект.** src/hunter/engine.py:349-400 `_mtf_break` принимает ЛЮБОЙ переприор нужной стороны, подтверждённый в любой момент после касания: нет ни ограничения давности, ни требования, чтобы зона ПП лежала у цены уровня. А `pereprior.detect` (src/hunter/pereprior.py:238) отдаёт ПОСЛЕДНИЙ подтверждённый ПП каждой стороны — он почти всегда позже касания. Замер на кадрах last: из 9517 отказов 6564 (69.0%) получили "младше этого ТФ рядов не собрано" и 2953 (31.0%) — "слом подтверждён"; ответов "слома на … после касания не было" и "касания ещё не было" — НОЛЬ. Воспроизведено на одном символе отдельно (BTC: 1307 / 641 / 0 / 0).

**Правка.** Ограничить окно поиска слома (например, N баров младшего ТФ после касания — число вывести из курса/корпуса, а не подобрать) и требовать, чтобы зона ПП лежала внутри зоны или коробки уровня; печатать долю ответов "слома не было" как контроль работоспособности проверки.

**Чем видно.** scratchpad/probe_entry.py: "слом на младшем ТФ у отказанных: {'младше рядов нет': 6564, 'слом подтверждён': 2953}" (сумма = 9517, других ответов нет). Отдельный прогон по BTC: {'младше рядов нет': 1307, 'слом подтверждён': 641}

**Проверка (опровергнуть не удалось).** Прочитал engine.py:349-400: единственная проверка — at >= touch_ms по последнему ПП нужной стороны, окна и привязки к уровню нет.

⚠ **Уточнение проверяющего.** Ветки "слома … после касания не было" и "касания ещё не было" в коде ЕСТЬ (engine.py:388, 399) — они не недостижимы по построению; верна суть находки: в _mtf_break нет ни ограничения давности слома, ни требования, чтобы зона ПП лежала у цены уровня, а pereprior.detect отдаёт […]

### D-67. RR_EMIT_MIN объявлен порогом эмиссии входа и владельцу доложен как действующий, но в коде не подключён ни к одной ветке

**Источник.** стр. 9 курса: ""Золотым стандартом" считаются сделки с РР 1к3 и выше"

**Проект.** src/hunter/geometry.py:277 — `RR_EMIT_MIN = 3.0` с докстрокой "Минимальное РР эмитируемого сигнала… Введено 2026-08-18… Владелец может сменить порог одной строкой". Журнал смены идёт дальше: docs/audit/session-2026-08-17-auto.md:1303 — "RR_EMIT_MIN = 3.0 отсекает 979 сигналов из 1136 (86.2%) на данных журнала и 142 из 145 на нынешнем расчёте… работает как фильтр ПО ТАЙМФРЕЙМУ". Грепом по ВСЕМУ репозиторию два вхождения: объявление и этот абзац. `engine.decide` фильтра по РР не содержит; `run.py:1594` пишет в леджер ВСЕ `d.emissions` без единой проверки РР.

**Правка.** Либо подключить порог (в `emit.hold_reason`/`engine.decide`) и предъявить дифф повтора с двумя числами сигналов, либо отозвать докстроку и абзац журнала как невоспроизводимые — сейчас владельцу доложено поведение, которого у системы нет.

**Чем видно.** git grep -n "RR_EMIT_MIN" — два вхождения: src/hunter/geometry.py:277 и docs/audit/session-2026-08-17-auto.md:1303; grep -n "rr\|risk_reward" src/hunter/engine.py src/hunter/emit.py — ни одной проверки перед записью сигнала

**Проверка (опровергнуть не удалось).** grep -rn RR_EMIT_MIN по src/gates/scripts даёт единственное вхождение — объявление geometry.py:277; в engine.py и emit.py нет ни одного упоминания rr/risk_reward/RR_.

### D-68. Вход эмитируется и без единой цели: 281 сигнал из 1154 записан с target=NULL, и выиграть такая сделка не может

**Источник.** стр. 24 курса: "Целью позиции от уровня 4ч ТФ - должен быть в первую очередь другой сопоставимый уровень 4ч ТФ, либо уровень 1Д тф, если он ближайший"; стр. 9: РР — мера сделки

**Проект.** src/hunter/emit.py:68-113 `hold_reason` — единственный порог эмиссии — про цели не спрашивает вовсе; src/hunter/run.py:1596-1606 пишет сигнал с `target=targets[0].price if targets else None`. При `target=None` `outcome.resolve` (src/hunter/outcome.py:59) физически способен вернуть только stop / open / not_filled — ветка `hit_tgt` требует `target is not None`. В леджере: 281 из 1154 сигналов уровня (24.3%) без цели; их исходы — 26 стопов против 8 "целей" (последние достались строкам, записанным до появления поля), тогда как у сигналов С целью 11 стопов против 73 целей. Средний R по журналу считается по смеси этих […]

**Правка.** Либо не эмитировать вход без основной цели (и назвать это числом в сводке отказов), либо считать R отдельно по подвыборкам "с целью" и "без цели" — сейчас четверть журнала по построению может дать только минус.

**Чем видно.** uv run python -c "import sqlite3;c=sqlite3.connect('file:data/ledger.sqlite3?mode=ro',uri=True);print(c.execute(\"select count(*), sum(case when target is null then 1 else 0 end) from signals where kind='level'\").fetchall())" → [(1154, 281)]; разбивка исходов — те же запросы с join outcomes

**Проверка (опровергнуть не удалось).** emit.py:68-113 hold_reason не спрашивает о целях вовсе; run.py:1596-1606 пишет target=targets[0].price if targets else None; outcome.py:90 hit_tgt требует target is not None, значит при NULL возможны только stop/open/not_filled. Числа леджера не перепроверял.

### D-69. Зонд качества эмиссии считает РР, R и дистанцию стопа по `stop_risky` — стопу, который не выставляется, не печатается и не пишется в леджер

**Источник.** Правило проекта, `CLAUDE.md`: "Если величина ПРЕДЪЯВЛЯЕТСЯ … то расчёт, инвариант и гейт обязаны брать ОДНО И ТО ЖЕ поле". То же дословно записано в самом коде: `src/hunter/emit.py:52-60` — "леджер обязан записывать именно его [`setup.stop`], иначе он измеряет не то, что советует".

**Проект.** `scripts/probes.py:868` `risk = abs(s.entry - s.stop_risky)`; `:871` `rr = s.rr(s.stop_risky)`; `:874` `outcome.resolve(..., stop=s.stop_risky)`. `stop_risky = edge` (`geometry.py:449`) — голая граница базы без запаса. Выставляемый и записываемый стоп — `setup.stop`, он на 3-5% дальше. То есть весь протокол качества (медиана РР, R по эмитируемым, медиана дистанции стопа) посчитан по стопу, которого система не советует. Дефект, который `src/hunter/emit.py` исправил у себя 2026-08-05, живёт в измеряющем приборе.

**Правка.** В `scripts/probes.py:868-876` заменить `s.stop_risky` на `s.stop` (или на `emit.Emission.ledger_stop`) и пересчитать все числа протокола заново. Отдельно: добавить в `gates/` проверку, что ни один зонд не считает РР по полю, отличному от `Setup.stop`.

**Чем видно.** `git grep -n "stop_risky" -- scripts src` → три вхождения в "probes.py" и одно объявление в `geometry.py:449`. Разница величин: `stop_risky` = граница, `setup.stop` ≥ граница·(1∓3%), значит РР зонда систематически ЗАВЫШЕН, а дистанция стопа — занижена.

**Проверка (опровергнуть не удалось).** scripts/probes.py:868-876 действительно берёт s.stop_risky в risk, s.rr() и outcome.resolve(stop=...); в geometry.py:449 stop_risky=edge — голая граница, тогда как выставляемый s.stop строится с запасом (geometry.py:450 stop/basis). Числа расхождения не перепроверял.

### D-70. Леджер смешивает две конвенции стопа; разрез по `recorded_at`, объявленный проектом ОБЯЗАТЕЛЬНЫМ, не сделан нигде

**Источник.** Собственная запись проекта, docs/audit/evidence/E-stop-beyond-anchor-2026-08-18/README.md: "⚠ Леджер после правки смешивает ДВЕ конвенции стопа: сигналы, записанные до 2026-08-18, несут стоп на цене якоря, после — за якорем с запасом. Любая будущая статистика R обязана резать выборку по `recorded_at` относительно коммита этой правки, иначе средний R посчитается по смеси приборов". Правило CLAUDE.md: "всякая накопленная статистика обязана называть, по какой ПОДВЫБОРКЕ она посчитана".

**Проект.** `src/hunter/run.py` → два вхождения, оба CHECK-ограничения схемы (`store.py:60`, `:680`). `store.outcome_survey` (`store.py:1195-1210`) группирует по kind × ТФ × стороне без единого условия на `recorded_at`. `store.OWNER_QUERIES` (`store.py:1332`) — тоже. `tgbot.signals_summary` (`tgbot.py:2059-2065`) — тоже. `run.print_report` — тоже. `store.record_signal` (`store.py:788-792`) при повторе ВОЗВРАЩАЕТ старую строку и не обновляет стоп, поэтому старые сигналы навсегда несут прежний стоп, а `_resolve_pending` (`run.py:1509`) продолжает досчитывать их исход по нему.

**Правка.** Добавить в `signals` колонку конвенции (или хотя бы принимать в `outcome_survey`/`OWNER_QUERIES`/`signals_summary` границу `recorded_at`) и печатать её рядом с каждым средним R. До этого любая цифра R, названная владельцу, обязана нести приписку "посчитано по смеси двух конвенций стопа".

**Чем видно.** `uv run python -c "import sqlite3;c=sqlite3.connect('file:data/ledger.sqlite3?mode=ro',uri=True);CUT=1787011200000;rows=c.execute(\"select s.kind,s.recorded_at,o.r from signals s join outcomes o on o.signal_id=s.id where o.r is not null\").fetchall();..."` — уровневые сигналы до 2026-08-18: n=85, средний R −0.246; с 2026-08-18: n=33, средний R +0.84. Итоговая цифра по журналу (−4.91) есть смесь этих двух, и знак […]

**Проверка (опровергнуть не удалось).** store.outcome_survey (store.py:1195-1210) группирует только по kind×timeframe×direction, OWNER_QUERIES (store.py:1332) — по timeframe; ни одного условия на recorded_at; record_signal (store.py:788-792) при повторном ключе возвращает старую строку и стоп не обновляет. Числа R по базе не перепроверял.

### D-71. Владельческий запрос "чем куплены выигрыши — геометрия сделок" усредняет дистанцию стопа по уровневым и ПП-сигналам вместе

**Источник.** Правило проекта (§10.6 условие 1): "Владелец может проверить состояние леджера тремя заготовленными SQL-запросами, не читая код". Правило CLAUDE.md: "у отказов и статистик обязана быть СВОДКА по измерению, вдоль которого возможен перекос".

**Проект.** `src/hunter/store.py:1332-1339` — `GROUP BY s.timeframe`, поля `s.kind` в группировке нет, хотя схема (`store.py:47`) знает два вида сигналов с несопоставимой геометрией.

**Правка.** Добавить `s.kind` в `SELECT` и в `GROUP BY` запроса `"чем куплены выигрыши — геометрия сделок"` в `store.py:1332`. Пока этого нет, число "средняя дистанция стопа" в этом запросе бессмысленно.

**Чем видно.** Тот же запрос с добавленным `s.kind`: на 5м смешанная средняя дистанция стопа 4.14% раскладывается на level 5.363% и pp 0.151% — разница в 35 раз. То же по среднему R: смешанный +0.089 против level +0.238 и pp −0.062. Воспроизведение: `uv run python -m hunter ledger` (запрос как есть) и тот же SQL с `GROUP BY s.kind, s.timeframe`.

**Проверка (опровергнуть не удалось).** store.py:1332-1339: SELECT s.timeframe … GROUP BY s.timeframe, поля s.kind нет ни в выборке, ни в группировке, при том что схема хранит два вида сигналов. Числа (5.363% vs 0.151%) не перепроверял.

### D-72. Гейт стопа зелёный на конфигурациях, которых боевой путь не производит

**Источник.** Правило проекта (CLAUDE.md): "Гейт, который не падает по коду возврата, не существует" и "прибор обязан смотреть на ТУ ЖЕ величину, которую видит владелец". Гейт обязан проверять достижимые состояния.

**Проект.** `gates/course_rules.py:474-478` — плечо `swing_far`: якорь 85 при `boundary_lo = 90` (`level()` по умолчанию, `course_rules.py:75`). Полоса поиска для лонга есть [90·0.95, 90·0.98] = [85.5, 88.2], значит `stop_anchor` свинг 85 НЕ ВЕРНЁТ никогда — состояние сконструировано через `model_copy` в обход продюсера. Плечо `puncture_far`: прокол 85, то есть на 5 НИЖЕ коробки, — по построению невозможно (см. находку о проколе: 0 из 6189). Два плеча из шести проверяют то, чего не бывает; при этом плечо "прокол побеждает полосу" и есть та ветвь, которую в производстве всегда перебивает пол 3%.

**Правка.** Строить фикстуры гейта ЧЕРЕЗ `levels.stop_anchor` и `accumulation.detect`, а не `model_copy`: если продюсер такого якоря не отдаёт, плечо обязано быть либо удалено, либо переписано под достижимое состояние. Добавить плечо "прокол внутри коробки" — оно достижимо в 100% случаев и сейчас не проверяется ничем.

**Чем видно.** "sed -n '441,485p' gates/course_rules.py" рядом с "sed -n '74,94p' gates/course_rules.py" (умолчания `lo=90.0, hi=110.0`) и с замером "scratchpad/probe_punct.py" (проколов за коробкой 0 из 6189).

**Проверка (опровергнуть не удалось).** course_rules.py:469-478 — swing_far и puncture_far задают stop_anchor/stop_anchor_source через level(...).model_copy, минуя levels.stop_anchor, при умолчаниях lo=90.0/hi=110.0 (course_rules.py:75), то есть якорь 85 лежит вне полосы. Недостижимость таких якорей в производстве проверялась бы прогоном — не проверял.

### D-73. Три справочных стопа и доля частичной фиксации вычисляются и не читаются никем; параметры запаса у `build_setup` кормят ТОЛЬКО мёртвые поля — ловушка для любого зонда чувствительности

**Источник.** Курс стр. 33 даёт ДВА стопа (безопасный и рисковый), стр. 19 — долю фиксации ("делаете тейк 50% - но не 100%"). Правило проекта §4.3: посчитанное обязано доходить до оператора.

**Проект.** `src/hunter/geometry.py:447-449` — `stop_safe_near`, `stop_safe_far`, `stop_risky`; `:175` — `partial_take_pct`. `git grep` показывает: `stop_safe_near/far` не читает НИКТО (только объявление и присвоение), `stop_risky` читает только scripts/probes.py, `partial_take_pct` — никто. Опаснее следствие: параметры `margin_min_pct`/`margin_max_pct` (`geometry.py:310-311`) кормят ИСКЛЮЧИТЕЛЬНО `stop_safe_near/far`, а выставляемый стоп берёт пол из модульной константы `DEFAULT_MARGIN_PCT` (`geometry.py:438`). Значит `build_setup(..., margin_max_pct=5.0)` не изменит стоп ни на тик, но зонд будет уверен, что померил […]

**Правка.** Либо печатать `stop_safe_near/far` и `partial_take_pct` в карточке (курс их называет — значит владелец имеет право их видеть), либо удалить вместе с параметрами `margin_min_pct/margin_max_pct`. И в любом случае провести `DEFAULT_MARGIN_PCT` через параметр функции, чтобы чувствительность стопа к запасу можно было замерить.

**Чем видно.** `git grep -n "stop_safe_near\|stop_safe_far\|partial_take_pct" -- src scripts gates` — вне `src/hunter/geometry.py` только `docs/DEFINITIONS.md:1022`. `src/hunter/geometry.py` — присваивается только в `stop_safe_far`.

**Проверка (опровергнуть не удалось).** grep по src/scripts/gates: stop_safe_near/far — только объявление (geometry.py:147-148) и присвоение (:447-448), partial_take_pct — только :175, stop_risky читает лишь scripts/probes.py:868-874; выставляемый стоп берёт floor=with_margin(DEFAULT_MARGIN_PCT) (:431), то есть модульную константу, а не параметры функции.

### D-74. `hunter check` — единственный вердикт по-русски — не содержит ни одной строки о стопе

**Источник.** Правило проекта §7.5: "проверка не требует помнить команды" и "всё предъявляемое обязано быть проверяемо без чтения кода". Курс задаёт стопу проверяемое численное требование (запас 1-3% за структуру, стр. 33/36/58; 1-5% — стр. 19).

**Проект.** `src/hunter/check.py` — все строки вердикта: `:242` доп-факторы, `:277`/`:340`/`:347` леджер читается, `:297` зоны не шире 39%, `:320` сила по объёму, `:331` исходы сведены. О стопе — ничего. Ни одна проверка не отвечает на вопрос "лежит ли запас за границей в курсовых рамках", хотя это ровно та величина, из-за которой владелец остановил смену 2026-08-18.

**Правка.** Добавить строку вердикта "Запас стопа за границей в курсовых рамках": снимать её с ТЕКСТА карточек прогона (как требует правило "мера снимается с артефакта"), печатать распределение запаса от границы по ТФ и знаменатель, краснеть при доле вне [1%, 5%] выше нуля.

**Чем видно.** `src/hunter/check.py` — семь строк, ни одной про стоп. `uv run python -m hunter check` — в выводе слова "стоп" нет.

**Проверка (опровергнуть не удалось).** В src/hunter/check.py семь lines.append (242, 277, 297, 320, 331, 340, 347), и grep -i "stop" по файлу не даёт ни одного вхождения — вердикта о запасе стопа нет.

### D-75. Ни леджер, ни карта не хранят стоп и границу вместе — курсовое правило о стопе по журналу проверить нечем

**Источник.** Правило CLAUDE.md: "прибор обязан смотреть на ТУ ЖЕ величину, которую видит владелец", и §10.6: леджер существует, чтобы владелец мог задать ему вопрос без чтения кода. Курсовое требование связывает ДВЕ величины — стоп и границу структуры (стр. 33/36/58).

**Проект.** Таблица `signals` (`store.py:46-63`) несёт `entry, stop, target` и не несёт границ, основания стопа (`stop_basis`), цены якоря (`structural_anchor`) и согласия с приоритетом (`agreement` есть в `emit.Emission:43`, но `store.record_signal` его не принимает, `store.py:772`). Таблица `levels` несёт `boundary_lo/hi` и не несёт стопа; общего ключа между таблицами нет (`signals.opened_at` — бар рождения уровня, `levels.to_ms` — конец окна структуры).

**Правка.** Добавить в `signals` колонки `boundary` (граница, от которой отмерен стоп), `stop_basis` и `anchor_source`; тогда один SQL отвечает и на "в рамках ли запас", и на "хуже ли стопы основания MARGIN, чем ANCHOR", и на "хуже ли сделки против приоритета" (стр. 47) — сейчас ни на один из трёх вопросов журнал ответить не может.

**Чем видно.** `uv run python -c "import sqlite3;c=sqlite3.connect('file:data/ledger.sqlite3?mode=ro',uri=True);print([r[1] for r in c.execute('PRAGMA table_info(signals)')]);print([r[1] for r in c.execute('PRAGMA table_info(levels)')])"` — пересечения полей, позволяющего посчитать запас за границей, нет.

**Проверка (опровергнуть не удалось).** SCHEMA таблицы signals (store.py:45-62) содержит только kind/symbol/timeframe/direction/opened_at/recorded_at/entry/stop/target/frames_ref — ни boundary, ни stop_basis, ни structural_anchor, ни agreement; границы живут в отдельной таблице levels.


---

# E. Гигиена — 2 пунктов

### E-76. Владельцу доступны ДВЕ разные процедуры области стоимости под одним именем: `hunter profile` печатает зону не по тому правилу, что карточка

**Источник.** Правило самого проекта (CLAUDE.md, "Прибор обязан смотреть на ТУ ЖЕ величину, которую видит владелец"): "Две сущности под одним человеческим именем — дефект по построению". Внешняя опора: у вендоров процедура одна на инструмент и она описана явно — TradingView (https://www.tradingview.com/support/solutions/43000502040-volume-profile-indicators-basic-concepts/) даёт правило "строка, добавление которой переваливает цель, не добавляется" и тай-брейк "ближе к ПОК, при равенстве — верхняя"; CQG (https://help.cqg.com/cqgic/25/Documents/marketprofilevalueareasmpva.htm) — "expands the value area one price at a time in […]

**Проект.** В одном модуле живут две несовпадающие процедуры VA, и различие не названо ни в одной докстроке: — C:\Users\Антон\Documents\hunter-v2\src\hunter\volume_profile.py:275-276 (build_tv, БОЕВАЯ): есть правило перевала "if covered + chosen > target: break" и тай-брейк по расстоянию до ПОК (:268-270); — C:\Users\Антон\Documents\hunter-v2\src\hunter\volume_profile.py:365-377 (build, тиковая): правила перевала НЕТ вовсе, тай-брейк "up_vol >= down_vol" всегда выбирает ВЕРХ независимо от расстояния до ПОК, плюс режим PAIRS. При этом build — не внутренняя деталь: […]

**Правка.** Свести процедуру к одной функции с явными параметрами (перевал вкл/выкл, тай-брейк, шаг расширения), как у Sierra Chart, и вызывать её обоими путями; в `hunter profile` печатать РЯДОМ с числами имя режима и окно, чтобы владелец видел, что смотрит не на карточную величину. Долю covered_fraction вывести в карточку или в отказ уровня — сейчас единственный признак того, что зона перестала держать […]

**Чем видно.** grep по src/, gates/, scripts/: covered_volume/covered_fraction встречаются только в volume_profile.py (объявление и присваивание) и в src/hunter/__main__.py:234 — в карточке и в гейтах их нет. uv run python "C:\Users\3EC2~1\AppData\Local\Temp\claude\C--Users-------Documents-hunter-v2\db8721f4-1545-4a47-9813-1ac82f4a3bd8\scratchpad\probe_va_coverage_2026-08-19.py" (кадры data/frames/shift18-live, 5 символов, 9718 […]

**Проверка (опровергнуть не удалось).** volume_profile.py:275-276 build_tv имеет правило перевала `if covered + chosen > target: break` и тай-брейк по расстоянию до ПОК, а build (:365-377) правила перевала не имеет и выбирает верх по `up_vol >= down_vol`; __main__.py:229-234 печатает владельцу именно build обоими режимами.

### E-77. Докстрока `PPSetup` утверждает, что сделка от ПП в эмиссию не идёт, — она идёт в леджер, и там уже 312 строк

**Источник.** Правило CLAUDE.md: "любое утверждение проекта о себе есть порождение ИИ, то есть гипотеза, а не свидетельство"; два текста о одном поведении обязаны сойтись, иначе один из них лжёт владельцу.

**Проект.** `src/hunter/geometry.py:465-467`: "Здесь сделка СТРОИТСЯ И ПЕЧАТАЕТСЯ картой; в эмиссию она не идёт — новый ТИП сигнала меняет состав леджера, и это отдельный заход". Против: `src/hunter/run.py:1662-1666` — `store.record_signal(..., kind="pp", ...)`, и `run.py:1683` пишет исход. `src/hunter/engine.py:140` (`PPSignal.emitted`) при этом мёртвое свойство — не вызывается ниоткуда. Та же неправда стоит в реестре: `docs/audit/course-gap-register-2026-08-09.md` — "в леджер не идёт (новый тип сигнала — отдельный заход)".

**Правка.** Переписать докстроку `PPSetup` и строку реестра под факт ("ПП пишется в леджер с 2026-08-10, kind='pp'"), удалить мёртвое `PPSignal.emitted` или подключить его к условию записи. Пока тексты расходятся, любой вывод о составе леджера, сделанный по докстроке, неверен.

**Чем видно.** `uv run python -c "import sqlite3;c=sqlite3.connect('file:data/ledger.sqlite3?mode=ro',uri=True);print(c.execute(\"select kind,count(*) from signals group by kind\").fetchall())"` → [('level', 1154), ('pp', 312)]. `src/hunter/run.py` → 1665.

**Проверка (опровергнуть не удалось).** geometry.py:465-467 утверждает "в эмиссию она не идёт", а run.py:1664-1668 вызывает store.record_signal(..., kind="pp", ...) и следом пишет исход (:1685); PPSignal.emitted (engine.py:141) ниоткуда не вызывается — единственное .emitted в коде это card.py:314 по decisions. Число 312 строк из evidence не перепроверял.

⚠ **Уточнение проверяющего.** Точнее: докстрока говорит о ЭМИССИИ, а запись идёт напрямую в леджер минуя emit; расхождение текста и поведения от этого не исчезает, но формально "emission" и "ledger" — разные пути.
