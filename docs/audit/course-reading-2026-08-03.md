# Чтение мини-курса: механика §2.1–§2.4 дословно

Дата: 2026-08-03. Источник — `docs/course/Мини Курс по трейдингу от PrizrakTrade.pdf`,
69 страниц. Курс — в основном картинки; страницы отрендерены в PNG и прочитаны, текстовый
слой извлечён. Воспроизведение:

```bash
uv run --group tools python -c "import pymupdf; d=pymupdf.open('docs/course/Мини Курс по трейдингу от PrizrakTrade.pdf'); print(d[22].get_text())"
```

Это **референт по §0.2 тип В**: источник методики ровно один — курс. Ни Wikipedia, ни
справки платформ здесь источником не являются.

---

## 1. Накопление — что это механически

| стр. | цитата | что даёт коду |
|---|---|---|
| 13 | «движение цены в горизонтальном ценовом диапазоне… при котором в течении периода времени **локальные хаи/лои повторяются и не меняются**» | точка границы = локальный экстремум, т.е. фрактал. Связка с `swings.py` — авторская, не моя |
| 22 | «накопление с конкретными границами, имеющая в рамках своего ТФ понятные **4 и более точек**» | порог 4+ |
| 23 | «на котором чётко видно его структуру (**4+6+ точки границ**)» | то же, 4+ |
| 24 | «**4+ точки границ**… работают на этом ТФ, **а так же на младших** по отношению к нему» | уровень действует на своём ТФ и ниже |
| 18 | «Границы базы (хай и лой) чаще всего определяются **первыми 2-мя точками**» | границу задают первые две точки, а не все |
| 18 | «если на 3++ точках были **проколы за границы** — стоп всегда ставится за этот прокол, т.к. это может быть **расширением базы**» | прокол границы не ломает структуру, а расширяет её |

## 2. Когда появляется уровень

| стр. | цитата |
|---|---|
| 23 | «**Пока цена не вышла из структуры — у нас нет уровня**, уровень появляется когда цена полноценно выходит из структуры на данном ТФ — тогда мы натягиваем профиль объёма и выделяем уровень» |
| 22 | «**Лонговое** накопление — из которого цена вышла **вверх**. **Шортовое** — вниз» |
| 26 | «Натягивая профиль на структуру — важно **захватить все свечи структуры**!» |

Профиль объёма строится по ВСЕМ барам структуры — это прямое указание, а не выбор окна.

## 3. Единый механизм подтверждения — главная находка

Стр. 55 задаёт **один** механизм для всех типов уровней сразу:

> «Уровень ПП, **как и уровни границ накопления или объёмные уровни**, могут Прокалываться
> или Пробиваться, и требуют подтверждения (то есть **закрытия под/над уровнем 2-3 полных
> тел свечей ЭТОГО ТФ**). Если же цена заходит под уровень и **возвращается той же или
> следующей свечей** того ТФ, на котором смотрим уровень — это просто **прокол БЕЗ
> подтверждения**, не берём позицию»

Отсюда: «цена полноценно выходит из структуры» (стр. 23) и «пробой» (стр. 7, 42) —
**одно и то же условие**. В `FOUNDATION.md` §2.2 и §2.4 они записаны как разные пункты, и связь между
ними там не выражена.

Прокол — стр. 6: «цена прошла за уровень и вернула обратно **той же или следующей 1-2
свечами**». Прокол есть **отработка** уровня, а не отказ.

## 4. Сила уровня — НЕ составная метрика

Стр. 22: «Сила уровня определяется **ТФ и объёмом** (смотрим по VRVP, **иногда в маленькой
часовой наторговке может быть объём больше, чем в 4ч-1д накоплении** — от таких структур
стараемся брать, ожидая импульсной реакции)».

Курс приводит контрпример к любому взвешенному сложению: младший ТФ с бо́льшим объёмом
предпочтительнее старшего с меньшим. Значит два факта сообщаются **раздельно**, свёртка в
число запрещена и по §3, и по самому курсу.

## 5. Цели и стоп

| стр. | цитата |
|---|---|
| 24 | цель — «другой **сопоставимый уровень того же ТФ**, либо уровень 1Д, если он ближайший»; ТФ-1 — промежуточные тейки; «Уровни ТФ-2 (15м и ниже) обычно **не берутся в расчёт**» |
| 33 | «Безопасный СТОП за дно структуры с запасом **1-3%**… Рисковый СТОП: прямо за лой структуры или же за объёмную зону» |
| 25 | «уровень отработан на 1 касание → **удаляем**»; вход от 2-3 касания только по факту слома структуры на младшем ТФ |

## 6. Расхождение ВНУТРИ курса — доложено, не выбрано молча

**Стр. 19: «СТОП прятать с запасом за структуру (границы) 1-5%».
Стр. 33: «за дно структуры с запасом 1-3%».**

`FOUNDATION.md` §2.7 фиксирует только «1–3%». Курс в двух местах даёт разные диапазоны.
Это не опечатка кода и не моя интерпретация — расхождение в самом источнике. Решение за
владельцем; до решения запас **не хардкодится**, а входит параметром.

Рядом стр. 18 даёт третью, более конкретную формулировку: «Если в диапазоне **2-5%** от
границы есть стоповый объём — база мелкого ТФ — или Лой того же ТФ или ТФ-1 — идеально
стоп прятать за них», то есть приоритет у структурного якоря, а процент — запасной вариант.

## 7. Решения, которые пришлось принять — курс их не задаёт

Отмечены явно, потому что это места будущей проверки на корпусе (этап 3).

**Р-6. Ширина границы берётся из первых двух точек, а не из константы.**
Стр. 18 говорит, что границу задают первые 2 точки, но не говорит, насколько близко
должны стоять последующие, чтобы считаться той же границей. Ввести допуск константой —
это магическое число (I-7 прошлого проекта). Взято вместо этого: **граница есть зона между
первыми двумя точками**; последующий фрактал — точка границы, если попадает в эту зону или
прокалывает её. Ширина приходит из данных, свободного параметра нет.

**Р-7. Из диапазона «2-3 полных тела» берётся 2.**
Курс даёт диапазон. Уровень, подтверждённый тремя телами, подтверждён и двумя, поэтому 2 —
нижняя граница и более раннее срабатывание. Фактическое число тел едет вместе со
структурой полем `bodies`, то есть ничего не прячется.

**Р-8. «Полное тело» = тело свечи целиком за границей.**
`min(open, close) > граница` для выхода вверх. Не только закрытие: курс говорит «полных
тел», а не «закрытий».

## 8. Что дал ТЕКСТ остальных 38 страниц (прочитаны позже, 69 из 69)

**Стр. 12 — механическое определение тренда, которого нет в `FOUNDATION.md` вовсе:**
«Восходящий тренд — движение цены на выбранном ТФ, при котором **каждый следующий ЛОЙ
выше предыдущего**. Нисходящий — при котором **каждый следующий ХАЙ ниже предыдущего**.»
Это прямо реализуемо на фракталах и обязательно для §2.8 («направление задаёт старший ТФ»),
где сейчас направление взять неоткуда.

**Стр. 5 — слом тренда:** «пробой с подтверждением последнего ЛОЯ на выбранном ТФ» (в шорт),
зеркально в лонг. Согласуется со стр. 50.

**Стр. 57, 58 — вымпел даёт конкретное число: «ждём 6 касание и берём на 6 касании».**
Плюс правило построения: тренд лонговый — первая точка сверху, шортовый — снизу.

**Стр. 9 — курс сам определяет R и RR.** «Золотым стандартом считаются сделки с РР 1к3 и
выше»; R — «один Риск», единица измерения эффективности. Значит учёт исходов в R —
требование источника, а не мой перенос из прошлого проекта.

**Стр. 34 — стоповый объём:** «небольшое накопление… на более мелком ТФ, чем основное
движение, и обычно более плотное (часто большой объём в мелком диапазоне)».

**Стр. 40 — ловушка ТФ:** выход из 4ч-накопления со стоповым на 15м может оказаться
проколом, от которого на 4ч остаётся только тень. «Всегда приоритетный тот ТФ, на котором
находится текущее накопление.»

**Стр. 62 — двойное/тройное дно = накопление, граница которого была сломом структуры (ПП).**
**Стр. 61 — ГИП = частный случай ПП.** Отдельной логики фигуры не требуют — подтверждено.

### Расхождение по стопу разрешается ВЕСОМ, а не выбором

Запас **1-3%** назван на стр. 33, 36 и 58 — три раза. **1-5%** — только на стр. 19, один раз.
Правило — 1-3%; стр. 19 остаётся зафиксированной как разночтение источника, но выбор больше
не произвольный.

## 9. Что дали КАРТИНКИ — и чего в тексте нет

Просмотрено 4 страницы из 69 как изображения: 18, 22, 23, 50.

**Стр. 18 — «ЛИМИТНЫЙ БАРЬЕР». Термина нет в текстовом слое ни разу.** У структуры на схеме
ДВЕ пары линий: внутренний бокс — границы базы, которых касаются точки, и снаружи от него
пара красных линий, подписанных «лимитный барьер». То есть у автора граница это линия плюс
полоса за ней, а не одна линия.

**Стр. 18 и 23 — точки границ строго ЧЕРЕДУЮТСЯ.** Автор нумерует их: 1, 3, 5, 7, 9 сверху и
2, 4, 6, 8, 10 снизу, последовательно по времени. Мой детектор чередование не требует.

**Стр. 18 — все верхние точки касаются ОДНОЙ горизонтальной линии**, а не образуют зону
между первыми двумя. Это ставит под сомнение решение Р-6 напрямую.

**Стр. 23 — уровень ПОК рисуется внутри структуры и продлевается вправо**, тест уровня
происходит много позже выхода. Подтверждает конструкцию «уровень живёт после структуры».

**Стр. 50 — схема истинного ПП.** Жёлтая линия рисуется на уровне сломанного лоя (для шорта);
вход — на ретесте этой линии снизу. «Последний лой, из которого был последний хай» = свинговый
лой, непосредственно предшествующий последнему свинговому хаю.

## Что осталось непроверенным

- Совпадение авторской разметки точек границ с фракталами Вильямса — не проверено. Стр. 13
  говорит «локальные хаи/лои», но не даёт правила их определения; фрактал — моя
  конвенция из этапа 2. Проверяется на корпусе, этап 3.
- Р-6 и Р-7 — мои решения при молчании источника. Обе подлежат проверке на разборах.
- «четко видно его структуру» (стр. 23, 24) — визуальный критерий, механического эквивалента
  в курсе нет. Реализовано как «4+ точек границ», то есть по единственному числовому
  признаку, который курс называет.
- Страницы 34–41 (стоповый объём) и 56–62 (фигуры) прочитаны по тексту, но в код пока не
  идут — это этап 4.
