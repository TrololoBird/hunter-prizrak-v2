# Документация ccxt и ccxt.pro целиком: что она даёт и что из этого мы не берём

**Задача поставлена владельцем 2026-08-11 дословно:** не хвататься за каждое расхождение,
а сначала скачать документацию ccxt и ccxt.pro ПОЛНОСТЬЮ и собрать всё, что касается
ccxt, ccxt.pro, REST, вебсокета, спота, фьючерсов и binance. Код при этом не менять.

Основание для подозрения владелец назвал сам: транспорт, возможно, реализован неверно, не
использует всего, что даёт библиотека, а используемая часть содержит ошибки или выдуманные
решения. Этот файл — сбор материала под такую проверку, а не её вывод.

⚠ **Установка, под которой всё это читалось:** истина — документация. Наш код истиной не
является: он порождён ИИ целиком, и всякое его утверждение о библиотеке — гипотеза.

---

## 1. Что скачано

Прежние заходы брали страницы через выдачу сайта, а она приходит **урезанной** —
руководство обрывается на середине раздела, спецификация binance теряет методы. Поэтому
взят исходник документации целиком, разреженным клоном репозитория ccxt:

```bash
git clone --filter=blob:none --no-checkout --depth 1 https://github.com/ccxt/ccxt.git
git sparse-checkout set wiki examples/py
git checkout
```

**Итого 1396 файлов, 8 687 969 байт.** Состав:

| раздел | файлов | байт | что это |
|---|---|---|---|
| корень вики | 21 | 1 248 128 | руководство, ccxt.pro, спецификация по методам, FAQ, установка, CLI |
| `wiki/exchanges/` | 102 | 3 828 599 | спецификация ЕДИНЫХ методов по каждой бирже |
| `wiki/exchanges-implicit/` | 103 | 1 283 123 | **сырые эндпоинты** по каждой бирже |
| `wiki/examples/` | 847 | 1 614 946 | описания примеров |
| `examples/py/` | 289 | 650 998 | сами примеры на Python |

Крупнейшие файлы: Manual.md 482 897 Б, baseSpec.md 376 204 Б, ccxt.pro.manual.md 124 217 Б,
exchanges/binance.md 183 202 Б.

⚠ **Каталога `exchanges-implicit` я до этого не открывал вовсе**, а именно он перечисляет
сырые эндпоинты. Как и `wiki/examples/` — 847 файлов описаний.

---

## 2. Полный перечень того, что доступно на нашей площадке

### 2.1. Единые методы binance

Всего в спецификации **143 метода**, из них **30 потоковых** (`watch*` и `unWatch*`).

Потоковые целиком:

```
watchTrades            watchTradesForSymbols      unWatchTrades      unWatchTradesForSymbols
watchOHLCV             watchOHLCVForSymbols       unWatchOHLCV       unWatchOHLCVForSymbols
watchOrderBook         watchOrderBookForSymbols   unWatchOrderBook   unWatchOrderBookForSymbols
watchTicker            watchTickers               unWatchTicker      unWatchTickers
watchMarkPrice         watchMarkPrices            unWatchMarkPrice   unWatchMarkPrices
watchBidsAsks          unWatchBidsAsks
watchLiquidations      watchLiquidationsForSymbols
watchMyLiquidations    watchMyLiquidationsForSymbols
watchBalance           watchOrders                watchPositions     watchMyTrades
```

### 2.2. Сырые публичные эндпоинты фьючерсов

Двадцать девять, из файла exchanges-implicit/binance.md:

```
fapiPublicGetAggTrades          fapiPublicGetKlines             fapiPublicGetTime
fapiPublicGetTrades             fapiPublicGetContinuousKlines   fapiPublicGetPing
fapiPublicGetHistoricalTrades   fapiPublicGetIndexPriceKlines   fapiPublicGetExchangeInfo
fapiPublicGetDepth              fapiPublicGetMarkPriceKlines    fapiPublicGetTicker24hr
fapiPublicGetRpiDepth           fapiPublicGetPremiumIndexKlines fapiPublicGetTickerPrice
fapiPublicGetOpenInterest       fapiPublicGetLvtKlines          fapiPublicGetTickerBookTicker
fapiPublicGetFundingRate        fapiPublicGetPremiumIndex       fapiPublicGetAssetIndex
fapiPublicGetFundingInfo        fapiPublicGetIndexInfo          fapiPublicGetConstituents
fapiPublicGetInsuranceBalance   fapiPublicGetSymbolAdlRisk      fapiPublicGetApiTradingStatus
fapiPublicGetTradingSchedule    fapiPublicGetConvertExchangeInfo
```

### 2.3. Что из этого зовёт наш код

**Всего четыре единых метода и один сырой:**

| зовём | где | зачем |
|---|---|---|
| `load_markets` / `reload_markets` | `exchange.open`, `reload_markets` | инструменты, шаг цены |
| `fetch_time` | `fetch_server_ms` | сведение часов |
| `fetch_ohlcv` | `fetch_closed_ohlcv` | бары всех ТФ |
| `fetch_trades` | `fetch_agg_trades_from`, `fetch_agg_trades_window` | добор сделок |
| `watch_trades` | `watch_agg_trades` | поток сделок |
| `fapiPublicGetExchangeInfo` (сырой) | `_read_weight_limit` | лимит веса |

**Отношение: пять из ста сорока трёх.** Само по себе это ничего не доказывает — большая
часть остального относится к торговле, которой система не ведёт. Ниже разобрано то, что
относится К ДЕЛУ.

---

## 3. Функционал, относящийся к делу, которым мы НЕ пользуемся

Ниже — только то, что касается публичных рыночных данных и могло бы менять устройство
транспорта. Каждый пункт — **гипотеза к проверке, а не установленный дефект.**

### 3.1. Подписка на МНОГО символов одним вызовом

**Документация:** `watchTradesForSymbols` — «Similar to `watchTrades` but allows
subscribing to multiple symbols in a single call». То же есть у баров
(`watchOHLCVForSymbols`) и у стакана.

**У нас:** вселенная — 27 символов, и на каждый заведена своя задача наблюдения. Докстрока
`desync_s` прямо описывает следствие: «27 задач сделок, разбуженных ОДНИМ сетевым
событием, переподписываются синхронной волной» — и против этого написан собственный
механизм фазового сдвига по crc32.

⚠ Стоит проверить, не решается ли исходная задача одной подпиской вместо двадцати семи
плюс самодельного разведения фаз.

### 3.2. Снятие подписки

**Документация:** «Even if you stop getting the return value from the `watchX` method, the
stream will keep sending that, which is handled and stored in the background. To stop those
background subscriptions, you should use `unWatch` method».

**У нас:** `unWatchTrades` не зовётся нигде. При остановке задачи наблюдения подписка,
по документации, остаётся жить в фоне до закрытия экземпляра.

### 3.3. Бары из сделок вместо баров от биржи

**Документация, раздел «Notes On Latency», дословно:** «**tickers and OHLCVs are always
slower than orderbooks and trades**». Причина названа там же: OHLCV — данные ВТОРОГО
порядка, биржа считает их из сделок, и на это нужно время, своё у каждой биржи. И вывод:
«the developers of time-critical trading strategies don't have to rely on secondary data
from the exchanges and can calculate the OHLCVs and tickers in the userland».

**ccxt даёт для этого готовый метод** — `build_ohlcvc(trades, timeframe, since, limit)` в
базовом классе.

**У нас:** бары берутся REST-опросом у биржи, причём с отступом `POLL_OFFSET_S = 3.0` от
границы ТФ, и докстрока этого числа сама признаёт: «⚠ ЧИСЛО НЕ ЗАМЕРЕНО», взято по
косвенному свидетельству.

⚠ **При этом сделки мы уже собираем — для профиля объёма.** То есть первичные данные, из
которых бар и считается, у нас есть. Проверить стоит три вещи разом: снимется ли
незамеренный отступ, совпадут ли наши бары с биржевыми, и не окажется ли, что бар и
профиль наконец считаются ИЗ ОДНОГО ИСТОЧНИКА.

### 3.4. Переподключение

**Документация:** «Upon a critical exception, a disconnect or a connection
timeout/failure, the next iteration of the tick function will call the `watch` method that
will trigger a reconnection. This way the library handles disconnections and reconnections
for the user transparently. **CCXT Pro applies the necessary rate-limiting and exponential
backoff reconnection delays.**»

**У нас:** собственный надзиратель `_supervise` с `RESTART_PAUSE_S`, счётчиками смертей и
подъёмов, плюс `desync_s`.

⚠ Проверить, что из этого дублирует библиотеку, а что нужно из-за нашей обёртки-генератора.
Дублирование само по себе не ошибка; ошибкой было бы приписывать себе то, что делает ccxt.

### 3.5. Параметры выборки, которые мы не передаём

* **`params.until`** у `fetch_ohlcv` — верхняя граница окна. Мы её не передаём и режем
  лишнее сами.
* **`params.paginate`** — у баров это `fetch_paginated_call_deterministic`, то есть
  запросы идут ПАРАЛЛЕЛЬНО с предвычисленными границами. У сделок не заявлено
  (`features['swap']['linear']['fetchTrades']` равно `null`), у баров — заявлено.
* **`fetchMarkOHLCV`, `fetchIndexOHLCV`** и `params.price` — маркировочная и индексная
  цена. Курс о них не говорит; отмечены как существующие.
* **`fapiPublicGetContinuousKlines`** — бары непрерывного контракта, без разрыва на
  экспирации. Для бессрочных не критично, но существует.

---

## 4. Расхождения между документацией ccxt и кодом ccxt

Найдено три. Все проверены вызовом или чтением исходника.

| что | документация говорит | код 4.5.71 делает | цена для нас |
|---|---|---|---|
| `newUpdates` | «in the future `newUpdates: true` will be the default» — то есть сейчас ложь | `True` | при `False` объём попал бы в профиль многократно. **Закреплено явно** |
| ключ настройки построителя баров | `options['buildOHLCV']` | читает `options['buildOHLCVC']` | документированный ключ не сработал бы вовсе |
| верхний предел баров | Binance даёт 1500 на фьючерсах, комментарий ccxt это признаёт | `maxLimit = 1000` жёстко | наш `CCXT_EFFECTIVE_LIMIT = 1000` верен |

Плюс одно расхождение у **Binance**, не у ccxt: фраза про `Retry-After` есть только в
спотовой документации, страница USDⓈ-M её не содержит. Наш код ссылался на фьючерсную —
исправлено, разбор в [`docs/audit/ccxt-manual-2026-08-11.md`](ccxt-manual-2026-08-11.md).

---

## 5. Числа нашего транспорта: у чего есть референт, а у чего нет

Сведено из докстрок, без правок.

| число | референт | статус |
|---|---|---|
| `KLINES_MAX_LIMIT = 1500` | Binance, Kline Data | подтверждено |
| `CCXT_EFFECTIVE_LIMIT = 1000` | исходник ccxt | подтверждено |
| `TRADE_RECOVER_PAGE = 1000` | спецификация binance | подтверждено |
| `REST_TRADES_WINDOW_MS = 3600000` | исходник ccxt | подтверждено |
| `RATE_LIMIT_BACKOFF_S = 60.0` | окно веса `REQUEST_WEIGHT/MINUTE/1` | подтверждено вызовом |
| `POLL_OFFSET_S = 3.0` | **нет замера**, косвенное свидетельство | ⚠ по докстроке — задание Ж-1 |
| `CATCHUP_MAX_BARS = 100` | замер ступени веса | по докстроке замерено |
| `BACKFILL_DAY_MAX_PAGES = 10 000` | **нет замера**, предохранитель | ⚠ названо в докстроке |
| `REST_CHECKPOINT_TRADES = 200 000` | **нет замера** | ⚠ названо в докстроке |
| `WS_TRADES_SILENCE_S = 300.0` | требует проверки | не сверялось в этом заходе |
| `tradesLimit` (не задан) | умолчание ccxt 1000 | оставлено осознанно |

---

## 6. Что осталось непрочитанным

Говорю точно, потому что «изучил полностью» я уже один раз сказал напрасно.

1. **Прочитаны целиком:** разделы руководства про ограничитель, пагинацию, публичные
   сделки, OHLCV и задержки, загрузку рынков; разделы ccxt.pro про специфику потоков,
   инкрементальные структуры, `newUpdates`, режимы реального времени и троттлинга,
   обработку ошибок, уборку ресурсов, `watchTradesForSymbols`; спецификация binance по
   методам, которые мы зовём; перечни всех методов и сырых эндпоинтов.
2. **Перечислено, но не прочитано построчно:** baseSpec.md (376 КБ) — читан выборочно;
   `wiki/examples/` (847 файлов) — только оглавление; `examples/py/` (289 файлов) —
   прочитано пять.
3. **Не открывалось:** спецификации остальных 101 биржи, приватная часть API, разделы про
   предсказательные рынки, CLI, установку.
4. **Не проверено на живой бирже:** всё из §3 — это гипотезы по документации. Ни одна не
   замерена, и ни одна не должна попасть в код без замера и диффа повтора.

---

# 7. Второй круг чтения: примеры и спецификация подробно

Владелец потребовал читать подробнее. Ниже — то, что нашлось при чтении самих примеров и
разделов, которые в первом круге были только перечислены. **Три из четырёх гипотез §3
пришлось уточнить или снять — документация опровергла их так же, как опровергала код.**

## 7.1. ⚠ Гипотеза «строить бары из сделок» — СУЖЕНА документацией

В §3.3 я записал, что бары можно считать из сделок и тем снять незамеренный отступ. Пример ccxt build-ohlcv-bars.py начинается с прямого предупреждения, которое эту мысль ограничивает:

> «if you want to build 100 ohlcv bars of 1-minute timeframe, then you have to fetch the
> 100 minutes of trading data. So, higher timeframe bars require more trading data (i.e.
> building 100 bars of 1-day timeframe OHLCV would require massive amount of trading data,
> which might not be desirable for user, because of data-usage rate limits)»

**Что это значит для нас числом.** Вселенная требует 1000 баров на каждый ТФ (§5
FOUNDATION). Для 1Н это 1000 недель — **девятнадцать лет сделок на символ**. Для 1Д —
почти три года. Скачать столько сделок нельзя ни архивом, ни REST-ом, а глубина REST по
замеру и вовсе 30 суток.

**Вывод:** `build_ohlcvc` историю НЕ заменяет и заменить не может. Самое большее, на что
он годится, — свежий край: последний бар, который иначе ждёт опроса с отступом 3 с.

Там же названы два свойства, о которых иначе пришлось бы догадываться:
* **первый бар всегда неполон** — «the first obtained trade timestamp might be somewhere
  in the middle of timeframe period»;
* у результата **семь полей, а не шесть**: седьмое — число сделок в баре, «not part of
  OHLCV standard structure». Величины, которой у нас сейчас нет вовсе.

## 7.2. ⚠ Гипотеза «27 задач вместо одной подписки» — СНЯТА

В §3.1 я предположил, что наши 27 отдельных задач наблюдения — самодеятельность, а надо
было брать `watchTradesForSymbols`. **Официальный пример ccxt делает ровно как мы**
— файл many-exchanges-many-symbols-watch-trades.py:

```python
async def exchange_loop(exchange_id, symbols):
    exchange = getattr(ccxt.pro, exchange_id)({'newUpdates': True})
    loops = [symbol_loop(exchange, symbol) for symbol in symbols]
    await gather(*loops)
```

То есть **по задаче на символ, все на одном экземпляре биржи** — наша схема. И
`newUpdates: True` там выставлен ЯВНО, как мы закрепили 2026-08-11: значит это не
перестраховка, а то, что делает сама библиотека в своих примерах.

`watchTradesForSymbols` остаётся альтернативой (одна подписка вместо многих), но
«правильным способом» её документация не называет. Гипотеза о самодеятельности снята.

## 7.3. Перезагрузка рынков — тоже совпала

Пример binance-reload-markets.py показывает ровно нашу схему: отдельная корутина в `gather`,
`load_markets(True)` по таймеру, рядом с потоком. Совпадает с `reload_markets` в нашем
`exchange.py`.

## 7.4. Оконный ограничитель — синтаксис подтверждён примером

Пример exchange-rate-limiter-rollingWindow.py:

```python
myex = ccxt.okx({'rateLimiterAlgorithm': 'rollingWindow', 'rollingWindowSize': 10000})
```

Остаётся кандидатом на замену протекающему ведру (§2 первого круга), но по-прежнему
незамерен и по-прежнему решение владельца.

## 7.5. Шаг цены: наш способ проверен и оказался ВЕРНЕЕ очевидного

Руководство различает три режима точности, и у binance это `TICK_SIZE`: тогда
`market['precision']['price']` САМ является шагом. Казалось бы, можно брать его.

**Замер (живой вызов):** `binanceusdm.precisionMode == 4 == TICK_SIZE`, и значения
совпадают у всех проверенных символов. Но совпадают они по величине, а не по типу:

| символ | `PRICE_FILTER.tickSize` | `precision.price` |
|---|---|---|
| BTC/USDT:USDT | `"0.10"` (строка) | `0.1` (float) |
| 1000RATS/USDT:USDT | `"0.0000100"` (строка) | `1e-05` (float) |
| ARPA/USDT:USDT | `"0.0000010"` (строка) | `1e-06` (float) |

**Наш код берёт СТРОКУ из сырого `PRICE_FILTER` и кладёт в `Decimal`**
(`exchange._price_filter_tick`). Это строго вернее: сетка бинов профиля строится делением
на шаг, а `1e-06` в двоичном float точным не бывает. Здесь наша реализация не отклоняется
от документации, а использует более надёжный из двух документированных источников.

## 7.6. Что осталось от гипотез §3

| гипотеза | судьба после подробного чтения |
|---|---|
| 3.1 подписка на много символов | **снята** — официальный пример делает как мы |
| 3.2 `unWatch` не зовётся | **остаётся** — документация прямо говорит, что подписка живёт в фоне |
| 3.3 бары из сделок | **сужена** — историю не заменяет, годится только на свежий край |
| 3.4 своё переподключение | **остаётся к проверке** — пример ловит исключение и выходит из цикла, наш надзиратель поднимает задачу заново |
| 3.5 `until` и `paginate` | **остаётся** — у баров пагинация заявлена и идёт параллельно |

⚠ Три из пяти пришлось поправить, и это главный результат подробного чтения: **мои
гипотезы о недостатках нашего кода оказались такими же непроверенными утверждениями, как
и сам код.** Проверять их полагается у документации, а не у собственного впечатления.

---

# 8. Проверка утверждения «скачано целиком» — 2026-08-11, второй контейнер

**Повод.** Владелец спросил, загружена ли документация ccxt полностью. Ответ «да, см. §1»
был бы ссылкой на собственное утверждение: §1 — это ЗАПИСЬ о выгрузке, а не выгрузка.
Проверка сделана заново, с нуля, и дала три разных «нет» разной цены.

⚠ Имена файлов репозитория ccxt здесь пишутся без обратных кавычек — по конвенции этого
файла (см. §7.1–7.4). Кавычки оставлены за НАШИМИ путями, которые гейт `gates/doc_refs.py`
обязан проверять на существование.

## 8.1. Отпечаток данных, которого у §1 не было

Правило CLAUDE.md: замер по растущему источнику обязан записывать отпечаток данных рядом
с числом. У §1 отпечатка нет, и числа разошлись — В ТЕ ЖЕ СУТКИ:

⚠ **Слово «дрейф» здесь ОТОЗВАНО (§9.3):** разница 1396 → 1390 не объяснена. Мои 1390 —
обычные файлы под `wiki` и `examples/py`, БЕЗ 35 файлов корня выгрузки и без 2
символических ссылок; что считал прошлый заход — неизвестно. Дрейф источника лишь одна из
гипотез, вторая — разная граница счёта.

| | §1 | перекачано заново |
|---|---|---|
| файлов | 1396 | **1390** обычных + 2 символические ссылки = 1392 записи дерева |
| байт | 8 687 969 | **8 661 724** |
| отпечаток | **не записан** | коммит `10973566cea9d178b5c4dc73630ce31a1da6c090`, 2026-08-11T14:03:35+00:00 |

Без отпечатка расхождение читается как опровержение, а это дрейф источника: ccxt
выпускает версии по нескольку раз в сутки.

```bash
git clone --filter=blob:none --no-checkout --depth 1 https://github.com/ccxt/ccxt.git
git -C ccxt sparse-checkout set wiki examples/py && git -C ccxt checkout
find ccxt/wiki ccxt/examples/py -type f | wc -l                      # 1390
git -C ccxt ls-tree -r --name-only HEAD wiki/ examples/py/ | wc -l    # 1392
```

⚠ **Контроль «способен ли счётчик дать иное число» НЕ сошёлся с первого раза**, и это
оказалось полезнее совпадения: два независимых способа дали 1392 и 1390. Расхождение
разобрано, а не списано на метод — и разбор оказался находкой 8.2.

## 8.2. ⚠⚠ ОТОЗВАНО ЦЕЛИКОМ — см. §9. Читать только вместе с §9

⚠⚠ **Вывод этого пункта ОПРОВЕРГНУТ 2026-08-11 в тот же день.** Файл был скачан той же
командой, ссылка не висячая, «не скачивалось ни разу» — неверно. Ниже текст оставлен как
был, чтобы видно было, что именно я утверждал и на каком основании. Разбор ошибки — §9.

## 8.2. ⚠ Главное: история изменений — страница ВИКИ, которой в выгрузке нет

Две записи дерева, которых нет на диске обычными файлами, — wiki/CHANGELOG.md и
wiki/CONTRIBUTING.md. Обе имеют режим `120000`, то есть символические ссылки, и ведут за
пределы выгрузки (в корень репозитория). Разрежённая выгрузка брала wiki и examples/py,
корня в ней нет — **ссылки висячие, содержимого не скачано.**

| страница вики | размер цели | что это |
|---|---|---|
| wiki/CHANGELOG.md | **1 632 637 Б**, 14 486 строк | история изменений по версиям |
| wiki/CONTRIBUTING.md | 53 906 Б | правила участия |

Это не мелочь по трём причинам:

1. **по объёму** — 1.63 МБ против 8.66 МБ всего скачанного, то есть за границей выгрузки
   осталась пятая часть корпуса той же вики;
2. **по содержанию** — это единственный документ, где записаны ИЗМЕНЕНИЯ ПОВЕДЕНИЯ. §4
   оставил открытым расхождение «newUpdates: руководство описывает умолчание как ложь,
   код 4.5.71 даёт истину», и ответ на такой вопрос ищется именно здесь.
   ⚠ Найден он НЕ был: поиск по слову newUpdates даёт на 14 486 строк истории **ровно одно
   упоминание** — правку watchLiquidations при `newUpdates = false`. Смены умолчания
   история не фиксирует, то есть расхождение §4 ею не снимается. Ценность страницы этим не
   отменяется (см. пункт 3), но конкретно этот вопрос остался открытым;
3. **по актуальности** — в замке `ccxt==4.5.71` (`pyproject.toml`, `uv.lock`), а на момент
   проверки вышла **4.5.73**. После нашей версии **176 записей истории, из них 14 задевают
   наши темы** (binance, watchTrades, fetchOHLCV, пагинация, ограничитель). Ни одна не
   читалась. Среди них есть прямо по открытому вопросу §3.5 — правка «anchor deterministic
   pagination at since for fully-historical ranges».

```bash
git -C ccxt show HEAD:CHANGELOG.md > CHANGELOG.md && wc -c CHANGELOG.md   # 1632637
awk '/id="v4.5.71"/{exit} {print}' CHANGELOG.md | grep -cE '^\* '          # 176
```

⚠ Ни одна из 14 записей здесь НЕ прочитана — названо только их число. Что в них, остаётся
непроверенным, и обновление версии из этого не следует: смена боевой зависимости — §10.

## 8.3. Что ещё не бралось выгрузкой

| раздел репозитория ccxt | файлов | относится ли к делу |
|---|---|---|
| examples на js, ts, php, cs, go, java, html | 528 | те же сюжеты на других языках — контроль 8.4 |
| docs/website/content-i18n | 84 | переводы руководства, содержание то же |
| docs/playground | ~137 | веб-приложение песочницы, не документация |
| .agents/skills | 103 | навыки Binance CLI: кошелёк, p2p, платежи — приватный API |

Из этого к делу могла относиться только первая строка, и её цена выяснена контролем ниже.

## 8.4. Контроль к числу «123 примера есть в js и нет в py»

Сравнение имён дало 123 примера, которых в examples/py нет, и среди них — прямо по нашим
открытым вопросам: calculate-ohlcvs-from-trades, builtin-rate-limiting-rest-poller,
fetch-ohlcv-from-to-mark-index-premium, binance-watch-ohlcv-many-symbols.

**Контроль (не лучше ли случайного): проверено, есть ли тот же СЮЖЕТ в py под другим
именем.** Взято восемь имён:

| пример только в js | сюжет в examples/py |
|---|---|
| calculate-ohlcvs-from-trades | есть — build-ohlcv-bars.py, он и цитируется в §7.1 |
| builtin-rate-limiting-rest-poller | есть — builtin-rate-limiting-long-poller.py |
| fetch-ohlcv-from-to-mark-index-premium | есть — fetch-ohlcv-mark-index-price.py |
| binance-watch-ohlcv-many-symbols | есть — binance-watch-ohlcv.py, build-ohlcv-many-symbols.py |
| binance-fetch-ohlcv-many-symbols-async-await | есть — async-fetch-ohlcv-multiple-symbols-continuously.py |
| bitfinex-fetch-trades | сюжет покрыт: 16 файлов py по сделкам |
| gate-watch-order-book | сюжет покрыт: 9 файлов py по стакану |
| fetch-funding-rate-history | **нет** — по ставке финансирования в py ноль файлов |

⚠ **Число 123 как пробел в покрытии НЕ ГОДИТСЯ: оно измеряет разницу ИМЁН.** Семь сюжетов
из восьми покрыты питоновскими примерами, названными иначе; восьмой действительно есть
только на других языках, но системы не касается — мы не торгуем и ставку финансирования не
считаем. Записано как отклонённое, а не как находка.

⚠ Выборка восьми из 123 сделана не случайно: взяты имена ПО НАШИМ темам. Для вопроса «не
потеряли ли мы нужное» это правильный отбор, но утверждать «все 123 — дубли» он не даёт.
Про остальные 115 не проверено ничего.

## 8.5. Сколько корпуса вообще было прочитано

По собственному перечню §6 затронуто шесть файлов:

| файл вики | байт |
|---|---|
| Manual.md | 483 046 |
| ccxt.pro.manual.md | 124 217 |
| exchanges/binance.md | 187 127 |
| exchanges-implicit/binance.md | 61 505 |
| FAQ.md | 34 748 |
| Examples.md | 7 781 |
| **итого** | **898 424 из 8 661 724 = 10.4%** |

⚠ Прочесть это как «прочитано 10.4%» нельзя: число — **верхняя граница**. Числитель суть
файлы, ЗАТРОНУТЫЕ хотя бы одним разделом, а §6 сам говорит, что читались разделы, а не
файлы целиком. Знаменатель — вики и examples/py на коммите `10973566`, БЕЗ истории
изменений из 8.2, которой в выгрузке не было вовсе.

## 8.6. Ответ на вопрос владельца

**Нет, не полностью, и это три разных «нет»:**

1. **в этой сессии её не было вовсе** — контейнер создан заново, выгрузка на 8.7 МБ жила
   на диске прошлого и в git не попадала; перекачана заново (8.1). Лечится командой;
2. ~~**скачано было не всё** — история изменений на 1.63 МБ является страницей вики и в
   выгрузку не попала~~ — ⚠⚠ **ОТОЗВАНО, §9.** Файл скачивался с самого начала, ссылка
   рабочая. Верно другое и меньшее: он не был ПРОЧИТАН, а вне выгрузки остались `.claude`,
   `.agents`, `docs`, `python`, `cli` — из них наше только собственный навык ccxt по Python
   и справочник Binance по фьючерсам USD-M (§9.5);
3. **прочитано не более 10.4%** скачанного, и это верхняя граница (8.5). Новостью не
   является: §6 это признавал, здесь оно только измерено.

⚠ После отзыва §8.2 ответ на вопрос владельца сокращается до двух «нет» из трёх, и
единственное содержательное — третье: **скачано практически всё, прочитано около десятой
части.**

## 8.7. Что осталось непроверенным

* содержание 176 записей истории после 4.5.71 — прочитано ноль, названо только число;
* 115 из 123 примеров на других языках — на совпадение сюжета не проверялись;
* CONTRIBUTING.md — не открывался, к расчёту отношения не имеет;
* сходится ли опись §1 сама с собой: 21 + 102 + 103 + 847 + 289 = 1362 при итоге 1396.
  Сегодняшняя выгрузка объясняет разрыв — три каталога шаблонов (partials, basePartials,
  exchangePartials, 24 файла `.hbs`, 5 397 Б, документации в них нет) плюс четыре
  неучтённых файла корня вики. Существенного за этим нет, но опись §1 своему итогу не
  равна.

---

# 9. ОТЗЫВ вывода §8.2 — правдоподобная причина вместо разбора по шагам

**Повод.** Владелец потребовал проверить, что было не доскачано. Проверка опровергла
не выгрузку, а МОЙ СОБСТВЕННЫЙ вывод, сделанный тремя часами раньше и уже отправленный в
запрос на слияние.

## 9.1. Что было заявлено и что оказалось

| §8.2 утверждал | проверено |
|---|---|
| wiki/CHANGELOG.md и wiki/CONTRIBUTING.md — **висячие ссылки**, содержимого нет | `test -e` на обеих: **цель СУЩЕСТВУЕТ**. Ссылки рабочие |
| история изменений «**не скачивалась ни разу**» | лежит в корне выгрузки, `stat` даёт время правки **14:16:47** — её положила ПЕРВАЯ ЖЕ выгрузка, за три часа до того, как я написал обратное |
| «дрейф уже случился: 1396 → 1390 файлов» | **не установлено**, см. 9.3 |

```bash
test -e ccxt/wiki/CHANGELOG.md && echo цель существует    # цель существует
stat -c '%y %n' ccxt/CHANGELOG.md                          # 2026-08-11 14:16:47
find ccxt -maxdepth 1 -type f | wc -l                      # 35
```

## 9.2. Механизм ошибки

Разрежённая выгрузка в режиме конуса **всегда берёт файлы корня** — значит команда из §1
скачивала их с самого начала, и у прошлого захода тоже. Мой счётчик их не видел по одной
причине: `find` был запущен по двум путям, `wiki` и `examples/py`, а корень выгрузки —
третий путь, и в нём **35 файлов**.

Дальше сработало ровно то, что CLAUDE.md называет самым опасным: расхождение 1392/1390
получило **правдоподобное объяснение вместо разбора по шагам**. Объяснение было наполовину
верным — это действительно символические ссылки, и `find -type f` их действительно не
считает, — а вторую половину («значит цели нет») я не проверил ни одной командой. `test -e`
занимает секунду и был бы решающим.

⚠ Усугубило то, что размер я взял командой `git show HEAD:CHANGELOG.md`, то есть **из
объекта git, а не с диска**. Проверка, которая физически не могла обнаружить файл на диске,
была принята за подтверждение того, что его на диске нет.

⚠ И третье: §8.1 хвалился тем, что контроль «не сошёлся с первого раза» и это «полезнее
совпадения». Контроль действительно поймал расхождение — а я закрыл его догадкой. **Гейт,
чей красный свет гасят объяснением, не гейт.**

## 9.3. Отзывается и число «дрейф 1396 → 1390»

Приписать разницу дрейфу источника было нельзя: у замера §1 нет ни отпечатка, ни указания,
ЧТО именно считалось. Мои 1390 — обычные файлы под `wiki` и `examples/py`, без 35 файлов
корня и без 2 символических ссылок. Считал ли прошлый заход корень — неизвестно, и
восстановить это нечем.

**Правильная формулировка: разница НЕ ОБЪЯСНЕНА.** Дрейф источника — лишь одна из гипотез,
вторая — разная граница счёта.

⚠ Правило отпечатка от этого не слабеет, а усиливается, и в новой части: рядом с числом
обязан стоять не только отпечаток источника, но и **ЧТО СЧИТАЛОСЬ**. Без знаменателя
число не воспроизводится даже на неизменившихся данных.

## 9.4. Что из §8 УСТОЯЛО

* история изменений **не была ПРОЧИТАНА** — это верно и остаётся. Скачана ≠ прочитана;
* 176 записей после 4.5.71, из них 14 задевают наши темы — верно, замер повторяется;
* поиск по `newUpdates` даёт на 14 486 строк одно упоминание, про watchLiquidations, —
  расхождение §4 историей не снимается;
* §8.4 (отклонение числа «123 примера») и §8.5 (верхняя граница 10.4%) не затронуты;
* опись §1 своему итогу не равна — верно, но объяснение из 8.7 теперь тоже под вопросом:
  корень выгрузки в ней не учтён.

## 9.5. Что НЕ бралось выгрузкой на самом деле

Команда `sparse-checkout set wiki examples/py` не берёт ничего, кроме этих двух путей и
корня. Значит вне выгрузки остались:

| путь | файлов | из них документация | наше? |
|---|---|---|---|
| `.claude/` | 26 | 10 | **да** — собственный навык ccxt по Python |
| `.agents/` | 103 | 81 | частично — справочник Binance, в т.ч. фьючерсы USD-M и их потоки |
| `docs/` | 221 | 86 | нет — переводы руководства и веб-песочница |
| `python/` | 858 | 4 | частично — README пакета |
| `cli/` | 12 | 1 | нет |
| исходники js, ts, php, cs, go, java, dist, build | ~6900 | ~11 | нет |

⚠ **А два файла были скачаны с самого начала и не замечены ни прошлым заходом, ни мной:**
llms.txt (5 571 Б) и llms-full.txt (29 658 Б) в корне — это документация, которую ccxt
собирает СПЕЦИАЛЬНО для чтения языковой моделью. Ни один из двух заходов в них не заглянул.

## 9.6. Цена ошибки

Утверждение прожило три часа, попало в коммит `a4d3bd1`, в тело запроса на слияние и в
устный доклад владельцу как «единственная настоящая находка проверки». Настоящей находкой
она не была. Владелец не мог поймать это чтением кода — поймала команда `test -e`,
которую полагалось выполнить сразу.
