# Обзор транспорта и движка против внешних проектов

**Дата:** 2026-08-17. **Повод:** вопрос владельца «почему закачка такая долгая — изучи
другие проекты, возможно стоит изменить архитектуру или дизайн транспорта и движка;
возможно есть другой способ получения всех необходимых свечей по всем таймфреймам».

Все числа воспроизводимы; команды при каждом. Среда: `evidence/env-2026-08-17.txt`.

---

## 1. Что показал замер НАШЕГО транспорта и движка

**Транспорт.** Пагинация свечей была строго последовательной: ~200 мс на страницу
(время сетевого круга, снято по mtime файлов бэкфилла), при этом бюджет веса биржи
использовался на 4–8% (80–160 из 1800/мин). `OHLCV_PAGE = 499` оптимизировал «бар на
единицу веса» — верный расчёт НЕ ТОЙ величины: узким местом было ожидание, а не вес.
**Починено** тем же днём: страницы вычислимы наперёд и летят параллельно под троттлером
ccxt (`exchange._fetch_ohlcv_paged`, PAGE_CONCURRENCY=8); заодно один таймаут больше не
обрывает хвост окна (в этот день ETH 1m потерял так 619 197 баров из 756 422).

**Движок.** Профиль исполнения повтора (`evidence/profile-replay-2026-08-17.txt`):
76% времени расчёта — `archive.window`: одни и те же сутки сделок перегруппировывались
под каждым перекрывающим окном структуры (329 окон на 3 символа), 8.7 млн строк
конвертировались в Python поштучно, 4 429 обходов каталога глобом (33 с). **Починено:**
кэш суточных гистограмм, накопление колонками в прежнем порядке, индекс частичных
файлов одним обходом. Повтор до/после — **побайтно идентичен** (§10.6):
`/tmp/replay-io-{before,after}.log`, дифф пуст. Гипотеза «затык — GIL в
detect-циклах» замером ОПРОВЕРГНУТА: detect в топ расходов не входит.

## 2. Как транспорт устроен у других — по проектам

| проект | транспорт свечей | что берём как урок |
|---|---|---|
| **jesse** | хранит ТОЛЬКО 1m, старшие ТФ строит сам «on the fly»; позже добавил ОПЦИЮ качать с биржи | главный кандидат: один поток вместо семи; см. §3 — проверено на наших данных |
| **nautilus_trader** | бары агрегирует ЛОКАЛЬНО из потока тиков/котировок (BarAggregator), событийная шина, детерминизм как у нас | свежий хвост можно собирать из уже идущего `watch_trades` без REST вовсе |
| **cryptofeed** | приём отдельно от счёта; в колбэках приёма «не делать ничего вычислительного» | наш снимок+`to_thread` соответствует; болело исполнение горячей точки (§1) |
| **hummingbot** | связь по WS (kline/book стримы), горячие пути на Cython | Cython — новая сборочная зависимость, §10 владельца |
| **OctoBot** | событийные каналы: пересчёт будится ПРИХОДОМ данных, а не циклом по таймеру | идея инкрементального пересчёта вместо полного каждые 300 с |
| **freqtrade** | async ccxt, но пагинация внутри пары тоже последовательная — и в их issue названа медленной | наша боль не уникальна; лечится параллельными страницами (§1) |
| **binance-public-data** (офиц.) | месячные/суточные ZIP klines: один GET вместо тысяч страниц | для ГЛУБОКОЙ истории; ограничено решением владельца 2026-08-11 (§5) |
| **Binance fapi** (док.) | klines limit ≤ 1500; вес: <100→1, <500→2, ≤1000→5, >1000→10 | при упоре в вес 499 оптимален (249.5 бар/вес); при упоре в RTT решает параллельность |
| **ccxt.pro** | `watch_ohlcv` — kline-стримы WS; лимиты Binance USD-M: 1024 стрима/соединение | 27×6=162 стрима влезают в одно соединение — REST-опрос рядов может уйти совсем |

Ссылки: jesse docs (Candles, changelog), nautilus docs (Architecture, Message Bus),
cryptofeed README/high_level.md, hummingbot Architecture p.1-2, OctoBot Developer Guide
(Async-Channel), freqtrade data-download + issue #1244, developers.binance.com
(USDS-M Kline endpoint), binance-public-data README.

## 3. ГЛАВНЫЙ ЗАМЕР: старшие ТФ строятся из 1m ТОЧНО — на наших же данных

Вопрос владельца «возможен ли другой способ получения всех свечей» сведён к проверяемому:
совпадают ли бары, собранные из 1m, с нативными барами биржи. На диске есть и то и
другое по 30 рынкам (11.8 млн минуток после сегодняшнего прохода).

**Наивная сборка** (все минуты подряд) расходится на тонких символах: 0.45% корзин 5м
(ASTR 1400, SUPER 975…), у BTC/ETH/SOL — ноль. Механизм найден разбором корзины ASTR
2026-02-18 09:40: **пустая минута (v=0) несёт фантомную цену** — перенесённый close
предыдущей минуты, сделок за которым нет; нативный бар строится по сделкам и фантома
не видит. Свидетель: `evidence/probe-resample-2026-08-17.txt`.

**Правило «минуты с v=0 не участвуют в OHLC» делает совпадение ТОЧНЫМ**:

| ТФ | полных корзин | OHLC расходится | объём (допуск 1e-9 отн.) |
|---|---|---|---|
| 5м | 1 473 924 | **0** | 0 |
| 15м | 491 298 | **0** | 0 |
| 1ч | 122 803 | **0** | 0 |
| 4ч | 30 689 | **0** | 0 |
| 1Д | 7 155 | **0** | 0 |
| 1Н | 1 026 | **0** | 0 |

Свидетель и команда: `evidence/probe-resample2-2026-08-17.txt` (зонд в теле файла).
Итого 2 127 895 корзин, ноль расхождений. Контроль фальсифицируемости: наивная сборка
(без правила) на тех же данных даёт 8 981 расхождение — прибор различает.

⚠ Чего замер НЕ говорит: (а) сравнение только по ПОЛНЫМ корзинам — корзины, где в
хранилище нет части минуток, не сверялись; (б) это исторические бары — поведение живой
границы (незакрытая свеча) отдельный вопрос; (в) точность равна точности самих 1m.

## 3а. РАСШИРЕННАЯ ВЫБОРКА по требованию владельца: 20 ccxt-проектов, транспорт по коду

Владелец забраковал первую выборку («минимум 20, самых популярных, на ccxt, без AI/ML»).
Отбор машинный: GitHub-поиск по topics (trading-bot, cryptocurrency+trading) с
сортировкой по звёздам плюс адресные кандидаты; **верификация и чтение транспорта — по
НЕГЛУБОКИМ КЛОНАМ и локальному грепу**, потому что прибор `gh search code` провалил
контроль (на заведомо существующее `fetch_ohlcv` в freqtrade вернул пусто; для
opentrader вернул 0 при ccxt-файлах, видимых в клоне) — его нулям верить нельзя.
Клоны — в scratchpad смены, в git не идут; локаторы файлов приведены.

| # | проект | ★ | как получает свечи (по коду) |
|---|---|---|---|
| 1 | freqtrade | 53k | страницы истории вычисляются наперёд (`range(since, until, one_call)`) и летят `asyncio.gather` ПАЧКАМИ ПО 100 «to avoid overwhelming ccxt Throttling»; тот же gather по парам; кэш `_klines` (exchange/exchange.py:2627-2657, 2896) |
| 2 | jesse | 8.3k | качает ТОЛЬКО 1m, последовательно со `sleep(driver.sleep_time)`, в БД (Peewee, on_conflict_ignore); старшие ТФ генерирует сам (import_candles_mode) |
| 3 | passivbot | ~3k | «lightweight **1m OHLCV manager**»: кэш посуточными шардами `{exchange}/{tf}/{symbol}/YYYY-MM-DD.npy`, глобальный `asyncio.Semaphore` на сеть, ДЫРЫ синтезируются нулевыми свечами и «not persisted» (candlestick_manager.py:1-40, 1086, 735) |
| 4 | OctoBot / OctoBot-Trading | 6.4k | ОТДЕЛЬНАЯ asyncio-задача на каждую пару×ТФ (`_candle_update_loop`), пересчёт будится приходом свечи через каналы (ohlcv_updater.py:80) |
| 5 | Crypto-Signal | 5.6k | последовательный REST по парам×периодам, кэш словарём на прогон (app/behaviour.py:143-283) |
| 6 | Haehnchen/crypto-trading-bot | 3.5k | ГИБРИД: `ccxt_candle_watch_service` — WS-вотчеры на пару×период + `ccxt_candle_prefill_service` — REST-досев с троттлом; sqlite |
| 7 | opentrader | 2.8k | последовательный курсор `since = lastCandle + 60000` (база 1m), до endDate (ccxt-candles.provider.ts:89-121) |
| 8 | vectorbt | 8.7k | последовательная пагинация `while True` + `sleep(delay=500мс)` + tqdm (data/custom.py:439-505) — самый медленный из увиденных паттернов, ровно наш вчерашний |
| 9 | peregrine | 1.2k | свечи не качает вовсе: массовый async-сбор стаканов/тикеров по десяткам бирж (арбитраж) |
| 10 | bt-ccxt-store | ~800 | поллинг `fetch_ohlcv` в фиде backtrader от fromdate (ccxtbt/ccxtfeed.py:96) |
| 11 | WolfBot | ~700 | ccxt как адаптер части бирж (BinanceCcxt.ts); свечи — собственные потоки/импортёры, fetchOHLCV в src не найден |
| 12 | catalyst (Enigma) | 2.6k | `fetch_ohlcv` с ретраями и `retry_sleeptime=5`, хранение бандлами (exchange/ccxt/ccxt_exchange.py:423-462, 929) |
| 13 | magic8bot | ~1k | бары строит ИЗ СДЕЛОК: «Trade sync … backfill for N days» + пакет `@magic8bot/timebucket` (engine/trade.ts:72) — nautilus-модель на ccxt |
| 14 | Roibal (учебный) | ~2.5k | разовые прямые `fetch_ohlcv` в скриптах, без хранения |
| 15 | tradingview-webhooks-bot | ~800 | свечи НЕ КАЧАЕТ: сигнал приходит вебхуком из TradingView, ccxt — только исполнение |
| 16 | investing-algorithm-framework | 1.7k | последовательная пагинация `while from < to` + `sleep(exchange.rateLimit/1000)` (data_providers/ccxt.py:674-702) |
| 17 | pyjuque | ~500 | `getOHLCVHistory`: последовательные страницы limit=1000 (Exchanges/CcxtExchange.py:38-43) |
| 18 | ccxt (сам, examples) | 43k | официальные образцы: async-пагинация, `fetch-ohlcv-multiple-symbols-continuously`, работа от closing time (examples/py) |
| 19 | supertrend-crypto-bot | 413 | канонический минимум: опрос `fetch_ohlcv('1m', limit=100)` по расписанию, без истории (supertrend.py:91) |
| 20 | kupi-terminal | ~2k | on-demand: `fetchOHLCV(symbol, tf, undefined, optimalLimit)` по запросу интерфейса (dataActions.ts:529) |
| 21 | Superalgos | 5.6k | последовательный сенсор-бот: ccxt `fetchOHLCV` limit=1000, rateLimit=500; биржи без OHLCV — эмуляция через fetchTrades; сырые файлы по датам (Sensor-Bot/Exchange-Raw-Data/HistoricOHLCVs.js:41-81; `"ccxt": "^2.7.62"` в package.json) |

⚠ Поправка того же дня: в первой редакции Superalgos стоял в исключённых
(«не верифицирован» — клон не материализовался, а поиску кода GitHub верить нельзя).
Дочитан напрямую из git-объектов клона и package.json — использует ccxt, строка 21.

**Исключены с названной причиной:** lumibot / zvt-ядро / OpenBB /
manu354 (ccxt в коде не найден). Не-ccxt лидеры (hummingbot, gocryptotrader,
nautilus_trader, cryptofeed, gekko, tribeca) — в таблице §2 как контекст.

**Синтез по 21 (что встречается и сколько раз):**
* **последовательная пагинация со sleep — 8 из 21** (vectorbt, jesse, opentrader,
  Crypto-Signal, catalyst, investing-algo, pyjuque, Superalgos): наш вчерашний паттерн — норма
  жанра, а не наш личный дефект; и именно поэтому freqtrade с gather×100 выделяется;
* **параллельные заранее вычисленные страницы — 1 из 21** (freqtrade, самый крупный) —
  ровно сегодняшняя наша правка, чужим кодом подтверждена;
* **база 1m с локальной сборкой старших ТФ — 3 из 21** (jesse полностью, passivbot
  «1m manager», opentrader курсор 1m): jesse-модель — не экзотика;
* **бары из сделок — 1 из 21 полностью** (magic8bot; у Superalgos — как эмуляция для бирж без OHLCV) плюс nautilus вне выборки;
* **WS-вотчеры свечей — 2 из 21** (Haehnchen гибрид, OctoBot задача-на-ряд);
* **пустые минуты — известная жанру проблема:** passivbot синтезирует нулевые свечи
  для дыр и НЕ хранит их — независимое подтверждение нашей находки о фантомной цене
  (§3): пустой минуте нельзя доверять цену.

## 3б. ПРОФИЛЬ ОБЪЁМА: 20 реализаций по требованию владельца

Вопрос: «найди 20 проектов с профилем объёма или аналогами — возможно, имеет смысл
изменить наш». Отбор машинный (GitHub по «volume profile», «TPO», «volume by price»,
«footprint», по владельцам школ orderflow), чтение — клоны + raw-файлы крупных.
Клоны в scratchpad/survey/vp.

| # | проект | данные → корзина | биннинг | ПОК/зона |
|---|---|---|---|---|
| 1 | QuantConnect **Lean** (21k★) | бар → корзина ЦЕНЫ ЗАКРЫТИЯ | шаг округления цены (0.05) | VA 70%, расширение одна-против-одной (большая); ничья ПОК — ближняя к середине; окно rolling N |
| 2 | **highcharts** VBP (12k) | бар → одна зона по close,± по направлению | 12 равных диапазонов | только гистограмма, без ПОК/VA |
| 3 | **pandas-ta** vp (5k) | close+volume → корзина close | width=10 корзин | без ПОК/VA (таблица с ±объёмом) |
| 4 | **technicalindicators** (2.4k) | объём бара в пересекающиеся диапазоны (деление объёма не читал — оговорка) | noOfBars равных | bull/bear на диапазон, без VA |
| 5 | **py-market-profile** (403) | TPO и объём по строкам row_size | tick_size 0.05 × prices_per_row | VA 70% одна-против-одной (большая); initial balance 1ч; сессии |
| 6 | **go-vp** | бары → строки | RowSize фикс. строк | VA: добавляется МЕНЬШАЯ сторона и стоп ДО перелива цели — отклонение от канона; школы расходятся даже в направлении расширения |
| 7 | **EarnForex MarketProfile** (MQL, эталон MT4/5) | бары графика по сессиям | шаг тика инструмента | сессионные профили Daily/Weekly, developing POC линия |
| 8 | **MarcosACH** pine | бары | **Number of Rows = 100 по умолчанию** (max 500) | ПОК-линия |
| 9 | **Tucsky/aggr(+server)** (1.1k) | СДЕЛКИ WS → бары с vbuy/vsell | по цене сделки | кластеры покупок/продаж, influx |
| 10 | **focus1691/orderflow** | aggTrades WS → footprint_candle по уровням цены | шаг цены | футпринт свечи |
| 11 | cenobar/TPO | бары ИЛИ тики | price_pace | классический TPO-график |
| 12 | fatfingererr/volume-profile-intro | образовательный (ноутбук+гифки) | — | — |
| 13 | notPlancha/volume-profiles-v2 | браузерный TS-рендер | — | — |
| 14 | Azhagesan-dev/OrderFlowMap | стакан (depth) WS в браузере | тик | heatmap ликвидности (аналог Bookmap) |
| 15 | mahmoud20138/OrderFlow-Analysis-Pro | сделки → дельта-аналитика | — | delta/imbalance |
| 16 | milocaetano/quantick | MT5-мост, тики | — | orderflow |
| 17 | izzylite (Bookmap addon C#) | стакан | тик | heatmap |
| 18 | WilliamYi951208/trading-assistant | ATAS-мост, бары | — | — |
| 19 | **TradingView** (справка; наш разбор tv-transfer-2026-08-09) | свечи младшего ТФ, объём РАВНОМЕРНО по диапазону свечи | Number of Rows / Ticks per Row | VA 70%, сторона — большая строка, ничья — ближняя |
| 20 | наш прежний тиковый `build()` + 62 источника row-size (row-size-decision-2026-08-10) | сделки | tickSize | VA 70% SINGLE/PAIRS |

**Сверка с НАШИМ прибором по измерениям:**

* **источник объёма**: три школы — сделки (footprint-жанр), «весь объём в корзину close»
  (Lean/pandas-ta/highcharts — самая грубая), «равномерно по диапазону свечи» (TV — наш
  способ). Наш богаче школы close и совпадает с инструментом автора; менять нечего;
* **биннинг**: обе школы живы (фикс. строки / шаг цены); наш умеет обе, боевой —
  100 строк, и community-pine (№8) ставит **те же 100 умолчанием** — независимое
  подтверждение выбора, который мы снимали со скриншотов автора;
* **зона**: расширение «одна-против-одной, большая» — большинство OSS (Lean,
  py-market-profile; go-vp вообще инвертирован); канона Далтона «парами» в прочитанном
  OSS НЕТ НИ У КОГО — наш дефолт SINGLE подтверждён жанром;
* **ничья ПОК**: Lean решает «ближней к середине» — у нас NotReady. Кандидат проверен
  спросом: в сегодняшнем полном проходе 27 символов ничьих **ноль** (`grep НИЧЬЯ
  serve-2026-08-17.log` → 0) — менять не на чем;
* **окно**: сессии/rolling/видимая область у других; у нас — вся структура, и это
  требование КУРСА (стр. 26), не предмет сверки с жанром;
* **чего нет ни у кого из 20**: детерминизм повтора, именованные отказы, сверка с
  эталоном в CI. Наш прибор строже жанра, не слабее.

**Вывод по вопросу «менять ли профиль»: основу менять не на что** — наш способ =
TradingView-способ (инструмент автора курса), а решение владельца 2026-08-09 «приборы
приводятся к TV» обзором только подтверждено. Единственный кусок вне мейнстрима TV —
VRVP-композит на СДЕЛКАХ (footprint-жанр): вопрос «сделки или свечи для VRVP» уже стоит
в queue-owner.md (вместе с судьбой aggcache).

## 4. Варианты архитектуры и их цена

**I. Остаться как есть (уже ускорено).** Параллельные страницы + кэши движка влиты.
Ожидание по расчёту: параллельность даёт предел ~450 тыс. бар/мин по весу против
~100 тыс. последовательно; движок — минус 76%-я горячая точка. Живой замер до/после —
после завершения идущего прохода. Цена: 0. Риск: 0.

**II. jesse-модель: качать и хранить ТОЛЬКО 1m, все ТФ строить локально.**
Транспорт сжимается с 7 рядов на символ до 1. Разовая цена глубины: 1Н требует 200
недель = 3.8 года 1m ≈ 2 млн минуток/символ ≈ 4 010 страниц ≈ 8 020 веса — при
параллельной пагинации ~4.5 мин на символ, разово; дальше — только свежие минутки.
Хранение ≈ 12 МБ/символ. Точность — §3: ноль расхождений на 2.1 млн корзин.
⚠ Это смена ИСТОЧНИКА расчётных рядов — решение владельца (внесено в queue-owner.md).

**III. WS kline-стримы вместо REST-опроса** (`ccxt.pro watch_ohlcv`): 162 стрима в
одном соединении (лимит 1024), REST остаётся только на засев и дыры. Инженерная правка
транспорта; хорошо сочетается и с I, и с II (при II — 27 стримов 1m). Цена: правка
`run._poll_bars` → подписка; замер устойчивости соединения.

**IV. Vision-архивы klines для глубокой истории** (binance-public-data): месячный ZIP
одним GET против тысяч страниц. Затрагивает решение владельца 2026-08-11 «архив удалён
целиком» — то решение мотивировано лагом СВЕЖИХ суток (1-2 дня), который к истории
месячной давности не относится; но пересмотр — только владельцем (queue-owner.md).

**Рекомендация смены:** I уже сделано; II+III — самое сильное сочетание по
простоте и скорости, ждёт владельца; IV даёт выигрыш только при холодном старте.

## 5. Команды воспроизведения

```bash
uv run python -m hunter replay --run-id last   # повтор: после правок движка побайтно тот же
```

Зонд §3 сохранён файлом (правило v>0, все ТФ):

```bash
uv run python docs/audit/probes/probe_resample_2026-08-17.py
```

Выводы обоих проходов (наивного и с правилом) — в
`evidence/probe-resample-2026-08-17.txt` и `evidence/probe-resample2-2026-08-17.txt`.
⚠ Отпечаток данных: хранилище растёт (боевой проход дописывает свежие бары), повтор
зонда на доросшем кэше даст бОльшие знаменатели — сверять по датам в отпечатке среды.

## 6. Что осталось непроверенным

* живой замер скорости параллельной пагинации (идёт боевой проход — два потребителя
  лимита недопустимы); будет дописан сюда же;
* `watch_ohlcv` живым вызовом не проверялся (только карта возможностей — а она
  объявление, не проверка);
* неполные корзины 1m (дыры хранилища) из сверки §3 исключены;
* поведение живой (незакрытой) границы при локальной сборке ТФ.
