__pycache__/
*.py[cod]
.venv/
data/
*.sqlite3
# Кадры прогонов — под data/, в репозиторий не идут.
# Но эталонный срез гейта-оракула идти ОБЯЗАН: без него гейт печатает
# «проверка не состоялась» и падает (проверено — так и случилось в CI 2026-08-03).
*.parquet
!docs/audit/reference-slice/*.parquet

# страницы курса — рендерятся локально одной командой (см. CLAUDE.md), в git не нужны
docs/course/pages/

# Личные переопределения Claude Code — у каждого свои, в репозиторий не идут.
# Общие настройки, хуки и команды проекта (.claude/settings.json, hooks/, commands/)
# наоборот КОММИТЯТСЯ: они часть того, как проект проверяется.
.claude/settings.local.json
.scratch/

# Выгрузка Telegram-канала автора — данные владельца, чужой контент; в git НЕ идёт
# (разбор с цитатами — docs/audit/tg-prizrak-2026-08.md, он идёт).
prizzrak-tg/

# ⚠ ЗДЕСЬ СТОЯЛИ ПЯТЬ ПРАВИЛ, ПРЯТАВШИХ `docs/audit/evidence/`. Сняты 2026-08-28 вместе
# с самим каталогом, по приказу владельца «да, удаляй evidence».
#
# Каталог запрещён сводом (CLAUDE.md, «зонд ИИ не проверяет код ИИ»): «каталоги
# `evidence/`, `probes/` и им подобные не заводятся заново. Никогда». Он был заведён
# заново 24-25 августа — через пять дней после запрета, — а эти правила делали нарушение
# невидимым для `git status`. Прибор был настроен не видеть.
#
# НЕ ЗАВОДИТЬ СНОВА, и в первую очередь не заводить правило игнорирования: разовый замер
# живёт в переписке, а живой журнал боевого процесса — вне репозитория.
