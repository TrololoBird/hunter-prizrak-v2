---
description: Все гейты одной командой — ruff, mypy и 18 проверок, падает по коду возврата
---

```bash
bash scripts/check.sh
```

⚠ Гонять именно так, а НЕ циклом по `gates/*.py`: цикл возвращает код последней итерации,
а не худшей, и `for … done && git commit` коммитит при красном гейте. Это случалось
дважды за сутки 2026-08-04 (CLAUDE.md).

Доложи результат числом: сколько гейтов прогнано и сколько провалено. Если провал —
покажи, какой гейт и на чём, и назови, что именно он проверяет по FOUNDATION.

Замер 2026-08-06: полный прогон занимает около 45 секунд.
