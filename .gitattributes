# Корпус переносится побайтово (FOUNDATION.md §6: "переносится как есть").
# core.autocrlf=true в окружении иначе перепишет переводы строк при checkout —
# это изменение файла. Здесь конверсия выключена, файлы хранятся как есть.
research/prizrak_corpus/** -text
